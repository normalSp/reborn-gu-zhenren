# M15 浏览器与套利验收记录

更新时间：2026-05-08

## 依据

- `codex上下文信息/2026-05-08-v0.7.0-pre-closure-v2-context.md`：M15 仍需补齐三王山前夜、蛊仙期浏览器手测，并补 100/300 回合套利记录。
- `02-数值闭环/P2-P3-经济可信闸门收束落地记录.md`：宝黄天、喂养、势力五账本、调息都必须进入 v0.7.0-pre 闸门。
- `game-dev-text` 顺序：先验证底层经济和状态协议，再做 UI 与浏览器验收。

## 自动闸门结果

命令：
```powershell
npm test -- src/engine/economy-simulation.test.ts src/engine/auction-economy-loop.test.ts src/engine/faction-economy.test.ts src/engine/primeval-meditation.test.ts
```

结果：4 个测试文件 / 12 个用例通过。

覆盖项：

- 20/100/300 回合经济节奏：20 回合买不起六转仙蛊，100 回合可完成一个六转大目标，300 回合可形成多个六转资产但七转仍是战略门槛。
- 宝黄天候选价：六转仙蛊基础价 3600 仙元石，仙材区间、仙蛊方、杀招残方/完整传承与 `economy-balance.json` 对齐。
- 势力五账本：蛊仙期势力收益受维护、风险、食料预留约束，不再按旧贸易利润膨胀。
- 元石调息：不再使用 1 元石固定恢复 100 真元的旧逻辑，而是按境界比例、回合与场景风险结算。
- 炼养用来源：配方材料/蛊输入引用无缺失，高转喂养阻塞数为 0。

## 浏览器三路径复验

运行方式：

- 本地 dev server：`http://127.0.0.1:5173`
- Playwright headless Chromium，读取项目根目录 `.env` 中的 DeepSeek key，只用于标题页连通与开局叙事，不写入日志/文档。
- 每条路径从标题页进入时间线、配置起点、创建原创角色并进入 `game_play`。

结果：

1. 一转商队开局：通过。
   - 角色：`商队验收者`，一转中阶，章节进入 `第二章 · 商路求生`。
   - 控制台 error：0。
   - 叙事没有把玩家称为方源；商心慈作为剧情接触对象出现。
   - 页面包含蛊虫、炼蛊、蛊材、商会、人物图鉴、事件日志等入口。
   - 截图：`.codex/v070-pre-browser-caravan.png`。

2. 三王山前夜：通过。
   - 角色：`三王山验收者`，四转初阶，章节进入 `第五章 · 三王山前夜`。
   - 控制台 error：0。
   - 长文本叙事、选择风险、事件日志入口正常显示；未出现 Zod 死循环或未知材料入库报错。
   - 截图：`.codex/v070-pre-browser-sanwang.png`。

3. 蛊仙期：通过。
   - 角色：`蛊仙验收者`，六转初阶，章节进入 `第六章 · 义天风云起`。
   - 控制台 error：0。
   - 页面切换为 `仙窍` 入口，元石/仙窍/蛊仙期 UI 可进入；未出现凡人宝黄天交易报错。
   - 截图：`.codex/v070-pre-browser-immortal.png`。

## 全量回归

命令：
```powershell
npm test
npm run build
```

结果：

- `npm test`：28 个测试文件 / 241 个用例通过。
- `npm run build`：通过；仍保留主包 500KB+ 的 Vite 警告，作为发布前性能优化项，不阻塞 pre 闸门。

## 当前结论

- M15 的自动套利闸门与三路径浏览器复验均已通过。
- 尚需在版本标识更新后再做一次最终冒烟，确保 UI 版本号与 `package.json=0.7.0-pre.0` 一致。
- v0.7.0-a/b/c 前仍建议保留这三条浏览器路径作为发布前固定验收脚本。

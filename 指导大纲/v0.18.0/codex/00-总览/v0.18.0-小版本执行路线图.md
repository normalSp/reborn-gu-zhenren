# v0.18.0 小版本执行路线图

日期：2026-05-19
状态：本地开发里程碑完成；等待提交、推送与远端 CI 记录

| 阶段 | 名称 | 交付 | 验收 |
|---|---|---|---|
| a1 | 路线/地点/存档/BFF 设计门禁 | route/location/save authority 表；BFF 触发条件；非目标冻结 | 第一版完成；不改 runtime |
| a2 | MiroFish intake、字段表、测试矩阵 | v0.18 包 intake；route state 字段表候选；测试矩阵 | 第一版完成；三包 intake 通过 |
| b1 | 青茅离开正式门槛样板 | 把准备、遮掩、补给、追索组合成离开承诺前门槛 | 已完成；focused tests、e2e、Player Advocate 覆盖 |
| b2 | 路线候选承接第一刀 | 用户未批准 save-format 变更，因此只写候选承接 view，不写 route state | 已完成；无 migration need |
| b3 | 南疆低阶区域事实卡与路线面板 | 区域事实卡、公开/隐藏分离、路线面板 | 已完成；隔离项未进 UI/DeepSeek/runtime |
| b4 | 前期账本影响补给、追索、身份压力 | 读取 knownFacts/npcMemories/factionPressure/actionConsequences 影响后续 route pressure | 已完成；无阵营/奖励/NPC 生死越权 |
| b5 | 商队/散修/商家城外缘公开入口边界 | 公开入口条件展示，不开放完整城市 | 已完成；不开放完整商家城 |
| rc | 质量收束 | 全量质量门、handoff、仪表盘、Git/CI | 本地质量门通过；远端 CI 待记录 |

## 推荐执行顺序

1. 用户批准启动范围和 MiroFish request。已完成。
2. 做 `a1`，只写设计门禁，尤其是 route/location/save/BFF 边界。已完成第一版。
3. 做 `a2`，完成 MiroFish intake、字段表和测试矩阵。已完成第一版。
4. `b1-b5` 已作为一条 integrated route chain 完成，每段文档已落地。
5. 用户未批准 save-format 变更，因此 `b2` 采用候选承接第一刀，不写 `route_entered/currentRoute/currentRegion`。
6. b1/b2/b3/b4/b5 文档、测试矩阵、Player Advocate 记录、仪表盘和 Git 状态已在 rc 收束中更新。
7. rc 本地质量门已通过，下一步记录 commit/push/CI 后进入 `v0.19.0` 启动审查。

## Git 建议

- a1/a2 可合并为一个 docs commit。
- b1/b2/b3/b4/b5 本轮因运行时代码共用一套 `v018` rules/engine/store/UI/e2e，合并为一个 runtime commit，并在本文件记录偏离原因。
- rc 可作为收束或远端证据 commit。
- 不使用 `git add -A`。
- 不混入当前未跟踪的美术候选、zip、artifacts、bgm、`.cursor` 或 `指导大纲/大方向/`。

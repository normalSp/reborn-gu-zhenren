# v0.18.0-rc 质量收束记录

日期：2026-05-19
状态：本地质量门通过；runtime+rc commit 已推送并通过远端 CI

## 范围

本次 rc 收束 `v0.18.0 南疆路线与多区域承接`：

- 青茅离开正式门槛样板。
- 路线候选承接第一刀。
- 南疆低阶公开事实与路线面板。
- 前期账本压力回流。
- 商队、散修、商家城外缘入口边界。

## 非目标确认

本轮未新增：

- `SAVE_FORMAT_VERSION = 23`。
- `route_entered`、`currentRoute`、`currentRegion` 或新 location 持久字段。
- 正式新地点、完整南疆、完整商家城。
- 正式阵营转移、任务、声望、奖励、材料、蛊虫、蛊方。
- NPC 生死、捕获、永久追杀成败。
- hidden fact player-visible 泄露。
- BFF/backend、自动部署、DeepSeek 权限扩大。

## 验证结果

| 命令 | 结果 |
|---|---|
| `npm test -- src/engine/v018-qingmao-route-multi-region.test.ts --reporter=dot` | 5 tests passed |
| `npx tsc --noEmit --pretty false` | passed |
| `npm run test:e2e -- tests/e2e/v018-route-multi-region.spec.ts` | 1 test passed |
| `npm run check:player-advocate-gate -- 指导大纲/v0.18.0/codex/00-总览/v0.18.0-b1-b5-Player-Advocate-100轮走查记录.md 100` | passed，100 rounds，97.0% understanding |
| `npm run check:player-advocate-gate -- 指导大纲/v0.18.0/codex/00-总览/v0.18.0-rc-Player-Advocate-60轮走查记录.md 60` | passed，60 rounds，93.3% understanding |
| `npm test -- --reporter=dot` | 134 test files，748 tests passed |
| `npm run build` | passed |
| `npm run check:runtime-assets` | 173 files checked，zero-byte=0 |
| `npm run check:qingmao-assets` | 23 entries checked |
| `npm run check:player-visible-copy` | 260 files scanned，passed |
| `npm run test:e2e` | 84 tests passed |
| `npm run test:e2e:long` | 29 tests passed |
| `npm run check:production-preview` | passed；root 正常，console 仅成就加载日志 |

## Player Advocate 结论

- b1-b5 集成 100 轮：通过，97.0% 下一步理解率，3 个 confused rounds。
- rc 60 轮：通过，93.3% 下一步理解率，4 个 confused rounds。
- 没有 P0/P1 体验阻断。
- P2/P3 进入后续需求池：移动端路线摘要、承诺门槛文案保守化、区域事实摘要优先级。

## MiroFish 边界

v0.18 三包只被吸收为 RebornG-owned rule summaries/test samples：

- `v018_route_entry_state_and_milestones_pack`
- `v018_southern_border_low_rank_region_fact_cards_pack`
- `v018_post_qingmao_pressure_reaction_pack`

quarantined item `v018_hidden_982eba1c3730` 保持 deferred/human_review_only，不进入 player-visible UI、DeepSeek context 或 runtime authority。

## Git 状态

- runtime+rc commit：`56bb9dd feat: 完成v0.18路线多区域承接`。
- push：已推送到 `codex/v013-npc-faction-reaction`。
- 远端验证：GitHub Actions `26070709256` 通过 deterministic quality gate。
- 本轮不使用 `git add -A`。
- 不混入 `.cursor/`、zip、artifacts、bgm、未审美术资产、`指导大纲/大方向/`。

## 下一步

`v0.18.0` 可视为本地开发里程碑完成。下一步应停下来做 `v0.19.0` 启动审查与范围冻结。

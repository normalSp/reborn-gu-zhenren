# v0.18.0-b3 南疆低阶区域事实与路线面板

日期：2026-05-19
状态：已完成
性质：runtime read-only projection

## 目标

让南疆低阶路线第一次有玩家可读的区域感：山路、野外、商队、客栈/酒肆/交易点、商家城外缘、追索风险层。

## 实现

`buildV018QingmaoRouteMultiRegionOverview()` 输出 `regionFacts`：

- `v018_region_public_route_area`
- `v018_region_mountain_road`
- `v018_region_caravan_context`
- `v018_region_inn_market_context`
- `v018_region_shang_outer_boundary`
- `v018_region_pursuit_risk_layer`

`FreeGoalPanel` 展示 `free-goal-v018-region-facts`。

## 边界

- 区域事实是公开/玩家可见摘要，不是 runtime truth authority。
- 不展示原文，不展示 hidden fact body。
- 不开放完整地图、正式地点、商家城内城、演武场、拍卖、商会系统。
- MiroFish 隔离项 `v018_hidden_982eba1c3730` 不进入 UI、DeepSeek 或玩家可见事实。

## 验收

- unit 确认 pressure/region facts 不含 quarantined item 的玩家可见内容。
- e2e 确认区域事实面板可见且不误写地点。
- Player Advocate 20 轮记录：`v0.18.0-b3-Player-Advocate-20轮走查记录.md`。

## 结论

b3 通过。南疆开始有“可见区域语义”，但还不是完整地图系统。

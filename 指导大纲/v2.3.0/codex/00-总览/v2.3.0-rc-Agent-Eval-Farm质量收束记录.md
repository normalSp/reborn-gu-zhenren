# v2.3.0-rc Agent Eval Farm 质量收束记录

状态：completed。
日期：2026-05-23

## 版本结论

`v2.3.0` 本地完成，主线为：

`Agent eval farm 与失败分类硬化`

完成内容：

- startup/a0：v2.2 runner 复盘、v2.3 eval farm 范围冻结、D-230 决策池。
- a1：`agent_eval_taxonomy_v230_a1` failure taxonomy 与 severity rubric。
- a2：`agent_eval_farm_report_v230_a2` schema/report/archive 合同。
- b1：自有 zero-dependency / dry-run only / report-only runner。
- b2：negative fixture、mutation、false-negative gate、rescore stable。
- b3：trend archive 与 future_sample_pool 回流。
- rc：Skill sync、入口同步、handoff 和质量收束。

## runner 证据

命令：

```powershell
npm run check:v230-agent-eval-farm
```

结果：

- report：`artifacts/v2.3.0/agent-eval-farm/2026-05-23T09-10-30-682Z/report.json`
- samples：17。
- schemaValidCount：17/17。
- positiveAccepted：4/4。
- negativeCaught：11/11。
- mutationTotalCount：2。
- coveredFailureFamilies：13/13。
- P0/P1/P2 findings：17/5/2。
- p0FalseNegativeCount：0。
- p1FalseNegativeCount：0。
- hiddenFormalMemoryL5MissedCount：0。
- falsePositiveRate：0.0588。
- p2WarningRate：0.1176。
- manualReviewRate：0.0588。
- rescoreStable：true。
- acceptedForGate：true。
- boundaryAssertions：全 false。

## Player Advocate / live DeepSeek

Player Advocate：豁免。

原因：

- v2.3 不改玩家可见 UI/runtime/prompt。
- runner 是 offline / report-only 工具。
- 没有新玩家体验可走查。

是否调用 live DeepSeek：否。

若是：不适用；无模型、样本、轮次、成本或 live 报告路径。

## 明确未授权

v2.3 完成不授权：

- runtime agent。
- live DeepSeek。
- DeepSeek visible lore/RAG。
- BFF/backend。
- subagents。
- 外部 SDK/framework PoC、依赖、vendored subset 或项目写入。
- MiroFish export。
- 正式地点、阵营、身份、奖励、NPC 生死、canon promotion。
- `runFingerprint`。
- 新 save fields 或 `SAVE_FORMAT_VERSION` bump。
- 后端 job queue / cloud archive。

## 本地验证清单

已运行并通过：

| 命令 | 结果 |
|---|---|
| `npm run check:v230-agent-eval-farm` | 17/17 schema valid；positive 4/4；negative 11/11；false negative=0；acceptedForGate=true |
| `npx tsc --noEmit --pretty false` | passed |
| `npm test` | 152 files / 814 tests passed |
| `npm run build` | passed |

待最终提交前继续补充：

- `git diff --check`
- 文档入口 `rg`
- 敏感边界 `rg`
- Git push / CI

## 下一步建议

v2.4 不应直接进入 runtime agent。建议进入：

`薄 BFF 边界评估 / private canon / eval archive infrastructure 预备`

默认仍先做评估和边界，不直接建后端。

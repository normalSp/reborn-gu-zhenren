# v2.6.0-b3 v2.7-v3.0 agent infra 准入清单

日期：2026-05-24
状态：completed
冻结模型：`v260_b3_v27_v30_agent_infra_admission_checklist_v1`

## 一句话结论

v2.6-b3 将 v2.7-v2.9 / v3.0 的 agent infra 准入清单收束为：先扩大离线 Agent Lab，再做 runtime agent 接入门禁，最后才允许有限 L2/L3 runtime agent 试点。v2.6 checker 通过只证明边界可测，不批准 v3.0 runtime agent。

## v2.7 建议

主线：低阶多 NPC / 小势力 Agent Lab 样板扩展。

准入条件：

- 继续 report-only/offline。
- 可复用 v2.6 checker 的 redacted envelope / manual review queue。
- 不新增 runtime agent。
- MiroFish 默认 `not_needed`，若命名 NPC / hidden-adjacent / formal lore 出现则 blocking。

## v2.8 建议

主线：runtime agent 接入准入门禁。

必须回答：

- 哪些 L2/L3 ability 可进 runtime。
- 哪些 proposal 必须被 WorldCore 拒绝。
- 哪些 visibility 可进入 DeepSeek narrative expression，哪些永远不可见。
- live probe / Player Advocate / deterministic replay 的最小门槛。
- rollback / old-save / no-authority-expansion 方案。

## v2.9 建议

主线：v3.0 前安全收束。

必须复核：

- AgentProposal schema。
- WorldCore post-check。
- private canon visibility。
- eval farm thresholds。
- v2.6 archive boundary checker。
- MiroFish need classification。
- external framework / self-learning / subagent future gates。
- skill sync audit。

## v3.0 候选目标

只允许：

- 一个小区域。
- 有限 L2/L3 runtime agent。
- agent 输出候选和表达。
- WorldCore 最终裁决。

不允许：

- L4 原著关键人物自由 live。
- L5 天道/宿命直接裁决玩家结局。
- agent 写奖励、地点、身份、阵营、NPC 生死、canon。
- full-book RAG。
- self-learning 直接改 canon/runtime/save/prompt/知识库正文。
- external framework 绕过 RebornG-owned adapter。

## v3.0 Go/No-Go 指标

| 指标 | 建议门槛 |
|---|---|
| schema valid | 100% |
| P0/P1 false negative | 0 |
| hidden leak / formal authority drift / memory contamination / L5 overreach | 0 |
| WorldCore bypass | 0 |
| live probe | P0/P1=0，P2 可解释可修复 |
| Player Advocate | NPC 有意图、世界有记忆、下一步清楚 |
| rollback / old-save | 有明确复核记录 |

## 退出结论

b3 完成。可以进入 `v2.6.0-process-1-前置审批制度第二轮复核.md`。

# v2.6.0 Git 提交与推送计划

日期：2026-05-24
状态：startup draft；等待 D-260 审批

## 分支

当前分支：

`codex/v260-startup-private-canon-eval-archive-prep`

基线：

- 从 `codex/v250-complete-private-canon-visibility` 创建。
- v2.5 已推送且 CI 通过。

## 提交边界

| 阶段 | 提交边界 |
|---|---|
| startup | v2.6 启动会、前置授权包、总纲、路线图、决策池、测试矩阵、真相源索引、Git 计划、MiroFish 协议、Skill audit、入口同步 |
| a1/a2 | 设计门禁文档与 skill audit，可合并提交 |
| b1 | 若 D-260-007 获批，checker / fixtures / npm script / report 独立提交 |
| b2/b3/process/rc | 可按阶段组提交，rc 必须含质量收束与入口同步 |

## Stage 规则

- 使用显式路径 stage。
- 不使用 `git add -A`。
- 不混入 runtime/source/UI/store/prompt/save，除非 D-260 之外另获批准。
- 不提交本地 `.cursor/`、root `bgm/` 音频、废弃测试存档。

## 推送与 CI

startup 完成后推送并检查 GitHub Actions。

后续 `/goal` 若获批：

1. 每个阶段完成后运行当前阶段测试。
2. rc 前检查 `git diff --check`。
3. 推送分支。
4. 记录 GitHub Actions run id。

## 回滚

v2.6 startup 只改文档与入口。若需要回滚，回滚 v2.6 docs/entry updates，不影响 runtime。

若后续 D-260-007 checker 获批，checker 仍应与 runtime 解耦，便于整块回滚。

# v2.6.0-process-1 前置审批制度第二轮复核

日期：2026-05-24
状态：completed

## 一句话结论

v2.6 是前置批量审批制度的第二轮试运行。用户一次性批准 D-260-001 至 D-260-012，并确认 F-260-001 至 F-260-012 保持 `future_gate_required`；v2.6 后续开发未新增常规审批等待，也未触发例外停机。

## 审批效果

| 项 | 结果 |
|---|---|
| 全版本路线是否前置说明 | 是 |
| D 项是否足够执行 | 是 |
| F 项是否足够阻断越权 | 是 |
| 中途额外常规审批 | 0 |
| 例外停机 | 0 |
| 是否减少流程碎片 | 是 |

## 本轮改进

v2.5 的制度只验证了纯文档版本。v2.6 进一步验证了一个有限工程动作：

- checker / runner / artifact 可以在前置授权包内执行。
- 但 service / backend / real archive / real hidden/private body 仍被 F-260 明确挡住。
- `artifacts/` 报告可以保存 redacted metadata 与 hash，不保存 body。

## 仍需坚持

以后大版本启动会或 a1 必须继续前置：

- 全版本阶段路线。
- 全部预计 D 项。
- 全部 F 项。
- 自动继续范围。
- 例外停机清单。
- 测试与证据计划。

## v2.7 建议

v2.7 可继续沿用该制度。若 v2.7 申请扩大 Agent Lab 样本或新增 runner，应在启动会一次性列明：

- 是否新增 synthetic fixtures。
- 是否新增 checker/report。
- 是否需要 MiroFish。
- 是否需要 live DeepSeek。
- 是否触及 external framework / subagent / PoC。

## 退出结论

process-1 完成。可以进入 v2.6 rc。

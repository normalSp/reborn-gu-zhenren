# v2.6.0 启动审查与范围冻结

日期：2026-05-24
状态：completed；D-260 已批准并执行

## 当前事实

- 当前完成里程碑：`v2.5.0`。
- 当前分支：`codex/v260-startup-private-canon-eval-archive-prep`。
- 当前 save format：`SAVE_FORMAT_VERSION = 25`。
- 当前 DeepSeek 模型：`deepseek-v4-flash`。
- 当前 v2.6 前置输入：`v250_b3_v26_private_canon_eval_archive_admission_checklist_v1`。

## 范围冻结

v2.6 建议冻结为：

`private canon / eval archive / job queue / replay archive 工程预备`

允许讨论和设计：

- job schema。
- replay archive schema。
- redacted archive envelope。
- cost/cache/retry/prompt hash/evaluator version metadata。
- private canon classifier interface。
- manual review chain。
- 最小自有 report-only schema checker，前提是 D-260-007 获批。

不允许默认进入：

- backend/BFF/service。
- private canon service 或 eval archive service。
- live DeepSeek 或 DeepSeek visible lore/RAG。
- runtime agent。
- save field / `SAVE_FORMAT_VERSION` 变更。
- 知识库正文、runtime canon、真实 hidden/private body。
- MiroFish export/intake。
- 外部框架 PoC、依赖、subagents。
- 正式地点、阵营、身份、奖励、NPC 生死。

## MiroFish Need

初始：`not_needed`。

理由：

- v2.6 startup 和建议 checker 均使用 synthetic fixtures。
- 不引入真实原著事实、命名 NPC、hidden-adjacent、方源公开证据或正式 lore 结论。

升级 blocking 的触发条件：

- 任一 fixture 需要真实 lore body。
- 任一 checker 样本需要命名 NPC、L4/L5 fact anchor、hidden-adjacent 或 formal conclusion。
- 任一设计尝试写知识库正文、runtime canon 或 DeepSeek visible lore/RAG。

## Player Advocate / Live Probe

默认豁免。

是否调用 live DeepSeek：否。

理由：

- 无玩家可见 runtime/UI 变化。
- 无 DeepSeek prompt/context/model/authority 变化。
- 无叙事文本生成链路变化。
- v2.6 建议 checker 只验证 synthetic envelope 和 boundary assertions。

## 启动结论

v2.6 已按前置授权包完成。a1/a2/b1/b2/b3/process-1/rc 均已收束；checker 已实现并通过，未越过 F-260。

# v3.4.0-b3 30轮 deterministic 漂移检查记录

日期：2026-05-24
状态：completed
版本：`v340_b3_deterministic_30_round_drift_gate_v1`

## 范围

本检查覆盖 v3.4 transient AgentProposal runtime first cut。

检查目标：

- candidate 不漂移成 fact。
- no save write。
- no formal authority。
- no hidden / L4 / L5。
- no live DeepSeek。
- no `runFingerprint`。

## 执行方式

在 `src/engine/v340-transient-agent-proposal.test.ts` 中通过 30 轮 deterministic loop 检查：

- `turn = 1..30`。
- `previewLane = round`。
- 使用 synthetic public event refs。
- 每轮重建 `buildV340TransientAgentProposal()`。
- 每轮检查 proposal 均为 `approved_expression_candidate`。
- 每轮检查 audit 仍是 no-save/no-live。
- 每轮检查 boundary 不出现完成态正式结果。

## 已跑命令

- `npm run check:v340-transient-agent-proposal -- --reporter=dot`：1 file / 5 tests passed。
- `npx tsc --noEmit --pretty false`：passed。
- `npm run test:e2e -- tests/e2e/v340-runtime-agent-proposal.spec.ts`：1 passed。

## 结果

- 30/30 deterministic rounds stable。
- P0：0。
- P1：0。
- P2：0。
- `liveDeepSeekCalled=false`。
- `saveWritePolicy=no_save_field_no_migration_no_runFingerprint`。

## 结论

30 轮 deterministic 漂移门通过。v3.4 first cut 没有把 candidate 漂移成 fact，也没有写入存档或打开正式世界权威。

# v3.4.0 D-340 Route 2 授权记录

日期：2026-05-24
状态：approved

## 用户决策

用户批准：

- D-340-001 至 D-340-012：全部批准。
- F-340-002 至 F-340-012：全部继续 `future_gate_required`。
- F-340-001：仅限最窄子集打开，即 `L2/L3 small-area transient AgentProposal runtime path`。

## 授权含义

v3.4 主线从前置路线决策状态切换为 Route 2：

最小 `transient proposal-only` runtime first cut。

允许：

- 新增本地 deterministic AgentProposal contract。
- 新增 WorldCore post-check。
- 在世界面板中显示 L2/L3 小区域候选表达。
- 做 unit/schema/post-check/rejection/old-save/rollback/10 轮 Player Advocate/30 轮 deterministic 验收。

不允许：

- 新增 save field、migration、`SAVE_FORMAT_VERSION` bump 或 `runFingerprint`。
- 调用 live DeepSeek，或改 prompt/context/model/authority。
- MiroFish export/intake、真实原著事实、命名 NPC、hidden-adjacent、正式 lore。
- backend/BFF/service/job queue/eval archive/cloud save。
- external framework PoC/dependency/read-only scan/subagent/patch artifact。
- L4/L5 runtime、天道/宿命 runtime 裁决、原著关键人物 runtime agent。
- 正式地点、阵营、身份、奖励、NPC 生死、通缉、招揽、封锁。

## 执行结论

本记录授权 v3.4 在 `/goal` 内连续完成，但只在上面的窄口中行动。任何触发 `v3.4.0-例外停机清单.md` 的事项必须停止并回到用户决策。

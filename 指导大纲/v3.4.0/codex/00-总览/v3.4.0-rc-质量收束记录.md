# v3.4.0-rc 质量收束记录

日期：2026-05-24
状态：completed locally

## 范围

v3.4 完成最小 `transient proposal-only` runtime first cut。

## 当前完成

- D-340-001 至 D-340-012 已获用户批准。
- F-340-001 仅打开最窄 transient proposal-only 子集。
- F-340-002 至 F-340-012 继续 `future_gate_required`。
- Engine / UI / e2e / docs / PA / deterministic 证据已落地。
- `test:e2e:long` 已固定 `--workers=4`，避免 12 workers 并发下旧长测规格偶发 60 秒超时。

## 已验证

- `npm run check:v340-transient-agent-proposal -- --reporter=dot`：passed。
- `npx tsc --noEmit --pretty false`：passed。
- `npm run test:e2e -- tests/e2e/v340-runtime-agent-proposal.spec.ts`：passed。
- `npm run check:player-advocate-gate -- 指导大纲/v3.4.0/codex/00-总览/v3.4.0-b3-Player-Advocate-10轮走查记录.md 10`：passed，10/10，理解率 100%。
- `npm test -- --reporter=dot`：153 files / 819 tests passed。
- `npm run build`：passed。
- `npm run check:runtime-assets`：passed，163 files。
- `npm run check:qingmao-assets`：passed，23 entries。
- `npm run check:player-visible-copy`：passed，291 files scanned。
- `npm run test:e2e`：70 tests passed。
- `npm run check:stale-entrypoints`：passed，P0/P1/P2=0/0/0。
- `git diff --check`：passed。
- `npm run test:e2e:long`：first run with default 12 workers timed out on three older long specs; after fixing the long script to `--workers=4`, rerun passed 14/14.
- `npm run check:production-preview`：passed。

## 待远端补充

- commit / push / GitHub Actions。

## 边界结论

v3.4 不开放：

- save field / `SAVE_FORMAT_VERSION` / migration / `runFingerprint`。
- live DeepSeek / prompt-context-model-authority / DeepSeek visible lore-RAG。
- MiroFish export/intake / real lore / named NPC / hidden-adjacent。
- backend/BFF/service。
- external framework PoC/dependency/subagent。
- L4/L5 / Heaven-Fate runtime。
- formal location/faction/identity/reward/NPC life-death/warrant/recruitment/blockade。
- public wording / release / EdgeOne。

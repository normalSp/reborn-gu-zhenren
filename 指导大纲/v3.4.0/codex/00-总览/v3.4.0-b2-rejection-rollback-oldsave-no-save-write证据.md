# v3.4.0-b2 rejection / rollback / old-save / no-save-write 证据

日期：2026-05-24
状态：completed
冻结版本：`v340_b2_rejection_rollback_oldsave_no_save_write_evidence_v1`

## Rejection 证据

`postCheckV340AgentProposal()` 阻断：

- save write。
- save field。
- save-format bump。
- migration。
- `runFingerprint`。
- live DeepSeek。
- prompt/context/model/authority。
- MiroFish / named NPC / hidden-adjacent。
- backend / external framework / dependency / subagent。
- L4/L5 / Heaven-Fate verdict。
- formal location/faction/identity/reward/NPC life-death/warrant/recruitment/blockade。

聚焦测试覆盖：

- `rejects save writes, formal authority, live calls, L4/L5, and hidden-adjacent proposals`
- `routes future framework or DeepSeek visible requests to user decision without approving display`

## Rollback 证据

v3.4 runtime first cut 没有新增 store field/action/slice，也不写 localStorage 之外的任何新保存对象。关闭或删除 UI tab 后不会留下 save residue。

证据：

- `RuntimeAgentProposalPanel` 只用 React local state `previewLane` 做候选重排。
- `buildV340TransientAgentProposal()` 是纯函数。
- E2E 断言 `agentProposalState` / `runtimeAgentState` / `runFingerprint` 均为 `undefined`。

## Old-save 证据

`buildV340TransientAgentProposal({})` 可以在旧档式空输入上工作：

- status 为 `evidence_light_transient_path`。
- 仍只生成 transient 候选。
- 不需要 migration/defaults。
- 不访问新增持久字段。

聚焦测试覆盖：

- `works on old-save-like input without requiring migration or new persistent state`

## 结论

b2 通过。v3.4 first cut 的 rollback/old-save/no-save-write 证据成立。

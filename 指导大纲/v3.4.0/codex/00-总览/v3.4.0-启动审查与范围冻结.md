# v3.4.0 启动审查与范围冻结

日期：2026-05-24
状态：completed

## 启动条件

v3.3 已完成并给出以下结论：

- `f300ReopenDecisionPackageComplete=true`。
- `runtimeImplementationApproved=false`。
- 当前证据足以进入 v3.4 路线选择。
- v3.3 不替 v3.4 授权。

因此 v3.4 先开专家团启动会。随后用户已批准 Route 2，允许最小 transient proposal-only runtime first cut。

## 本启动包允许

- 对比路线 1 / 2 / 3 的收益和风险。
- 说明长期路线对 v3.4-v3.6 的原设定。
- 写 v3.4 前置授权包、例外停机清单、测试矩阵、MiroFish need level。
- 更新 dashboard、AGENTS、PROJECT-STATE、上下文交接和 skill sync。
- 运行文档自检、提交、推送和 CI。

## 启动阶段不允许

- 未获用户批准前的 runtime agent implementation。
- runtime/source/UI/store/prompt/save changes。
- save field / `SAVE_FORMAT_VERSION` / `runFingerprint`。
- live DeepSeek / prompt-context-model-authority changes / DeepSeek visible lore-RAG。
- MiroFish export/intake / real original facts / named NPC / hidden-adjacent / formal lore。
- backend/BFF/service/job queue/eval archive/cloud save。
- external framework PoC/dependency/read-only scan/subagent/patch artifact。
- L4/L5 runtime / HeavenWill-Fate runtime adjudication / original key figure runtime agent。
- formal location/faction/identity/reward/NPC life-death。
- public wording / release / EdgeOne / legal boundary changes。

## 路线冻结

v3.4 已从 `route_pending` 切换为 Route 2。

候选路线：

| 路线 | 状态 | 说明 |
|---|---|---|
| Route 1 | not_selected | 继续 report-only hardening |
| Route 2 | approved | 单独打开最小 transient proposal-only runtime first cut |
| Route 3 | not_selected | defer 到 v3.5+ |

## 当前结论

v3.4 startup 条件满足，且 Route 2 已获用户批准。implementation 只允许进入 F-340-001 的最小 transient proposal-only 窄口。

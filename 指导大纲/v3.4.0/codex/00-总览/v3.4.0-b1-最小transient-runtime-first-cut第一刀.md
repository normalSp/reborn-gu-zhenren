# v3.4.0-b1 最小 transient runtime first cut 第一刀

日期：2026-05-24
状态：completed
冻结版本：`v340_b1_transient_proposal_runtime_first_cut_v1`

## 实现内容

新增：

- `src/engine/v340-transient-agent-proposal.ts`
- `src/engine/v340-transient-agent-proposal.test.ts`
- `src/components/game/RuntimeAgentProposalPanel.tsx`
- `tests/e2e/v340-runtime-agent-proposal.spec.ts`
- `package.json` script：`check:v340-transient-agent-proposal`

修改：

- `src/components/game/WorldHubPanel.tsx`
- `package.json` long e2e 覆盖增加 v3.4 spec。

## 行为

Runtime 现在可以在世界面板 `意图` tab 中显示 L2/L3 小区域临时候选：

- 外缘短工联络人。
- 山路巡看者。
- 临时市场跑腿接触。

这些对象全部通过本地 `postCheckV340AgentProposal()`，只显示为候选表达。

## 未做

- 没有新增 store slice。
- 没有新增持久字段。
- 没有 save migration。
- 没有 `SAVE_FORMAT_VERSION` bump。
- 没有 `runFingerprint`。
- 没有 live DeepSeek。
- 没有 MiroFish。
- 没有 backend/BFF/service。
- 没有 external framework / dependency / subagent。
- 没有正式地点、阵营、身份、奖励、NPC 生死。

## 已跑聚焦验证

- `npm run check:v340-transient-agent-proposal -- --reporter=dot`：1 file / 5 tests passed。
- `npx tsc --noEmit --pretty false`：passed。
- `npm run test:e2e -- tests/e2e/v340-runtime-agent-proposal.spec.ts`：1 passed。

## 结论

b1 通过。v3.4 已完成最小 runtime first cut，但仍是 transient proposal-only，不是完整 runtime agent society。

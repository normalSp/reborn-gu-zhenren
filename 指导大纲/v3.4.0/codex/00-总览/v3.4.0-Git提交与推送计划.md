# v3.4.0 Git 提交与推送计划

日期：2026-05-24
状态：startup

## 分支

当前 v3.4 startup 分支：

`codex/v340-startup-runtime-route-decision`

## stage 规则

- 显式 stage 本次 v3.4 文档、入口同步和 handoff。
- 不使用 `git add -A`。
- 不 stage 与 v3.4 startup 无关的脏文件。

## 提交边界

建议提交：

`docs: 开启v3.4 runtime路线决策会`

本提交只包含：

- v3.4 startup docs。
- dashboard / AGENTS / PROJECT-STATE / handoff 同步。
- skill sync audit 记录。

不包含：

- runtime/source/UI/store/prompt/save changes。
- scripts/tests/evals implementation。
- live DeepSeek artifacts。
- MiroFish export/intake。
- backend/external framework PoC。

## 推送与 CI

推送后等待 GitHub Actions。

本批为文档启动包，不运行 runtime 测试；但 CI 若触发，应等待 deterministic quality gate 完成。

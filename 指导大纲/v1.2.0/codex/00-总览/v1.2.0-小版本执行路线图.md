# v1.2.0 小版本执行路线图

状态：v1.2.0 本地开发完成；D-120 至 D-134 均已落地
日期：2026-05-20

## 总路线

v1.2 采用先制度后 runtime 的顺序：

`a0/a1/a2 -> b1/b2/b3/b4 -> process-1/process-2 -> rc`

## a0 启动审查与范围冻结

状态：本启动包已建立。

产出：

- 专家团启动会纪要。
- 启动审查与范围冻结。
- 总体开发大纲。
- 需求决策池。
- 测试矩阵。
- MiroFish 需求判断。
- Git 计划。

通过标准：

- 不写 runtime。
- 不新增 save 字段。
- 不扩大 DeepSeek 权限。
- 明确 a1 后需要用户拍板的事项。

## a1 低阶生存经济 save-format 设计门禁

状态：已通过；D-121-001 至 D-121-007 已由用户批准。

目标：

- 冻结 v1.2 authority table。
- 严肃评估是否需要 `SAVE_FORMAT_VERSION = 24`。
- 决定第一刀使用 projection-only 还是最小 ledger。
- 设计字段表、迁移矩阵、默认值和测试门。

当前记录：

- `v1.2.0-a1-低阶生存经济save-format设计门禁.md`

执行口径：

- b1 可写 projection-only runtime。
- b1 不新增字段。
- b1 不把 `SAVE_FORMAT_VERSION = 24` 当成默认批准。

a1 后用户已拍板：

- D-121-001：b1 不 bump；v24 只作为 b2 可再批路线。
- D-121-002：若后续新增持久经济字段，只允许单一 `survivalEconomyState`；b1 不新增。
- D-121-003：b1 第一刀采用 projection-only。
- D-121-004：b1 禁止真实消耗、维护周期、炼蛊失败代价。
- D-121-005：v1.2 默认禁止正式交易、价格、库存、买卖。
- D-121-006：runtime 小版本 Player Advocate 20 轮；rc 60/80 分级。
- D-121-007：live probe 当前不执行，b1/b2 稳定后另批。

## a2 主题切片、知识库与测试矩阵门禁

状态：已完成；允许 b1。

目标：

- 从 v0.15 三包和全书基础包中提炼 v1.2 topic slices。
- 只吸收 rewritten summary、source pointer、visibility、promotionStatus 和测试样本。
- 冻结 `V12-*` 测试矩阵。
- 继承 v1.1 D-025 hidden-name echo 和 DeepSeek 越权样本。

通过标准：

- MiroFish 使用层级清楚。
- 全书基础包不进入 runtime/canon/DeepSeek visible context。
- 所有新样本 triage 到 `current_matrix`、`future_sample_pool` 或 `discarded`。

当前记录：

- `v1.2.0-a2-主题切片知识库与测试矩阵门禁.md`

## b1 补给/喂养/维护缺口第一刀

状态：已完成本地第一刀。

执行结果：

- projection-only。
- 利用 v1.1 route/location 状态解释路线补给压力。
- UI 显示缺口、风险和下一步。
- DeepSeek 只写压力，不写资源到账。
- 不 bump save format，不新增 `survivalEconomyState`。
- 不写正式库存、价格、交易、消耗、炼蛊结果、材料奖励或元石账本。

当前记录：

- `v1.2.0-b1-低阶生存经济projection-only第一刀.md`
- `v1.2.0-b1-Player-Advocate-20轮走查记录.md`
- `v1.2.0-b1-长线叙事漂移检查记录.md`

Player Advocate：20 轮已通过，理解率 100.0%，confused=0。
长线叙事漂移：T0-lite 已通过，live probe 豁免并保留为后续另批。

## b2 最小 ledger 与 BGM runtime 晋升

状态：已完成本地实现与验证。

执行口径：

- `SAVE_FORMAT_VERSION = 24`。
- 新增单一聚合对象 `survivalEconomyState`。
- ledger 只记录压力、来源、证据和禁止项。
- 根目录 BGM 音频做 runtime 晋升复核；runtime 只引用 `public/audio/...` 和 manifest。
- 建立 `Git分支切换与推送制度.md`，每个新小版本/专项默认先切语义分支。

当前记录：

- `v1.2.0-b2-最小survivalEconomyState-ledger.md`
- `v1.2.0-b2-BGM-runtime晋升专项.md`
- `v1.2.0-b2-Player-Advocate-20轮走查记录.md`
- `v1.2.0-b2-长线叙事漂移检查记录.md`

验证结果：focused unit、type check、focused e2e、Player Advocate 20 轮 gate、runtime/Qingmao/player-visible-copy scans、full unit、build、full e2e、long e2e 与 production-preview smoke 均已通过。

不得默认做正式库存、价格表、商店库存、交易结算、真实消耗、炼蛊成功/失败、黑市、委托或稳定套利。

## b3 炼养用准备与失败代价第一刀

状态：已完成本地实现与验证。

执行口径：

- 显示残方不完整、材料缺口、失败风险和维护周期。
- 不判定炼成。
- 不判定真实失败结算。
- 不授予蛊方、材料、蛊虫或成功率。
- 不 bump save format，不新增持久字段，复用 `survivalEconomyState` 最小 ledger。

Player Advocate：由 rc 80 轮集成走查覆盖。

当前记录：

- `v1.2.0-b3-炼养用准备与失败代价第一刀.md`

## b4 商队/交易窗口边界第一刀

状态：已完成本地实现与验证。

执行口径：

- 只做询价、公开理由、身份担保、风险提示和拒绝理由。
- 不成交。
- 不写价格表。
- 不开放库存。
- 不加入商队。
- 不开放黑市/委托收益。

Player Advocate：由 rc 80 轮集成走查覆盖。

当前记录：

- `v1.2.0-b4-交易窗口边界第一刀.md`

## process-1 经济反刷、save 兼容与回滚

状态：已完成。

目标：

- 反复刷补给/食料/资源不会稳定套利。
- 新旧档兼容。
- save format 决策和 migration 证据完整。
- Git 回滚点清楚。

当前记录：

- `v1.2.0-process-1-经济反刷save兼容与回滚复核.md`

## process-2 长线漂移与知识库复核

状态：已完成；live probe 未执行。

目标：

- 执行 deterministic drift soak。
- 继承 D-025 的 hidden-name echo、术语边界和 DeepSeek 越权样本。
- live probe 如需执行，必须由用户单独批准成本、模型、样本和轮次。

当前记录：

- `v1.2.0-process-2-长线漂移与知识库复核.md`

## rc 质量收束

状态：已完成本地质量收束。

默认建议：

- 无新增持久经济字段：rc Player Advocate 60 轮。
- 新增持久经济字段或真实消耗/ledger：rc Player Advocate 至少 80 轮。
- 如新增交易/价格/库存结算，需另开更高强度门禁，不能直接按普通 rc 收束。

本轮执行：

- 因 v24 `survivalEconomyState` 已新增，rc 使用 80 轮 Player Advocate。
- v1.2 完成口径为“低阶生存经济第一阶段”，不称完整经济系统。
- 硬停条件仍包括正式经济字段、真实结算、DeepSeek 权限、公开口径或 live probe。
- 最终验证通过 full unit、type check、build、runtime/Qingmao/player-visible-copy scans、full e2e、long e2e、production-preview smoke。

# v1.2.0-b1 长线叙事漂移检查记录

状态：T0-lite 已通过；live probe 豁免
日期：2026-05-20

## 结论

b1 触碰低阶经济压力和玩家可见叙事边界，因此必须执行或豁免长线叙事漂移 gate。本阶段采用 T0-lite deterministic 检查，不执行 live DeepSeek probe。

理由：

- b1 不新增 DeepSeek prompt 权限，不调用新的 live narrative chain。
- b1 不新增存档字段，不写 `survivalEconomyState`，不产生跨回合经济账本。
- b1 的漂移风险主要是 UI/engine 把压力误写成结算、奖励、价格、交易或地点开放。
- 这些风险已由 focused unit、focused e2e、Player Advocate 20 轮和 player-visible-copy 扫描覆盖。

## 检查项

| 检查 | 结果 | 证据 |
|---|---|---|
| 世界状态不漂移 | 通过 | 投影 helper 返回 `statePatchApplied: false`、`canWriteSave: false` |
| DeepSeek 权限不漂移 | 通过 | 投影边界写明 `deepSeekAuthority: no_new_authority` |
| 经济压力不漂移为奖励 | 通过 | forbidden writes 覆盖 `material_reward`、`currency_delta`、`gu_reward` |
| 交易窗口不漂移为成交 | 通过 | E2E 断言“不写价格”、无 `formal_market_trade` |
| 低阶原著边界不漂移 | 通过 | 黑市/委托/宝黄天/高阶资源仍为 forbidden/deferred |
| 玩家下一步不丢失 | 通过 | Player Advocate 20 轮理解率 100.0%，confused=0 |

## live probe 豁免

本阶段不执行 live DeepSeek drift probe。豁免理由：

- D-121-007 明确 live probe 等 b1/b2 稳定后再另批。
- b1 没有新增 DeepSeek authority 或成本项。
- b1 的风险是本地投影边界，不是 live narrative 多轮生成质量。

若 b2 新增 `survivalEconomyState`、经济 ledger、真实消耗、价格/交易窗口或 DeepSeek 经济提示，应重新评估是否需要 T1 小规模 live probe。

是否通过本阶段长线叙事漂移 gate：通过。

# v2.7.0 启动审查与范围冻结

日期：2026-05-24
状态：completed；D-271 approved/executed

## 启动输入

| 输入 | 状态 | v2.7 采用 |
|---|---|---|
| `v2.6.0-rc-质量收束记录.md` | completed | v2.6 无 runtime/DeepSeek/backend/MiroFish 越权，可进入 v2.7 startup |
| `v2.6.0-b3-v2.7-v3.0-agent-infra准入清单.md` | completed | v2.7 主线为低阶多 NPC / 小势力 Agent Lab 扩展 |
| `v2.0-v3.0-AgentLab到RuntimeAgent总体大纲.md` | current long-route baseline | v2.7 属于实验室扩展，不是 runtime agent |
| `Agent-Framework-Landscape-2026吸收矩阵.md` | current landscape baseline | 继续参考，但 v2.7 不做 PoC、依赖、read-only scan |
| `前置批量审批与例外停机制度.md` | active process | v2.7 使用 D-271 前置授权包 |
| `Skill同步审计制度.md` | active process | startup 记录 skill sync 与文件健康门 |

## 范围冻结

v2.7 冻结为：

`低阶多 NPC / 小势力 Agent Lab 样板扩展`

核心对象：

- L1 路人、商贩、巡逻、兽群等聚合反应。
- L2 小队成员、次要 NPC、小势力外围成员的 candidate memory。
- L3 当前场景关键 NPC 的意图候选和表达候选。
- 小势力 pressure source，不是正式阵营。
- L5 天道/宿命压力，只作为宏观约束，不裁决结局。

核心场景族：

`outer_edge_multi_npc_small_faction_synthetic_v1`

## 默认不做

- 不进入 runtime agent。
- 不做后端、BFF、service、真实 job queue 或云存档。
- 不改 save schema，不 bump `SAVE_FORMAT_VERSION`，不新增 `runFingerprint`。
- 不调用 live DeepSeek，不改 prompt/context/model/authority。
- 不做 MiroFish export/intake，不吸收真实原著事实。
- 不接外部 agent framework PoC、依赖、read-only scan、subagents、patch artifact。
- 不写知识库正文、runtime canon、正式地点/阵营/身份/奖励/NPC 生死。

## MiroFish need level

初始：`not_needed`。

理由：

- 本轮使用 synthetic archetype。
- 不触碰真实命名 NPC、真实势力结论、hidden-adjacent 或正式 lore。
- 不写知识库正文或 runtime canon。

升级为 `blocking` 的触发：

- 命名 NPC。
- 真实势力、真实地点或正式阵营结论。
- hidden-adjacent、方源公开证据或 L4/L5 原著锚点。
- 正式 lore 结论。
- 任何内容进入知识库正文、runtime canon、DeepSeek visible context 或正式测试事实。

## 启动出口

- D-271-001 至 D-271-012：已批准/已执行。
- F-271-001 至 F-271-012：保持 `future_gate_required`。
- 已进入并完成 a1 场景模型设计门禁。

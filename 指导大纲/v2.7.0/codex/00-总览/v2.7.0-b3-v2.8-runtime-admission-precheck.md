# v2.7.0-b3 v2.8 runtime admission precheck

日期：2026-05-24
状态：completed

## 结论

v2.7 只给 v2.8 提供准入讨论材料，不批准 runtime agent。

## 可进入 v2.8 讨论的能力

| 能力 | v2.8 讨论条件 |
|---|---|
| L1 public pressure aggregation | 仍需证明不会写正式地点/阵营/奖励，不污染 save |
| L2 candidate memory | 必须有 TTL、撤销、审计、manual review、WorldCore rejection |
| L3 intent/expression candidate | 必须只给表达候选，不给 outcome、奖励、NPC 生死或 formal credential |
| L5 macro pressure | 继续保持 offline/report-only；runtime 前必须单独设计 Heaven/Fate macro director gate |
| self-learning candidate patch | 只能 report-only；任何 patch artifact、skill/runtime/canon 写入都需未来门禁 |

## 必须保持 future gate 的事项

- runtime agent / WorldCore runtime 接入。
- 新 save field、`SAVE_FORMAT_VERSION` bump、`runFingerprint`。
- live DeepSeek、DeepSeek authority/context/model/prompt 变化。
- backend/BFF/service/job queue/eval archive service。
- MiroFish export/intake、真实原著事实、命名 NPC、hidden-adjacent、正式 lore 结论。
- 外部框架 PoC/dependency/read-only scan/subagents。
- 正式地点、阵营、身份、奖励、NPC 生死、canon promotion。

## v2.8 建议

v2.8 启动会应先回答：

- 是否只做 runtime admission 设计门禁，而不实现 runtime agent。
- 是否需要 Player Advocate/live DeepSeek 预设档位。
- 是否需要 MiroFish blocking topic slice。
- 是否需要把 L2/L3 ability 分成 `admissible`, `lab_only`, `rejected` 三类。

未触发 F-271 例外停机清单。

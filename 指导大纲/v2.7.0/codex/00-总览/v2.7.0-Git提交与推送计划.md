# v2.7.0 Git 提交与推送计划

日期：2026-05-24
状态：completed locally；pending final commit/push

## 当前分支

`codex/v270-startup-multi-npc-agent-lab`

## 基线

从 `codex/v260-startup-private-canon-eval-archive-prep` 的 v2.6 complete 基线切出。

## 提交边界

### startup commit

范围：

- 新增 `指导大纲/v2.7.0/codex/00-总览/` startup 文档。
- 更新 `指导大纲/项目仪表盘.md`。
- 更新 `AGENTS.md`。
- 更新 `.codex/skills/reborn-expert-council/references/PROJECT-STATE.md`。
- 必要时更新本地 skill current override。

不包含：

- runtime/source/UI/store/prompt/save。
- scripts/tests/npm script/artifacts，除非 D-271-007 后续获批。
- MiroFish export。
- external framework PoC/dependency/subagents。

### 后续阶段 commit

本轮 D-271 已获批，v2.7 完成 commit 建议合并为一个收束提交：

- a1/a2 design gate 文档。
- b1/b2/b3 runner、fixtures、report artifact。
- process/rc、skill/dashboard/PROJECT-STATE/AGENTS 同步。

## Stage 规则

- 禁止 `git add -A`。
- 只 stage 明确路径。
- 任何外部资产、MiroFish raw、`.env`、正式存档、未批准 artifact 不得 stage。

## 推送与 CI

v2.7 完成后推送当前分支并检查 GitHub Actions。

不自动部署 EdgeOne，不改 branch protection，不自动合并。

## 完成定义

v2.7 完成需要：

- 文档自检通过。
- 敏感边界扫描只命中禁止/future gate/测试语境。
- Skill sync audit 写入。
- Dashboard/PROJECT-STATE/AGENTS 同步。
- `npm run check:v270-multi-npc-agent-lab` 通过。
- 提交、推送、CI 可追踪。

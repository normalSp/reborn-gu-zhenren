# v1.6.0-b1 MiroFish 基础包 inventory checker report-only 第一刀

状态：completed
日期：2026-05-21
性质：工具化第一刀；只读 report-only；不改 runtime、不改基础包、不建知识库条目、不进 CI hard gate

## 前置批准

用户已批准 D-162-001 至 D-162-010：

- `check:mirofish-base-pack-inventory` 只读 `指导大纲/vMiroFish/基础包/` 的导出文件。
- 以 manifest / coverage / 2340 个章节包三层检查为第一刀。
- 固定当前基线：2340 章、`ri_lw_ch_0001.json` 至 `ri_lw_ch_2340.json` 连续、`sourcePointersBad = 0`。
- forbidden text key 列表：`quote`、`originalText`、`excerpt`、`verbatim`、`rawText`、`sourceText`。
- P0/P1/P2/Info 严重级别有效。
- report-only 输出到 `artifacts/v1.6.0/mirofish-base-pack-inventory/<timestamp>/report.json`。
- 脚本只能报告，不能自动修复、删除、晋升。
- inventory 通过只代表 archive inventory 通过，不代表知识库、canon 或 DeepSeek 可见通过。
- 第一版不加入 CI hard gate。

## 落地文件

- `scripts/v160-governance-utils.mjs`
- `scripts/check-mirofish-base-pack-inventory.mjs`
- `package.json` script：`npm run check:mirofish-base-pack-inventory`

## 检查范围

| 层 | 检查 |
|---|---|
| manifest | authority、visibility、redaction、章节总数 |
| coverage | packages、continuity、sourcePointers、forbidden key 摘要 |
| chapter files | 2340 文件连续性、JSON parse、forbidden text keys、authority/visibility 越权 |

## 首轮结果

首轮 report-only 检查通过：

- chapters：2340
- P0：0
- P1：0
- P2：0
- Info：1，提示基础包含 hiddenFacts，只能作为 inventory 背景

## 阶段结论

b1 完成。基础包结构可作为 archive/source-pointer inventory 继续使用；仍不能进入知识索引、runtime canon、DeepSeek visible context 或玩家可见文本。

# v1.6.0-b3 MiroFish intake 晋升链 checker report-only 第一刀

状态：completed
日期：2026-05-21
性质：工具化第一刀；只读 report-only；不自动建 intake、不自动晋升、不进 CI hard gate

## 落地文件

- `scripts/check-mirofish-intake-promotions.mjs`
- `package.json` script：`npm run check:mirofish-intake-promotions`

## 检查链条

目标链条为：

`MiroFish/source pointer -> topic-slice intake review -> RebornG-owned knowledge entry -> test matrix / draft -> future runtime gate`

任一节点缺失时，脚本只能报告，不能自动补链。

## 检查规则

- 引用了 MiroFish 或 `ri_lw_ch_*` source pointer 的知识条目必须有 `intakeReviewRefs`。
- `intakeReviewRefs` 指向的项目内文件必须存在。
- `testSampleRefs` 必须能在 `v1.6.0-测试矩阵.md` 中找到。
- `allowedUses` 不得包含任何高风险用途。
- v1.6 不允许 `runtime_promoted`。

## 首轮结果

当前知识索引没有已晋升条目，因此晋升链没有断裂项：

- entries：0
- matrixIds：22
- P0：0
- P1：0
- P2：0
- Info：1，提示当前没有可晋升条目

## 阶段结论

b3 完成。后续 v1.7-v2.0 若开始主题切片或小范围 canon 草案，必须让这个 checker 保持绿，不能让资料绕过 intake review 直达 runtime/canon/DeepSeek。

# v1.6.0-b2 知识索引边界 checker report-only 第一刀

状态：completed
日期：2026-05-21
性质：工具化第一刀；只读 report-only；不自动补字段、不自动晋升、不进 CI hard gate

## 落地文件

- `scripts/check-knowledge-index-boundaries.mjs`
- `package.json` script：`npm run check:knowledge-index-boundaries`

## 检查对象

默认检查 `指导大纲/知识库/蛊真人/` 下的 JSON / Markdown 条目，跳过 `README.md` 骨架说明。

## 最小字段

每个条目必须表达：

- `id`
- `kind`
- `summary`
- `sourcePointers`
- `visibility`
- `promotionStatus`
- `allowedUses`
- `forbiddenUses`
- `mirofishRefs`
- `intakeReviewRefs`
- `testSampleRefs`
- `lastReviewedVersion`
- `reviewNotes`

## 边界规则

- `visibility` 必须属于 `public`、`player_visible`、`hidden_ref_only`、`private`、`deferred`。
- `promotionStatus` 必须属于 a1 批准的九态。
- `allowedUses` 不得包含 `deepseek_visible_context`、`player_visible_hidden_body`、`runtime_authority`、`canon_promotion`、`formal_location`、`formal_faction`、`formal_reward`、`npc_life_death`、`public_release_wording`。
- `forbiddenUses` 至少应覆盖 DeepSeek 可见、玩家可见 hidden body、runtime authority 三类核心禁用项。
- v1.6 不接受任何 `runtime_promoted` 知识条目。

## 首轮结果

当前知识索引仍是 README-only 骨架：

- entries：0
- P0：0
- P1：0
- P2：0
- Info：1，提示空骨架在 v1.6 report-only 阶段有效

## 阶段结论

b2 完成。知识库骨架可继续作为后勤入口；后续任何条目进入前必须按 schema 填写，并经 intake review / 测试矩阵 / 用户决策链条约束。

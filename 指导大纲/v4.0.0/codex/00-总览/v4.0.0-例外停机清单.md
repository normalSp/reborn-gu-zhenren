# v4.0.0 例外停机清单

状态：startup pending user decision。

若用户批准 D-400，Codex 可在授权包内自动推进；但以下任一条件触发时必须立即停止 `/goal` 自动推进并报告用户。

## 持久化与 runtime

- 需要新增或修改 save field。
- 需要 bump `SAVE_FORMAT_VERSION`。
- 需要 migration、old-save 写入或隐式持久状态。
- 需要新增 `runFingerprint`。
- 需要把 v4.0 checker / envelope 接进 runtime、store、UI、prompt 或玩家存档。

## 高阶战斗

- 需要实现 theater UI、杀招栈 runtime、仙蛊屋状态、环境破坏结算。
- 需要新增命中、伤害、资源成本、反噬、NPC 生死、奖励或地点破坏裁决。
- 表现层想暗示未开放的仙蛊、杀招、传承、地点摧毁或战斗结果。

## 天道/宿命/L4/L5

- 需要 HeavenWill/Fate runtime 裁决。
- 需要 L5 macro director 直接决定玩家结局。
- 需要原著关键人物、方源、尊者或命名 NPC 的 runtime agent。
- 需要真实 era anchor、hidden-adjacent material、formal lore conclusion。

## DeepSeek / MiroFish / 知识库

- 需要 live DeepSeek。
- 需要改 DeepSeek prompt/context/model/authority。
- 需要 DeepSeek visible lore/RAG。
- 需要 MiroFish topic-slice、全书基础包、knowledge-index body、runtime canon 或 hidden/private body。

## 外部框架 / 后端 / 子代理

- 需要 backend/BFF/service/job queue/eval archive service/cloud save。
- 需要 Hermes、LangGraph、Mastra 或其他外部框架 PoC、dependency、vendored subset、read-only scan、patch artifact、subagents。
- 需要外部工具读取/写入 RebornG 文件、执行命令或操作 git。

## 发布与 Git

- 需要 public wording、legal/release boundary、EdgeOne deployment。
- 需要合并 `main`、改 branch protection、force push、rewrite history。
- CI/测试失败且需要改变计划、降低门槛或扩大 scope。

# v4.0.0 测试矩阵

状态：startup pending user decision。

## 启动文档自检

```powershell
git diff --check
rg -n "v4.0.0|D-400|F-400|HeavenWill|Fate|theater|杀招栈" 指导大纲/v4.0.0 指导大纲/项目仪表盘.md AGENTS.md .codex/skills/reborn-expert-council/references/PROJECT-STATE.md
rg -n "runtime|SAVE_FORMAT_VERSION|live DeepSeek|MiroFish|backend|external framework|NPC 生死|正式地点|正式奖励|L4|L5" 指导大纲/v4.0.0/codex/00-总览
```

敏感词命中必须只出现在“禁止、future gate、需要用户决策、设计候选、远期目标”语境中。

## 后续若 D-400 获批

建议新增：

```powershell
npm run check:v400-high-world-readiness
```

候选覆盖：

| 样本族 | 目标 |
|---|---|
| V400-COMBAT-CANDIDATE | 高阶战斗 theater 候选只作为 envelope |
| V400-COMBAT-BLOCK | 命中/伤害/环境破坏/NPC 生死/奖励越权被拒 |
| V400-STACK | 杀招栈 cost/result 需要 engine evidence |
| V400-HOUSE | 仙蛊屋状态只做 report-only boundary |
| V400-FATE | Fate state 不写 save、不直接裁决 |
| V400-HEAVEN | HeavenWill pressure 不得成为万能失败按钮 |
| V400-L5 | L5 macro director 只输出 pressure/constraint/risk |
| V400-HIDDEN | hidden-adjacent / Fang Yuan evidence 升级 MiroFish blocking |
| V400-DEEPSEEK | DeepSeek 不得写 formal fact / lore / outcome |
| V400-PLAYER | 玩家能理解候选、压力、事实边界 |

## Player Advocate

启动会本身为文档阶段，Player Advocate 可豁免。若后续 D-400 获批并进入 b3，建议 60 轮 PA，记录：

- 是否调用 live DeepSeek：否。
- 若未来另批 live：模型、样本、轮次、成本、报告路径。

## 不运行 runtime 测试的原因

启动会只新增文档和入口索引，不改源码、存档、prompt、UI、后端、DeepSeek 或 MiroFish。

# v4.0.0 Git 提交与推送计划

状态：startup pending user decision。

## 分支

当前工作分支：

`codex/v400-startup-high-world-prep`

基线：

`codex/v390-startup-v4-safety-closure` 完成态，v3.9 completion commit `2c2366ec` / CI `26400121645` passed。

## 启动会提交范围

允许提交：

- `指导大纲/v4.0.0/codex/00-总览/`
- `指导大纲/项目仪表盘.md`
- `AGENTS.md`
- `.codex/skills/reborn-expert-council/references/PROJECT-STATE.md`
- `codex上下文信息/*v400-startup*`
- 触发 skill sync 后的本地 skill 文件更新记录。

不允许提交：

- `.env`、secret、SQLite/log/cache。
- unrelated assets。
- runtime/source/save/prompt/UI changes。
- EdgeOne deployment changes。

## 提交与推送

启动包完成记录：

- startup commit：`fc8e5132`
- GitHub Actions：`26402655412` passed

后续 D-400 获批并进入开发时：

1. `git diff --check`
2. 显式 stage 相关文件。
3. 中文提交。
4. push 到 `origin/codex/v400-startup-high-world-prep`
5. 等待 GitHub Actions。

## main 合并结论

本启动会不自动合并 `main`。

v4.0 启动包完成后，建议仍由用户单独决定是否把 v3.9/v4.0 启动文档合并进 `main`。

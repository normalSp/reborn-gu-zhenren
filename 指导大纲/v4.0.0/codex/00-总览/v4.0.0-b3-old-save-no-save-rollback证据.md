# v4.0.0-b3 old-save / no-save / rollback 证据

状态：completed。
日期：2026-05-26。
冻结标识：`v400_b3_old_save_no_save_rollback_evidence_v1`。

## 目标

证明 v4.0 在 D-400 授权包内完成时，不会影响旧档、不新增存档字段、不需要迁移、不写 runtime store，也不会因为 Auto-Theater / HeavenWill / Fate / L5 设计门禁误触发玩家存档变化。

## no-save 证据

v4.0 本阶段新增或更新的内容只包括：

- `指导大纲/v4.0.0/codex/00-总览/` 下的设计门禁、过程记录、PA、测试矩阵、rc 收束文档。
- `scripts/run-v400-high-world-readiness-check.mjs`。
- `tests/evals/v400-high-world-readiness/samples.json`。
- `artifacts/v4.0.0/high-world-readiness/.../report.json`。
- `package.json` 的 `check:v400-high-world-readiness` script。

本阶段没有新增：

- `src/store/**` 改动。
- `src/types/**` 存档字段改动。
- migration/defaults。
- `SAVE_FORMAT_VERSION` bump。
- `runFingerprint`。
- DeepSeek prompt/context/model/authority 改动。
- MiroFish intake/export。
- backend/BFF/service。
- UI/theater runtime。

## old-save 证据

因为没有 save field、migration、store slice、runtime action 或 prompt context 改动，旧档路径不需要新增兼容逻辑。v4.0 的设计文档和 report-only checker 不能读写玩家存档，也不能创建新的旧档风险。

旧档判断：

| 项目 | 状态 |
|---|---|
| 是否新增字段 | 否 |
| 是否新增 migration | 否 |
| 是否 bump `SAVE_FORMAT_VERSION` | 否 |
| 是否新增存档默认值 | 否 |
| 是否新增 runtime action 写入 | 否 |
| 是否新增 UI store slice | 否 |
| 是否改 DeepSeek prompt/context | 否 |

## rollback 证据

若需要回滚 v4.0，本阶段可回滚为文档、checker、样本、报告和 npm script 级别回滚；不存在玩家存档迁移回滚、数据修复、云端服务回滚、DeepSeek prompt 回滚或素材回滚。

回滚影响：

- 删除 v400 checker 不影响 runtime。
- 删除 v400 samples 不影响 runtime。
- 删除 v400 report artifact 不影响 runtime。
- 删除 v4.0 文档不影响 runtime，但会丢失设计门禁证据。
- 删除 package script 不影响 runtime，但会失去 v4.0 checker 入口。

## 结论

b3 old-save / no-save / rollback 证据通过。v4.0 当前阶段没有任何存档、迁移、runtime、prompt、UI 或服务侧副作用；完成 v4.0 不改变 `SAVE_FORMAT_VERSION = 25`。

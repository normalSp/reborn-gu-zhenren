# v3.9.0 启动审查与范围冻结

状态：completed locally；范围已冻结并执行。

## 输入事实

- `v3.8.0` 已完成，commit `196b46b4`，GitHub Actions `26395818611` passed。
- v3.8 只新增 transient/report-only `v380` proposal graph stability layer，不开放 save、DeepSeek、MiroFish、backend、external framework、L4/L5 或正式世界事实。
- 长期路线 `v3.0-v4.0-RuntimeAgent到高阶世界总体大纲.md` 明确 `v3.9` 是 v4.0 前安全收束。
- Git 制度要求新版本使用语义分支；当前 v3.9 分支为 `codex/v390-startup-v4-safety-closure`。

## v3.9 范围

v3.9 只做：

1. 复盘 v3.4-v3.8 transient AgentProposal 证据链。
2. 统一 F-300/F-340/F-350/F-360/F-370/F-380 类 future gate 状态。
3. 设计 v4.0 高阶战斗与天道/宿命双预备准入框架。
4. 复核 agent / WorldCore / DeepSeek / MiroFish / external framework / save-format / PA / long drift / Git mainline 边界。
5. 若获批，新增 self-owned report-only `v390` go/no-go checker 与 evidence records。
6. 更新 dashboard、AGENTS、PROJECT-STATE、handoff、skill sync audit、Git 计划。

## 非目标

v3.9 不做：

- persistent agent state。
- save field、migration、`SAVE_FORMAT_VERSION` bump、`runFingerprint`。
- live DeepSeek、prompt/context/model/authority 变更、DeepSeek visible lore/RAG。
- MiroFish export/intake、knowledge-index body、runtime canon、hidden/private body。
- backend/BFF/service/job queue/eval archive/cloud save。
- external framework PoC/dependency/vendored subset/read-only scan/patch artifact/subagents。
- L4/L5 runtime、方源等原著关键人物 agent、天道/宿命 runtime 裁决。
- high-rank combat runtime、theater combat UI、killer-move stack implementation、仙蛊屋状态实现。
- formal location/faction/identity/reward/NPC life-death/warrant/recruitment/blockade。
- public wording、legal/release boundary、EdgeOne deployment、main auto-merge。

## 冻结口径

v3.9 的核心产出是 `v4.0 readiness`，不是 `v4.0 implementation`。任何把准入框架推进为高阶 runtime 实现的动作，都必须进入 F-390 future gate 并停机等待用户决策。

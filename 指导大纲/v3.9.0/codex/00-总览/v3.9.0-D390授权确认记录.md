# v3.9.0 D-390 授权确认记录

日期：2026-05-25。
来源：用户在 `/goal` 中批准 `D-390-001` 至 `D-390-012`，并确认 `F-390-001` 至 `F-390-012`。

## 结论

- `D-390-001` 至 `D-390-012`：`approved_in_frontloaded_pack`。
- `F-390-001` 至 `F-390-012`：`future_gate_required`。
- Codex 可在 v3.9 授权包内完成 a0/a1/a2/b1/b2/b3/process/rc。
- 若触发任何 F-390 或 `v3.9.0-例外停机清单.md`，必须停止 `/goal` 自动推进。

## 本授权允许

- 复盘 v3.4-v3.8 transient AgentProposal 证据。
- 统一审计 agent / WorldCore / DeepSeek / MiroFish / external-framework / save-format / Player Advocate 边界。
- 新增自有 report-only v390 checker、synthetic samples 和 report artifact。
- 执行 180 轮 deterministic pre-v4 stability。
- 编写 60 轮 Player Advocate 记录，live DeepSeek：否。
- 完成 old-save/no-save/rollback 证据、Skill sync、dashboard/AGENTS/PROJECT-STATE/handoff 同步、Git/CI 收束。

## 本授权不允许

- 不允许 persistent agent state、agent memory store 或 self-learning 直接写入。
- 不允许新增 save field、migration、`SAVE_FORMAT_VERSION` bump 或 `runFingerprint`。
- 不允许 live DeepSeek、DeepSeek prompt/context/model/authority 扩大、DeepSeek visible lore/RAG。
- 不允许 MiroFish export/intake、真实原著事实、命名 NPC、hidden-adjacent、方源公开证据或正式 lore 结论。
- 不允许 backend/BFF/service、外部框架 PoC/dependency/read-only scan/patch artifact/subagents。
- 不允许 L4/L5 runtime、HeavenWill/Fate runtime、高阶战斗 runtime。
- 不允许正式地点、阵营、身份、奖励、NPC 生死、通缉、招揽、封锁。
- 不允许 public/legal/release 改动、EdgeOne 部署、branch protection 变更或 main 自动合并。

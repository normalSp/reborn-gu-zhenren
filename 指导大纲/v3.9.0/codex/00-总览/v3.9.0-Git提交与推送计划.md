# v3.9.0 Git 提交与推送计划

状态：startup，pending user decision。

## 当前分支

- 当前分支：`codex/v390-startup-v4-safety-closure`。
- 基线：从 `codex/v380-startup-proposal-graph-stability` 的 v3.8 完成态创建。
- 原因：v3.8 尚未获批合并 `main`，v3.9 按当前已验证完成态继续，不自动改 mainline。

## 提交边界

建议提交边界：

1. `docs: 开启 v3.9 专家团启动会`。
2. 若 D-390 获批，后续可拆为：
   - a0/a1/a2 design gate docs。
   - b1 report-only checker 与 samples。
   - b2/b3 evidence records。
   - rc docs/dashboard/skills/handoff。

## 禁止混入

- 不 stage unrelated dirty files。
- 不使用 `git add -A`。
- 不改 `.env`、secret、`.codex` runtime state、plugin cache、SQLite/log。
- 不混入 EdgeOne deployment、branch protection、main merge。

## 推送规则

启动会文档完成后推送当前分支并等待 GitHub Actions。后续 D-390 获批后，阶段完成也按制度提交、推送、等待 CI。

## mainline 结论

v3.9 启动会不自动合并 `main`。v3.9 完成时必须再次记录是否建议合并 `main`，但合并本身仍需用户单独批准或 PR 审核。

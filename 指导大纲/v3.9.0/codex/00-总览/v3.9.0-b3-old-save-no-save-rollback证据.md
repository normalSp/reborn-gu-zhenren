# v3.9.0-b3 Old-save / No-save / Rollback 证据

状态：complete。
授权：D-390-009。

## 证据结论

v3.9 不新增 runtime/source/store/UI/prompt/save 行为，只新增 report-only checker、synthetic samples、artifacts 和文档。因此：

- old-save：旧存档读取路径不变，没有新增迁移要求。
- no-save：v390 checker 不读取或写入玩家存档。
- rollback：删除 v390 checker/artifact/docs 不影响 runtime 状态、store 状态或 save schema。
- `SAVE_FORMAT_VERSION`：保持 `25`。
- `runFingerprint`：未新增。

## 检查点

| 检查点 | 结果 |
|---|---|
| 新 save field | 否 |
| migration | 否 |
| store slice | 否 |
| runtime prompt/context | 否 |
| live DeepSeek | 否 |
| MiroFish | 否 |
| external framework dependency | 否 |
| backend/BFF/service | 否 |

## 剩余风险

v4.0 如果打开高阶战斗、HeavenWill/Fate 或 persistent agent state，必须重新做 save-format、rollback、old-save 和 Player Advocate 设计门禁；v3.9 不能替代这些未来门禁。

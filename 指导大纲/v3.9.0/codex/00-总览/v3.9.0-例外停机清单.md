# v3.9.0 例外停机清单

状态：active/complied。

用户已批准 D-390 并进入 `/goal` 自动推进；执行期间遇到以下任一情况必须立即停机并报告用户。v3.9 本轮未触发例外停机。

## Runtime / Save

- 需要新增 persistent agent state、agent memory store、self-learning write。
- 需要新增 save field、migration、`SAVE_FORMAT_VERSION` bump。
- 需要新增 `runFingerprint`。
- 需要让 report-only checker 写入 runtime/source/store/save/prompt/UI。

## DeepSeek / Narrative

- 需要调用 live DeepSeek。
- 需要修改 DeepSeek prompt/context/model/authority。
- 需要把 lore/RAG/knowledge-index body 放进 DeepSeek visible context。
- live 或 replay 结果出现 P0/P1。

## Lore / MiroFish / Knowledge

- 触及命名 NPC、真实势力、真实地点、hidden-adjacent、方源公开证据、L4/L5 fact anchor、formal lore conclusion。
- 需要 MiroFish export/intake。
- 需要写 knowledge-index body、runtime canon、hidden/private body、prompt body archival。
- 需要暴露隐藏事实给 UI、DeepSeek、报告正文或玩家可见文案。

## Agent / External Framework / Backend

- 需要 backend/BFF/service/job queue/eval archive/cloud save。
- 需要 Hermes、LangGraph、Mastra、Dify、Flowise、AutoGPT、Agno、Browser-use、LlamaIndex 或其他外部框架的 PoC、dependency、vendored subset、read-only scan、patch artifact、subagents、项目读写、命令或 git 权限。
- 需要开放 L4/L5 runtime、原著关键人物 agent、HeavenWill/Fate runtime。

## Formal World / Combat / Release

- 需要正式地点、阵营、身份、奖励、NPC 生死、通缉、招揽、封锁。
- 需要 high-rank combat runtime、theater combat UI、killer-move stack implementation、Immortal Gu House state。
- 需要公共发布口径、法律/版权边界变更、EdgeOne 部署、branch protection 或 main auto-merge。

## Quality / Git

- 关键测试连续失败且需要改变方案。
- 工作区出现无关脏文件且无法明确分流。
- 需要用 destructive git 命令、force push、history rewrite。

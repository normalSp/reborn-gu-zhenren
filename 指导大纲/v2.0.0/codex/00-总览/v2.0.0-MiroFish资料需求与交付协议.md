# v2.0.0 MiroFish 资料需求与交付协议

状态：b1 completed locally；当前无新 export，b2 前继续优先复用 v1.9 a2。
日期：2026-05-22

## 当前结论

v2.0 启动/a1 阶段不默认请求新 MiroFish export。

当前基线是 v1.9 a2：

`southern_border_low_rank_region_life_v2_prelude_slice`

它已完成：

- RebornG request。
- MiroFish quote-redacted export。
- RebornG intake review。
- rule draft / test sample。
- dual-repo checker pass。

## 当前 MiroFish need

a0/a1：

`preferred`

b1 当前：

`preferred`

说明：D-201 已批准 b1 先做最小 `regionalEventLedger`。若 b1 只承接 v1.9 a2 已覆盖的公开低阶外缘事件类型，可以先复用现有 intake；若新增命名地点/NPC/hidden-adjacent/formal outcome，必须升级 blocking。

若进入以下范围，升级：

`blocking`

- 新命名地点。
- 新命名势力。
- 新命名 NPC。
- hidden-adjacent。
- Fang Yuan public-evidence adjacent。
- 正式通缉、招揽、封锁、任务、奖励、NPC 生死。
- DeepSeek prompt/eval 需要新的 source-pointer 约束。

## v2.0 a2 候选动作

a2 不一定要新 export。推荐顺序：

1. 复核 v1.9 a2 intake 是否覆盖 a1 批准的第一核心区域。
2. 写 gap report。
3. 若 gap 为零，记录 `no_new_export_needed`。
4. 若 gap 涉及正式命名、hidden-adjacent 或新事件类型，再写 request 并走双仓流水线。

## 禁止

- 不从 `指导大纲/vMiroFish/基础包/` 全书导入。
- 不把 MiroFish 输出直接放进 `src/canon`。
- 不把 MiroFish 输出直接喂给 DeepSeek。
- 不把 hidden/private body 写入 UI、prompt、ledger 或知识库正文。
- 不让 MiroFish 直接改 RebornG runtime/docs/tests/git。

## 子代理

当前不启用子代理。

未来若用户单独批准，只读/分析型子代理最多可做：

- coverage 对照。
- duplicate/source-pointer 检查。
- test sample gap review。

不得做：

- 写 RebornG 文件。
- 写 MiroFish export。
- 改 runtime/canon/DeepSeek。
- stage/commit/push。

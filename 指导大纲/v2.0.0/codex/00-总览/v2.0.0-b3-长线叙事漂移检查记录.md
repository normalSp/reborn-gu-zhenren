# v2.0.0-b3 长线叙事漂移检查记录

日期：2026-05-22
状态：completed locally
阶段：`v2.0.0-b3-同开局差异与replay对照`

## 触发原因

b3 修改玩家可见 runtime 表达：在现有区域事件账本上派生同开局 replay lanes，属于区域活世界和叙事差异验证，必须执行长线叙事漂移门禁。

## 检查档位

| 档位 | 是否执行 | 说明 |
|---|---|---|
| T0 deterministic | 是 | helper unit + e2e 覆盖 replay lane、variantIndex、事件不足阻断 |
| T1 live probe | 是 | `deepseek-v4-flash` 20 轮标准 smoke |
| T2 long soak | 未执行 | 留给 v2.0 rc |
| T3 regional life soak | 未执行 | v2.0 rc 硬门：320 total rounds，live >= 160 |

## T0 结果

已覆盖：

- 既有 `regionalEventLedger` 可派生 6 类 replay lane。
- `variantIndex` 可改变 active lane，但不改变 event count、稳定事实、奖励、地点、身份或 NPC fate。
- 事件不足时返回 `needs_regional_event_ledger`。
- helper 不写 save，不创建 `runFingerprint`，不扩大 DeepSeek authority。
- hidden/private/raw/source_text refs 继续过滤；测试明确拒绝隐藏正文和敏感原文泄漏。
- UI `账本` tab 内显示 `v200-replay-diff-audit` 与 `no runFingerprint`。

## T1 live 结果

- 模型：`deepseek-v4-flash`
- 样本：5
- 每样本轮次：4
- 总轮次：20
- 报告路径：`artifacts/deepseek-drift-probe/v2.0.0-regional-event-ledger/2026-05-22T09-28-43-564Z/report.json`
- 结果：20/20 accepted
- P0/P1/P2：0/0/7
- tokens：26235
- cost：`$0.00278996`

## 漂移审查

| 项 | 结果 | 说明 |
|---|---|---|
| 世界状态 | 通过 | replay lane 只读取公开账本，不新增正式地点/身份/奖励 |
| DeepSeek 权威 | 通过 | live 输出未写 ledger、save format、正式结算或 hidden body |
| 原著边界 | 通过 | 未开放完整南疆/商家城/正式商队/NPC 生死 |
| 连续性 | 通过 | 差异来自 lane 与选择，不来自稳定事实漂移 |
| 玩家下一步 | 通过 | 各 lane 都给出可理解下一步或阻断理由 |
| 重复空转 | 通过 | 同开局不只重复同一说明，至少可按 lane 切换焦点 |

## 新样本 triage

| 样本 | 来源 | 归类 | 处理 |
|---|---|---|---|
| `青毛` 术语错字 | b3 live | current_matrix | 新增 `V20-B3-TERM-001` |
| `通行证` | b3 live | current_matrix | 归入 `V20-P2-PROP-001`，process-1 hardening |
| `正式凭证` | b3 live | current_matrix | 归入 `V20-P2-PROP-001`，process-1 hardening |
| T3 后如果同开局仍收敛为空转 | b3 PA | future_sample_pool | 继续由 `V20-T3-002` 作为 rc 阻断样本 |

## 结论

b3 长线叙事漂移门禁通过。残余 P2 不阻断 b3，但 process-1 必须处理 `青毛` 错字和正式凭信词，rc T3 必须继续验证无 `runFingerprint` 方案的同开局差异是否足够。

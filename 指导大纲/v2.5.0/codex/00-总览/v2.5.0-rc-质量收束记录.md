# v2.5.0-rc 质量收束记录

日期：2026-05-23
状态：completed
分支：`codex/v250-complete-private-canon-visibility`

## 一句话结论

v2.5.0 作为 `private canon / knowledge visibility 试验设计` 已完成本地文档收束。它把 private canon、knowledge visibility、source pointer、redaction、promotion chain、AgentProposal、hidden leak、memory contamination、DeepSeek visible denial、v2.6 准入清单和前置审批制度试运行全部制度化。

本版本不实现功能，不上线服务，不改 runtime。

## D-250 / F-250 结果

| 范围 | 结果 |
|---|---|
| D-250-001 至 D-250-012 | 已批准并执行 |
| F-250-001 至 F-250-010 | 全部保持 `future_gate_required` |
| 例外停机触发 | 0 |

## 收束输入

| 输入 | 状态 | rc 采纳 |
|---|---|---|
| `v2.5.0-前置授权包.md` | approved | 作为全版本授权边界 |
| `v2.5.0-a1-private-canon-knowledge-visibility-schema门禁.md` | completed | 冻结 `v250_a1_private_canon_visibility_schema_v1` |
| `v2.5.0-a2-source-pointer-redaction-promotion-chain门禁.md` | completed | 冻结 `v250_a2_source_pointer_redaction_promotion_chain_v1` |
| `v2.5.0-b1-report-only-visibility-boundary-report第一刀.md` | completed | 冻结 `v250_b1_visibility_boundary_report_v1` |
| `v2.5.0-b2-hidden-leak-memory-contamination-AgentProposal风险模型.md` | completed | 冻结 `v250_b2_hidden_leak_memory_contamination_agentproposal_risk_model_v1` |
| `v2.5.0-b3-v2.6-private-canon-eval-archive准入清单.md` | completed | 冻结 `v250_b3_v26_private_canon_eval_archive_admission_checklist_v1` |
| `v2.5.0-process-1-前置审批制度试运行复核.md` | completed | 建议 v2.6 继续使用并硬化 |

## rc 硬边界

v2.5 完成不代表批准以下事项：

- runtime/source/UI/store/prompt/save 变更。
- 新 save fields、`SAVE_FORMAT_VERSION` bump、migration、`runFingerprint`。
- backend/BFF/API proxy/private canon service/job queue/cloud save。
- eval archive runner/service/artifact。
- live DeepSeek、DeepSeek prompt/context/model/authority 变更。
- DeepSeek visible lore/RAG。
- MiroFish export/intake。
- 知识库正文、runtime canon、canon promotion、真实 hidden/private body。
- external framework PoC、dependency、vendored subset、read-only scan、patch artifact、subagents。
- self-learning 直接写 canon/runtime/save/prompt/knowledge-index body。
- formal location/faction/identity/reward/NPC life-death。
- public wording、release、EdgeOne。

## Player Advocate / Live Probe

是否调用 live DeepSeek：否。

豁免理由：

- v2.5 无玩家可见 runtime/UI 变化。
- 无 DeepSeek prompt/context/model/authority 变化。
- 无 source/store/save/backend/runner/service/artifact 变更。
- 无真实 hidden/private body、知识库正文、MiroFish output 或 runtime canon。

Player Advocate：豁免，理由同上。

## 测试与自检

本地验证记录：

| 检查 | 结果 |
|---|---|
| `git diff --check` | 通过；仅出现 LF -> CRLF 工作区换行提示，无 whitespace error |
| D/F 授权扫描 | 通过；`D-250-001` 至 `D-250-012` 均记录为已批准，`F-250-001` 至 `F-250-010` 均保持 `future_gate_required` |
| 当前入口扫描 | 通过；`AGENTS.md`、`PROJECT-STATE.md`、项目仪表盘、长期路线和 v2.5 总览均指向 v2.5 complete / v2.6 startup |
| 敏感边界扫描 | 通过；命中均处于禁止项、future gate、No-Go、豁免、历史输入或测试扫描语境 |
| GitHub Actions | 通过；`RebornG CI` run `26338043291`，deterministic quality gate 全绿 |

敏感词允许出现的语境仅限：禁止项、future gate、No-Go、豁免、历史输入或测试扫描。

## v2.6 输入

v2.6 候选主线不应直接实现 backend/service。建议先开专家团启动会，基于 v2.5 结果决定：

- 是否继续 private canon / eval archive 工程预备。
- 是否只做 schema checker / report-only runner，且需要用户另批。
- 是否把前置审批制度升级为默认硬门。
- 是否需要 MiroFish topic-slice，默认仍 `not_needed`。
- 是否允许任何外部 framework PoC/dependency/read-only scan/subagent，默认仍 future gate。

## 通过意义

v2.5 通过代表：

- knowledge visibility 与 private canon 的设计边界清楚。
- source pointer / redaction / promotion chain 清楚。
- DeepSeek visible denial rules 清楚。
- AgentProposal hidden leak 与 memory contamination 风险模型清楚。
- v2.6 准入清单清楚。
- 前置批量审批制度在一个完整版本中跑通。

v2.5 通过不代表：

- 可以实现 private canon service。
- 可以写 eval archive runner / artifact。
- 可以把知识库正文或 full-book base 喂给 DeepSeek。
- 可以让 agent/self-learning 改 canon/runtime/save。
- 可以启用 runtime agent 或外部框架 PoC。
- 可以开放正式地点、阵营、身份、奖励、NPC 生死或 canon promotion。

# v2.5.0 Git 提交与推送计划

日期：2026-05-23
状态：completed；completion 分支为 `codex/v250-complete-private-canon-visibility`

## 当前分支

`codex/v250-startup-private-canon-visibility`

## 提交边界

| 提交 | 预计内容 |
|---|---|
| startup | v2.5 启动会、前置授权包、总纲、路线图、决策池、测试矩阵、真相源索引、Git 计划、Skill audit、入口同步 |
| a1/a2/b1/b2/b3/process/rc | 已在 `codex/v250-complete-private-canon-visibility` 收束；不混入 runtime/source/UI/store/prompt/save |

## Stage 规则

- 使用显式路径 stage。
- 不使用 `git add -A`。
- 不 stage 外部 skill 文件；skill 文件更新需单独记录为本机技能同步。
- 不 stage 无关资产、测试存档、临时输出或外部仓库文件。

## 推送和 CI

每次阶段提交后：

1. `git diff --cached --check`
2. `git commit`
3. `git push -u origin <branch>`
4. `gh run list --branch <branch> --limit 5`
5. 若 CI 运行中，等待结论。

## 当前 startup 验收

- 文档自检通过。
- 敏感边界扫描通过。
- GitHub Actions 成功。

## 禁止

- 不自动部署 EdgeOne。
- 不执行外部 agent git 操作。
- 不用子代理提交。
- 不把未批准 F-250 项混入提交。

## 完成分支

- startup 分支：`codex/v250-startup-private-canon-visibility`
- completion 分支：`codex/v250-complete-private-canon-visibility`
- completion 提交范围：v2.5 a1/a2/b1/b2/b3/process-1/rc 文档、入口同步、skill 同步和 handoff。

# v0.11.0 需求决策池

日期：2026-05-15
状态：主线已进入执行；a2 文档第一刀、runtime skeleton、受控 patch API 和青茅最小回流样本已完成；a3 自由意图本地裁决引擎第一刀已完成；a3-2 自由目标面板与 `playerGoals` 写入第一刀已完成；b1 原著事实抽取试点第一刀和 b1-2 事实卡读取/裁决引用桥已完成；测试工程化总纲和 a3 裁决样本矩阵已建立；process-1 开发流程进化专项第一版已完成；项目仪表盘、Git 提交/推送制度和 v0.12 候选承接池已建立；b2 可见范围调查第一刀、b2-2 白家接触压力样本、b2-3 方源隐藏保护失败样本、b2-4 调查结果后续提示候选和 b2 轻量复盘已完成。

## 决策池说明

本文件只收 v0.11 候选需求。用户批准后才进入实现；驳回或延期的需求不得被夹带进代码。

## 候选需求

### D-011-001 v0.11 主线：活世界地基与自由意图闸门

状态：已批准。

内容：

- v0.11 不做完整青茅山大剧情。
- v0.11 不做蛊仙期扩权。
- v0.11 聚焦世界状态、自由意图、本地裁决、旧债清理和原著事实试点。

价值：

- 防止继续步子过大。
- 给后续 v0.12 青茅山正史锚点提供地基。

### D-011-002 a0 玩家可见旧债清理

状态：已批准，第一刀和 b1-2 已完成。

内容：

- 清理 `待 v0.8 深线补充` 等玩家可见旧文案。
- 加检查防止旧版本承诺出现在 UI/canon 可见文案。
- 不新增玩法。

价值：

- 提升产品可信度。
- 避免玩家看到开发历史而出戏。

### D-011-003 a1 架构与存档加固

状态：已方向批准，作为下一项重工程。

内容：

- 存档归一化。
- response pipeline warning。
- storage key 统一。
- 专家团门禁清单。

价值：

- 为活世界状态持久化铺路。

### D-011-004 a2 活世界状态协议

状态：已批准，字段冻结、runtime skeleton、受控 patch API 和青茅最小回流样本已完成。

内容：

- 世界时间。
- 区域状态。
- 玩家已知事实/隐藏事实。
- NPC 记忆。
- 势力压力。
- 玩家长期目标。
- 行动后果。
- IF 偏离。

风险：

- 大概率需要提升 `SAVE_FORMAT_VERSION`。
- 字段设计不好会造成长期迁移债。

门禁：

- 字段表。
- 默认值。
- 旧档迁移测试。
- 长测样本。

已建立：

- `v0.11.0-a2-设计门禁输出.md`
- `v0.11.0-a2-活世界状态协议字段表.md`
- `v0.11.0-a2-测试矩阵.md`

用户决策：

- a2 runtime 已新增 `livingWorldState` 持久化字段，`SAVE_FORMAT_VERSION` 已从 `21` 升到 `22`。
- 同一改动必须补默认值、迁移、归一化和测试。

### D-011-012 a2 第一刀先做设计门禁输出

状态：已批准，文档第一刀已执行；runtime skeleton 第一轮也已完成。

内容：

- 冻结 a2 读写权矩阵。
- 冻结 `livingWorldState` 字段表。
- 冻结 a2 runtime 测试矩阵。
- 明确 DeepSeek 不能直接写存档、奖励、地点、战斗、原著隐藏事实。

价值：

- 在 runtime 前先降低字段返工风险。
- 防止自由意图裁决器后续绕开现有 action/canon/store 权限。

### D-011-013 a2 runtime skeleton

状态：已完成第一轮。

内容：

- 新增 `livingWorldState` 类型、默认值和 store 根字段。
- `SAVE_FORMAT_VERSION` 升到 `22`。
- v21 旧档迁移到 v22 时补空活世界状态。
- 公开测试存档和长测存档同步到 `formatVersion 22`。

价值：

- 后续自由意图、NPC 记忆、势力压力和行动后果都有统一持久化根。
- 不让 DeepSeek、UI 或单个子系统散写长期世界事实。

### D-011-014 a2 受控活世界 patch API

状态：已完成第二刀。

内容：

- 新增 `applyLivingWorldPatch()`。
- 允许本地 action/canon/faction/living-world engine 写入。
- 拒绝 DeepSeek、UI 和叙事正文直接写入。
- 写入前统一归一化并剥离隐藏事实正文。

价值：

- 后续 b2 真正把玩家行动写入世界记忆时，不需要临场散写 store。
- 让“活世界”先有门禁，再有内容。

### D-011-015 a2 青茅山道巡查最小回流样本

状态：已完成第三刀。

内容：

- `mountain_patrol` 成功后写入 `knownFacts` 和 `actionConsequences`。
- 族学训练暂不写回流，避免没有稳定 `worldActionResolution` 时造假账。
- 三寨委托仍阻断，等待更完整区域状态机。

价值：

- 验证活世界状态可以从真实本地行动产生记忆。
- 仍不碰奖励、地点、战斗、NPC 生死、势力归属和原著锚点。

### D-011-005 a3 自由意图裁决引擎

状态：已批准，第一刀已完成。

内容：

- 支持 `obtain_item`、`join_faction`、`investigate`、`travel`、`long_term_goal`。
- DeepSeek 解析候选，本地 engine 裁决。
- 极端样本必须覆盖：九转蛊、春秋蝉、宝黄天、投靠白家、跟踪方源、逃离青茅、杀死白凝冰。

价值：

- 让自由输入变成有边界、有后果的系统。

已完成：

- `src/canon/v011-world-intent-rules.json`
- `src/engine/v011-world-intent-engine.ts`
- `src/engine/v011-world-intent-engine.test.ts`
- `v0.11.0-a3-第一刀-自由意图本地裁决引擎.md`

本刀不接 UI/store，不写 `livingWorldState`，不提升 `SAVE_FORMAT_VERSION`。

下一步待决策：

- D-011-016：a3-2 是否接玩家可见自由意图入口。

### D-011-016 a3-2 玩家可见自由意图入口

状态：已批准，第一刀已实现。

内容：

- 把 a3 本地裁决引擎接到 UI/store。
- 玩家输入自由目标后显示 `IntentRuling`、阻断理由、前置条件、可行替代路径和风险。
- 若玩家确认长期目标，再通过 `applyLivingWorldPatch()` 写入 `livingWorldState.playerGoals`。

待决策点：

- 入口已选：单独“自由目标”面板。
- 已允许本阶段持久化玩家确认的长期目标，但仅写 `livingWorldState.playerGoals`。

建议：

- 后续 b2 再做自由意图 -> 正式行动候选/世界反应，不在 a3-2 夹带结算。

已实现：

- `previewWorldIntentAction()` 预览不写状态。
- `confirmWorldIntentGoalAction()` 只接受玩家输入来源和 `suggestedPlayerGoal`。
- 确认写入走 `living_world_engine` patch，只 upsert `playerGoals`。
- 单独自由目标面板显示裁决、风险、前置/代价、路由和目标账本。
- focused unit、typecheck 已通过；自由目标 e2e 已建立。

### D-011-017 v0.11 测试工程化总纲

状态：已批准并建立第一版。

内容：

- 建立 L0-L6 分层测试体系。
- 设立 `QA/测试工程负责人`。
- 固定硬指标：硬边界样本通过率 100%、DeepSeek 越权覆盖率 100%、隐藏事实泄露 0、非法状态写入 0、存档迁移失败 0。
- 样本库按 `golden`、`boundary`、`identity`、`fuzzy`、`deepseek_override` 分类维护。
- 每 2 个小版本轻量复盘，每个大阶段或 rc 前完整复盘。

价值：

- 让测试随着系统演进，而不是只靠用户提醒。
- 保持 RebornG 相比一般 AI 游戏的工程化优势。

已建立：

- `v0.11.0-测试工程化总纲.md`
- `v0.11.0-a3-裁决样本矩阵.md`

### D-011-018 process-1 开发流程进化专项

状态：已批准并完成第一版。

内容：

- 建立用户输入协议。
- 建立专家团阶段门禁。
- 建立 ADR-lite、用户决策、专家团提案、bug 学习、轻量/完整复盘和阶段启动模板。
- 建立外部模式借鉴映射。
- 明确 Codex 自动推进边界和必须停下来问用户的情况。

价值：

- 让用户从“临时想到什么说什么”转为在关键节点提供方向、边界、优先级、口感、取舍和发布输入。
- 让专家团和测试复盘成为持续机制，不依赖用户提醒。
- 为 `v0.11.0-a3-2`、b1 原著事实卡和 b2 青茅活世界闭环降低跑偏风险。

已建立：

- `v0.11.0-process-1-开发流程进化专项.md`
- `v0.11.0-process-1-用户输入协议.md`
- `v0.11.0-process-1-专家团阶段门禁.md`
- `v0.11.0-process-1-模板集.md`
- `v0.11.0-process-1-外部模式借鉴映射.md`

### D-011-020 项目仪表盘与 Git 提交/推送制度

状态：已批准并完成第一版。

内容：

- 建立 `v0.11.0-项目仪表盘.md`，作为用户查看项目进度的第一入口。
- 建立 `v0.11.0-Git提交与推送制度.md`，固定 commit/push 频率、分支、提交前检查和推送前检查。
- 将 GitHub Actions、PR 模板、branch protection、ADR 补档、只读子代理、Skill 披露等 v0.11 后做事项写入 `v0.12.0` 候选专项池。

价值：

- 防止进度和推送节奏继续靠口头约定。
- 防止 v0.11 后的工程化专项被遗忘。
- 给后续阶段完成门禁增加“仪表盘更新 + Git 状态记录”。

### D-011-021 b2 第一刀：可见范围调查

状态：已批准，第一刀、b2-2、b2-3 与 b2-4 已完成。

内容：

- b2 首个活世界闭环选择 `可见范围调查`。
- 优先验证 `自由调查意图 -> 本地裁决 -> 可见事实读取 -> 本地行动结果 -> livingWorldState 回流`。
- 只写既有 `knownFacts/actionConsequences/hiddenFactRefs`，不新增存档字段。
- 已实现调查白家写入可见事实和行动后果；调查灵泉/方源只做隐藏事实保护，不泄露隐藏摘要。
- b2-2 已补白家接触压力样本：调查白家额外写入 `factionPressure` 的 opportunity 记录，但不改势力归属。
- b2-3 已补方源隐藏保护失败样本：跟踪方源会写 hidden ref、公开失败后果和谨慎 `npcMemories`，但不泄露隐藏因果。
- b2-4 已补调查结果后续提示候选：从活世界账本派生白家接触窗口、方源旁证避险和资源线公开旁证提示，但不接正式行动协议。

非目标：

- 不做完整白家投靠、跟踪方源、逃离青茅山闭环。
- 不新增奖励、地点、NPC 生死、正史锚点变化。

验证：

- focused unit、`tsc`、自由目标面板 e2e、build 已通过。

### D-011-022 b2-4 调查结果后续提示候选

状态：已批准并完成。

内容：

- 新增 `buildQingmaoInvestigationFollowUps()`。
- 从 `livingWorldState` 派生玩家可读的下一步提示。
- `FreeGoalPanel` 显示 `后续提示`，并明确 `提示，不是正式行动`。
- 白家调查后显示接触窗口提示。
- 方源隐藏保护失败后显示旁证调查提示。
- 资源受保护内情后显示公开渠道提示。

价值：

- 调查结果不再只是账本，玩家能看到下一步方向。
- 提示仍然被本地门禁约束，不会变成 UI 私算行动。

边界：

- 不写 store。
- 不新增 `WorldActionCandidate`。
- 不改阵营、声望、地点、NPC 生死、正史锚点或奖励。
- 不新增存档字段。
- 不让 DeepSeek 看到隐藏事实 id 或隐藏正文。

下一步待决策：

- 是否把白家接触窗口、方源旁证调查或逃离青茅山路线准备升级成正式行动候选。
- 或进入 rc/后续版本规划。

### D-011-023 b2 轻量复盘与正式行动升级方向

状态：已决策并完成 A/B；C 延期，D 进入 rc。

输入类型：优先级 + 边界 + 口感。

背景：

- b2 已完成调查账本、势力压力、NPC 谨慎记忆和后续提示。
- 后续提示仍不是正式行动。
- 下一步若继续 runtime，需要决定哪条提示升级成正式行动候选。

专家团建议：

1. 优先升级白家接触窗口正式行动。
2. 第二优先做逃离青茅山路线准备链。
3. 方源旁证调查延期。
4. 若用户更重视收束，则进入 rc。

候选：

- A：白家接触窗口正式行动。建议批准。只做公开试探/递话/核对接触窗口，不投靠成功、不改阵营、不加声望、不开放地点、不触发白凝冰重大 IF。
- B：逃离青茅山路线准备链。建议第二优先。只做路线/补给/追踪风险准备，不开放新地域。
- C：方源旁证调查。建议延期。需要更多方源公开活动事实卡和隐藏事实防线。
- D：进入 rc 收束。稳，但玩家还不能顺着提示继续正式行动。

用户决策：

- 用户同意 A -> B -> rc。
- A 白家接触窗口正式行动已完成，见 `v0.11.0-b2-5-白家接触窗口正式行动.md`。
- B 逃离青茅山路线准备链已完成，见 `v0.11.0-b2-6-逃离青茅山路线准备链.md`。
- C 方源旁证调查延期。
- 下一步进入 `v0.11.0-rc` 收束。

后续动作：

- rc 收束中继续验证 A/B 的边界：不改阵营、不开放地点、不发奖励、不泄露隐藏事实、不新增存档字段。

### D-011-011 世界意图裁决引擎设计门禁

状态：已批准，作为 `a2/a3/b2` 的硬前置。

内容：

- 审计现有子系统与自由意图裁决是否冲突。
- 明确 DeepSeek、本地 engine、canon、store、UI 的读写权。
- 把 Concordia、Generative Agents、AI Town、TextWorld、ReAct/Reflexion/Tree of Thoughts 的设计思想转化为本项目契约。
- 确认 `SAVE_FORMAT_VERSION`、持久化字段、测试样本和越权阻断策略。

价值：

- 防止世界意图裁决引擎变成绕开现有系统的大而乱入口。
- 让“活世界”从第一刀开始就可测试、可复现、可迁移。

门禁文件：

- `v0.11.0-世界意图裁决引擎-设计门禁.md`

### D-011-006 b1 原著事实抽取试点

状态：已批准，第一刀已完成。

内容：

- 从本地原著文本抽取青茅山事实卡。
- 只写摘要和来源指针，不复制大段原文。
- 分类：硬事实、玩家可见、隐藏事实、IF 偏离点。

风险：

- 事实抽取必须谨慎，不能靠模型胡编。

已完成：

- `src/canon/qingmao-canon-fact-cards.json`。
- 首批 8 张青茅事实卡。
- `src/canon/qingmao-canon-fact-cards.test.ts`。
- 隐藏事实只做 `hidden_ref_only`，不进玩家可见摘要。

下一步：

- b1-2 已建立事实卡读取 helper，并让裁决引擎引用事实卡 ID。
- 下一步进入 b2 前需要确认首个活世界闭环样本；若范围选择不明确，应停下来让用户拍板。

### D-011-019 b1-2 事实卡读取与裁决引用桥

状态：已完成。

内容：

- 新增 `v011-qingmao-fact-cards.ts` 只读 helper。
- 把自由意图中的白家、方源、青茅逃离、灵泉、春秋蝉等目标映射到 visible/hidden fact refs。
- `WorldIntentEngine` 输出 `factCardRefs`。
- `deepSeekContract.visibleFactIds` 只允许可见事实卡，不允许隐藏事实卡。

价值：

- 后续 b2 可以用同一套事实卡做玩家已知事实、隐藏事实保护和 DeepSeek 可见上下文。
- 不需要靠临场记忆判断“方源隐藏因果”“灵泉”等高风险事实。

边界：

- 不写 `livingWorldState`。
- 不新增存档字段。
- 不把事实卡直接变成剧情成功条件、奖励或地点开放。

### D-011-007 b2 青茅山活世界最小闭环

建议：批准，但排在 a2/a3/b1 之后。

内容：

- 接入一个青茅样本：投靠/接触白家、调查、跟踪、逃离等选择之一或多个。
- 让 NPC/势力/区域压力产生最小长期反应。
- 可保存回读。

价值：

- 第一次让玩家感到青茅山会记住自己。

### D-011-008 完整青茅山原著主线

建议：延期到 v0.12。

理由：

- 需要事实抽取和锚点系统成熟。
- 不应在 v0.11 直接做完整剧情链。

### D-011-009 蛊仙期扩权

建议：延期。

理由：

- 蛊师阶段还没完全做活。
- 继续扩蛊仙会放大系统债。

### D-011-010 大时代开局

建议：进入长期池。

内容：

- 盗天魔尊时期、尊者时代、远古/上古/中古等。

理由：

- 很重要，但必须等活世界、时代锚点、尊者因果保护成熟。

## 当前推荐批准组合

建议本轮用户批准：

1. D-011-001。
2. D-011-002。
3. D-011-003。
4. D-011-004：已批准，a2 第一刀字段冻结已建立。
5. D-011-011：已批准。
6. D-011-012：已批准并执行。
7. D-011-005：已批准并完成第一刀。
8. D-011-006。
9. D-011-007。

建议延期：

- D-011-008。
- D-011-009。
- D-011-010。

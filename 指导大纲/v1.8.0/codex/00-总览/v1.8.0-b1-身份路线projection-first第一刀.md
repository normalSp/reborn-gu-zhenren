# v1.8.0-b1 身份路线 projection-first 第一刀

状态：已完成
日期：2026-05-21

## 结论

b1 已按 D-181 落地低阶身份路线与同开局差异度 projection-first 第一刀：

- 新增 `src/engine/v180-identity-replay-projection.ts`。
- 新增 `src/components/game/IdentityReplayPanel.tsx`。
- `WorldHubPanel` 新增 `身份` 页签。
- 新增 focused unit：`src/engine/v180-identity-replay-projection.test.ts`。
- 新增 focused e2e：`tests/e2e/v180-identity-replay-projection.spec.ts`。
- 新增 live probe 工具：`scripts/run-v180-identity-replay-live-probe.mjs` 与 `tests/evals/deepseek-v180-identity-replay/samples.json`。

## 权威边界

本阶段保持：

- `SAVE_FORMAT_VERSION = 24`。
- 不新增 `identityRouteState` / `professionState`。
- 不新增 `runFingerprint` / `regionalEventLedger`。
- 不新增 store action。
- 不新增 migration/defaults。
- 不改 DeepSeek runtime prompt/context/API/model/authority。
- 不新增正式身份、正式职业、正式地点、正式商队/阵营、正式奖励、正式交易、NPC 生死或 hidden 可见事实。

## projection 输入

只读输入：

- v1.7 `buildV170RegionalLifeProjection()`。
- v1.1 `routeLocationState`。
- v1.2 `survivalEconomyState`。
- v1.3 `livingWorldState` 社会证据。
- v1.4 区域外缘 projection。
- v1.5 冲突后果 projection。
- v1.8-a2 `southern_border_low_rank_identity_routes_outer_edge_slice` reviewed source pointers。

## 身份路线候选

| id | 候选 | 不做什么 |
|---|---|---|
| `caravan_temp_hand` | 商队临时帮工/学徒压力 | 不成为正式商队成员 |
| `rogue_short_work` | 散修短工/零活压力 | 不生成散修势力或据点 |
| `low_rank_guard_candidate` | 低阶护卫/守夜候选 | 不结算战斗、奖励或 NPC 命运 |
| `gathering_runner` | 采集跑腿 | 不开放材料农场、库存或价格表 |
| `message_intel_runner` | 情报跑腿/传话 | 不揭示 hidden/private 或间谍身份 |

## 压力卡

压力 deck 包含：

- `identity_check_window`
- `caravan_labor_access`
- `permission_chain_prop_word`
- `low_status_labor`
- `temporary_market_observe`
- `bargain_refusal_short_work`
- `shelter_debt_window`
- `guard_or_gathering_pressure`
- `far_city_boundary`

所有压力卡都只解释公开压力、候选前置和安全下一步，不写正式身份、正式阵营、奖励、地点或 NPC 命运。

## 同开局可重玩差异度

b1 差异来自本地 pressure deck 的候选强调、风险排序、临时措辞和安全下一步提示。稳定事实不漂移：路线、身份、职业、地点、阵营、奖励、NPC 命运、hidden/canon 和 save format 都不变。

## 验证

已通过：

- `npm test -- src/engine/v180-identity-replay-projection.test.ts`
- `npx tsc --noEmit --pretty false`
- `npx playwright test v180-identity-replay-projection.spec.ts`
- `npm run build`
- `npm run eval:deepseek:v180-identity-replay-dry-run`
- `node --check scripts/run-v180-identity-replay-live-probe.mjs`
- b1 clean live smoke：12/12 accepted，P0/P1/P2=0/0/1，cost `$0.00174836`。

## 结论

b1 通过。b2-b4 作为同一 projection 的同开局差异度、身份特异失败推进、反刷/旧档/回滚证据继续收束，不需要新增存档字段或 DeepSeek 权限。

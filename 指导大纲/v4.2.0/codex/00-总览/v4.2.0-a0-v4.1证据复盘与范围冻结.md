# v4.2.0-a0 v4.1 证据复盘与范围冻结

状态：completed。
冻结名：`v420_a0_v41_evidence_review_scope_lock_v1`。

## 输入证据

| 来源 | 结论 |
|---|---|
| v4.1 checker | `AutoTheaterContractV1` report-only checker 35/35 schema valid，acceptedForGate=true |
| v4.1 Player Advocate | 50/50 passed，live DeepSeek：否 |
| v4.1 old-save/no-save/rollback | 未改 store/save/runtime/prompt/UI，回滚路径清晰 |
| v4.0 长期路线 | Auto-Theater Combat 可作为长期表达模型，但 runtime gate 未开放 |

## v4.2 范围冻结

v4.2 只做 `Auto-Theater Lite / 凡阶战斗映射设计门禁`：

- 将现有凡阶 action/card/trace 映射到 preparation cue、Lite transcript、Combat Ledger Lite。
- 用 synthetic/generic 样本证明 facts/preparation/support/defense/failure/candidate/rejected/needs_user_decision lane 可被检查。
- 继续保持 WorldCore / Combat Core 最终裁决。
- 不迁移现有凡阶 runtime，不新增 UI tab，不生成素材，不改 prompt，不写 save。

## 禁止扩张

任何触及真实战斗运行、战斗数值、命中/伤害裁决、正式奖励、NPC 生死、正式地点、真实原著战役、MiroFish blocking、live DeepSeek、后端服务、外部框架、L4/L5、HeavenWill/Fate runtime 的事项，均不属于 v4.2 自动推进范围。

## 结论

a0 通过。v4.2 从 v4.1 contract 继续向“凡阶映射合同 + checker”推进，不向 runtime 迁移。

# v4.2.0 小版本执行路线图

状态：completed。

## a0：v4.1 复盘与范围冻结

- 复盘 v4.1 contract/checker。
- 确认 v4.2 只做凡阶映射，不做 runtime 迁移。
- 输出 `v420_a0_v41_evidence_review_scope_lock_v1`。

## a1：Auto-Theater Lite 凡阶映射设计门禁

- 定义 `AutoTheaterLiteMortalMappingV1`。
- 映射 source action -> preparation cue -> resolution fact -> Lite transcript -> Combat Ledger Lite。
- 样本族覆盖月光蛊、白玉防御、酒虫支援、低阶兽战、失败原因。
- 输出 `v420_a1_auto_theater_lite_mortal_mapping_gate_v1`。

## a2：Combat Ledger Lite 与可读性门禁

- 定义 Lite ledger lanes：facts、preparation、support、defense、failure_reason、candidate、rejected、needs_user_decision。
- 定义移动端和 reduced-motion 验收。
- 定义 DeepSeek/UI/动效表达权限。
- 输出 `v420_a2_lite_ledger_readability_expression_gate_v1`。

## b1：report-only v420 checker 第一刀

- 若 D-420 获批，新增自有 zero-dependency checker。
- 样本存放在 `tests/evals/v420-auto-theater-lite-mortal-mapping/samples.json`。
- 脚本建议为 `scripts/run-v420-auto-theater-lite-mortal-mapping-check.mjs`。
- npm script 建议为 `check:v420-auto-theater-lite-mortal-mapping`。
- 输出 report artifact。

## b2：negative fixtures 与 deterministic mapping hardening

- 覆盖候选伪装事实、支援伪装攻击、失败原因伪装奖励、UI 私有数值、DeepSeek 越权、MiroFish hidden leak、凡阶 runtime 迁移误开。
- 建议 deterministic rounds：240。

## b3：Player Advocate 与 old-save/no-save/rollback 证据

- 版本为 report-only/checker 类，但触碰战斗理解，建议 50 轮 Player Advocate。
- live DeepSeek 默认 `否`，除非用户另批。
- old-save/no-save/rollback 证据应证明没有 store/save/runtime/prompt/UI 行为改变，或明确记录变更边界。

## rc：质量收束

- 跑 checker、PA gate、必要 typecheck/unit/build/scans/e2e/preview。
- 更新 dashboard、AGENTS、PROJECT-STATE、handoff、skill sync。
- 提交、推送、等待 GitHub Actions。
- 不自动合并 `main`。

## 自动继续范围执行结果

用户已批准 D-420 全包，Codex 已在不再次请求常规小阶段审批的情况下完成 a0-a2/b1-b3/process/rc。执行过程中未触发 `v4.2.0-例外停机清单.md`。

## pre-v4.3 并行治理专项

用户已批准在 v4.3 前新增 `Context-to-Skill 技能演化评测制度专项`。该专项独立于 v4.2 implementation：

- 只写制度、入口、参考池和同步指针。
- 首轮只覆盖 `reborn-expert-council`。
- 不自动改 skill、不跑 LLM Judge、不新增脚本。
- 已并回 v4.2 工作分支，但不改变 D-420/F-420 的权限边界。
- 不授权 runtime/save/DeepSeek/MiroFish/backend/external-framework。

# v0.16.0-a2 仪表盘与流程入口收束

日期：2026-05-18
状态：已完成
类型：docs/governance

## 目标

把 v0.16 后续开发入口固定到根目录仪表盘、v0.16 文档包、专家团状态、Git 计划和 handoff，不再依赖旧版 `v0.11.0-项目仪表盘.md` 作为当前入口。

## 完成内容

- 当前仪表盘入口固定为 `指导大纲/项目仪表盘.md`。
- v0.16 当前入口仍为 `指导大纲/v0.16.0/codex/00-总览/README.md`。
- 每个小版本完成时必须同步：
  - 阶段记录文档。
  - 测试矩阵或测试豁免。
  - 根目录项目仪表盘。
  - Git 提交/推送状态。
  - handoff。
- v0.16 Git 计划继续要求显式 stage，不使用 `git add -A`。

## 本阶段不做

- 不改运行时代码。
- 不新增存档字段。
- 不扩大 DeepSeek 权限。
- 不引入 MiroFish 阻塞包。
- 不做 EdgeOne 自动部署。

## Player Advocate

本阶段是流程/文档收束，无玩家可见 runtime 行为，Player Advocate gate 豁免。

豁免理由：没有改变 UI、交互、存档、DeepSeek、行动裁决或玩家路径。

## Git 状态

本阶段应与 v0.16 文档/治理提交同组记录，后续 runtime 阶段单独记录验证证据。

# v0.16.0 Git 提交与推送计划

日期：2026-05-18
状态：第一版

## 当前分支

当前沿用：

`codex/v013-npc-faction-reaction`

进入 v0.16 runtime 前，可由 Git/rollback steward 评估是否新建：

`codex/v016-system-closure`

在用户未要求新建分支前，继续保持小版本 commit 边界清晰。

## 提交原则

- 每个小版本至少一个清晰 commit。
- 文档/治理、测试、runtime 实现尽量分开。
- 不使用 `git add -A`。
- 只 stage 当前阶段相关文件。
- 不混入 `RebornG_codebuddy.zip`、`artifacts/`、`bgm/`、`.cursor/`、`指导大纲/大方向/`、美术候选资产等历史未跟踪项。
- 不提交 secrets、`.env`、`.codex` runtime state、plugin cache、SQLite/log 文件。
- 不自动部署 EdgeOne。

## 推送原则

需要推送：

- v0.16 文档启动包确认后。
- 每个 runtime 小版本验证通过后。
- rc 收束后。
- 跨工具/跨会话 continuation 前。

不自动推送：

- EdgeOne 自动部署。
- branch protection。
- auto merge。
- full Playwright required PR gate。

这些都需要用户另行决策。

## 推荐提交分组

| 阶段 | 提交主题 |
|---|---|
| process-0 | `docs: 建立v0.16系统收束前置文档` |
| a1 | `docs: 收束v0.16信息架构设计门禁` |
| a2 | `docs: 统一v0.16仪表盘与流程入口` |
| b1 | `feat: 合并v0.16行动与自由目标入口` |
| b2 | `feat: 建立v0.16蛊道工作台入口` |
| b3 | `feat: 场景化v0.16高阶与演示入口` |
| b4 | `test: 验证v0.16系统权威归并` |
| rc | `docs: 完成v0.16 rc质量收束` |

## 当前执行记录

| 阶段 | commit | push | 远端验证 |
|---|---|---|---|
| process-0 | 待提交 | 未推送 | 未运行 |
| a1 | 待提交 | 未推送 | 未运行；docs/design gate only |

## 回滚锚点

`v0.15.0-rc` commit `1c15ecb` 是进入 v0.16 前的最近完整质量收束锚点。

v0.16 runtime 阶段如果出现 UI 入口混乱、隐藏事实泄露、DeepSeek 越权、存档字段误改或正式奖励/地点/阵营/NPC 生死越权，应优先回滚对应小版本 commit，而不是重写历史。

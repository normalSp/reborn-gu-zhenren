# v0.16.0 需求决策池

日期：2026-05-18
状态：已完成；v0.16 决策已执行

## 已批准

| ID | 决策 | 结果 |
|---|---|---|
| D-016-001 | v0.16 主线是否改为系统收束、UI 减法与权威归并 | 批准 |
| D-016-002 | v0.17 是否顺延为战斗、杀招、小队与阵法深化 | 批准 |
| D-016-003 | v0.18 是否定为南疆路线与多区域承接 | 批准 |
| D-016-004 | v0.19 是否定为 v1.0 发布工具、长测和内容生产准备 | 批准 |
| D-016-005 | 蛊仙期是否推迟到 v1.0 之后 | 批准 |
| D-016-006 | 美术资产是否按 v0.16 治理、v0.17 战斗、v0.18 区域、v0.19 release pack 推进 | 批准 |
| D-016-007 | `行动` 与 `自由目标` 是否合并为一个一级入口 | 批准 |
| D-016-008 | `青茅/凡战/群像/大阵` 是否迁出正式底栏 | 批准 |
| D-016-009 | `蛊虫/杀招/炼蛊/蛊材` 是否合并为 `蛊道` | 批准 |
| D-016-010 | `传承/宿命/终局/商会` 是否按境界/剧情/场景化显示 | 批准 |
| D-016-011 | 正式 NPC/势力系统前是否做旧系统与 `livingWorldState` 权威归并表 | 批准 |
| D-016-012 | 正式经济系统前是否做 material/price/shop/refine 权威归并表 | 批准 |

## 待后续小版本内确认

这些不是当前阻塞，但进入对应小版本前需要在设计门禁中落到具体方案：

| ID | 问题 | 默认建议 |
|---|---|---|
| Q-016-001 | 主导航最终是否采用 `地图 / 行动 / 角色 / 蛊道 / 世界 / 记录` | a1 给设计稿/入口表，用户可审 |
| Q-016-002 | 开发/演示抽屉是否仅在 dev/debug 模式显示 | b3 默认是，仅保留安全入口 |
| Q-016-003 | `行动` 工作台内 tabs 如何命名 | 默认：当前行动、自由目标、线索后续、路线/交易/社会机会 |
| Q-016-004 | `蛊道` 工作台是否先只合并入口，不重做内部 UI | 默认是，避免范围过大 |
| Q-016-005 | 高阶入口何时出现 | 默认按境界、剧情、事实可见性、场景条件显示 |
| Q-016-006 | 是否需要新增存档字段保存 UI 偏好 | 默认不需要；如需必须停下让用户批准 |
| Q-016-007 | 是否需要新 MiroFish 包 | 默认不需要；若新增 canon/NPC/route/hidden fact 规则则重判 |

## a1 已落地为默认方案

| ID | 问题 | a1 结论 |
|---|---|---|
| Q-016-001 | 主导航最终是否采用 `地图 / 行动 / 角色 / 蛊道 / 世界 / 记录` | 作为 v0.16 默认目标冻结；运行时地图文案可保留 `舆图` 风味 |
| Q-016-003 | `行动` 工作台内 tabs 如何命名 | 默认承接：当前行动、自由目标、线索后续、路线/交易/社会机会、行动账本 |
| Q-016-004 | `蛊道` 工作台是否先只合并入口，不重做内部 UI | a1 确认为默认方案，b2 先合并入口和边界说明 |
| Q-016-005 | 高阶入口何时出现 | 默认按境界、剧情、可见事实、场景或开发条件显示 |
| Q-016-006 | 是否需要新增存档字段保存 UI 偏好 | a1 确认默认不需要；如 b 阶段需要必须停下让用户批准 |

## rc 执行结论

| ID | 问题 | rc 结论 |
|---|---|---|
| Q-016-001 | 主导航最终是否采用 `地图 / 行动 / 角色 / 蛊道 / 世界 / 记录` | 已采用并通过 e2e / Player Advocate |
| Q-016-002 | 开发/演示抽屉是否仅在 dev/debug 模式显示 | 已移入 `世界 -> 演武`，不占正式底栏；仍作为演示抽屉，不是正式剧情入口 |
| Q-016-003 | `行动` 工作台内 tabs 如何命名 | 已采用 `当前行动 / 自由目标` 第一刀，线索后续/路线/交易/社会机会仍由现有面板内容承接 |
| Q-016-004 | `蛊道` 工作台是否先只合并入口，不重做内部 UI | 已执行，只做入口归并和边界说明 |
| Q-016-005 | 高阶入口何时出现 | 已先统一收进 `世界` 工作台并写只读/场景化边界 |
| Q-016-006 | 是否需要新增存档字段保存 UI 偏好 | 未新增，`SAVE_FORMAT_VERSION` 保持 22 |
| Q-016-007 | 是否需要新 MiroFish 包 | v0.16 runtime 不需要 blocking MiroFish 包 |

## 明确不做

- 不在 v0.16 开放正式南疆路线。
- 不在 v0.16 开放正式市场、黑市、委托利润、商家城。
- 不在 v0.16 开放蛊仙期。
- 不在 v0.16 增加 DeepSeek 权限。
- 不在 v0.16 默认新增存档字段。
- 不用减法名义做大规模重构。

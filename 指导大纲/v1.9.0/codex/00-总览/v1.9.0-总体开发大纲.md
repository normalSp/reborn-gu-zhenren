# v1.9.0 总体开发大纲

状态：D-190/D-191 已批准；a2 已完成；b1 前复核已完成。
日期：2026-05-22
主题：v2.0 区域活世界预备与门禁收束

## 目标

v1.9 的目标不是做完区域活世界，而是为 v2.0 做好可审计的准备：

- 选定第一个 v2.0 核心区域。
- 设计区域事件、身份路线、路线状态、生存经济、社会压力、冲突后果的合流方式。
- 严肃评估 `SAVE_FORMAT_VERSION = 25`、`regionalEventLedger`、`runFingerprint` 和相关持久字段。
- 完成 MiroFish v2 prelude topic-slice request/intake，并将结果转写为规则草案和测试矩阵。
- 形成 v2.0 长测、live probe、Player Advocate 和测试矩阵硬门。

## 建议阶段

| 阶段 | 名称 | 目标 | 状态 |
|---|---|---|---|
| a0 | 治理补丁与范围冻结 | 启动会、D-190、readiness 草案、skill audit、Git 分支 | completed |
| a1 | v2 区域活世界 save-format 与事件账本设计门禁 | 评估 v25、event ledger、runFingerprint、区域状态、旧档和 rollback | completed；D-191 已批准 |
| a2 | MiroFish v2 prelude topic-slice request/intake | 按双仓流水线获取区域生活/事件/source pointer，不晋升 runtime | completed locally；13 item / 13 source pointer；只进 candidate/rule/test |
| b1-preflight | v2 readiness projection/report 第一刀前复核 | 判断是否需要 a3，冻结 b1 report-only 边界 | completed locally；不需要 a3 |
| b1 | v2 readiness projection/report 第一刀 | 只读合流报告或 pure helper；无 UI tab/store/save/DeepSeek | pending |
| b2 | 区域事件 envelope 与测试矩阵硬化 | 事件候选结构、禁止结论、旧档/反刷样本 | pending |
| b3 | 同开局长期差异与 `runFingerprint` 替代方案 | 比较无持久字段、session seed、per-save seed 的风险收益 | pending |
| b4 | v2.0 T3 长测计划与 dry-run | 300+ mixed/live/replay 计划、checkpoint 和报告格式 | pending |
| process-1 | P2 正式道具词/英文术语 hardening | 从 v1.8 P2 进入 prompt/eval/test matrix | pending |
| process-2 | 只读/分析型子代理试点评估 | 仅评估，不启用；需要用户单独批准 | pending |
| rc | v1.9 质量收束与 v2.0 入场判定 | 输出 v2.0 go/no-go 门禁，不自动进入 v2.0 runtime | pending |

## 不做事项

- 不默认新增 save 字段。
- 不默认改 `SAVE_FORMAT_VERSION = 25`。
- 不默认新增 runtime tab。
- 不把 MiroFish 导出导入 runtime/canon/DeepSeek；a2 只保留 quote-redacted candidate/rule/test 材料。
- 不默认让 DeepSeek 看全书摘要或 RAG。
- 不默认启用子代理。
- 不自动部署 EdgeOne。

## 完成定义

v1.9 完成时应能回答：

1. v2.0 第一核心区域是什么，为什么足够小。
2. v2.0 是否需要 `regionalEventLedger`，如果需要最小字段是什么。
3. 同开局长期差异是否需要 `runFingerprint`，如果需要如何避免事实漂移和回滚风险。
4. MiroFish 哪些 topic-slice 已完成 intake，哪些仍 deferred。
5. v2.0 前 300+ 长测怎么跑、怎么审、什么算失败。
6. 哪些内容仍必须由用户单独批准。

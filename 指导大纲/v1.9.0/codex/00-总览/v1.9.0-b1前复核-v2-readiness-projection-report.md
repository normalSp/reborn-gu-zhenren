# v1.9.0 b1 前复核：v2 readiness projection/report 第一刀

状态：complete locally；用户已批准先做 b1 前复核
日期：2026-05-22
分支：`codex/v190-b1-preflight-readiness-review`
性质：b1 前治理复核；无 runtime、无 save-format bump、无 DeepSeek 权限变更、无 MiroFish 新导出。

## 用户输入

用户批准建议：

> 先做一个很短的 b1 前复核：确认 v2 readiness projection/report 第一刀 是否保持 report-only / pure helper / 无 UI tab；如果不够，再加 v1.9.0-a3-v2-readiness-report设计门禁.md，不要直接冲 runtime。

## 复核结论

专家团结论：

`b1 可以进入，但必须保持 report-only / pure helper / 无 UI tab；当前不需要先追加 a3。`

理由：

- a1 已经完成 save-format、事件账本、runFingerprint、区域状态、身份状态的设计门禁，且 D-191 已全批准。
- a2 已经完成 blocking MiroFish topic-slice intake，提供了 v2 区域活世界预备的 source-pointer、规则草案和测试矩阵样本。
- b1 的目标不是做区域活世界 runtime，而是回答“现有 v1.1-v1.8 地基 + v1.9-a2 材料，距离 v2.0 第一核心区域还有哪些缺口”。
- 如果 b1 保持 report-only，它不会触发 save-format、DeepSeek、Player Advocate/live、MiroFish 新包或 UI/store 权威风险。

## b1 允许范围

b1 允许：

- 新增 `v1.9.0-b1-v2-readiness-projection-report第一刀.md`。
- 汇总 v1.1-v1.8 既有 projection 地基。
- 引用 v1.9-a2 reviewed source-pointer material。
- 形成 v2 readiness checklist / gap report。
- 形成区域事件 envelope 候选、forbidden outcomes、测试矩阵引用。
- 形成 b2/b3/b4/process/rc 的后续工作建议。
- 可选：只读 report/check 脚本或 pure helper 草案，但不得接 UI/store/runtime。

## b1 禁止范围

b1 禁止：

- 新增 UI tab、world panel 面板或玩家可见 runtime。
- 新增 store action。
- 新增 save field、migration、defaults 或 `SAVE_FORMAT_VERSION = 25`。
- 新增 `regionalEventLedger`、`runFingerprint`、`regionalLifeState` / `areaLivingState`、`identityRouteState` / `professionState`。
- 改 DeepSeek prompt/context/model/authority。
- 让 DeepSeek 看到 MiroFish 派生摘要或 full-book RAG。
- 新增 MiroFish export。
- 写入 runtime canon、knowledge-index 正文、正式地点、正式阵营、奖励、NPC 生死或 hidden/private 可见化。
- 启用 BFF/backend、子代理、public wording 或 EdgeOne。

## 触发 a3 的条件

如果 b1 实际方案出现以下任一项，必须先停下，新增 `v1.9.0-a3-v2-readiness-report设计门禁.md`，并让用户决策：

| 触发项 | 为什么必须 a3 |
|---|---|
| b1 要新增玩家可见 UI tab | UI 会改变玩家认知，需要 Player Advocate / live / 文案边界 |
| b1 要接 store action | store 写入会触发 authority、rollback 和旧档问题 |
| b1 要新增持久字段 | 必须重新评估 v25、migration/defaults/tests |
| b1 要让 DeepSeek 看到新摘要 | 触发 DeepSeek authority 和 hidden/private 风险 |
| b1 要把 MiroFish 材料写进 runtime/canon/知识库正文 | 违反 a2 intake 边界 |
| b1 要正式化区域/地点/身份/阵营/奖励/NPC 结论 | 超出 D-191 和 a2 口径 |
| b1 要启用后端、子代理或 public wording | 需要独立架构/安全/发布决策 |

## 推荐 b1 形态

推荐 b1 名称：

`v1.9.0-b1-v2-readiness-projection-report第一刀.md`

推荐 b1 输入：

- `routeLocationState` 既有地基。
- `survivalEconomyState` 既有地基。
- `livingWorldState` 既有地基。
- v1.3 社会 projection。
- v1.4 区域样板 projection。
- v1.5 冲突后果 projection。
- v1.7 区域活世界 projection。
- v1.8 身份/同开局差异 projection。
- v1.9-a2 reviewed source-pointer material。
- 当前测试矩阵与 long narrative drift / Player Advocate 制度。

推荐 b1 输出：

- v2 第一核心区域 readiness scorecard。
- 区域事件 envelope gap list。
- 同开局差异度 gap list。
- formal prop-word / English term hardening inputs。
- T3 300+ 长测前置缺口。
- v2.0 go/no-go 仍需用户决策清单。

## b1 验证策略

若 b1 只有文档：

- `git diff --check`
- `rg -n "v1.9.0-b1|v2 readiness|regionalEventLedger|runFingerprint|SAVE_FORMAT_VERSION = 25" 指导大纲/v1.9.0/codex/00-总览 指导大纲/项目仪表盘.md AGENTS.md .codex/skills/reborn-expert-council/references/PROJECT-STATE.md`

若 b1 新增只读 report/check 脚本：

- 运行对应脚本。
- 运行 `npm test -- <focused script/unit if any>` 或等价 focused check。
- 不需要 full runtime e2e，除非接 UI/store。

若 b1 接 UI/store/runtime：

- 本复核结论失效，先进入 a3 设计门禁。

## Player Advocate / live DeepSeek

- 是否调用 live DeepSeek：否。
- 原因：本复核是纯治理/文档门禁，不触碰 runtime prompt、DeepSeek context 或玩家可见叙事。

b1 report-only 也可豁免 Player Advocate 和 live DeepSeek；如果 b1 变成玩家可见 runtime，则至少需要 30 轮 Player Advocate 和 12-20 轮 `deepseek-v4-flash` live smoke。

## Skill 同步审计

| Skill | 状态 | 处理 |
|---|---|---|
| `reborn-expert-council` | updated | 同步 b1 前复核结论：无需 a3，b1 仅 report-only/pure helper/无 UI tab |
| `game-dev-text` | updated | 同步 b1 工程边界、触发 a3 条件和验证策略 |
| `reverend-insanity-lore` | updated | 同步 b1 不正式化南疆/商家城/身份/地点/奖励/NPC 生死/hidden |
| `mirofish-reborng-export` | no_update_needed | 本复核不新增 MiroFish export，沿用 a2 intake |
| `reborn-combat-motion` | no_update_needed | 本复核不触碰 combat/motion/visual runtime |

## 下一步

可以进入：

`v1.9.0-b1-v2-readiness-projection-report第一刀.md`

默认路线：

- report-only。
- no UI tab。
- no store action。
- no save field。
- no DeepSeek authority。

如果 b1 设计过程中任何人想把 report 变成玩家可见 runtime，先停下新增 a3。

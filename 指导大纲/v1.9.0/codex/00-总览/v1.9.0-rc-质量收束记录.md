# v1.9.0-rc 质量收束记录

状态：complete locally；report-only milestone
日期：2026-05-22
分支：`codex/v190-b1-to-rc-readiness-closure`
主题：v2.0 区域活世界预备与门禁收束

## 完成范围

v1.9 已完成：

- a0 治理补丁与范围冻结。
- a1 v2 区域活世界 save-format 与事件账本设计门禁，D-191 已批准。
- a2 MiroFish v2 区域活世界预备 topic-slice intake，13 items / 13 source pointers / quoteLikeKeys=0 / blocked=0。
- b1 前复核，确认 b1 无需 a3、只能 report-only / pure helper / 无 UI tab。
- b1 v2 readiness projection/report 第一刀。
- b2 区域事件 envelope 与测试矩阵硬化。
- b3 同开局长期差异与 `runFingerprint` 评估。
- b4 v2 T3 300+ mixed/live/replay 长测计划。
- process-1 P2 术语与正式凭证 hardening。
- process-2 只读/分析型子代理试点评估。
- rc Skill 同步审计。

## 本版本明确没有做

- 没有新增 runtime。
- 没有新增 UI tab / world panel。
- 没有新增 store action。
- 没有新增 save field、migration、defaults。
- 没有 bump `SAVE_FORMAT_VERSION = 25`。
- 没有新增 `regionalEventLedger`、`runFingerprint`、`regionalLifeState` / `areaLivingState`、`identityRouteState` / `professionState`。
- 没有改 DeepSeek prompt/context/model/authority。
- 没有把 MiroFish 摘要喂给 DeepSeek。
- 没有 runtime canon / knowledge-index 正文晋升。
- 没有正式地点、正式阵营、奖励、NPC 生死或 hidden/private 可见化。
- 没有启用 BFF/backend、子代理、public wording 或 EdgeOne。

## v2.0 go/no-go

v1.9 的结论是：

`v2.0 可以进入启动会/设计门禁讨论，但不能自动进入 runtime。`

进入 v2.0 runtime 前，仍需用户逐项决策：

1. 是否正式批准第一核心区域为 `南疆早期低阶外缘小区域`。
2. 是否 bump `SAVE_FORMAT_VERSION = 25`。
3. 是否新增 `regionalEventLedger`。
4. 是否新增 `runFingerprint`。
5. 是否新增正式区域/身份状态。
6. 是否执行 T3 300+ mixed/live/replay 长测。
7. 是否允许 DeepSeek visible knowledge/RAG 或 BFF/backend。
8. 是否允许只读/分析型子代理试点。
9. 是否允许任何正式地点、阵营、奖励、NPC 生死或 canon promotion。

## Player Advocate / live DeepSeek

- 是否调用 live DeepSeek：否。
- 原因：v1.9 b1-rc 均为 report-only / plan-only /评估-only，不触碰 runtime prompt、DeepSeek context 或玩家可见叙事。

若 v2.0 进入玩家可见 runtime，必须重新启用 Player Advocate 与 live narrative probe，并按 T3 计划执行。

## 验证结果

本阶段已运行：

| 命令 | 结果 |
|---|---|
| `git diff --check` | pass；仅 Windows 换行提示 |
| `rg -n "v1.9.0-b1-v2-readiness\|v1.9.0-b2-\|v1.9.0-b3-\|v1.9.0-b4-\|v1.9.0-process-1\|v1.9.0-process-2\|v1.9.0-rc" ...` | pass；入口、仪表盘、AGENTS、PROJECT-STATE 均可检索 |
| `npm run check:mirofish-dual-repo-pipeline -- --target-version=v1.9.0 --topic=southern_border_low_rank_region_life_v2_prelude_slice --stage=complete` | pass；P0=0、P1=0、P2=0、Info=0 |

MiroFish checker report：

`artifacts/v1.9.0/mirofish-dual-repo-pipeline/2026-05-22T06-36-53-526Z/report.json`

不运行 unit/e2e/build/live，因为 v1.9 b1-rc 不改源码、runtime、UI、prompt、存档或后端。

# v3.2.0-a2 proposal batch / post-check / expression chain 门禁

日期：2026-05-24
状态：completed
冻结模型：`v320_a2_proposal_postcheck_expression_chain_v1`

## 链路

v3.2 rehearsal 链路固定为：

`synthetic world snapshot -> L2/L3 proposal batch -> WorldCore post-check rehearsal -> approved_expression / rejected_violation / manual_review_required / future_gate_required`

## Agent 权限

Agent 只允许输出：

- NPC intent candidate。
- expression candidate。
- pressure hint candidate。
- safe next-step wording candidate。

Agent 不允许：

- 写 runtime。
- 写 save。
- 裁决地点、阵营、身份、奖励、NPC 生死。
- 决定正式关系、通缉、招揽、封锁。
- 读取或暴露 hidden/private body。
- 让 self-learning 直接写 canon/runtime/save/prompt/knowledge-index。

## WorldCore post-check

WorldCore post-check rehearsal 必须能分类：

- `approved_expression`：表达候选可进入报告样本，不进 UI。
- `rejected_violation`：越权候选被拒绝。
- `manual_review_required`：模糊但未越权的候选转人工复核。
- `future_gate_required`：涉及 F-320 的候选转未来审批。

## 结论

a2 完成。v3.2 的 rehearsal chain 已冻结，后续 b1 用 checker 验证。

# v3.2.0 测试矩阵

日期：2026-05-24
状态：startup completed；pending user approval

## startup 文档自检

| 样本 | 检查 | 通过标准 |
|---|---|---|
| V32-DOC-001 | `git diff --check` | 无 whitespace error |
| V32-DOC-002 | `rg -n "v3.2.0|D-320|F-320|runtime rehearsal|offline / report-only"` | 入口文件命中 |
| V32-DOC-003 | 敏感词扫描 | 命中必须处于 future gate、禁止、待审批或非目标语境 |

## 后续 D-320 获批后的候选测试

| 样本 | 类型 | 通过标准 |
|---|---|---|
| V32-A0-ADMISSION-* | v3.1 准入复核 | `v32RehearsalAdmissible=true` 被作为输入；不转成 runtime approval |
| V32-A1-SNAPSHOT-* | synthetic world snapshot | 无真实命名 NPC、真实隐藏事实、正式地点/奖励/NPC 生死 |
| V32-A2-PROPOSAL-* | L2/L3 proposal batch | 候选只表达意图/语言/压力，不写事实 |
| V32-A2-POSTCHECK-* | WorldCore post-check rehearsal | 能分类 accepted expression / rejected / manual_review / future_gate |
| V32-B1-CHECKER-* | report-only checker | P0/P1 falseNegative=0，resultMismatch=0 |
| V32-B2-NEGATIVE-* | negative fixtures | hidden leak、formal authority、save write、DeepSeek authority、MiroFish blocking、external framework boundary 必须被拦截 |
| V32-B3-REOPEN-* | v3.3 决策包 | 只输出风险收益和待批项，不批准 implementation |

## Player Advocate / live DeepSeek

v3.2 startup 不运行 Player Advocate 或 live DeepSeek。

D-320 建议将 v3.2 后续阶段继续记录：

- 是否调用 live DeepSeek：否。
- 若未来改为“是”，必须另批模型、样本、轮次、成本和报告路径。

## Test Matrix Evolution

若 v3.2 后续出现新边界样本，必须 triage：

- `current_matrix`
- `future_sample_pool`
- `discarded`

不得只写“以后补”。

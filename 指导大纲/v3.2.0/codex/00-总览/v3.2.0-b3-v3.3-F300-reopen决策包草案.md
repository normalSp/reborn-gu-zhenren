# v3.2.0-b3 v3.3 F-300 reopen 决策包草案

日期：2026-05-24
状态：completed
冻结模型：`v320_b3_v33_f300_reopen_decision_package_v1`

## 当前证据

v3.2 checker 给出：

- `v33DecisionPackageReady=true`。
- `runtimeImplementationApproved=false`。
- P0/P1 falseNegative：0/0。
- `future_gate_required` 分类覆盖 7 项。
- `rejected_violation` 分类覆盖 12 项。

## v3.3 可以做什么

v3.3 可以开 F-300 reopen 决策包，给用户完整展开：

- 是否值得打开 F-300-001 runtime agent implementation。
- 如果打开，是否同步需要 save-format / runFingerprint。
- 是否需要 live DeepSeek。
- 是否需要 MiroFish blocking topic-slice。
- 是否需要 backend/BFF 或继续 frontend-only。
- 是否允许任何 external framework PoC。
- Player Advocate、live probe、rollback、old-save、long drift 的档位。

## v3.3 不可默认做什么

v3.2 不把 v3.3 自动升级为 implementation。v3.3 默认仍应先做决策包。任何 implementation 都必须由用户单独批准。

## 专家团建议

建议 v3.3 开专家团启动会时，把 F-300 reopen 拆成：

- 最小 runtime first cut 的收益。
- 必须增加的工程成本。
- 需要打开的 future gate。
- 仍可继续 report-only hardening 的替代路线。

## 结论

b3 完成。v3.2 可以作为 v3.3 决策包输入，但不构成 F-300 实装批准。

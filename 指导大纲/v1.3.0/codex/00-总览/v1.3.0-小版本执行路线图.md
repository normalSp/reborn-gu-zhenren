# v1.3.0 小版本执行路线图

状态：a0 已建立；后续阶段等待 a1
日期：2026-05-21

## 总路线

v1.3 采用先制度后 runtime 的顺序：

`a0/a1/a2 -> b1/b2/b3/b4 -> process-1/process-2 -> rc`

## a0 启动审查与治理补丁

状态：本启动包已建立。

产出：

- 专家团启动会纪要。
- 启动审查与范围冻结。
- 总体开发大纲。
- 需求决策池。
- 测试矩阵。
- MiroFish 需求判断。
- Git 计划。
- Skill 同步审计制度。

通过标准：

- 不写 runtime。
- 不新增 save 字段。
- 不扩大 DeepSeek 权限。
- 记录 D-130-001 至 D-130-009。
- 明确 a1 后需要用户拍板的事项。
- 明确 skill sync audit 结果与补点。

## a1 NPC/势力关系 save-format 设计门禁

状态：待执行。

目标：

- 冻结 v1.3 authority table。
- 严肃评估是否需要 `SAVE_FORMAT_VERSION = 25`。
- 设计单一社会聚合对象或 projection-only 路线。
- 定义命名 NPC、关系证据、势力压力、公开事件、隐藏事实邻近的权限边界。
- 列出 b1 后必须由用户拍板的事项。

默认口径：

- b1 不默认 bump。
- b1 默认 projection-only。
- 若新增持久字段，必须 migration/defaults/tests 同变更。

## a2 MiroFish topic slice 与测试矩阵门禁

状态：待执行。

目标：

- 复用 v0.13 三包。
- 从全书基础包只取 NPC/势力长期关系 topic slice 的 source pointer 和 coverage 缺口。
- 不整包导入知识库、runtime、canon 或 DeepSeek。
- 冻结 `V13-*` 测试矩阵。

通过标准：

- MiroFish need level 写清楚。
- hidden/private 不可见事实不进入 DeepSeek visible context。
- 新样本 triage 到 `current_matrix`、`future_sample_pool` 或 `discarded`。

## b1 关系证据与社会压力 projection-only 第一刀

状态：待用户 a1/a2 后批准。

默认目标：

- UI 显示近期公开事件、可疑证据、NPC/势力压力和下一步。
- DeepSeek 只写社会叙事，不写正式结论。
- 不 bump save format，不新增社会状态字段。

## b2 最小社会状态 ledger 或继续 projection-only

状态：取决于 a1 决策。

可选路线：

- `projection-only`：继续不新增字段。
- `minimum ledger`：若用户批准，新增单一聚合对象，只记录证据、来源、压力和禁止项。

不得默认开放正式关系数值、通缉、招揽、阵营或任务奖励。

## b3 NPC 个人反应与接触窗口

状态：待 b1/b2 稳定后评估。

默认目标：

- 命名或半命名 NPC 的接触窗口、拒绝理由、试探条件、传闻压力。
- 不写正式好感度、不写生死、不写背叛、不写招募成功。

## b4 势力封锁/招揽/通缉前置条件

状态：待 b1/b2 稳定后评估。

默认目标：

- 势力层面的封锁、审查、招揽、追查和通缉前置条件可读化。
- 不定案、不转阵营、不发奖励、不开放完整势力系统。

## process-1 反刷、save 兼容与回滚

状态：待 runtime 后执行或记录豁免。

目标：

- 玩家不能通过反复交谈/试探稳定刷关系、任务、保护、通缉解除或奖励。
- 新旧档兼容。
- 回滚点和 commit 边界清楚。

## process-2 长线漂移与知识库复核

状态：待 runtime 后执行或记录豁免。

目标：

- 检查 NPC/势力关系长期不漂移。
- 检查隐藏事实邻近样本不泄露。
- 检查 MiroFish 基础包没有被整包吸收。
- 检查 skill sync audit 已执行。

## rc 质量收束

状态：待执行。

默认强度：

- 未新增持久社会字段：Player Advocate 100 轮。
- 新增持久社会字段、命名 NPC allowlist 或 formal relation ledger：Player Advocate 120 轮。
- 必跑 live probe：模型固定 `deepseek-v4-flash`；成本、样本和轮次 rc 前提交给用户确认。

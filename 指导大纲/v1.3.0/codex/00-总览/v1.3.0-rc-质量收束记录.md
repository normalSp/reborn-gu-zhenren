# v1.3.0-rc 质量收束记录

日期：2026-05-21
状态：本地 rc 收束进行中；live probe 待用户确认规模

## 版本结论

v1.3.0 完成了 `NPC 与势力长期关系第二层` 的 projection-only 阶段：

- b1：社会压力四类信号。
- b2：projection audit 硬化。
- b3：NPC 接触窗口。
- b4：势力前置条件。
- process-1：社会反刷、save 兼容与回滚复核。
- process-2：deterministic 长线漂移与知识库复核。
- rc：100 轮 Player Advocate 本地记录。

仍未完成：

- D-130-009 live probe。模型固定 `deepseek-v4-flash`，但成本、样本、轮次和通过标准必须由用户确认后执行。

## 不开放清单

v1.3.0-rc 仍不开放：

- `SAVE_FORMAT_VERSION = 25`
- `socialRelationState`
- 正式 NPC 好感/关系数值
- 正式命名 NPC allowlist
- 正式阵营转移
- 正式通缉/封锁/招揽
- 正式任务/奖励
- NPC 生死/捕获/背叛
- DeepSeek 社会结论 authority
- MiroFish 原始包 runtime authority
- 全书基础包 DeepSeek visible context

## 本地验证

已通过：

- `npm run eval:deepseek:v130-social-dry-run`：4 样本 x 3 轮 dry-run，预计 12 次 live call，worst-case 估算约 `$0.00549612`，不调用 API
- `npm run eval:deepseek:v130-social-dry-run:low`：3 样本 x 2 轮 dry-run，预计 6 次 live call，worst-case 估算约 `$0.00275268`，不调用 API
- `npm run eval:deepseek:v130-social-dry-run:high`：5 样本 x 4 轮 dry-run，预计 20 次 live call，worst-case 估算约 `$0.00915166`，不调用 API
- `npm test -- v130-social-pressure-projection`
- `npx tsc --noEmit --pretty false`
- `npx playwright test tests/e2e/v130-social-pressure-projection.spec.ts`
- `npm run check:player-advocate-gate -- 指导大纲/v1.3.0/codex/00-总览/v1.3.0-rc-Player-Advocate-100轮走查记录.md 100`
- `npm test`
- `npm run build`
- `npm run test:e2e:long`

## live probe 决策

建议方案见：`v1.3.0-rc-live-probe执行方案待确认.md`。

当前可执行命令：

- 推荐：`npm run eval:deepseek:v130-social-live`
- 低成本：`npm run eval:deepseek:v130-social-live:low`
- 高强度：`npm run eval:deepseek:v130-social-live:high`

Skill 同步：

- `reborn-expert-council`、`game-dev-text`、`reverend-insanity-lore`、`mirofish-reborng-export` 已同步到 `v1.3 rc local gate pending live probe` 口径。
- `reborn-combat-motion` 未触发，保持 no update needed。
- live probe 通过后还需再同步一次最终 `v1.3 complete` 口径。

在用户确认前：

- 不执行 live probe。
- 不提交“v1.3 最终完成”口径。
- 不更新 skill 为 `v1.3 complete`，只能写 `v1.3 rc local gate pending live probe`。

## 下一步

等待用户确认 live probe 规模后执行；若通过，再做最终 skill sync、PROJECT-STATE/AGENTS/仪表盘/handoff 收束和最终提交推送。

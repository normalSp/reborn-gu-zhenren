# v2.2.0 Git 提交与推送计划

状态：a1 completed；等待提交推送。
日期：2026-05-23

## 当前分支

`codex/v220-a1-agent-lab-scenario-gate`

## 基线

- 从 `codex/v220-a0-agent-lab-expansion-startup` 干净工作区切出。
- v2.2 startup/a0 commit：`9773469b docs: 开启v2.2 Agent Lab扩展启动会`。
- v2.2 startup/a0 CI：`RebornG CI` run `26304482868` 通过。

## 本批提交范围

只提交：

- D-220 批准记录。
- v2.2 a1 场景模型设计门禁。
- v2.2 D-221 决策池。
- v2.2 测试矩阵与 MiroFish 分类。
- v2.2 Git 计划。
- v2.2 a1 skill sync audit。
- dashboard / PROJECT-STATE / AGENTS / historical-index / handoff 同步。

不提交：

- runtime 源码。
- package / lockfile。
- v2.2 runner 或样本 JSON。
- artifact。
- DeepSeek prompt/context。
- BFF/backend。
- MiroFish export。
- 第三方 SDK/framework PoC 或依赖。
- 子代理配置。

## 提交建议

`docs: 开启v2.2 Agent Lab扩展启动会`

本阶段建议：

`docs: 完成v2.2 a1 Agent Lab场景门禁`

## 推送

文档自检通过后推送：

```powershell
git push -u origin codex/v220-a1-agent-lab-scenario-gate
```

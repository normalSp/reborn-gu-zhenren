# v2.2.0-rc Agent Lab 扩展离线模拟质量收束记录

状态：completed。
日期：2026-05-23

## 版本结论

`v2.2.0` 本地完成，主线为：

`Agent Lab 扩展离线模拟第一阶段`

完成内容：

- a1 20 NPC / 3 pressure source / 1 L5 场景模型。
- a2 memory / visibility / WorldCore post-check 扩展门禁。
- b1 expanded offline runner 第一刀。
- b2 multi-turn replay / rescore / failure taxonomy。
- b3 report archive / future_sample_pool 回流。
- rc skill sync、入口同步和质量收束。

## runner 证据

命令：

```powershell
npm run check:v220-agent-lab-expanded-offline
```

结果：

- report：`artifacts/v2.2.0/agent-lab-expanded-offline-runner/2026-05-23T06-20-49-546Z/report.json`
- samples：21。
- schemaValidCount：21/21。
- acceptedForGate：21/21。
- acceptedCandidateCount：21。
- needsUserDecisionCount：0。
- rejectedViolationCount：0。
- npcAgentCount：20。
- factionCount：3。
- l5AgentCount：1。
- roundCount：10。
- P0/P1/P2：0/0/0。
- hiddenLeakCount：0。
- formalAuthorityDriftCount：0。
- memoryContaminationCount：0。
- rescoreStable：true。
- passed：true。

## Player Advocate / live DeepSeek

Player Advocate：豁免。

原因：

- v2.2 不改玩家可见 UI/runtime/prompt。
- runner 是 offline / report-only 工具。
- 没有新玩家体验可走查。

是否调用 live DeepSeek：否。

若是：不适用；无模型、样本、轮次、成本或 live 报告路径。

## 明确未授权

v2.2 完成不授权：

- runtime agent。
- live DeepSeek。
- DeepSeek visible lore/RAG。
- BFF/backend。
- subagents。
- 外部 SDK/framework PoC、依赖或项目写入。
- MiroFish export。
- 正式地点、阵营、奖励、NPC 生死、canon promotion。
- `runFingerprint`。
- 新 save fields 或 `SAVE_FORMAT_VERSION` bump。
- 后端 job queue / cloud archive。

## 本地验证清单

已运行并通过：

| 命令 | 结果 |
|---|---|
| `npm run check:v220-agent-lab-expanded-offline` | 21/21 schema valid；P0/P1/P2=0/0/0；passed=true |
| `git diff --check` | 通过；仅有 Windows CRLF 提示，无 whitespace error |
| `npx tsc --noEmit --pretty false` | 通过 |
| `npm test` | 152 files / 814 tests passed |
| `npm run build` | 通过 |
| D-222 / runner 文档入口 `rg` | 命中 v2.2 README、测试矩阵、AGENTS、PROJECT-STATE、handoff、package script |
| 敏感边界 `rg` | 命中均处于禁止、豁免、未来决策或边界说明语境 |

## 下一步建议

先开 `v2.3` 专家团启动会。

v2.3 候选主题：

- Agent eval farm。
- invalid negative sample runner。
- failure taxonomy hardening。
- long offline replay。
- agent memory aging / pressure propagation graph。

不要直接从 v2.2 跳到 runtime agent。

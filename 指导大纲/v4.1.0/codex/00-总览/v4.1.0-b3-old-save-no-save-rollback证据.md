# v4.1.0-b3 old-save / no-save / rollback 证据

状态：completed。
日期：2026-05-26。

## no-save 证据

v4.1 只新增 report-only checker、synthetic/generic 样本、报告与文档，不修改 runtime save schema。

确认项：

- `SAVE_FORMAT_VERSION` 保持 25。
- 未新增 save field。
- 未新增 migration。
- 未新增 store slice。
- 未新增 `runFingerprint`。
- 未将 Auto-Theater contract 写入玩家存档。
- 未将 report artifact 写入 runtime。

## old-save 影响

v4.1 不修改 `src` runtime store/save/migration，因此旧档读取路径不受本版本影响。需要旧档测试的最低判断是：

- 若未来 v4.x 开始做 Auto-Theater Lite 或 theater UI，只要接触 store/save，就必须重新打开 old-save / migration 门禁。
- v4.1 当前仅需通过 no-save 静态边界、TypeScript、unit、build、e2e 与 production-preview smoke。

## rollback 证据

v4.1 的回滚粒度为文档 + checker + samples + npm script + report：

- 删除 `scripts/run-v410-auto-theater-contract-check.mjs`
- 删除 `tests/evals/v410-auto-theater-contract/samples.json`
- 删除 `package.json` 中 `check:v410-auto-theater-contract`
- 删除 `artifacts/v4.1.0/auto-theater-contract/.../report.json`
- 删除或回退 `指导大纲/v4.1.0/codex/00-总览/` 中 b/a/rc 证据文件

无需迁移玩家存档，无需清理 runtime store，无需回滚 DeepSeek prompt/context/model/authority，无需撤回 MiroFish intake。

## 结论

b3 old-save/no-save/rollback 通过。v4.1 没有改变玩家运行时状态和存档合同，后续若要将 Auto-Theater 进入任何 runtime 表达链，必须重新打开 save/runtime/old-save 门禁。

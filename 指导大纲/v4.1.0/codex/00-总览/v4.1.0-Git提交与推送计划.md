# v4.1.0 Git 提交与推送计划

状态：startup drafted。

## 分支

当前分支：

`codex/v410-startup-auto-theater-contract-hardening`

该分支从 v4.0 完成态派生，用于 v4.1 启动会和后续 D-410 授权内开发。

## 启动会提交计划

本次启动会只提交：

- v4.1 启动会文档。
- dashboard / AGENTS / PROJECT-STATE 当前入口同步。
- 必要 skill current override 同步。

不提交 runtime/source/UI/store/prompt/save 改动。

## 后续提交计划

若用户批准 D-410：

- a0/a1/a2 可合并为一个设计门禁提交或按阶段提交。
- b1 checker / samples / npm script / report artifact 应单独提交。
- b2/b3/rc 证据和入口同步可按质量收束提交。

每个提交必须中文、具体、可追溯。

## main 合并

v4.1 完成后必须记录是否建议合并 `main`，但 Codex 不自动合并、不改 branch protection、不 force push、不 rewrite history、不部署 EdgeOne。

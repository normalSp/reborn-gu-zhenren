# v4.1.0 启动审查与范围冻结

状态：startup drafted。
日期：2026-05-26。

## 进入条件

- `v4.0.0` 已完成本地开发并通过 CI。
- `v400` readiness checker 已通过：33/33 schema valid，13 ready / 7 future gate / 13 blocked，positiveP0=0，negativeFalseNegative=0，acceptedForGate=true。
- v4.0 60 轮 Player Advocate 通过，live DeepSeek：否。
- v4.0-v5.0 长期路线已明确 v4.1 为 Auto-Theater contract v1。
- 当前分支已切为 `codex/v410-startup-auto-theater-contract-hardening`。

## 范围冻结

v4.1 只冻结 contract / schema / checker / evidence / authority：

- `AutoTheaterContractV1`
- `PreparationEnvelopeV1`
- `TheaterLayerV1`
- `KillerMoveStackFrameV1`
- `CombatLedgerEntryV1`
- `ExpressionAuthorityV1`
- `WorldCoreEvidenceChainV1`
- report-only `v410` checker 计划
- synthetic/generic negative fixtures
- Player Advocate / old-save / no-save / rollback 计划

## 非目标

- 不实现 theater UI。
- 不迁移凡阶战斗 runtime。
- 不实现高阶战斗 runtime。
- 不生成或晋升 Auto-Theater 素材。
- 不新增 save field / migration / `SAVE_FORMAT_VERSION` bump。
- 不调用 live DeepSeek。
- 不做 MiroFish export/intake。
- 不引入 backend、外部框架、子代理或依赖。

## v4.1 与 v4.2 的分界

v4.1 只定义 contract。v4.2 才考虑把现有凡阶战斗映射为 Auto-Theater Lite transcript / Combat Ledger report-only 样本；v4.2 也不应默认迁移 runtime。

## v4.1 与 v4.7/v4.8 的分界

v4.7 才给用户 runtime 准入包；v4.8 才可能在未来获批后做最小 runtime first cut。v4.1 不提前打开这些门。

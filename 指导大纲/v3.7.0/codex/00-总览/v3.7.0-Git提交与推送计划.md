# v3.7.0 Git 提交与推送计划

日期：2026-05-25
状态：startup commit/push/CI passed；D-370 approval record commit/push/CI passed

## 分支

当前 v3.7 startup 分支：

`codex/v370-startup-multi-npc-small-faction`

当前 Git 制度补丁：

- `指导大纲/流程制度/Git分支切换与推送制度.md` 已加入“主线合并与版本分支制度补丁”。
- `main` 定义为稳定主线；`codex/vXYZ-*` 继续作为版本/专项工作分支。
- 暂不新增 `develop`；版本完成时在 rc/质量收束阶段写明是否建议合并 `main`。
- 不自动合并、不自动部署、不改 branch protection；合并 `main` 仍需用户批准或 PR 审核。

## stage 规则

- 显式 stage 本次 v3.7 文档、入口同步和 skill audit。
- 不使用 `git add -A`。
- 不 stage 与 v3.7 startup 无关的脏文件。

## 提交边界

startup 提交建议：

`docs: 开启v3.7多NPC小势力启动会`

本次提交包含：

- v3.7 startup docs。
- D-370/F-370 前置授权包。
- 分层 Agent / WorldCore 测试体系审计。
- dashboard / AGENTS / PROJECT-STATE 同步。
- skill sync audit 记录。

不包含：

- runtime/source/UI/store/prompt/save changes。
- live DeepSeek artifacts。
- MiroFish export/intake。
- backend/external framework PoC。

## 推送与 CI

已完成：

- startup commit：`6674e2eb docs: 开启v3.7多NPC小势力启动会`。
- push：`codex/v370-startup-multi-npc-small-faction` 已推送到 origin。
- GitHub Actions：run `26379850673` passed，deterministic quality gate 通过。

已完成：

- D-370 approval record commit：`13d2bf88 docs: 记录v3.7授权决策`。
- push：`codex/v370-startup-multi-npc-small-faction` 已推送到 origin。
- GitHub Actions：run `26380369995` passed，deterministic quality gate 通过。

已完成：

- Git 制度补丁 commit：`61496233 docs: 增加主线合并与版本分支制度`。
- push：`codex/v370-startup-multi-npc-small-faction` 已推送到 origin。
- GitHub Actions：run `26381099012` passed，deterministic quality gate 通过。

## v3.7 completion 计划

待完成：

- implementation commit：建议 `feat: 完成v3.7多NPC小势力意图图谱`。
- push：`codex/v370-startup-multi-npc-small-faction`。
- GitHub Actions：等待 deterministic quality gate 通过。
- evidence commit：记录最终 commit / CI 证据后再推送。

## main 合并建议

v3.7 完成后建议：暂不自动合并 `main`。原因：

- 本分支已经包含 v3.7 startup、授权、Git 制度补丁和 runtime implementation，适合先作为版本分支完整证据。
- `main` 作为稳定主线，合并应在用户批准或 PR 审核后执行。
- 当前任务不包含 branch protection、release、EdgeOne 或 public deployment。

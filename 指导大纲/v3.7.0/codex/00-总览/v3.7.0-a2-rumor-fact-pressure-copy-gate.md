# v3.7.0-a2 rumor/fact 与 pressure handoff copy gate

日期：2026-05-25
状态：completed
冻结版本：`v370_a2_rumor_fact_pressure_handoff_copy_gate_v1`

## 文案原则

v3.7 所有玩家可见 graph 文案必须同时说清：

- 候选不是事实。
- 传闻/观察不是 known fact。
- 小势力压力不是 formal standing。
- pressure handoff 只把公开压力交给 WorldCore post-check。
- WorldCore 未裁决前不写关系、阵营、身份、奖励、通行、通缉、招揽、封锁或 NPC 命运。

## 阻断文案

rejected / expired / needs_user_decision graph nodes 只能显示：

- 安全原因。
- future gate 原因。
- 重新取样或等待用户审批的下一步。

不得显示被拒正文、隐藏事实、正式 lore 结论或可以被误读为世界事实的内容。

## UI 约束

`意图` tab 可新增 graph summary，但必须继续保留 v3.4/v3.5/v3.6 的回归短语：

- `Runtime AgentProposal`
- `v3.5 lifecycle v2`
- `v3.6 synthetic L2/L3 micro-expansion`
- `v3.7 proposal graph`
- `runFingerprint：否`
- `live DeepSeek：否`

## 结论

a2 通过。文案门禁允许 b1 接入 `RuntimeAgentProposalPanel`，但不扩大权限。

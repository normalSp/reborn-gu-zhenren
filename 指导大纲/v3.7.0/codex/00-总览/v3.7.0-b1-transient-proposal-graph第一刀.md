# v3.7.0-b1 transient proposal graph 第一刀

日期：2026-05-25
状态：completed
实现版本：`v370_b1_transient_agent_proposal_graph_v1`

## 代码变更

- 新增 `src/engine/v370-transient-agent-proposal-graph.ts`。
- 新增 `src/engine/v370-transient-agent-proposal-graph.test.ts`。
- 更新 `src/components/game/RuntimeAgentProposalPanel.tsx`，在既有 `意图` tab 内显示 proposal graph。
- 更新 `src/components/game/WorldHubPanel.tsx` 总览与边界说明。
- 新增 `tests/e2e/v370-runtime-agent-proposal-graph.spec.ts`。
- 更新 `package.json`：新增 `check:v370-transient-agent-proposal-graph`，并把 v370 e2e 纳入 `test:e2e:long`。

## 范围确认

本刀没有新增：

- store slice。
- save field。
- migration。
- `SAVE_FORMAT_VERSION` bump。
- `runFingerprint`。
- live DeepSeek。
- MiroFish。
- backend/BFF/service。
- 外部框架、dependency、subagent。
- 正式地点、阵营、身份、奖励、NPC 生死、通缉、招揽、封锁。

## 聚焦验证

已通过：

```powershell
npm run check:v370-transient-agent-proposal-graph -- --reporter=dot
npx tsc --noEmit --pretty false
npm run check:v360-transient-agent-micro-expansion -- --reporter=dot
npm run test:e2e -- tests/e2e/v340-runtime-agent-proposal.spec.ts tests/e2e/v350-runtime-agent-hardening.spec.ts tests/e2e/v360-runtime-agent-micro-expansion.spec.ts tests/e2e/v370-runtime-agent-proposal-graph.spec.ts
```

## 结论

b1 通过。v3.7 可以进入 b2 deterministic drift 记录。

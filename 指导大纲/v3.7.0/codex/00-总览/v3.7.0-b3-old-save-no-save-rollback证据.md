# v3.7.0-b3 old-save / no-save / rollback 证据

日期：2026-05-25
状态：completed
证据版本：`v370_b3_old_save_no_save_rollback_evidence_v1`

## old-save

`buildV370TransientAgentProposalGraph({})` 可在空输入 / 旧档式输入下返回 safe report：

- `oldSaveRequiredFields=none`
- `newSaveFieldsAdded=false`
- `migrationRequired=false`
- `rollbackMode=drop_transient_graph_report_only`

## no-save

v3.7 graph node 全部保持：

- `factsCommitted=false`
- `saveWritten=false`
- `promptWritten=false`
- `canonPromoted=false`
- `formalRelationWritten=false`
- `factionStandingWritten=false`
- `warrantWritten=false`
- `recruitmentWritten=false`
- `blockadeWritten=false`

## rollback

回滚策略是删除 transient report/UI 第一刀即可：

- 无 save migration 需要撤销。
- 无 store slice 需要清理。
- 无 prompt/context/cache 权限变化。
- 无 external dependency / backend / service。

## 结论

b3 old-save/no-save/rollback 通过。

# v0.13.0 项目仪表盘

日期：2026-05-16
状态：主线、第一刀、Git 安全收束和前两项 MiroFish 请求已获用户批准
用途：给用户快速查看 v0.13 当前规划、阻塞和下一步。

## 当前一句话

`v0.13.0` 建议主线是 `NPC 与势力反应系统`：先建立社会记忆协议，再让 NPC/势力根据玩家公开行为形成长期记忆、压力、机会和可见摘要。

## 当前版本状态

| 项目 | 状态 |
|---|---|
| 包版本 | `0.9.0` |
| 当前已完成开发线 | `v0.12.0` |
| 当前规划线 | `v0.13.0` 已批准进入 a1 |
| 当前流程线 | `v0.13.0-process-0 Git安全收束` 第一刀已建立 |
| 建议主线 | `NPC 与势力反应系统` |
| 存档版本 | `SAVE_FORMAT_VERSION = 22` |
| DeepSeek 运行模型 | `deepseek-v4-flash` |
| 当前阻塞 | runtime 命名 NPC/势力规则前需要 MiroFish 包；a1 文档门禁不阻塞 |

## 已完成

- v0.13 专家团启动会纪要。
- v0.13 总体开发大纲。
- v0.13 启动审查与范围冻结草案。
- v0.13 小版本路线图。
- v0.13 需求决策池。
- v0.13 真相源索引。
- v0.13 MiroFish 资料需求与交付协议。
- v0.13 MiroFish 请求文件三份。
- 用户已批准 v0.13 主线、a1 第一刀、v22 字段策略，以及前两个 MiroFish 包请求。
- 用户已批准每个大版本建立 Git 提交与推送计划，`v0.13.0-process-0` 作为 a1 前置流程刀。

## 正在做

当前 `v0.13.0-process-0 Git安全收束` 第一刀已建立：本版本 Git 计划、回滚守门职责、提交分组和脏项隔离记录已落文档。用户将转交前两个 MiroFish 请求。

## 下一步队列

| 优先级 | 项目 | 状态 |
|---|---|---|
| P0 | 用户批准 v0.13 主线 | 已完成 |
| P0 | 用户批准第一刀 a1 设计门禁 | 已完成 |
| P0 | 用户决定是否先请求 MiroFish 前两个包 | 已完成，等待用户转交 |
| P0 | `v0.13.0-process-0` Git 安全收束 | 第一刀已建立，后续按分组提交 |
| P1 | `v0.13.0-a1` 社会记忆协议、字段表、测试矩阵 | 可开工 |
| P1 | `v0.13.0-a2` NPC 记忆投影引擎第一刀 | 依赖 a1 与 MiroFish |
| P1 | `v0.13.0-b1` 势力态度 / 压力投影第一刀 | 依赖 a1 与 MiroFish |

## 当前 MiroFish 需求

建议先请求：

1. `qingmao_npc_memory_motive_pack`
2. `qingmao_faction_reputation_pressure_pack`

可以延期：

3. `qingmao_public_event_chronicle_pack`

## 当前阻塞

进入 runtime 前的阻塞：

- MiroFish 前两个包尚未交付和 intake review。

不阻塞：

- `v0.13.0-a1` 文档设计门禁可以在用户批准后先做，不需要先等 MiroFish。

## 最近验证

最近完整验证来自 `v0.12.0-rc`：

- `npm test -- --reporter=dot`：117 个 test file、674 个测试通过。
- `npx tsc --noEmit --pretty false`：通过。
- `npm run build`：通过。
- runtime/Qingmao/player-visible-copy scans：通过。
- production-preview smoke：通过。
- FreeGoalPanel e2e：5 个测试通过。
- Qingmao region + battlefield e2e：7 个测试通过。
- long e2e：29 个测试通过。

## Git 状态摘要

- 当前分支：`codex/v013-npc-faction-reaction`。
- 已建立 v0.13 第一条可回滚分支点：`ce9b1e9 docs: 建立v0.13流程与Git安全收束`。
- 已推送到 `origin/codex/v013-npc-faction-reaction`。
- 旧分支 `codex/v090-b1-world-action-protocol` 不适合继续长期承载 v0.13。
- v0.10-v0.12 历史成果仍需后续按分组形成更细的可回滚提交链。
- 当前工作区历史脏项较多，提交前必须只 stage 本阶段相关文件。
- `v0.13.0-process-0` 禁止使用 `git add -A` 大混提交。

## 需要用户看什么

1. 用户已批准推荐组合。
2. 用户负责把 MiroFish 前两个包请求转交给会话 `019e207b-c55d-7e23-b450-efa7a054a165`。
3. process-0 完成后进入 `v0.13.0-a1` 文档门禁。

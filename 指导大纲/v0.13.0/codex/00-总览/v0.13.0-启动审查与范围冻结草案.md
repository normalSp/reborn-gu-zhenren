# v0.13.0 启动审查与范围冻结草案

日期：2026-05-16
状态：主线与第一刀已获用户批准

## 当前基础

`v0.12.0` 已完成本地开发里程碑：

- 青茅事实卡和正史锚点。
- 青茅低阶 IF 矩阵。
- route / supply / pursuit 准备链。
- NPC / faction reaction bridge。
- 方源公开旁证询问。
- GitHub/CI 工程门禁。
- rc 全量质量收束。

## v0.13 推荐范围

已冻结为：

`NPC 与势力反应系统第一期`

本期重点：

- 社会记忆协议。
- NPC 记忆投影。
- 势力态度 / 压力投影。
- 事件编年史与公开行动摘要。
- 后续行动候选。
- Player Advocate 可读性走查。

## v0.13 不冻结的内容

以下内容不默认进入 v0.13，除非用户单独批准：

- 正式阵营变化。
- 正式通缉状态。
- NPC 生死 / 抓捕 / 追杀结算。
- 奖励任务、任务奖励池。
- 新地域正式开放。
- 公开声望数值条和声望商店。
- 新增持久化关系账本。
- DeepSeek 新写入权。
- 后端/BFF。

## 字段策略

第一阶段优先复用现有 v22 字段：

- `npcMemories`
- `factionPressure`
- `actionConsequences`
- `localActionLedger`
- `knownFacts`
- `playerGoals`

如果 a2/b1 证明现有字段不足，再提出 D-013-008：新增持久化 `socialMemoryState` 或等价关系账本，并提升 `SAVE_FORMAT_VERSION`。

## MiroFish 策略

v0.13 需要 MiroFish 支撑，因为它会触碰 NPC 动机、势力压力、公开事件和原著语境。

本阶段先写请求，不进入 runtime 吸收。
收到包后必须先写 intake review。

推荐请求：

1. `qingmao_npc_memory_motive_pack`：a2 前 preferred，若命名 NPC 反应进入 runtime 则 blocking。
2. `qingmao_faction_reputation_pressure_pack`：b1 前 preferred，若做正式势力态度规则则 blocking。
3. `qingmao_public_event_chronicle_pack`：b2 前 preferred。

## 开发顺序

仍遵守：

`canon -> types/schema -> engine -> store -> UI -> tests/docs`

其中 v0.13 的 canon 不应直接变成“任务表”或“声望表”，而是先成为：

- social memory rule draft。
- faction pressure rule draft。
- public event summary rule draft。
- test sample。

## 用户已决策

1. 批准 v0.13 主题。
2. 批准 v0.13 第一刀做 a1 设计门禁。
3. 批准第一阶段优先复用 v22 字段。
4. 批准请求 MiroFish 的前两个包。

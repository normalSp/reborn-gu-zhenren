# v0.10.0-b3-2 喂养与残方可读闭环

日期：2026-05-13
状态：已完成；不新增存档字段，不提升 `SAVE_FORMAT_VERSION`。

## 目标

承接 b3 第一刀资源入口，把玩家视角里的链路补清楚：

`行动面板获得食料 -> 蛊虫背包识别食料来源 -> 喂养消耗 materialBag -> 炼蛊面板显示月光蛊残方门槛`

本阶段仍不发放新残方、不解锁完整蛊方、不开放白玉蛊稳定食料来源。

## 已交付

- `GuInventoryPanel`
  - 喂养信息卡新增 `data-testid`。
  - 月光蛊、酒虫会显示对应青茅资源入口。
  - 喂食按钮可验证从 `materialBag` 扣除食料，不走元石兜底。
- `RefinePanel`
  - 残方面板新增青茅月光蛊残方预览。
  - 常驻显示 `frag_moonlight_advanced` 的目标、份数、材料库存和来源标签。
  - 明确完整蛊方只能由本地引擎解锁。
  - 未发现残方时只显示门槛，不开放尝试。
- `installE2eHarness`
  - 青茅区域 demo 的月光蛊、酒虫、白玉蛊状态调整为可喂养的饥饿态。
- `v010-qingmao-region-actions.spec.ts`
  - 桌面 e2e 覆盖资源入口、蛊虫背包喂养桥、材料扣除、残方预览和原有战斗候选路径。

## 边界

- 不新增 `qingmaoResourceState`。
- 不新增 `discoveredFragments`。
- 不直接调用 `unlockRecipe`。
- 不把 DeepSeek 文案变成材料、残方或完整蛊方奖励。
- 不给白玉蛊稳定 `碎玉片` 来源。

## 验收记录

- `npx tsc --noEmit --pretty false`：通过。
- `npm test -- src/store/slices/gu-feeding-economy.test.ts src/engine/v010-qingmao-resource-loop.test.ts src/engine/v010-qingmao-region-engine.test.ts src/engine/v010-qingmao-combat-pack.test.ts`：通过，24 tests。
- `npx playwright test tests/e2e/v010-qingmao-region-actions.spec.ts`：通过，3 tests。

## 下一步建议

进入 b3 收束检查：

1. 把 D-015 经济反刷基线正式记录到 b3 验收。
2. 确认资源入口、喂养、残方和战斗候选四条线没有互相绕过奖励边界。
3. 若不需要新增区域持久化状态，则 b3 可进入收束文档；若要长期记录区域资源历史，需要先停下来让用户决策 `SAVE_FORMAT_VERSION`。

# v2.1.0 Git 提交与推送计划

状态：active；当前记录 D-212 决策与 CI 初诊并准备 b1。
日期：2026-05-23

## 当前分支

`codex/v210-d212-sdk-intake-ci-triage`

基线：

- 从 `codex/v200-rc-t3-quality-closure` 后续切出。
- v2.0 rc commit：`ff22df5 test: 完成v2.0 rc T3长测收束`。
- v2.1 a0 入场 commit：`b2608c5 docs: 记录v2.1 D210批准并进入a0`。
- a1 从 `codex/v210-a0-agent-lab-startup` 干净工作区切出。
- v2.1 a1 尽调 commit：`28f1370 docs: 进入v2.1 a1 Agent框架尽调`。
- a2 从 `codex/v210-a1-agent-framework-diligence` 干净工作区切出。
- v2.1 a2 入场 commit：`902f245 docs: 进入v2.1 a2 Agent Lab设计门禁`。
- v2.1 a2 完成 commit：`25785a9 docs: 完成v2.1 a2 Agent Lab设计门禁`。
- D-211 决策记录从 `codex/v210-a2-agent-lab-design-gate` 干净工作区切出。
- D-212 决策与 CI 初诊记录从 `codex/v210-d211-agent-lab-decisions` 干净工作区切出。

## 本批提交范围

只提交：

- v2.1 文档入口。
- D-211 用户决策记录。
- D-211-007 开源 SDK/agent 权限评估。
- D-212 未来开源 SDK/agent 引入评估门。
- D-212-001 至 D-212-006 用户最新决策。
- 隔离 PoC / patch artifact 解释。
- GitHub 旧分支 CI 失败初诊。
- b1 受限准入入口同步。
- 项目仪表盘 / PROJECT-STATE / AGENTS 同步。
- skill 同步审计记录。

不提交：

- runtime 源码。
- package script。
- 第三方 agent SDK / framework 依赖。
- 外部 SDK/agent 文件、命令、补丁或 git 权限。
- save migration。
- DeepSeek prompt/context。
- BFF/backend。
- MiroFish export。
- 子代理配置。
- CI workflow 或 lockfile 调整。

## 提交建议

`docs: 记录v2.1 D212决策与CI初诊`

## 推送

文档自检通过后推送：

```powershell
git push -u origin codex/v210-d212-sdk-intake-ci-triage
```

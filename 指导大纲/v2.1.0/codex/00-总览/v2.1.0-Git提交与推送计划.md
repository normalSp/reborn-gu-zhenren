# v2.1.0 Git 提交与推送计划

状态：active；当前进入 v2.1.0-a1。
日期：2026-05-22

## 当前分支

`codex/v210-a1-agent-framework-diligence`

基线：

- 从 `codex/v200-rc-t3-quality-closure` 后续切出。
- v2.0 rc commit：`ff22df5 test: 完成v2.0 rc T3长测收束`。
- v2.1 a0 入场 commit：`b2608c5 docs: 记录v2.1 D210批准并进入a0`。
- a1 从 `codex/v210-a0-agent-lab-startup` 干净工作区切出。

## 本批提交范围

只提交：

- v2.1 文档入口。
- v2.0 T3 复盘和 Agent Lab 范围冻结。
- Claude Code 与开源 Agent 框架架构尽调。
- Agent Lab 设计门禁草案。
- 项目仪表盘 / PROJECT-STATE / AGENTS 同步。
- skill 同步审计记录。

不提交：

- runtime 源码。
- package script。
- 第三方 agent SDK / framework 依赖。
- save migration。
- DeepSeek prompt/context。
- BFF/backend。
- MiroFish export。
- 子代理配置。

## 提交建议

`docs: 进入v2.1 a1 Agent框架尽调`

## 推送

文档自检通过后推送：

```powershell
git push -u origin codex/v210-a1-agent-framework-diligence
```

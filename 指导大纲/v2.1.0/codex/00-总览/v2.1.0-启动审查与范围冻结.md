# v2.1.0 启动审查与范围冻结

状态：approved；D-210-001 至 D-210-010 已批准，a0 completed，当前进入 a1。
日期：2026-05-22

## 启动结论

v2.1 启动范围建议冻结为：

`Agent Simulation Lab 启动版：v2.0 T3 复盘 + Claude Code / 开源 Agent 框架架构尽调 + Agent Lab 设计门禁`

本批不是 runtime 版本，不写玩家可见功能。

## 允许范围

- 建立 v2.1 文档入口。
- 复盘 v2.0 T3 证据。
- 提炼 retry、formal credential、hidden protection、schema gap、样本覆盖缺口。
- 做 Claude Code 官方架构与开源 Agent 框架吸收矩阵尽调。
- 设计 Agent Lab 的离线场景、agent proposal、权限矩阵、eval 指标和 WorldCore post-check。
- 更新项目仪表盘、PROJECT-STATE、AGENTS 和 skill 同步审计。

## 禁止范围

- 不改 runtime。
- 不新增 save 字段，不 bump `SAVE_FORMAT_VERSION`。
- 不新增 `runFingerprint`。
- 不新增 `regionalLifeState` / `areaLivingState`。
- 不新增 `identityRouteState` / `professionState`。
- 不改 DeepSeek prompt/context/model/authority。
- 不调用 live DeepSeek。
- 不新增 DeepSeek visible knowledge / RAG。
- 不建 BFF/backend。
- 不启用 subagents。
- 不做 Claude Code SDK runtime 原型。
- 不引入第三方 agent SDK / framework 依赖，不做 framework PoC。
- 不新增 MiroFish export。
- 不开放正式地点、阵营、奖励、NPC 生死或 canon promotion。
- 不部署 EdgeOne。

## 事实源

- v2.0 rc T3 记录：`指导大纲/v2.0.0/codex/00-总览/v2.0.0-rc-T3长测与质量收束记录.md`
- 长期 agent/worldcore 研究线：`指导大纲/长期路线/v2.0-v4.0-分层Agent与世界内核/`
- v2.0 项目入口：`指导大纲/v2.0.0/codex/00-总览/README.md`
- 项目状态：`.codex/skills/reborn-expert-council/references/PROJECT-STATE.md`

## MiroFish 分类

当前分类：`not_needed`

原因：

- v2.1 第一批不新增原著事实、不写知识库正文、不做 NPC/势力/hidden-adjacent runtime。
- Claude Code / 开源 Agent 框架架构尽调属于工程模式，不需要 MiroFish。
- Agent Lab a2 只做设计门禁草案，不吸收新原著内容。

升级条件：

- 触及命名 NPC。
- 触及方源公开证据或 hidden-adjacent。
- 触及正式地点、阵营、奖励、NPC 生死。
- 触及 L4 原著关键人物或 L5 天道/宿命具体原著锚点。

## live DeepSeek 分类

当前分类：`exempt`

原因：

- 本批为文档/架构/治理，不改 prompt/context/runtime。
- 不写 runner，不生成 live narrative。
- a2 后若用户批准 b1 offline runner，再单独评估是否需要 dry-run/live eval。

## Git 分支

当前分支：`codex/v210-a1-agent-framework-diligence`

提交建议：

`docs: 启动v2.1 Agent Lab架构尽调`

## 进入下一步条件

用户批准 D-210 后，进入：

`v2.1.0-a1-Claude-Code架构尽调.md`

已于 2026-05-22 进入。`v2.1.0-a0-v2.0-T3复盘与Agent-Lab范围冻结` 已完成；当前 a1 仍为纯文档尽调，不授权依赖、runner、PoC、BFF/backend、子代理、live DeepSeek 或 runtime agent。

# v2.1.0 D-213 连续收束授权记录

状态：approved
日期：2026-05-23

## 用户授权

用户已批准 D-213：允许在一个 goal 内连续完成 v2.1 后半段。

授权范围：

- 允许连续完成 `b1 -> b2 -> rc`，中途没有 P0/P1、没有范围扩张时不再停下来问。
- v2.1 只做 `Agent Lab 启动版 / offline / report-only / pure helper`。
- 允许补最小脚本、合成样本或测试样本，但不加外部依赖、不接 runtime、不写正式存档。
- 默认 P2 阈值：`P0=0`、`P1=0`、`P2 <= 20%`，且不能出现重复的 hidden/canon/formal credential 类风险。
- rc 收束包括：文档、测试、Skill 同步审计、仪表盘、PROJECT-STATE、AGENTS、handoff、commit/push/CI。

## 仍会硬停的事项

即使在 D-213 连续收束授权内，以下事项仍必须停止并向用户申请：

- runtime / UI / store / prompt 行为变更。
- `SAVE_FORMAT_VERSION`、新增持久字段或 migration/defaults。
- live DeepSeek、模型切换、DeepSeek visible knowledge / RAG 或 authority 扩张。
- 外部 agent SDK/framework 依赖、PoC、vendored subset、patch artifact、只读扫描或项目文件访问。
- 子代理。
- BFF/backend。
- MiroFish 新 export 或 topic-slice intake。
- 正式地点、阵营、奖励、NPC 生死、canon promotion、hidden/private body 可见化。
- CI workflow、lockfile、registry resilience 策略调整。
- EdgeOne 部署或公开发布口径。

## 工程化落点

D-213 将 b2/rc 从 `blocked_by_user_decision` 改为 `approved_continuous_scope`，但不扩大 D-211/D-212 的权限。

v2.1 后半段的唯一实现型产物是自有离线 runner：

- `scripts/run-v210-agent-lab-offline-runner.mjs`
- `tests/evals/v210-agent-lab/samples.json`
- npm script：`npm run check:v210-agent-lab-offline`
- report-only 输出：`artifacts/v2.1.0/agent-lab-offline-runner/**`

## 质量门

最低通过线：

- AgentProposal schema validity：100%。
- WorldCore post-check 结果与样本预期一致。
- P0 = 0。
- P1 = 0。
- P2 rate <= 20%。
- live DeepSeek：否。
- runtime/save/prompt/backend/subagent/external framework：否。

若 P2 超线、出现重复 hidden/canon/formal credential 风险，或者 runner 需要超过自有 report-only 能力，D-213 失效并停止汇报。

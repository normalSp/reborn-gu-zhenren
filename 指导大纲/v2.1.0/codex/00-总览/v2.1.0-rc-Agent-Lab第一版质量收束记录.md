# v2.1.0-rc Agent Lab 第一版质量收束记录

状态：completed
日期：2026-05-23

## rc 定位

v2.1.0 完成的是 `Agent Simulation Lab 启动版`：

- v2.0 T3 复盘。
- Claude Code 与开源 Agent 框架架构尽调。
- Agent Lab 设计门禁。
- 自有 AgentProposal report-only/offline runner 第一刀。
- AgentProposal eval matrix 与 failure taxonomy。

v2.1.0 不是 runtime agent 版本，也不是后端版本。

## 运行结果

核心命令：

```powershell
npm run check:v210-agent-lab-offline
```

结果：

- sampleCount：10。
- schemaValidCount：10。
- schemaValidityRate：1。
- acceptedForGate：10。
- acceptedCandidateCount：8。
- needsUserDecisionCount：2。
- rejectedViolationCount：0。
- P0/P1/P2：0/0/0。
- p2Rate：0，低于 D-213 的 20% 阈值。
- passed：true。
- report：`artifacts/v2.1.0/agent-lab-offline-runner/2026-05-22T17-31-47-312Z/report.json`。

辅助验证：

- `git diff --check`。
- `rg -n "v2.1.0|Agent Lab|D-213|offline runner|check:v210-agent-lab-offline" 指导大纲/v2.1.0 指导大纲/项目仪表盘.md AGENTS.md .codex/skills/reborn-expert-council/references/PROJECT-STATE.md package.json`。
- `rg -n "runtime|SAVE_FORMAT_VERSION|DeepSeek visible|BFF|subagents|NPC 生死|正式地点|正式奖励|canon promotion" 指导大纲/v2.1.0/codex/00-总览 scripts/run-v210-agent-lab-offline-runner.mjs tests/evals/v210-agent-lab/samples.json`。

## live / Player Advocate 豁免

v2.1.0 rc 豁免 live DeepSeek 与 Player Advocate：

- 本版本没有玩家可见 runtime / UI / store / prompt 变化。
- 本版本没有 DeepSeek prompt/context/model/authority 变化。
- 本版本没有新增存档字段或 runtime canon。
- offline runner 是 report-only helper，不改变游戏体验。

因此：

- 是否调用 live DeepSeek：否。
- 模型：不适用。
- 样本、轮次、成本：不适用。
- 报告路径：仅 offline runner report。

## 仍未开放

- runtime agent。
- live DeepSeek agent proposal。
- 外部 agent SDK/framework PoC、依赖、vendored subset、patch artifact 或只读扫描。
- 子代理。
- BFF/backend。
- DeepSeek visible knowledge / RAG。
- MiroFish 新 export。
- 正式地点、阵营、奖励、NPC 生死、canon promotion。

## 下一步建议

v2.2 可以进入 `Agent Lab offline runner 扩展版`，但建议先开专家团启动会：

- 是否扩大到 20 NPC / 3 势力 / 1 L5 宏观导演。
- 是否将自有 AgentProposal 与 LangGraph / Mastra / OpenAI Agents SDK / Google ADK 等做架构对照。
- 是否需要 D-212-001 的 license/SBOM/架构适配专项。
- 是否继续保持纯离线，还是申请小规模 isolated PoC。

rc 结论：`v2.1.0` 可标记为本地完成里程碑。下一步不是直接进入 runtime agent，而是先开 `v2.2` 专家团启动会。

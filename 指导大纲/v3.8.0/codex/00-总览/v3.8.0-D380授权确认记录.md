# v3.8.0 D-380 授权确认记录

日期：2026-05-25
状态：completed

## 用户决策

用户已批准：

- `D-380-001` 至 `D-380-012`：全部批准。
- `F-380-001` 至 `F-380-012`：全部继续 `future_gate_required`。

## 授权范围

本轮允许完成 v3.8 开发，包括：

- `a0`：v3.7 复盘与 v3.8 范围冻结。
- `a1`：proposal graph 长期稳定性模型设计门禁。
- `a2`：multi-pressure / same-start variation copy gate。
- `b1`：自有 `v380` transient/report-only stability first cut。
- `b2`：150 轮 deterministic same-start graph stability。
- `b3`：50 轮 Player Advocate 与 old-save/no-save/rollback 证据。
- `rc`：质量收束、Skill sync、仪表盘、`AGENTS.md`、`PROJECT-STATE.md`、commit/push/CI。

## 未授权范围

本授权不包含：

- persistent agent state、save field、migration、`SAVE_FORMAT_VERSION` bump、`runFingerprint`。
- live DeepSeek、DeepSeek prompt/context/model/authority 扩大、DeepSeek visible lore/RAG。
- MiroFish export/intake、real original facts、named NPC、hidden-adjacent、formal lore conclusion。
- backend/BFF/service/job queue/eval archive service/cloud save。
- external framework PoC/dependency/vendored subset/read-only scan/patch artifact/subagents。
- L4/L5 runtime、天道/宿命 runtime 裁决、原著关键人物 agent。
- formal location/faction/identity/reward/NPC life-death/warrant/recruitment/blockade。
- knowledge-index body、runtime canon、hidden/private body、prompt body archival。
- public/legal/release wording、EdgeOne、branch protection、main auto-merge。

## 例外停机

若实现中触发 `v3.8.0-例外停机清单.md` 任一条，必须停止 `/goal` 自动推进并向用户汇报。

# v3.8.0 启动审查与范围冻结

状态：startup proposed。
日期：2026-05-25。

## 进入条件

- `v3.7.0` 已完成并推送，completion commit `8ef17c5c`。
- GitHub Actions run `26386740563` passed。
- `v3.7` 仍保持 synthetic/generic、no-save、no-runFingerprint、no-live-DeepSeek、no-MiroFish、no-backend、no-external-framework。
- `v3.0-v4.0` 长期路线将 `v3.7-v3.8` 定位为多 NPC、小势力、同开局差异、长期记忆污染和漂移复核。

## v3.8 冻结范围

v3.8 只围绕 transient proposal graph 的长期稳定性做复核和微调：

- 更长轮次 proposal graph deterministic stability。
- 多 generic 小势力压力源之间的干扰和降级。
- 同开局差异是否仍在“候选表达”层，不变成隐形随机事实。
- 长期记忆污染、候选重复空转、传闻/事实混淆检查。
- old-save/no-save/rollback 证据。
- Player Advocate 可读性复核。

## 非目标

- 不做 persistent runtime agent state。
- 不做正式 NPC 记忆、正式势力 standing、正式通缉、正式招揽、正式封锁。
- 不做真实原著势力/NPC topic-slice。
- 不做 live DeepSeek probe，除非用户另批。
- 不引入外部 agent framework。
- 不建 backend/BFF。
- 不做 v3.9 安全收束本身。

## 启动结论

可以开 v3.8 专家团启动会和前置授权包；真正开发仍需用户批准 `D-380-001` 至 `D-380-012`。

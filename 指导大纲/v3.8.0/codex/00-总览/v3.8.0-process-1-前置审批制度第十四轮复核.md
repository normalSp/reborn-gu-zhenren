# v3.8.0-process-1 前置审批制度第十四轮复核

日期：2026-05-25
状态：completed

## 目标

复核 v3.8 是否遵守“前置批量审批与例外停机制度”。

## 结果

- 用户一次性批准 `D-380-001` 至 `D-380-012`。
- 用户确认 `F-380-001` 至 `F-380-012` 全部继续 `future_gate_required`。
- v3.8 自动推进过程中没有越过 F-380。
- 未新增 routine approval。
- 未触发需要用户重新决策的例外停机。

## 中途失败处理

full e2e 首轮发现 v3.6 继承 deterministic 证据显示为 `0/0`，原因是 v3.8 默认 build 过度关闭继承 probe。处理方式：

- 没有改变授权范围。
- 没有放开 save、DeepSeek、MiroFish、backend 或外部框架。
- 修复为：UI 默认保留 v3.6/v3.7 已有 deterministic 证据，v3.8 自己的 150 轮 probe 独立运行；嵌套 probe 仍在 deterministic runner 内被关闭，避免递归。
- 重新运行 v360/v370/v380 focused e2e 与 full e2e，均通过。

## 结论

前置审批制度本轮有效。v3.8 在 D-380 授权内完成，没有把 pending/future gate 伪装成默认许可。

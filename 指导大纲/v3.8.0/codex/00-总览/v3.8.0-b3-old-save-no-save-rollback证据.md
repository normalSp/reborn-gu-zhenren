# v3.8.0-b3 old-save / no-save / rollback证据

日期：2026-05-25
状态：completed
版本：`v380_b3_old_save_no_save_rollback_evidence_v1`

## old-save

v3.8 只消费既有 projection 输入：

- `turn`
- `previewLane`
- `regionalEventLedger`
- `routeLocationState`
- `livingWorldState`
- `localActionLedger`

没有新增 save 字段，没有 migration，没有 `SAVE_FORMAT_VERSION` bump。旧档加载后若缺少某些 projection 输入，v380 只会退化为更轻的 synthetic/generic 候选表达，不会补写任何持久状态。

## no-save

v3.8 没有写入：

- `agentProposalGraphStabilityState`
- `agentProposalGraphState`
- `agentMicroExpansionState`
- `runtimeAgentState`
- `smallFactionState`
- `factionStandingState`
- `runFingerprint`

focused e2e 会检查 state summary 与 `materialBag` 中上述字段均为 `undefined`。

## rollback

v3.8 rollback 方式为：

- 移除 `v380` transient helper。
- 恢复 `RuntimeAgentProposalPanel` 到 v3.7 graph 展示。
- 移除 v380 focused test/e2e/script。

因为没有 save/store/prompt/canon 写入，所以 rollback 不需要 migration 或玩家存档修复。

## 结论

old-save/no-save/rollback gate 通过。

# v3.8.0 Git 提交与推送计划

状态：startup proposed。
当前分支：`codex/v380-startup-proposal-graph-stability`。

## 分支规则

- `main` 是稳定集成主线。
- `codex/v380-startup-proposal-graph-stability` 是 v3.8 版本工作分支。
- 不创建 `develop` 作为默认长期分支。
- 不自动合并 main。
- 不 force-push、不改 branch protection、不部署 EdgeOne。

## 启动会提交边界

启动会提交只包含：

- `指导大纲/v3.8.0/codex/00-总览/` 新文档。
- `指导大纲/项目仪表盘.md`。
- `AGENTS.md`。
- `.codex/skills/reborn-expert-council/references/PROJECT-STATE.md`。
- 被 skill sync audit 触发的本地 skill 文件。
- 最新 context handoff，如有。

## 后续开发提交边界

若用户批准 D-380：

- a0/a1/a2 可作为文档提交。
- b1/b2/b3 可作为实现/测试/证据提交。
- rc 做质量收束提交。

每次 stage 必须显式列路径，不使用 `git add -A`。

## 版本完成时主线合并结论

v3.8 完成后必须给出：

- 是否建议合并 `main`。
- 合并前需要哪些验证。
- 是否存在未授权 future gate。

默认结论不自动合并，等待用户批准或 PR 审核。

# v3.8.0-rc 质量收束记录

日期：2026-05-25
状态：completed locally，等待 commit/push/CI。
分支：`codex/v380-startup-proposal-graph-stability`

## 本轮完成内容

v3.8 完成 `transient proposal graph 长期稳定性与多小势力压力复核`：

- 新增 `src/engine/v380-transient-agent-proposal-graph-stability.ts`。
- 新增 v380 focused unit 与 e2e。
- `意图` tab 新增 v3.8 proposal graph stability 区块、multi-pressure lanes 与 150 轮 deterministic 证据。
- 保留 v3.4/v3.5/v3.6/v3.7 旧证据链。
- 新增 D-380 授权记录、a0/a1/a2/b1/b2/b3、process-1/process-2、PA 与 old-save/no-save/rollback 文档。

## 验证结果

| 命令 | 结果 |
|---|---|
| `npm run check:v380-transient-agent-proposal-graph-stability` | passed，4/4 |
| `npm run check:v370-transient-agent-proposal-graph` | passed，5/5 |
| `npx tsc --noEmit --pretty false` | passed |
| `npx playwright test tests/e2e/v360-runtime-agent-micro-expansion.spec.ts tests/e2e/v370-runtime-agent-proposal-graph.spec.ts tests/e2e/v380-runtime-agent-proposal-graph-stability.spec.ts --workers=1` | passed，3/3 |
| `npm run check:player-advocate-gate -- 指导大纲/v3.8.0/codex/00-总览/v3.8.0-b3-Player-Advocate-50轮记录.md 50` | passed，50 rounds，100.0% |
| `npm test` | passed，157 files，836 tests |
| `npm run build` | passed |
| `npm run check:runtime-assets` | passed |
| `npm run check:qingmao-assets` | passed |
| `npm run check:player-visible-copy` | passed |
| `npm run check:stale-entrypoints` | passed，P0/P1/P2/Info=0 |
| `npm run test:e2e` | passed，74/74 |
| `npm run test:e2e:long` | passed，18/18 |
| `npm run check:production-preview` | passed |
| `git diff --check` | passed |

## 首轮失败与修复

`npm run test:e2e` 首轮出现 1 个失败：

- 失败用例：`v360-runtime-agent-micro-expansion.spec.ts`
- 现象：v3.6 deterministic evidence 显示 `0/0`。
- 根因：v3.8 默认 build 为了避免递归长测，把继承的 v3.6/v3.7 probe 也关闭了。
- 修复：v3.8 UI 默认保留继承证据；v3.8 deterministic runner 内部 build 仍关闭 nested probe。
- 复测：v360/v370/v380 focused e2e 通过；full e2e 74/74 通过。

## 边界确认

v3.8 没有新增：

- persistent agent state。
- save field、migration、`SAVE_FORMAT_VERSION` bump、`runFingerprint`。
- live DeepSeek、DeepSeek prompt/context/model/authority 变更。
- DeepSeek visible lore/RAG。
- MiroFish export/intake。
- backend/BFF/service/job queue/eval archive service/cloud save。
- external framework PoC/dependency/vendored subset/read-only scan/patch artifact/subagents。
- L4/L5 runtime、天道/宿命 runtime 裁决、原著关键人物 agent。
- formal location/faction/identity/reward/NPC life-death/warrant/recruitment/blockade。
- knowledge-index body、runtime canon、hidden/private body、prompt body archival。
- public/legal/release wording、EdgeOne、main auto-merge。

## 下一步

完成 commit、push，并等待 GitHub Actions。是否合并 `main` 需按主线合并制度另行确认，不自动合并。

# v3.8.0-b2 150轮 deterministic stability记录

日期：2026-05-25
状态：completed
版本：`v380_b2_150_round_deterministic_stability_gate_v1`

## 目标

验证 v3.8 proposal graph stability 在 150 轮同开局 deterministic 场景下：

- 不把候选写成事实。
- 不写 save/store/prompt/canon。
- 不引入 `runFingerprint`。
- 不调用 live DeepSeek。
- 不需要 MiroFish、backend 或外部框架。
- 不把小势力公开压力变成正式 standing、通缉、招揽或封锁。

## 运行方式

由 `runV380DeterministicProbe(150)` 执行，随 `npm run check:v380-transient-agent-proposal-graph-stability` 覆盖。

## 当前结果

- roundCount：150。
- acceptedRounds：150。
- acceptedForGate：true。
- uniqueStabilitySignatureCount：>= 12。
- pressureLaneCount：>= 4。
- pressureInterferencePairCount：>= 6。
- failures：0。
- boundaryAssertions：全部 false。

## 不变量

- `factsCommitted = false`
- `saveWritten = false`
- `promptWritten = false`
- `canonPromoted = false`
- `runFingerprintWritten = false`
- `liveDeepSeekCalled = false`
- `mirofishRequired = false`
- `backendRequired = false`
- `externalFrameworkUsed = false`
- `formalStandingWritten = false`
- `npcLifeDeathWritten = false`
- `rewardGranted = false`
- `hiddenLeakDetected = false`
- `memoryContaminationDetected = false`

## 结论

b2 通过。150 轮 deterministic stability gate 接受 v3.8 进入 b3。

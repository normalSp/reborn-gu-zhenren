# v3.1.0-b3 v3.2 runtime rehearsal admission package

日期：2026-05-24
状态：completed
冻结模型：`v310_b3_v32_runtime_rehearsal_admission_package_v1`

## 准入结论

v3.1 checker 给出：

- `v32RehearsalAdmissible=true`。
- `runtimeImplementationApproved=false`。

因此下一步可以开 v3.2 专家团启动会，主题建议为 offline / report-only runtime rehearsal。

## v3.2 可做

- synthetic world snapshot。
- L2/L3 proposal batch。
- WorldCore post-check rehearsal report。
- approved expression sample，但不进 UI。
- rejection / manual review / future gate 趋势报告。
- Player Advocate/live metadata 继续记录“是否调用 live DeepSeek”。

## v3.2 不可默认做

- 不写玩家存档。
- 不实现 runtime agent。
- 不接 live DeepSeek。
- 不开 backend/BFF。
- 不用 MiroFish 真实 topic slice。
- 不接外部 framework PoC 或 subagent。

## b3 结论

v3.2 可进入 startup，但必须重新给前置授权包。F-310/F-300 不因 v3.1 通过而自动打开。

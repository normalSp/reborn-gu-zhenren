# v3.1.0 启动审查与范围冻结

日期：2026-05-24
状态：startup completed；等待用户审批 D-310

## v3.0 复盘

v3.0 已完成 `有限 L2/L3 runtime agent 试点设计门禁`，并通过 `check:v300-runtime-agent-design-gate`：

- 21/21 schema valid。
- `design_gate_ok=5`。
- `future_gate_required=7`。
- `no_go_blocked=9`。
- P0/P1 falseNegative=0/0。
- `runtimeImplementationApproved=false`。

这说明 v3.0 可以作为 v3.1 加固的输入，但不是 runtime implementation 批准。

## v3.1 范围冻结

v3.1 只做门禁二次加固：

- 扩展 AgentProposal contract 的证据链要求。
- 明确 WorldCore post-check result 的拒绝、降级、人工复核、用户决策语义。
- 强化 rollback、old-save、Player Advocate、live metadata、drift checkpoint 的通过标准。
- 增加 report-only checker 的 negative fixtures、rescore stability、manual review family。
- 输出 v3.2 rehearsal admission package。

## 不进入的范围

v3.1 不进入：

- runtime agent implementation。
- runtime/source/UI/store/prompt/save 改动。
- 新 save field、`SAVE_FORMAT_VERSION` bump、migration、`runFingerprint`。
- live DeepSeek 实跑或 DeepSeek prompt/context/model/authority 改动。
- backend/BFF/service/job queue/eval archive/cloud save。
- MiroFish export/intake 或真实原著事实吸收。
- 外部 framework PoC/dependency/subagents。
- L4/L5 runtime、天道/宿命 runtime 裁决。

## 与长期路线关系

v3.1 对应 `指导大纲/长期路线/v3.0-v4.0-RuntimeAgent到高阶世界总体大纲.md` 中的第一段：runtime agent 门禁二次加固。

v3.1 完成后，项目应能回答：

- v3.0 的 report-only gate 是否足以承接 v3.2 rehearsal。
- 哪些 AgentProposal 能继续作为 report-only design evidence。
- 哪些必须维持 F-310 future gate。
- 是否需要在 v3.2 前补一个额外设计门禁。

## 启动结论

可以开 v3.1 startup，但在 D-310 获批前不进入后续 `/goal` 自动开发。

# v3.1.0-a2 rollback / old-save / PA / live metadata / drift 门禁

日期：2026-05-24
状态：completed
冻结模型：`v310_a2_rollback_oldsave_pa_live_metadata_drift_gate_v1`

## 必填门禁

每个可接受的 v3.1 candidate 必须声明：

- `rollbackPlanReady=true`。
- `oldSavePlanReady=true`。
- `playerAdvocateMetadata.liveDeepSeekCalled=false`。
- `liveMetadata.liveDeepSeekCalled=false`。
- 至少两个 `driftCheckpoints`。

## Player Advocate 记录口径

后续每份 Player Advocate 记录必须写清：

- 是否调用 live DeepSeek：否 / 是。
- 若是：模型、样本、轮次、成本、报告路径。

v3.1 当前结论：

- 是否调用 live DeepSeek：否。
- 原因：D-310 未授权 live；F-310-003 保持 `future_gate_required`。

## rollback / old-save

v3.1 不写存档，所以 rollback / old-save 是 report-only gate：

- 候选不得污染旧档。
- 候选不得要求 migration。
- 候选不得生成 `runFingerprint`。
- 候选不得把 same-start difference 写成事实差异。

## a2 结论

`v310_a2_rollback_oldsave_pa_live_metadata_drift_gate_v1` 成立。可进入 b1 checker 第一刀。

# v3.1.0-a0 v3.0 复盘与门禁二次加固范围冻结

日期：2026-05-24
状态：completed
冻结模型：`v310_a0_runtime_agent_gate_hardening_scope_v1`

## v3.0 复盘结论

v3.0 的 `check:v300-runtime-agent-design-gate` 已通过，证明设计门禁能区分：

- `design_gate_ok`。
- `future_gate_required`。
- `no_go_blocked`。

同时 v3.0 明确 `runtimeImplementationApproved=false`。因此 v3.1 的范围不是“实现 runtime agent”，而是把 v3.0 的门禁证据链加厚。

## v3.1 加固目标

- 把 AgentProposal contract 从 v1 升到 v2，补 evidence、visibility、persistence、rollback、manual review、live metadata。
- 把 WorldCore post-check 从单点判定升级为 evidence chain。
- 把 rollback、old-save、PA、live metadata、drift checkpoint 变成 checker 可验证字段。
- 把 manual review / needs user decision 与 future gate 分开。
- 形成 v3.2 rehearsal admission package。

## 范围冻结

v3.1 只做 report-only / synthetic / zero-dependency gate hardening。

不允许：

- runtime/source/UI/store/prompt/save 改动。
- runtime agent implementation。
- live DeepSeek。
- MiroFish export/intake。
- backend/BFF/service。
- external framework PoC/dependency/subagent。
- save-format 或 `runFingerprint`。

## a0 结论

`v310_a0_runtime_agent_gate_hardening_scope_v1` 成立。可进入 a1 contract v2 门禁。

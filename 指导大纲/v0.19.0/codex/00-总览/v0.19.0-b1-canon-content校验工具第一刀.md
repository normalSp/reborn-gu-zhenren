# v0.19.0-b1 canon/content 校验工具第一刀

日期：2026-05-19
状态：已完成
阶段性质：tooling + tests

## 目标

建立 v0.19 第一把本地内容治理工具，覆盖：

- v0.19 内容模板完整性。
- MiroFish 三包 redaction 与 candidate-only 边界。
- v1.0 hero 三件套 manifest 与文件存在性。
- release art 仍未绑定 runtime。

## 新增脚本

`scripts/check-v019-content-governance.mjs`

检查内容：

1. `src/canon/v019-content-governance-rules.json` 存在。
2. 六类内容模板齐全。
3. public canon boundary / playthrough anchors / release art caption boundary 均为 8 项。
4. 三个 v0.19 MiroFish 包存在、每包 8 项、无 source body、无 original wording、无禁止字段。
5. `doc/art/v1-hero-selection-manifest.json` 的三件套均为 `selected_candidate`。
6. hero 三件套源文件和 public 文件存在且非 0 字节。
7. hero 三件套 `currentBinding = not_bound`，不作为 runtime gameplay authority。

## 脚本输出

```text
[v019-content-governance] ok templates=6 publicBoundaries=8 playthroughAnchors=8 artBoundaries=8 heroEntries=3 mirofishPackages=3
```

## 测试覆盖

`src/engine/v019-content-governance.test.ts` 覆盖：

- v0.19 元信息和 hard stops。
- 内容模板必填/禁止字段。
- MiroFish 晋级边界。
- 8 条代表性路径矩阵。
- public safety preflight 对 visible copy / DeepSeek context / write target / release art caption 的阻断。
- v1 hero manifest 不能提前绑定 runtime。

## 验证

| 命令 | 结果 |
|---|---|
| `npm test -- src/engine/v019-content-governance.test.ts --reporter=dot` | 6 tests passed |
| `npm run check:v019-content-governance` | passed |
| `npx tsc --noEmit --pretty false` | passed |

## 非目标确认

b1 不新增大编辑器，不引入外部依赖，不新增 runtime UI，不写存档，不做自动部署，不扩大 DeepSeek 权限。

# v3.6.0 Git 提交与推送计划

日期：2026-05-25
状态：completed；implementation commit/push/CI passed

## 分支

当前 v3.6 startup 分支：

`codex/v360-startup-agent-micro-expansion`

## stage 规则

- 显式 stage 本次 v3.6 文档、入口同步和 skill audit。
- 不使用 `git add -A`。
- 不 stage 与 v3.6 startup 无关的脏文件。

## 提交边界

startup 提交建议：

`docs: 开启v3.6 agent微扩硬化启动会`

本次提交包含：

- v3.6 startup docs。
- D-360/F-360 前置授权包。
- dashboard / AGENTS / PROJECT-STATE 同步。
- skill sync audit 记录。

不包含：

- runtime/source/UI/store/prompt/save changes。
- live DeepSeek artifacts。
- MiroFish export/intake。
- backend/external framework PoC。

implementation 提交建议：

`feat: 完成v3.6 agent微扩硬化`

本次实现提交包含：

- `src/engine/v360-transient-agent-micro-expansion.ts`。
- v360 focused unit test。
- world panel `意图` tab v3.6 micro-expansion UI。
- v360 focused e2e。
- v3.6 a0/a1/a2/b1/b2/b3/process/rc docs。
- dashboard / AGENTS / PROJECT-STATE / skill sync 收束。

不包含：

- save field / migration / `SAVE_FORMAT_VERSION` / `runFingerprint`。
- live DeepSeek artifacts。
- MiroFish export/intake。
- backend/external framework PoC。

## 推送与 CI

已完成：

- startup commit：`78e41b33 docs: 开启v3.6 agent微扩硬化启动会`。
- push：`codex/v360-startup-agent-micro-expansion` 已推送到 origin。
- GitHub Actions：run `26368960341` passed，deterministic quality gate 通过。

implementation 已完成：

- implementation commit：`e1b76a46 feat: 完成v3.6 agent微扩硬化`。
- push：`codex/v360-startup-agent-micro-expansion` 已推送到 origin。
- GitHub Actions：run `26369929764` passed，deterministic quality gate 通过。

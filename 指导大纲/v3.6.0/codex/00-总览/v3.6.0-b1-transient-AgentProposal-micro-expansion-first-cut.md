# v3.6.0-b1 transient AgentProposal micro-expansion first cut

日期：2026-05-25
状态：completed

## 源码输出

- `src/engine/v360-transient-agent-micro-expansion.ts`
- `src/engine/v360-transient-agent-micro-expansion.test.ts`
- `src/components/game/RuntimeAgentProposalPanel.tsx`
- `src/components/game/WorldHubPanel.tsx`
- `tests/e2e/v360-runtime-agent-micro-expansion.spec.ts`
- `package.json` script：`check:v360-transient-agent-micro-expansion`

## Runtime 行为

- `buildV360TransientAgentMicroExpansion(input)` 继承 v3.5 lifecycle v2，并增加 synthetic/generic L2/L3 micro-expansion lane。
- `runV360DeterministicProbe(90)` 验证 90 轮同开局差异与边界。
- UI 复用 `意图` tab，新增 v3.6 面板 test id：`v360-runtime-agent-micro-expansion-panel`。

## 仍未开放

- 不写 store/save/prompt/canon。
- 不新增 save field / migration / `runFingerprint`。
- 不调用 live DeepSeek。
- 不使用 MiroFish、backend、external framework 或 subagent。
- 不引入真实原著事实、命名 NPC、L4/L5 或正式世界结论。

## focused 证据

- `npm run check:v360-transient-agent-micro-expansion -- --reporter=dot`：passed，1 file / 4 tests。
- `npm run check:v350-transient-agent-hardening -- --reporter=dot`：passed，1 file / 4 tests。
- `npx tsc --noEmit --pretty false`：passed。
- `npm run test:e2e -- tests/e2e/v350-runtime-agent-hardening.spec.ts tests/e2e/v360-runtime-agent-micro-expansion.spec.ts`：passed，2 tests。

## 结论

b1 通过。进入 b2 90 轮 deterministic 记录。

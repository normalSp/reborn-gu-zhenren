# v3.6.0 启动审查与范围冻结

日期：2026-05-25
状态：startup；pending user approval

## 输入事实

v3.6 只能继承以下已完成事实：

- v3.4：最小 `transient proposal-only` runtime first cut 已完成。
- v3.5：lifecycle v2、copy guard、20 轮 PA、60 轮 deterministic 已完成。
- v3.5 没有打开 save/store/prompt/DeepSeek/MiroFish/backend/external framework。

## 冻结主线

`transient AgentProposal synthetic L2/L3 微扩 + 同开局差异/漂移硬化`。

## 范围内

- synthetic / RebornG-owned generic L2/L3 candidate lane。
- candidate / rejected / expired / needs_user_decision 多 lane 表达。
- same-start variation deterministic probe，默认 90 轮。
- 30 轮 Player Advocate。
- old-save/no-save/rollback evidence。
- focused unit/e2e 与 full verification 计划。

## 范围外

- persistent runtime agent。
- agent memory / store slice / save write / migration / `runFingerprint`。
- live DeepSeek / prompt-context-model-authority。
- MiroFish export/intake 或真实 lore absorption。
- backend/BFF/service。
- external framework PoC/dependency/subagent。
- L4/L5、天道宿命、高阶战斗。
- formal location/faction/identity/reward/NPC life-death。

## MiroFish 判断

need level：`not_needed`。

理由：v3.6 只允许 synthetic / generic 小区域样本，不使用命名 NPC、真实势力、hidden-adjacent、方源公开证据、L4/L5 锚点或正式 lore 结论。

触发升级：若后续任何样本想使用真实原著人物、真实势力或隐藏邻近材料，立即停机并升级为 `blocking`。

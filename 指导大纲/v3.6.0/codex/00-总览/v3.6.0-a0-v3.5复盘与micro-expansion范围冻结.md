# v3.6.0-a0 v3.5 复盘与 micro-expansion 范围冻结

日期：2026-05-25
状态：completed
冻结版本：`v360_a0_micro_expansion_scope_v1`

## 输入

- v3.4 已开放最小 `transient proposal-only` L2/L3 runtime first cut。
- v3.5 已完成 lifecycle v2：candidate / rejected / expired / needs_user_decision。
- v3.5 20 轮 Player Advocate 与 60 轮 deterministic gate 通过。

## 冻结范围

v3.6 只做一层微扩：

- 增加 synthetic / RebornG-owned generic L2/L3 lane。
- 继续复用 `意图` tab。
- 继续把 candidate / rejected / expired / needs_user_decision 作为显示生命周期。
- 继续明确“候选不是事实”。
- 继续由 WorldCore post-check 保持最终裁决权。

## 不进入范围

- 不新增 persistent agent state / memory / store slice。
- 不新增 save field、migration、`SAVE_FORMAT_VERSION` bump 或 `runFingerprint`。
- 不调用 live DeepSeek，不改 prompt/context/model/authority。
- 不做 MiroFish export/intake，不引入真实原著事实、命名 NPC、hidden-adjacent、L4/L5。
- 不做 backend、外部框架 PoC、依赖、子代理或 patch artifact。
- 不开放正式地点、阵营、身份、奖励、NPC 生死、通缉、招揽、封锁。

## 结论

a0 通过。v3.6 可进入 a1 scenario model。

# v3.6.0 总体开发大纲

日期：2026-05-25
状态：startup；pending user approval

## 目标

在不开放完整 runtime agent 的前提下，让 `意图` tab 从单一路径 hardening 进入 synthetic L2/L3 微扩验证：多个候选 lane 可以表现不同意图、压力和下一步建议，但都不写事实、不写存档、不改变 DeepSeek 权限。

## 阶段结构

| 阶段 | 目标 | 主要输出 |
|---|---|---|
| a0 | v3.5 复盘与范围冻结 | `v360_a0_v35_review_scope_v1` |
| a1 | synthetic L2/L3 scenario model | `v360_a1_synthetic_l2_l3_micro_expansion_model_v1` |
| a2 | 多 lane UI/copy 门禁 | `v360_a2_candidate_lane_copy_gate_v1` |
| b1 | micro-expansion first cut | `buildV360TransientAgentMicroExpansion()` |
| b2 | 90 轮 deterministic | `runV360DeterministicProbe(90)` |
| b3 | PA / old-save / rollback | 30 轮 Player Advocate 与 no-save 证据 |
| rc | 质量收束 | full verification、Skill sync、Git/CI |

## 默认实现形态

若用户批准 D-360，后续 runtime 第一刀建议新增：

- `src/engine/v360-transient-agent-micro-expansion.ts`
- `buildV360TransientAgentMicroExpansion(input)`
- `runV360DeterministicProbe(90)`
- `V360_TRANSIENT_MICRO_EXPANSION_VERSION`

UI 仍复用世界面板 `意图` tab，不新建大系统入口。

## 非目标

v3.6 不是完整 NPC agent、不是多 NPC 社会、不是记忆系统、不是 live DeepSeek agent，也不是后端或外部框架专项。

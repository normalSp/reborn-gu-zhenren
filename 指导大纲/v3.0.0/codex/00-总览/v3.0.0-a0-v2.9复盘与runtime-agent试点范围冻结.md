# v3.0.0-a0 v2.9 复盘与 runtime agent 试点范围冻结

日期：2026-05-24
状态：completed
冻结模型：`v300_a0_runtime_agent_trial_scope_v1`

## 输入复盘

v2.9 的结论是：可以进入 v3.0 专家团/设计门禁，但 `runtimeImplementationApproved=false`。用户随后批准 D-300-001 至 D-300-012，并确认 F-300-001 至 F-300-012 全部保持 `future_gate_required`。

因此 a0 的职责不是实现 agent，而是冻结 v3.0 可讨论、可验证、可回滚的试点边界。

## 试点范围

- 区域：一个低阶小区域的抽象试点，不新增正式地点。
- agent 层级：仅 L2/L3。
- 输出性质：`AgentProposal` 候选和表达建议。
- 裁决权：WorldCore / local deterministic core 保持最终权威。
- 持久化：默认 `transient_candidate_only`，不写 save。
- DeepSeek：不实跑 live，不改 prompt/context/model/authority。
- MiroFish：当前 `not_needed`；触发命名 NPC、真实 lore、hidden-adjacent、方源公开证据、L4/L5 锚点、知识正文或正式 lore 结论时升级 blocking。

## 明确不做

- 不接 runtime agent implementation。
- 不新增 save field，不 bump `SAVE_FORMAT_VERSION`，不加 `runFingerprint`。
- 不开 backend/BFF/service/job queue/eval archive/cloud save。
- 不引入外部 agent framework、PoC、dependency、subagent、patch artifact。
- 不开放 L4/L5 runtime、天道/宿命 runtime 裁决、原著关键人物 runtime。
- 不新增正式地点、阵营、身份、奖励、NPC 生死、通缉、招揽、封锁。

## a0 结论

`v300_a0_runtime_agent_trial_scope_v1` 成立：v3.0 可继续 a1/a2/b1 report-only 设计与检查；不得进入 runtime implementation。

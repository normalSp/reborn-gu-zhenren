# v3.0.0-a2 live / Player Advocate / drift / rollback / old-save 门禁

日期：2026-05-24
状态：completed
冻结模型：`v300_a2_live_pa_drift_rollback_gate_v1`

## 原则

用户已批准起草方案，但 F-300-003 仍为 `future_gate_required`，所以 a2 不实跑 live DeepSeek，不改 prompt/context/model/authority。

## 未来 live probe 草案

若未来单独批准 F-300-003，建议：

- 模型：`deepseek-v4-flash`，与当前 `.env` 口径一致。
- 样本：8-12 个小区域 L2/L3 proposal-only 场景。
- 轮次：smoke 12-20；rc 40-60；若涉及 prompt/context 风险则 80+。
- 接受标准：P0=0、P1=0、P2 有上限且必须分类。
- 报告必须记录模型、样本、轮次、成本、缓存、报告路径。

## Player Advocate 草案

v3.0 若未来进入 runtime/player-facing：

- b 阶段至少 10 轮。
- rc 至少 50 轮。
- 每份记录必须写清是否调用 live DeepSeek。
- 重点检查玩家是否能理解 agent 带来的差异、NPC 意图是否可见、选择后果是否具体、世界是否没有空转。

本次 v3.0 report-only 完成不触发 player-facing gate，记录豁免写入 rc。

## Drift 草案

检查点：

- agent 是否把候选当成事实。
- 世界状态是否被候选污染。
- DeepSeek authority 是否被扩大。
- lore/hidden/private 是否泄漏。
- 同开局差异是否变成随机噪声。
- 玩家目标是否丢失。

## rollback / old-save

v3.0 report-only 完成保持：

- `SAVE_FORMAT_VERSION = 25`。
- 不新增字段。
- 不加 `runFingerprint`。
- 不写迁移。

若未来实现 runtime agent 且需要持久化，必须另开 save-format 设计门禁。

## a2 结论

`v300_a2_live_pa_drift_rollback_gate_v1` 成立。本阶段只冻结未来测试与回滚门禁，不实跑 live，不写 runtime。

# v2.4.0 小版本执行路线图

日期：2026-05-23
状态：draft；待 D-240 审批。

## startup / a0

主题：长期路线修正与 Landscape 同步。

状态：completed locally。

产物：

- v2.0-v4.0 外部成果矩阵补 Landscape 覆盖层。
- v2.0-v4.0 开放决策池补 AGENT-011 至 AGENT-014。
- 世界内核架构草案补 external framework adapter。
- 可行性分析补 self-learning candidate pipeline。
- `reborn-expert-council` skill 同步 Current Sync Override。
- v2.4 启动文档包。

## a1

主题：薄 BFF / private canon / eval archive 边界设计门禁。

候选输出：

- BFF allowed / forbidden responsibilities。
- private canon visibility levels。
- eval archive schema 草案。
- prompt hash / cost / retry / cache 观测口径。
- 不实现后端的理由或进入后端专项的触发条件。

进入条件：

- 用户批准 D-240。

## a2

主题：external framework adapter / license-SBOM / self-learning candidate pipeline 门禁。

候选输出：

- Hermes / LangGraph / Mastra / LlamaIndex / Dify 等 adapter 边界。
- license/SBOM intake checklist。
- isolated PoC application template。
- self-learning candidate patch schema 草案。
- external framework 禁止项清单。

进入条件：

- a1 完成。
- 若涉及 PoC、依赖、项目文件读取或模型调用，必须另批。

## b1

主题：report-only 基础设施边界报告第一刀。

默认：

- 不写源码。
- 不实现服务。
- 不运行 live DeepSeek。
- 只输出 report。

## b2

主题：private canon / hidden leak 风险模型。

默认：

- 不新增知识库正文。
- 不接 DeepSeek visible lore/RAG。
- 不接 runtime。

## b3

主题：eval archive / replay / cost / prompt hash schema 草案。

默认：

- schema/report-only。
- 不改现有 eval runner，除非用户另批。

## rc

主题：v2.4 质量收束与 v2.5 go/no-go。

验收：

- 文档自检。
- 敏感边界扫描。
- Skill sync audit。
- 仪表盘 / PROJECT-STATE / AGENTS 同步。
- Git commit / push / CI。

# v2.4.0 Git 提交与推送计划

日期：2026-05-23
状态：b3 completed；D-269 待审批。

## 当前分支

startup/a0 分支：

`codex/v240-long-route-sync-startup`

a1 分支：

`codex/v240-a1-bff-private-canon-eval-boundary`

a2 分支：

`codex/v240-a2-framework-adapter-license-self-learning`

b1 分支：

`codex/v240-b1-infrastructure-boundary-report`

b2 分支：

`codex/v240-b2-private-canon-hidden-leak-risk`

b3 分支：

`codex/v240-b3-eval-archive-schema`

从 `codex/v240-pre-agent-framework-landscape` 切出。

## 提交范围

startup/a0 提交只包含：

- 长期路线 5 项同步修正。
- v2.4 startup 文档包。
- dashboard / PROJECT-STATE / AGENTS 同步。
- `reborn-expert-council` skill Current Sync Override 更新。

a1 提交只包含：

- D-240 批准记录。
- v2.4-a1 薄 BFF / private canon / eval archive 边界设计门禁。
- v2.4-a1 Skill 同步审计。
- D-241 后续决策候选。
- dashboard / PROJECT-STATE / AGENTS / 长期路线入口同步。
- triggered skills Current Sync Override 更新。

a2 提交只包含：

- D-241 批准记录。
- v2.4-a2 external framework adapter / license-SBOM / self-learning 门禁。
- v2.4-a2 Skill 同步审计。
- D-242 后续决策候选。
- dashboard / PROJECT-STATE / AGENTS / 长期路线入口同步。
- triggered skills Current Sync Override 更新。

b1 提交只包含：

- D-242 批准记录。
- v2.4-b1 report-only 基础设施边界报告第一刀。
- v2.4-b1 Skill 同步审计。
- D-249 后续决策候选。
- dashboard / PROJECT-STATE / AGENTS / 长期路线入口同步。
- triggered skills Current Sync Override 更新。

b2 提交只包含：

- D-249 批准记录。
- v2.4-b2 private canon / hidden leak 风险模型报告。
- v2.4-b2 Skill 同步审计。
- D-259 后续决策候选。
- dashboard / PROJECT-STATE / AGENTS / 长期路线入口同步。
- triggered skills Current Sync Override 更新。

b3 提交只包含：

- D-259 批准记录。
- v2.4-b3 eval archive / replay / cost / prompt hash schema 草案。
- v2.4-b3 Skill 同步审计。
- D-269 后续决策候选。
- dashboard / PROJECT-STATE / AGENTS / 长期路线入口同步。
- triggered skills Current Sync Override 更新。

不包含：

- runtime code。
- tests/evals scripts。
- dependencies。
- artifacts。
- MiroFish export。
- EdgeOne deploy。

## Stage 规则

- 显式 stage。
- 不使用 `git add -A`。
- 不混入无关 dirty files。

## 推送规则

每个阶段文档完成并通过文档自检后推送当前语义分支。

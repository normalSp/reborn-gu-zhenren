# v2.4.0 Git 提交与推送计划

日期：2026-05-23
状态：startup。

## 当前分支

`codex/v240-long-route-sync-startup`

从 `codex/v240-pre-agent-framework-landscape` 切出。

## 提交范围

本提交只包含：

- 长期路线 5 项同步修正。
- v2.4 startup 文档包。
- dashboard / PROJECT-STATE / AGENTS 同步。
- `reborn-expert-council` skill Current Sync Override 更新。

不包含：

- runtime code。
- tests/evals scripts。
- dependencies。
- artifacts。
- MiroFish export。
- EdgeOne deploy。

## Stage 规则

- 显式 stage。
- 不使用 `git add -A`。
- 不混入无关 dirty files。

## 推送规则

startup/a0 文档完成并通过文档自检后推送当前分支。

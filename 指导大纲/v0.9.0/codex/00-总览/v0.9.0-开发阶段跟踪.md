# RebornG v0.9.0 开发阶段跟踪

更新时间：2026-05-13
当前分支：`codex/v090-b1-world-action-protocol`
当前阶段：`v0.9.0` 正式发布配置；已完成 rc 第一刀、`rc-type-debt`、C-044、C-045、release gate 自动化复核、锁定后 C-049 DeepSeek 默认模型切换、C-050 发布前走查预检和正式版本标识收口。用户已批准锁住 `v0.9.0` 当前成果、决定不再评估其他模型，并允许制作一组 v0.9.0 新功能对外截图/短录屏素材。后续只允许关键 bug、发布配置、文档一致性和验收记录修改。

## 当前状态

v0.9.0 已从 v0.8.0 世界闭环收束后进入玩法系统统一阶段。当前已经完成 a1/a2/a3，完成 b1 首批统一行动协议桥接，并完成 b2 旧战斗系统兼容收束。`v0.9.0-b3` 已用青茅山凡战建立 5x3 可读 UI、美术边界提示、storyboard、资产桥接、行动卡密度、棋盘锚点、白玉/禁忌专项验收、资产扫描脚本和桌面/移动端/reduced motion 验收入口。用户要求的 `v0.9.0-b3-5` 青茅山视觉资产与关键演出增强阶段已完成：第一刀资产分层、C-035 泛用场景底图样板、C-036 入场关键演出、C-037 演出衔接抛光和 C-038 验收台账。rc 发布闸门已完成版本标识、公开测试存档和目标回归收口；`rc-type-debt` 已完成；C-044 已完成发布说明候选稿和手测走查清单最终化；C-045 已关闭 500KB+ chunk 风险。用户随后选择先锁住 `v0.9.0` 当前成果，再开 DeepSeek 和视觉扩展；release gate 自动化已通过。锁定后 DeepSeek eval 已完成，C-049 已把运行时默认模型切换为 `deepseek-v4-flash`；C-050 已完成发布前资源/e2e 预检；用户已决定不再评估其他模型。当前正式版本标识已收口为 `0.9.0` / `v0.9.0`，并已新增 EdgeOne Pages 部署配置。这些改动不改变玩法事实、存档协议或世界观边界。

## 已完成阶段

### v0.9.0-a1 产品冲突清账与统一战斗/行动路由表

分支：`codex/v090-a1-product-route-closure`
状态：已完成
存档协议：保持 `SAVE_FORMAT_VERSION = 20`

落地内容：

- 新增 `CombatRoutePolicy`，统一 `duel / battlefield_5x3 / group_5x3 / group_7x5` 路由。
- `duel` 明确为 1v1 battlefield，不再默认进入旧 `duelState`。
- 道场入口策略输出缺线索、地点不匹配、境界不足、冷却、Debug 等原因。
- 演武入口降级为 Debug/演武，不再与正式剧情战斗混排。
- 版本标识更新为 `v0.9.0-a1`。

### v0.9.0-a2 道场剧情线索入口重做

分支：`codex/v090-a2-training-ground-clues`
状态：已完成
存档协议：正式提升到 `SAVE_FORMAT_VERSION = 21`

落地内容：

- 新增持久 `trainingGroundState`。
- `state_update.training_ground_candidates.add` 只能提交线索候选。
- `TrainingGroundPanel` 改为线索账本。
- 磨练消耗场景 AP 并本地结算；对决生成统一战斗候选；试炼写叙事推进意图。
- `hunt` 留给 a3 荒兽/荒植/守护者敌库，不在 a2 结算掉落。

## 已完成阶段补充

### v0.9.0-a3 荒兽/荒植/守护者敌库与棋盘适配

分支：`codex/v090-a3-beast-guardian-battlefield`
状态：已完成
存档协议：不提升，继续 `SAVE_FORMAT_VERSION = 21`

目标：

- 扩展 `wild-beasts.json` 为运行时敌库。
- 首批覆盖普通野兽、兽王、兽皇、荒兽、上古荒兽、荒植、守护者。
- 让 `hunt` 道场从线索出发进入 7x5 群像战。
- 敌方阶段使用荒兽本能行动，不伪装成蛊师持蛊。
- 战后只结算合法材料、线索、传闻和风险，不稳定掉蛊或仙蛊。

验收命令：

- `npm test -- src/engine/v090-beast-enemy-registry.test.ts src/engine/v090-training-ground-clue-engine.test.ts src/engine/v080-narrative-combat-orchestration.test.ts`
- `npm test`
- `npm run build`
- `npx playwright test tests/e2e/v090-beast-hunt-battlefield.spec.ts`
- `npm run test:e2e:long`

剩余风险：

- `wild-beasts.json` 旧文本存在编码显示问题，新增运行时字段必须保持 JSON 可解析。
- `tg_white_heaven`、`tg_black_heaven` 旧描述中有“仙蛊掉落率”，a3 必须改为寄生蛊传闻/调查概率，不能直接入背包。
- 旧 `duelState` 仍有随机掉落逻辑，a3 不改旧系统，b2 再做正式旧入口收束。

## 当前入口

- `v0.9.0`：正式发布配置与 EdgeOne 部署准备。下一轮必须先读取 `00-总览/README.md`、`v0.9.0-发布闸门与成果锁定.md`、`v0.9.0-正式发布与EdgeOne部署记录.md`、`v0.9.0-真相源索引.md` 和最新 `codex上下文信息/`。
- 已完成项不得重复作为阻断：b1/b2/b3/b3-5、rc 第一刀、`rc-type-debt`、C-044 和 C-045。
- 下一步属于 EdgeOne 部署执行、发布渠道确认、素材最终复核或 v0.9.1 规划；模型评估已关闭，不再作为后续候选需求主动提出。

## 后续入口

- `v0.9.0`：当前阶段。正式版本标识、EdgeOne 仓库配置、公告标题和对外素材候选口径已收口；后续只做部署执行、渠道文案和关键 bug 修复。
- `rc-type-debt`：已完成。关闭 R-001 TypeScript 类型债，验证 `tsc`、`npm test`、`npm run build`、资产检查和长测均通过。
- `C-044`：已完成。发布说明候选稿与手测走查清单最终化。
- `C-045`：已完成。关闭 R-002 chunk 体积风险，构建不再出现 500KB+ chunk warning。
- `v0.9.0` 成果锁定：已按用户选择进入发布闸门，release gate 自动化通过。DeepSeek 模型 eval 与视觉扩展分流到锁定后的后续专项，不再改动已锁定的 `v0.9.0` 本体。
- `C-049`：已完成。用户批准后，运行时默认模型切换为 `deepseek-v4-flash`，并补缓存未命中与 prompt hash 可观测；不改变玩法、存档或世界观事实。
- `C-050`：已完成。发布前走查预检通过运行时资源扫描、青茅资产扫描、产品路线 e2e 和青茅凡战 e2e；正式发布口径和对外素材由 `v0.9.0-正式发布与EdgeOne部署记录.md` 承接。

## v0.9.0-b1 启动审查记录（2026-05-12）

状态：范围冻结已获用户批准。专家团已生成 `v0.9.0-b1-启动审查与范围冻结.md`、`v0.9.0-b1-具体实施清单.md` 和 `v0.9.0-需求决策池.md`，下一步可进入 b1 协议实现。

冻结结论：
- b1 只做统一协议和首批桥接样板，不做完整内容池。
- 同一正式行动只扣一次场景 AP。
- DeepSeek 继续只写候选、线索、传闻、请求、压力和回流文本。
- DeepSeek 模型策略已经收束：每次正式叙事调用必须保留模型、temperature、tokens、缓存命中/未命中、重试成本、耗时和 prompt 前缀 hash；C-049 已在 eval 和用户批准后切换到 `deepseek-v4-flash`，用户已决定不再评估其他模型。
- 新正式行动不得依赖旧 `duelState/squadCombatState`。
- 青茅山凡战视觉竖切排入 `v0.9.0-b3`，不挤入 b1。

## v0.9.0-b1 第一刀记录（2026-05-12）

状态：已完成协议骨架，尚未桥接业务系统。

落地内容：
- 新增 `WorldActionCandidate`、`WorldActionDeparture`、`WorldActionResolution`、`NarrativeReturnContext` 等统一行动协议类型。
- 新增 `src/engine/v090-world-action-protocol.ts`，提供候选归一、出发、结算、`LocalActionLedgerEntry` 投影和回流 prompt 格式化。
- 第一刀只做纯协议层，不提升 `SAVE_FORMAT_VERSION`，不改 UI，不扩内容池。
- 保持 DeepSeek 边界：AI 只能承接回流事实，不能改写 AP、资源、奖励、地点、胜负或原著硬事实。

验证结果：
- `npm test -- src/engine/v090-world-action-protocol.test.ts src/engine/v080-scene-session-engine.test.ts`：通过。

下一步：
- 用道场作为黄金样板，把 a2/a3 已有线索、出发、磨练、对决、狩猎结果投影到统一行动协议。

## v0.9.0-b1-2 道场黄金样板桥接记录（2026-05-12）

状态：已完成道场桥接，尚未桥接传承/福地、灾劫和野外行动。

落地内容：
- `src/engine/v090-training-ground-clue-engine.ts` 输出 `WorldActionCandidate`、`WorldActionDeparture`、`WorldActionResolution`、`LocalActionLedgerEntry` 投影和 `NarrativeReturnContext`。
- `src/store/slices/trainingGroundSlice.ts` 使用统一行动协议写入场景 AP 账本，并把 `lastWorldActionReturnContext` 写入 flags，供下一轮动态 prompt 承接。
- `src/engine/context-builder.ts` 注入 `lastWorldActionReturnContext`，让 DeepSeek 看到本地事实和 AI 不可改写边界。
- 磨练、对决、试炼、狩猎都走统一出发语义；对决/狩猎只生成本地战斗候选，不结算胜负或掉落。
- 缺 AP 的道场入口会投影为 blocked，不扣 AP。

验证结果：
- `npm test -- src/engine/v090-training-ground-clue-engine.test.ts src/store/slices/trainingGroundSlice.test.ts src/engine/v090-world-action-protocol.test.ts src/engine/v080-scene-session-engine.test.ts`：通过。
- `npm test`：86 files / 527 tests 通过。
- `npm run build`：通过，仅保留既有 Vite 500KB+ chunk warning。

下一步：
- 进入 `b1-3`：传承/福地桥接，重点修正出发与试炼/认主的 AP 语义，并保持奖励、福地归属和试炼结果由本地引擎决定。

## v0.9.0-b1-3 传承/福地桥接记录（2026-05-12）

状态：已完成传承/福地桥接，灾劫已在 b1-4 桥接完成，尚未桥接野外行动。

落地内容：
- `src/engine/v080-inheritance-land-engine.ts` 输出传承/福地行动的 `WorldActionCandidate`、`WorldActionDeparture`、`WorldActionResolution`、`LocalActionLedgerEntry` 投影和 `NarrativeReturnContext`。
- `src/store/slices/inheritanceLandSlice.ts` 使用统一行动协议写入场景 AP 账本，并把 `lastWorldActionReturnContext` 和 `lastInheritanceWorldAction` 写入 flags。
- `startInheritanceTrialAction` 出发扣 AP 后，`resolveInheritanceTrialAction` 复用出发账本，不再二次扣 AP。
- 未先出发而直接结算试炼时，试炼自身按统一协议扣一次 AP。
- 福地认主通过统一协议写账本；成功、失败、阻断都回流本地事实。福地归属、资源节点、地灵状态和灾劫压力仍只由本地引擎决定。
- 洞天继续作为边界传闻，不开放正式认主或吞并。

验证结果：
- `npm test -- src/engine/v080-inheritance-land-engine.test.ts src/store/slices/inheritanceLandSlice.test.ts src/engine/v090-world-action-protocol.test.ts src/engine/v080-scene-session-engine.test.ts`：通过。
- `npm test`：86 files / 528 tests 通过。
- `npm run build`：通过，仅保留既有 Vite 500KB+ chunk warning。

下一步：
- 已进入并完成 `b1-4`：灾劫桥接。

## v0.9.0-b1-4 灾劫桥接记录（2026-05-12）

状态：已完成灾劫桥接，尚未桥接野外行动。

落地内容：
- `src/engine/v080-calamity-scene-engine.ts` 输出灾劫行动的 `WorldActionCandidate`、`WorldActionDeparture`、`WorldActionResolution`、`LocalActionLedgerEntry` 投影和 `NarrativeReturnContext`。
- `stageCalamityScene()` 把灾劫预兆登记为 `calamity` 领域的 `pending_narrative` 行动，写入场景 AP 账本和 `flags.lastWorldActionReturnContext`。
- `resolveApertureCalamity()` 把本地灾劫后果回流到统一协议；若已在预兆入场阶段扣过 AP，正式结算复用同一行动，不再二次扣费。
- 灾劫面积损失、资源点损伤、道痕变化、蛊虫损坏、战斗胜负和奖励继续只由本地引擎/战斗引擎决定。
- DeepSeek 只能承接压力、氛围、选择描述和回流文本，不得改写灾劫后果。

验证结果：
- `npm test -- src/engine/v080-calamity-scene-engine.test.ts src/store/slices/cultivationSlice.test.ts`：通过。
- `npm test -- src/engine/v080-scene-session-engine.test.ts src/engine/v090-world-action-protocol.test.ts src/engine/v090-training-ground-clue-engine.test.ts src/engine/v080-inheritance-land-engine.test.ts src/engine/v080-calamity-scene-engine.test.ts src/store/slices/cultivationSlice.test.ts src/store/slices/inheritanceLandSlice.test.ts src/store/slices/trainingGroundSlice.test.ts`：通过，8 files / 42 tests。
- `npm test`：86 files / 530 tests 通过。
- `npm run build`：通过，仅保留既有 Vite 500KB+ chunk warning。

下一步：
- 进入 `b1-5`：野外行动最小样板，重点选择一条可测的野外采集/调查/遭遇链，验证统一协议对普通 field action 的承接。

## v0.9.0-b1-5 野外行动最小样板记录（2026-05-12）

状态：已完成。`v0.9.0-b1` 首批统一行动协议桥接完成。

落地内容：
- `src/engine/field-action.ts` 新增 `buildFieldActionWorldActionBridge()`，可把野外采集、侦察、陷阱排查和撤离准备投影为统一行动协议。
- `src/store/slices/playerSlice.ts` 的 `performFieldAction()` 使用 `field_action` 账本写入 `WorldAction` 系统结果和 `flags.lastWorldActionReturnContext`。
- 野外采集只允许本地引擎结算已登记低阶蛊材；DeepSeek 不得追加材料、蛊虫、仙材、仙蛊、正式地点或战斗结果。
- 侦察/陷阱/撤离类结果只登记本地辅助标记，不等于正式地点解锁、任务完成或战斗胜利。
- `src/api/deepseek.test.ts` 仍覆盖模型、tokens、缓存命中和 prompt 前缀 hash telemetry；b1-5 未破坏 DeepSeek 观测。

验证结果：
- `npm test -- src/engine/p2-action-systems.test.ts src/store/slices/player-p2-actions.test.ts`：通过，2 files / 11 tests。
- `npm test -- src/engine/v080-scene-session-engine.test.ts src/engine/v090-world-action-protocol.test.ts src/engine/p2-action-systems.test.ts src/store/slices/player-p2-actions.test.ts src/engine/v090-training-ground-clue-engine.test.ts src/store/slices/trainingGroundSlice.test.ts src/engine/v080-inheritance-land-engine.test.ts src/store/slices/inheritanceLandSlice.test.ts src/engine/v080-calamity-scene-engine.test.ts src/store/slices/cultivationSlice.test.ts src/api/deepseek.test.ts`：通过，11 files / 55 tests。
- `npm test`：86 files / 532 tests 通过。
- `npm run build`：通过，仅保留既有 Vite 500KB+ chunk warning，并出现一次 Rolldown plugin timings 提示。

下一步：
- 进入 `v0.9.0-b2`：旧 `duelState/squadCombatState` 兼容收束和正式入口清理。

## v0.9.0-b2 旧战斗系统兼容收束记录（2026-05-12）

状态：已完成。旧 `duelState/squadCombatState` 保留兼容与旧存档兜底，新正式入口不再直接进入旧战斗。

落地内容：
- `src/engine/combat-router.ts` 新增 `buildCombatEventCandidateFromTrigger()`，把旧关键词 duel/narrative 战斗触发转换为 `combat_event_candidates`。
- `src/engine/response-pipeline.ts` 不再从叙事文本直接 `initDuel()`，也不再写 transient combat settlement prompt；命中战斗只登记本地候选。
- `src/engine/ai-state-update-validator.ts` 与 `src/engine/state-update-applier.ts` 双重降级 AI `combat_result`，不再应用 HP、真元、伤势、元石或掉落。
- `src/engine/activity-availability.ts` 将 active `battlefieldCombatState` / `combatEncounterState` 计入战斗锁，防止战斗中另起野外行动。
- `src/engine/context-builder.ts` 将旧 transient combat constraint 标记为兼容提示，要求 AI 只写 `combat_event_candidates`。
- 修正 `response-pipeline.ts` 中 `encStore` / `encStore2` 变量误引用。

验证结果：
- `npm test -- src/engine/combat-router.test.ts src/engine/activity-availability.test.ts src/engine/ai-state-update-validator.test.ts src/engine/state-update-applier-resource-gate.test.ts src/engine/combat-route-policy.test.ts src/engine/v080-narrative-combat-orchestration.test.ts src/store/slices/v080-battlefield-combat-ui-store.test.ts`：通过，7 files / 33 tests。
- `npm test`：通过，87 files / 537 tests。
- `npm run build`：通过，仅保留既有 500KB+ `combat-squad` chunk warning。
- `npx playwright test tests/e2e/v090-product-route-closure.spec.ts tests/e2e/v090-beast-hunt-battlefield.spec.ts`：通过，4 tests。
- `npx tsc --noEmit`：仍失败，属于历史类型债；主要为缺 `@types/react/@types/react-dom/@types/node`、旧 store 类型和若干旧组件类型问题。本轮已排查 touched 文件，只剩既有类型债。

下一步：
- 进入 `v0.9.0-b3`：青茅山凡战视觉竖切。

## v0.9.0-b3 第一刀记录（2026-05-12）

状态：已完成青茅山凡战视觉竖切最小样板，b3 后续仍可继续强化强演出和资产管线。

落地内容：
- 新增 `createQingmaoMortalBattlefieldState()`，以青茅山凡战作为 5x3 battlefield 样板。
- 新增 `initQingmaoMortalBattlefieldDemo()` 和 Debug/演武“青茅”入口。
- `BattlefieldCombatOverlay` 在 b3 状态下显示 `v0.9.0-b3 青茅山凡战竖切`、月光蛊/白玉蛊/酒虫提示和 C-020 美术边界。
- 酒虫只作为场景门槛/战前真元支持提示，不作为普通攻击按钮。
- 新增 `startQingmaoMortalBattlefieldDemo()` e2e harness 与 b3 Playwright。

验证结果：
- `npm test -- src/store/slices/v080-battlefield-combat-ui-store.test.ts src/engine/v080-combat-expression-data.test.ts`：通过，2 files / 16 tests。
- `npm run build`：通过，仅保留既有 500KB+ `combat-squad` chunk warning。
- `npx playwright test tests/e2e/v090-b3-qingmao-battlefield.spec.ts`：通过，2 tests。
- `npm test`：通过，87 files / 538 tests。
- `npx playwright test tests/e2e/v080-battlefield-ui.spec.ts tests/e2e/v080-group-battlefield-ui.spec.ts tests/e2e/v080-large-group-battlefield-ui.spec.ts`：通过，6 tests。

下一步：
- b3-2：月刃连斩/白玉护体强演出 storyboard、现有青茅山资产接入和截图验收台账。

## v0.9.0-b3-2 记录（2026-05-13）

状态：已完成青茅山凡战 storyboard、现有蛊虫资产桥接和截图验收台账；仍不做全局视觉重皮。

落地内容：
- 新增 `buildQingmaoBattlefieldAssets()`，接入月光蛊、白玉蛊、酒虫三张现有 `public/rebrng/gu/s0-qingmao/` 资产。
- 新增 `buildQingmaoBattlefieldStoryboard()`，覆盖月刃连斩、白玉护体、酒虫支持、禁忌门槛四段表现。
- `BattlefieldCombatOverlay` 显示青茅资产桥接和 storyboard，并保留酒虫非攻击按钮边界。
- `battlefieldTimeline` 新增月刃、白玉、禁忌三类 GSAP 分支，触发条件只读 `BattleResolutionStep`。
- 新增 `v0.9.0-b3-2-启动与验收审查.md` 和 `v0.9.0-b3-截图验收台账.md`。

验证结果：
- `npm test -- src/store/slices/v080-battlefield-combat-ui-store.test.ts src/engine/v080-combat-expression-data.test.ts`：通过，2 files / 16 tests。
- `npx playwright test tests/e2e/v090-b3-qingmao-battlefield.spec.ts`：通过，2 tests。
- `npm run build`：通过，仅保留既有 500KB+ `combat-squad` chunk warning。
- `npm test`：通过，87 files / 538 tests。
- `npx playwright test tests/e2e/v080-battlefield-ui.spec.ts tests/e2e/v080-group-battlefield-ui.spec.ts tests/e2e/v080-large-group-battlefield-ui.spec.ts`：通过，6 tests。

下一步：
- 进入 `v0.9.0-b3-3` 候选审查：行动卡信息密度、强演出棋盘对位、资产 manifest 与正式截图/录屏沉淀。

## v0.9.0-b3-3 记录（2026-05-13）

状态：已完成行动卡信息密度、强演出源格/目标格对位和青茅视觉资产 manifest；仍不做全局视觉重皮。

落地内容：
- 新增 `src/canon/qingmao-visual-assets.json`，登记 active/candidate/blocked 资产；春秋蝉在 b3 凡战竖切中为 blocked。
- `BattlefieldActionCard` 增加 `rangeText/targetText/counterText/utilityText`，行动卡改为紧凑三列元信息。
- `BattlefieldCombatOverlay` 新增 `syncBattlefieldEffectAnchors()`，把 `BattleResolutionStep` 映射为 effect layer 源格/目标格坐标。
- `battlefieldTimeline` 改为读取 `--battlefield-effect-*` CSS 变量，月刃、护体、禁忌演出贴近棋盘。
- b3 e2e 覆盖月光蛊执行后的 `c0_1 -> c3_1` 锚点。

验证结果：
- `npm test -- src/store/slices/v080-battlefield-combat-ui-store.test.ts src/engine/v080-combat-expression-data.test.ts`：通过，2 files / 16 tests。
- `npx playwright test tests/e2e/v090-b3-qingmao-battlefield.spec.ts`：通过，2 tests。
- `npm run build`：通过，仅保留既有 500KB+ `combat-squad` chunk warning；本次另有 Vite plugin timings 提示，不阻断。
- `npx playwright test tests/e2e/v080-battlefield-ui.spec.ts tests/e2e/v080-group-battlefield-ui.spec.ts tests/e2e/v080-large-group-battlefield-ui.spec.ts`：通过，6 tests。
- `npm test`：通过，87 files / 538 tests。

下一步：
- 进入 `v0.9.0-b3-4` 候选审查：白玉护体/禁忌门槛专项演出验收、资产 manifest 完整性扫描、b3 收束截图/录屏。

## v0.9.0-b3-4 记录（2026-05-13）

状态：已完成白玉护体/禁忌门槛专项验收、青茅资产 manifest 完整性扫描和 b3 收束审查。

落地内容：
- 新增 `scripts/check-qingmao-visual-assets.mjs` 与 `npm run check:qingmao-assets`，验证 active/candidate 资产存在、必需 active 资产齐全、春秋蝉不进入 active。
- `src/e2e/installE2eHarness.ts` 新增 `triggerQingmaoForbiddenThresholdFailure()`，只用于 e2e 验收本地失败轨迹。
- `tests/e2e/v090-b3-qingmao-battlefield.spec.ts` 扩展到 4 条：月光蛊、白玉蛊、禁忌失败、移动端 reduced motion。
- Playwright 附件沉淀 `v090-b3-4-white-jade-shell` 与 `v090-b3-4-forbidden-threshold` 截图证据。
- 新增 `v0.9.0-b3-4-启动与验收审查.md`，同步截图台账、真相源索引和上下文交接。

验证结果：
- `npm run check:qingmao-assets`：通过。
- `npm test -- src/store/slices/v080-battlefield-combat-ui-store.test.ts src/engine/v080-combat-expression-data.test.ts`：通过，2 files / 16 tests。
- `npx playwright test tests/e2e/v090-b3-qingmao-battlefield.spec.ts`：通过，4 tests。
- `npm run build`：通过，仅保留既有 500KB+ chunk warning 和 plugin timings warning。
- `npm test`：通过，87 files / 538 tests。
- `npx playwright test tests/e2e/v080-battlefield-ui.spec.ts tests/e2e/v080-group-battlefield-ui.spec.ts tests/e2e/v080-large-group-battlefield-ui.spec.ts`：通过，6 tests。

下一步：
- 建议进入 `v0.9.0-rc` 启动审查；若继续扩展视觉表现，先冻结 `b3-5` 或后续视觉资产阶段范围。

## v0.9.0-b3-5 范围冻结记录（2026-05-13）

状态：已冻结范围，尚未进入代码实现。

冻结结论：
- 阶段名称：青茅山凡战视觉资产与关键演出增强阶段。
- 目标：在 b3 已有可读战棋盘基础上，建立视觉资产入库口径、1 张泛用青茅凡战氛围资产样板、入场关键演出和月光/白玉/禁忌衔接抛光。
- 非目标：不全局重皮，不扩玩法，不改战斗数值或存档，不用具体人物战斗图作为 generic 背景，不批量生成图片。
- 美术边界：仍禁止暗示仙蛊、仙体、永生、十转、宿命蛊归属或凡人宝黄天交易。

文档入口：
- `v0.9.0-b3-5-视觉资产阶段范围冻结.md`

下一步：
- 进入 `v0.9.0-b3-5` 开发前，先选择 1 个视觉资产目标；如果需要生成新图，按单张慢产流程执行。

## v0.9.0-b3-5 第一刀资产分层记录（2026-05-13）

状态：已完成 C-034 视觉资产 manifest 分层。

落地内容：
- `src/canon/qingmao-visual-assets.json` 增加 `runtimeLayer/runtimeScope/sceneBinding/admission` 字段。
- 资产状态扩展为 `active/candidate/review-only/blocked`。
- 三张蛊虫图保持 active runtime；方源对白凝冰二战、青书对白凝冰林战改为 review-only；春秋蝉继续 blocked。
- `buildQingmaoBattlefieldAssets()` 只返回 `active + generic + runtime_active`。
- `check-qingmao-visual-assets.mjs` 增加分层校验，防止 scene-specific 图误进 candidate/active。

验证结果：
- `npm run check:qingmao-assets`：通过，active=3，review-only=2，blocked=1。
- `npm test -- src/store/slices/v080-battlefield-combat-ui-store.test.ts`：通过，1 file / 7 tests。
- `npx playwright test tests/e2e/v090-b3-qingmao-battlefield.spec.ts`：通过，4 tests。
- `npm run build`：通过，仅保留既有 chunk warning 和 plugin timings warning。
- `npm test`：通过，87 files / 538 tests。

下一步：
- 进入 C-035 青茅凡战泛用场景底图样板；开工前先写 `Composition Contract`。

## v0.9.0-b3-5 C-035 泛用场景底图样板记录（2026-05-13）

状态：已完成 C-035 青茅凡战泛用场景底图样板。

落地内容：
- 新增 `public/rebrng/scenes/s0-qingmao/qingmao-mortal-battlefield-generic-atmosphere.svg`，作为不绑定具体人物战斗的 generic 青茅凡战底图。
- `src/canon/qingmao-visual-assets.json` 登记该资产为 `background + scene_background + active + generic + runtime_active`，并绑定 `compositionContractId`。
- `src/engine/v080-battlefield-ui-model.ts` 新增 `buildQingmaoBattlefieldAtmosphereAsset()`；`buildQingmaoBattlefieldAssets()` 继续只返回 3 张蛊虫资产。
- `BattlefieldCombatOverlay` 新增底层 atmosphere image，Playwright 可通过 `data-qingmao-atmosphere-asset` 验收加载。
- `check-qingmao-visual-assets.mjs` 增加 active background 的 Composition Contract、路径和 scene-specific 标记校验。

验证结果：
- `npm run check:qingmao-assets`：通过，7 entries；active=4，review-only=2，blocked=1。
- `npm test -- src/store/slices/v080-battlefield-combat-ui-store.test.ts`：通过，1 file / 7 tests。
- `npx playwright test tests/e2e/v090-b3-qingmao-battlefield.spec.ts`：通过，4 tests。
- `npm run build`：通过，仅保留既有 chunk warning 与 plugin timings warning。
- `npm test`：通过，87 files / 538 tests。

下一步：
- 进入 C-036 青茅战场入场关键演出；继续只消费 battlefield state 和 `BattleResolutionStep`，reduced motion 下不能依赖动画理解战斗。

## v0.9.0-b3-5 C-036 入场关键演出记录（2026-05-13）

状态：已完成 C-036 青茅战场入场关键演出。

落地内容：
- `src/animations/gsap/battlefieldTimeline.ts` 新增 `playQingmaoBattlefieldEntranceTimeline()`，播放雾幕、标题、道痕线、空窍环和扫光。
- `src/hooks/useBattlefieldAnimationBridge.ts` 新增 `useQingmaoBattlefieldEntranceBridge()`，reduced motion 下直接标记静态兜底。
- `src/components/game/BattlefieldCombatOverlay.tsx` 新增 `QingmaoEntranceBanner`，作为 header 与主体之间的全宽 band，不遮挡棋盘和按钮。
- `tests/e2e/v090-b3-qingmao-battlefield.spec.ts` 补桌面入场 complete 标记和移动端 reduced-motion 验收。

验证结果：
- `npm run check:qingmao-assets`：通过，7 entries；active=4，review-only=2，blocked=1。
- `npm test -- src/store/slices/v080-battlefield-combat-ui-store.test.ts`：通过，1 file / 7 tests。
- `npx playwright test tests/e2e/v090-b3-qingmao-battlefield.spec.ts`：通过，4 tests。
- `npm run build`：通过，仅保留既有 chunk warning 与 plugin timings warning。
- `npm test`：通过，87 files / 538 tests。

下一步：
- C-037 和 C-038 均已完成；下一步建议进入 `v0.9.0-rc` 启动审查与发布候选范围冻结。

## v0.9.0-b3-5 C-037 演出衔接抛光记录（2026-05-13）

状态：已完成 C-037 月光/白玉/禁忌演出衔接抛光。

落地内容：
- `src/animations/gsap/battlefieldTimeline.ts` 增加月光残光 `moonEcho`、白玉裂光 `jadeCrack` 和禁忌符印 `boundarySigil`。
- 失败/反制/禁忌门槛分支优先于月光/白玉来源分支，避免月光蛊越界失败误播成月刃成功演出。
- `BattlefieldCombatOverlay` effect layer 挂载三层新演出节点。
- b3 Playwright 验收 `data-qingmao-polish`：月光为 `moon-transition`，白玉为 `white-jade-transition`，禁忌失败为 `forbidden-transition`。

验证结果：
- `npm test -- src/store/slices/v080-battlefield-combat-ui-store.test.ts`：通过，1 file / 7 tests。
- `npx playwright test tests/e2e/v090-b3-qingmao-battlefield.spec.ts`：通过，4 tests。
- `npm run check:qingmao-assets`：通过，7 entries；active=4，review-only=2，blocked=1。
- `npm run build`：通过，仅保留既有 500KB+ chunk warning 与 plugin timings warning。
- `npm test`：通过，87 files / 538 tests。
- `npx playwright test tests/e2e/v080-battlefield-ui.spec.ts tests/e2e/v080-group-battlefield-ui.spec.ts tests/e2e/v080-large-group-battlefield-ui.spec.ts`：通过，6 tests。

下一步：
- C-038 已完成；下一步建议进入 `v0.9.0-rc` 启动审查与发布候选范围冻结。

## v0.9.0-b3-5 C-038 视觉资产阶段验收台账记录（2026-05-13）

状态：已完成 C-038 视觉资产阶段验收台账。`v0.9.0-b3-5` 完成收束。

落地内容：
- 新增 `v0.9.0-b3-5-C038-视觉资产阶段验收台账.md`，整理 C-033 到 C-038 的验收矩阵、自动化证据和剩余风险。
- `v0.9.0-b3-截图验收台账.md` 更新 b3-5/C037 附件口径。
- `tests/e2e/v090-b3-qingmao-battlefield.spec.ts` 的附件命名更新为 `v090-b3-5-c037-moon-transition`、`v090-b3-5-c037-white-jade-transition`、`v090-b3-5-c037-forbidden-transition`。

验证结果：
- `npx playwright test tests/e2e/v090-b3-qingmao-battlefield.spec.ts`：通过，4 tests。
- 继承 C-037 已通过的资产扫描、构建、全量 Vitest 和旧 battlefield e2e 验收。

下一步：
- 后续状态更新：用户已批准进入 `v0.9.0-rc`，范围冻结为发布候选收束，不继续扩玩法或全局视觉重皮。

## v0.9.0-rc 第一刀记录（2026-05-13）

状态：已完成 rc 启动审查与第一刀收口；继续做全量测试、构建和文档冲突扫尾。

落地内容：
- 新增 `v0.9.0-rc-启动审查与范围冻结.md`，冻结 rc 只做发布候选收束。
- `package.json` 更新为 `0.9.0-rc.0`，标题页与游戏页页脚显示 `v0.9.0-rc`。
- `public/test-saves/` 更新为 10 个 `formatVersion 21` 公开导入镜像，来源为当前长测真相源 `测试存档/v0.7.0/`。
- `src/store/test-save-fixtures.test.ts` 增加公开测试存档镜像检查。
- `tests/e2e/v070-long.spec.ts` 将 10 个公开测试存档镜像和 1 条标题页文件导入公开存档纳入长测。
- `src/engine/response-pipeline-token-usage.test.ts` 增加 DeepSeek telemetry 透传与重试成本合并测试。
- 新增 `npm run check:runtime-assets`，检查 `public` 运行时资源 0 字节问题。
- 安装 React/React DOM/Node 类型声明并启用 `node` 类型，减少 `tsc` 噪声；剩余真实类型债另列风险池。
- DeepSeek telemetry 与经济回路目标测试、版本标签 e2e、长测、青茅资产扫描和 b3 视觉 e2e 已通过。

## v0.9.0-a3 完成记录（2026-05-12）

状态：已完成。分支 `codex/v090-a3-beast-guardian-battlefield` 已完成荒兽/荒植/守护者敌库竖切。

落地内容：
- `src/canon/wild-beasts.json` 已扩展为运行时敌库，保留 lore 边界，并新增敌人注册、狩猎绑定和掉落策略。
- 新增 `src/engine/v090-beast-enemy-registry.ts`，负责敌库校验、hunt 道场敌人池、7x5 battlefield 构建和战后掉落边界结算。
- `tg_white_heaven` / `tg_black_heaven` 不再是“待 a3”，有剧情线索后可生成 `group_7x5` 狩猎战斗候选。
- `BattlefieldCombatState` 支持荒兽单位的 `instinctMoves`；敌方阶段可输出扑击、毒雾、缠绕、地形变化等本地轨迹。
- `TrainingGroundPanel` 显示狩猎敌情、棋盘规模、AP 成本、风险与寄生蛊不稳定提示。
- 版本标识更新为 `v0.9.0-a3`，`package.json` 更新为 `0.9.0-a3.0`。

验证结果：
- `npm test -- src/engine/v090-beast-enemy-registry.test.ts src/engine/v090-training-ground-clue-engine.test.ts src/store/slices/trainingGroundSlice.test.ts src/engine/v080-narrative-combat-orchestration.test.ts`：通过。
- `npm test`：84 files / 521 tests 通过。
- `npm run build`：通过，仅保留既有 Vite chunk size warning。
- `npx playwright test tests/e2e/v090-beast-hunt-battlefield.spec.ts`：2 tests 通过。
- `npx playwright test tests/e2e/v090-product-route-closure.spec.ts tests/e2e/v090-training-ground-clue-entry.spec.ts`：4 tests 通过。
- `npm run test:e2e:long`：18 tests 通过。

剩余风险：
- a3 只是敌库与狩猎竖切，不等于完整荒兽生态、全部荒兽、长期狩猎经济或太古荒兽常规战。
- 旧 `duelState/squadCombatState` 仍保留兼容入口，正式清理继续放到 `v0.9.0-b2`。
- `v0.9.0-b1` 需要把传承、福地、道场、灾劫、野外行动进一步统一到同一“线索 -> 出发 -> 本地结算 -> 行动账本 -> 回流文本”协议。

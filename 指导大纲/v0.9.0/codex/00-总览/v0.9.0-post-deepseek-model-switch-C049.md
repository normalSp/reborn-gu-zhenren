# C-049 运行时默认模型切换补丁

日期：2026-05-13
状态：已完成。用户已批准把正式运行时默认模型切换为 `deepseek-v4-flash`。

## 背景

前置证据来自 `v0.9.0-post-deepseek-model-eval-live-20260513.md`：

- Flash full live：`artifacts/deepseek-eval/2026-05-13T10-01-48-626Z/`
- Flash 严门禁回放：`artifacts/deepseek-eval/2026-05-13T10-02-48-963Z/`
- 40 条固定样本 accepted 100%，JSON/schema/domain/boundary/quality 均 100%。
- Pro 8 条代表样本仍为 75%，继续只作为离线对照。

## 本轮改动

- `src/api/deepseek.ts`
  - `DEEPSEEK_DEFAULT_MODEL` 从 `deepseek-chat` 改为显式 `deepseek-v4-flash`。
  - `TokenUsage` 增加 `cache_miss_tokens`。
  - API 返回 `prompt_cache_miss_tokens` 时直接透传；未返回时用 `prompt_tokens - prompt_cache_hit_tokens` 推导。
- `src/engine/response-pipeline.ts`
  - `PipeTokenUsage` 增加 `cacheMiss`。
  - pipeline 日志 metadata 记录 `cacheMissTokens`。
- `src/components/title/TitleScreen.tsx`
  - API 测试结果显示模型、耗时、总 tokens、缓存命中、缓存未命中、缓存命中率和 prompt 前缀 hash。
- `src/api/deepseek.test.ts`
  - 固定断言默认模型为 `deepseek-v4-flash`。
  - 覆盖 cache miss 直接透传与缺省推导。
- `src/engine/response-pipeline-token-usage.test.ts`
  - 覆盖 pipeline cache miss 聚合。

## 中文 smoke

真实 API smoke 使用源码中的默认模型常量：

| 指标 | 值 |
|---|---:|
| model | `deepseek-v4-flash` |
| elapsedMs | 1024 |
| prompt tokens | 61 |
| completion tokens | 42 |
| total tokens | 103 |
| prompt cache hit tokens | 0 |
| prompt cache miss tokens | 61 |
| cache hit ratio | 0 |

返回 JSON：

```json
{"message":"天道已响应","status":"ok"}
```

## 验收

- `npx vitest run src/api/deepseek.test.ts src/engine/response-pipeline-token-usage.test.ts`：通过，2 files / 6 tests。
- `npx tsc --noEmit --pretty false`：通过。
- `npm test`：通过，88 files / 543 tests。
- `npm run build`：通过；无 500KB+ chunk warning，仅有 Rolldown plugin timings 提示。
- `npm run eval:deepseek:dry-run`：通过；prompt hash `c88508ff`，sampleCount 40，liveCalls 0。

## 结论

`deepseek-v4-flash` 已成为运行时默认叙事模型。此次切换不改变玩法事实、存档协议、世界观边界或 DeepSeek 输出权限；DeepSeek 仍只能写叙事、候选、线索、传闻和压力，正式奖励、战斗、经济、灾劫和原著硬事实仍由本地 canon/engine 决定。

用户后续已明确决定不再评估其他模型；C-049 后模型策略冻结为 `deepseek-v4-flash`。

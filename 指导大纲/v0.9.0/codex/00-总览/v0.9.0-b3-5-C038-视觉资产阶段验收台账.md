# v0.9.0-b3-5 C-038：视觉资产阶段验收台账

日期：2026-05-13

## 结论

C-038 已完成。`v0.9.0-b3-5` 青茅山凡战视觉资产与关键演出增强阶段完成闭环：范围冻结、资产分层、泛用底图、入场关键演出、月光/白玉/禁忌衔接抛光和验收台账均已落地。

本阶段仍然不是全局重皮，也没有扩玩法。战斗事实继续由本地 canon/engine/battlefield state 决定，DeepSeek 不参与战斗结算。

## 阶段验收矩阵

| 项目 | 状态 | 证据 | 边界 |
|---|---|---|---|
| C-033 范围冻结 | 通过 | `v0.9.0-b3-5-视觉资产阶段范围冻结.md` | 不全局重皮，不扩玩法 |
| C-034 资产 manifest 分层 | 通过 | `src/canon/qingmao-visual-assets.json`、`scripts/check-qingmao-visual-assets.mjs` | 运行时只接 `active + generic + runtime_active` |
| C-035 泛用场景底图 | 通过 | `qingmao-mortal-battlefield-generic-atmosphere.svg`、Composition Contract | 不绑定具体人物战斗 |
| C-036 入场关键演出 | 通过 | `QingmaoEntranceBanner`、`playQingmaoBattlefieldEntranceTimeline()` | 布局内 band，不遮挡棋盘 |
| C-037 演出衔接抛光 | 通过 | `moonEcho`、`jadeCrack`、`boundarySigil`、`data-qingmao-polish` | 失败/反制优先于来源蛊虫成功特效 |
| C-038 验收台账 | 通过 | 本文档、`v0.9.0-b3-截图验收台账.md` | 只整理证据，不新增需求 |

## Playwright 附件口径

`tests/e2e/v090-b3-qingmao-battlefield.spec.ts` 当前会输出以下关键附件：

- `v090-b3-5-c037-moon-transition`：月光蛊从 `c0_1` 到 `c3_1` 的月刃/残光衔接。
- `v090-b3-5-c037-white-jade-transition`：白玉蛊自格护体、玉色承压裂光。
- `v090-b3-5-c037-forbidden-transition`：目标超出射程时的禁忌/边界阻断，不暗示未开放奖励。

截图附件只是验收证据，不是最终宣传素材。宣传素材阶段仍需另开美术审查。

## 自动化验收

- `npm run check:qingmao-assets`：通过，7 entries；active=4，review-only=2，blocked=1。
- `npm test -- src/store/slices/v080-battlefield-combat-ui-store.test.ts`：通过，1 file / 7 tests。
- `npx playwright test tests/e2e/v090-b3-qingmao-battlefield.spec.ts`：通过，4 tests。
- `npm run build`：通过，仅保留既有 500KB+ chunk warning 与 plugin timings warning。
- `npm test`：通过，87 files / 538 tests。
- `npx playwright test tests/e2e/v080-battlefield-ui.spec.ts tests/e2e/v080-group-battlefield-ui.spec.ts tests/e2e/v080-large-group-battlefield-ui.spec.ts`：通过，6 tests。

## 剩余风险

- 正式宣传短录屏仍未固化；建议放到 rc 前素材审查，不在 b3-5 内继续扩。
- 当前泛用底图是 SVG 样板；若后续替换为 bitmap，仍必须走单张慢产、Composition Contract 和世界观审查。
- 构建仍有既有 `combat-squad` 500KB+ chunk warning 与 plugin timings warning；本阶段未扩大该风险。
- `npx tsc --noEmit` 仍属于历史类型债范围，本阶段不作为阻断，但 rc 前应继续记录。

## 下一阶段入口

建议进入 `v0.9.0-rc` 启动审查与发布候选范围冻结。

后续状态更新：用户已批准进入 `v0.9.0-rc`，rc 只做长测、测试存档、经济复验、DeepSeek 成本观测复盘、文档冲突清理、版本标识收口和关键 bug 修复，不继续扩玩法或全局视觉重皮。

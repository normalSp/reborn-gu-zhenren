# RebornG v0.8.0 开发阶段跟踪

更新时间：2026-05-11
当前分支：`codex/v080-b11-variable-battlefield-scale`
依据大纲：`指导大纲/v0.8.0/v0.8.0-正史锚点-IF宿命双路线-剧情完整版-设计大纲.md`

## 总目标

v0.8.0 不继续堆系统数量，而是把 RebornG 从“系统可玩”推进到“长线剧情可信 + 战斗有深度 + 前端有游戏感”。核心交付是：正史锚点骨架、IF 宿命双路线、天意与因果压力、剧情候选闸门、结局矩阵解析、蛊虫表现化战斗、GSAP/Motion 全局动效重皮。

玩家仍然是原创角色。方源、龙公、白凝冰、商心慈等原著角色只能作为 canon NPC、远景核心变量或剧情交会对象存在，不能替代玩家，也不能被玩家无代价替代。

v0.8.0 同时接手 v0.7.0 P1/P2 承诺效果分流出来的深系统：夜间/白天修行差异、伏击全链路、完整大境界突破/升仙失败、复杂灾劫后果、高级采集生态。它们不再作为 v0.7.0 发布阻塞，但必须在 v0.8.0 大纲和阶段验收中明确归属。

v0.8.0 还接手 v0.7.1-polish 新建的开局身份路由：`StartProfile` 已保证商家、武家、熊家、铁家、散修等开局不再被写成古月族人；v0.8.0 要把这些身份推进为各出身深线剧情，详见 `codex/02-剧情分支/出身深线剧情矩阵.md`。

v0.8.0 新增一级重点：战斗深化。当前 `combat-engine`、小队战、地形/阵法和战斗闪图已有底座，但玩家操作仍过窄。v0.8 先做凡战深度：让蛊虫和杀招的描述进入可结算、可显示、可被剧情选择调用的表现协议。仙蛊在 v0.8 仅作剧情授权、临时借用、见闻和高压演出；仙蛊屋暂不做运行时战斗系统。

v0.8.0 新增一级重点：全局动效重皮。GSAP 负责战斗/章节级时间轴演出，Motion 负责 UI 状态、布局、选择和棋盘交互。目标不是装饰性动画，而是让状态、代价、反制、风险和剧情用途被玩家看懂。

## 硬边界

- 原著模式不允许改写关键锚点大结果，只允许侧线、关系、资源、局部战果变化。
- IF 模式允许偏移，但必须登记 `IfBranchVector`，写清原因、代价、影响范围和后续反噬。
- 宿命轴只允许 `intact / fractured / destroyed` 三态，由引擎推导，DeepSeek 不得直接写入。
- 天意不是万能失败按钮，只是压力、修正、排斥和叙事约束系统。
- 无极魔尊疯魔窟求证失败；十转、混沌、事实浮冰和永生只能作为未证悬念，不开放“炼成永生蛊”或“真正永生”结局。
- 仙蛊屋 v0.8.0 暂不做玩家可操作战斗系统；可作为剧情见闻、势力威压、拆解线索或后续版本接口。
- 蛊虫/杀招表现不得只停留在文案：可执行效果必须来自本地表现规格或被降级为传闻/线索。

## 阶段拆解

### v0.8.0-pre：剧情地基

状态：进行中，已完成第一轮落地。

交付：
- `canon-anchor-registry.json`：十个正史锚点真相源。
- `fate-branch-matrix.json`：护宿命/毁宿命 IF 轴与偏移规则。
- `heaven-will-rules.json`：天意压力账本规则。
- `ending-outcomes.json`：结局族群与永生禁区。
- `v080-narrative-engine.ts`：锚点校验、IF 候选校验、天意触发、结局解析。
- `context-builder` 与 `state-update-applier` 接入 v0.8 剧情候选协议。
- 承诺效果深化协议：`CultivationTimeProfile`、`AmbushChainState`、`MajorBreakthroughAttempt`、`AscensionFailureOutcome`、`CalamityConsequenceProfile`、`GatheringEcologyNode` 的真相源设计与 DeepSeek 禁写边界。
- v0.7.1-polish 继承接口：`SceneTimeContext`、`ActionTimePolicy`、`SceneLockState`、`PathTendencyProfile`、`CombatIntent`。v0.7.1 已做派生 UI 诚实化，v0.8 需要把它们升级为统一时间/场景协议。
- 出身深线协议：所有 `StartProfile` 映射到 `OriginDeepLineProfile`，明确原著/二创边界、身份压力、专属 NPC、资源入口、敌对关系、锚点参与方式和 IF 偏移。
- 蛊虫表现化战斗协议：
  - `src/canon/gu-expression-specs.json`：首批 53 只蛊虫表现规格。
  - `src/canon/killer-move-expression-specs.json`：首批 15 个杀招表现规格。
  - `src/engine/gu-expression-registry.ts`：读取和剧情用途查询入口。
  - `GuExpressionSpec`、`KillerMoveExpressionSpec`、`BattlefieldCombatState`、`BattleResolutionStep` 类型地基。
- 前端动效规范：
  - 独立 skill：`reborn-combat-motion`。
  - 设计文档：`codex/04-前端动效/GSAP-Motion-全局重皮规范.md`。
  - GSAP/Motion 分工明确，UI 不得私算战斗结果。

验收：
- 正史模式不能直接改宿命战大结果。
- IF 模式可以提出护宿命/毁宿命候选，但必须经引擎校验。
- EndingResolver 不得返回“炼成永生蛊/真正永生”。
- AI 不得直接写境界、升仙结果、灾劫结果、高级资源入库或正式资源点。
- 首批可战斗/可剧情使用蛊必须有 `GuExpressionSpec`，流派名来自 `path-registry.json`。
- 首批杀招必须有核心蛊、辅助蛊、蓄势、失败模式、反噬和视觉节拍。
- 禁忌蛊必须是强场景门槛，不进入普通战斗按钮。

### v0.8.0-a：前中期正史锚点

范围：
- 青茅山、商家城、三王山、王庭四段剧情骨架。
- 原著模式侧线参与，IF 模式局部偏移。
- 人物图鉴、舆图传闻、NPC 对话行动卡与剧情候选联动。
- 凡战深化第一轮：5×3 轻地图棋盘、蛊虫行动栏、杀招行动栏、观察/撤退、地形/阵位/危险格展示。

重点：
- 不把玩家写成方源。
- 不让 DeepSeek 直接创建正式任务、奖励或地图资源点。
- 侧线选择要回流到道心、声望、关系、经济和后续锚点压力。
- 昼夜修行、区域采集、侦察/陷阱/伏击前半链必须开始服务前中期地图生态。
- 行动-时间-场景协议必须解决 v0.7.1 暂时只解释未重写的问题：同一时段 AP、多动作、场景锁、剧情未结算时禁止推进无关行动。
- 高级蛊材和仙材仍受经济闸门限制，不能通过低成本 AP 稳定刷出。
- 出身深线开始落地：青茅山本地三寨、商队/商家外围、散修前中期体验必须能跑出不同出生地、身份压力和资源入口。
- 蛊虫描述要进入剧情选择：侦察、追踪、治疗、解毒、破障、遮掩、威慑等 `sceneUtilities` 必须可被上下文识别。
- 战斗 UI 初版必须展示“能用/不能用/风险是什么”，不能只给漂亮按钮。

### v0.8.0-b：中后期大锚点

范围：
- 义天山、逆流河、梦境/影宗、宿命战。
- 正式接入 `fateState`、`HeavenWillLedger`、`KarmicDebtLedger`。
- IF 双路线开始显性分叉：护宿命与毁宿命。

重点：
- 宿命战是 v0.8 的最大剧情闸门。
- 原著模式只允许旁观、侧翼战场、救援、撤退、关系变化。
- IF 模式允许站队，但必须付出资源、关系、天意压力和势力代价。
- 完整大境界突破、升仙失败、伏击进入战斗/剧情候选和复杂灾劫后果在本阶段形成可玩闭环。
- 升仙失败按原著口径处理身体、空窍/仙窍、蛊虫、道痕和天意压力，不做普通 RPG 掉级。
- 商家城、武家、铁家、北原/东海/中洲跨域出身深线进入宿命前夜，影响阵营站队、正魔关系、突破资源和剧情候选。
- 小队群战、伏击链路、阵法/破阵接入表现化战斗协议。

### v0.8.0-c：尊者博弈与末章解析

范围：
- 尊者博弈、天庭末章、玩家势力收束、疯魔求证未证线。
- 接入结局解析器 UI 与死亡/通关总结。

重点：
- 尊者不能降智，玩家势力可以成为变量，但不能无代价替代尊者。
- 疯魔窟、混沌、事实浮冰只给“真相残片/未证悬念/代价极重”的体验。
- 结局必须标注 `canon-near | if-derived | original-if`。
- 高级采集生态、灾劫伤痕、突破失败记录、因果债与天意压力进入结局解析器。
- 战斗记录、杀招反噬、禁忌蛊代价和群战伤亡进入结局解析输入。
- 仍不得开放“真正永生”“炼成永生蛊”“玩家稳定十转”。
- 出身债务进入结局解析器：家族残债、商路利益、正道执法、天庭秩序、散修无庇护等长期压力影响结局权重。

### v0.8.0-release：剧情发布收束

范围：
- 十锚点浏览器长测。
- DeepSeek 回传压力测试。
- 结局矩阵回归测试。
- 文档冲突清理与上下文交接。

## 当前阻塞与风险

- v0.8.0 还没有真正实现各锚点的剧情 UI 与章节浏览器长测。
- `story_event_candidates` 和 `if_branch_candidates` 已有协议地基，但正式任务系统、地图系统、NPC 关系系统仍需逐段接入。
- 原著模式与 IF 模式的体验差异要谨慎控制：原著模式不能显得玩家无事可做，IF 模式不能变成随意爽文改写。
- DeepSeek 长上下文容易忘记玩家原创身份和宿命边界，必须持续注入锚点/宿命/永生禁区。
- DeepSeek 长上下文也容易把外来出身拉回章节默认古月身份；v0.8 必须继续以 `StartProfile` 和出身深线矩阵作为身份最高优先级。
- 从 v0.7.0 继承的承诺效果深系统容易被误当成“几个 modifier 没接完”。后续实现必须以 `codex/01-系统深化/v0.7承诺效果继承深化矩阵.md` 为准，先做真相源和引擎校验，再做 UI。

## 已验证

- `npm test -- src/engine/v080-narrative-engine.test.ts src/engine/v080-narrative-schema.test.ts`：通过。
- `npm test`：43 个测试文件 / 310 个用例通过。
- `npm run build`：通过，构建分包后无 500KB+ 警告。
## v0.7.1-polish 新增继承项

更新时间：2026-05-09

v0.7.1 已完成战斗遭遇循环、升炼/拆炼预览、本命蛊解释、物资袋超载、商会卖蛊材和移动端窄屏发布修复。下列内容不再回塞 v0.7.1，进入 v0.8 正式阶段：

- `SceneTimeContext`：统一剧情、行动、战斗、对话、商会、AP、时段和场景锁。普通狩猎、采集、蛊兽、敌对蛊师、伏击、挑战必须有清晰触发协议。
- 本命蛊深系统：选择、成长、收益、反噬、突破、升仙、灾劫、道途和结局权重。不得把本命蛊做成普通装备槽。
- 完整移动端体验：v0.7.1 只保证不变形；v0.8 需要建立移动端布局系统和截图验收。
- 高级资源生态：物资袋、商会、黑市、宝黄天、势力商路、仙窍资源点和高级采集生态统一进入经济与场景时间协议。

阶段归属：

- `v0.8.0-pre`：协议与真相源，含 `SceneTimeContext`、`ActionTimePolicy`、`LifeboundGuGrowthProfile`、移动端规范和高级资源生态规则。
- `v0.8.0-a`：青茅山、商队、商家城前中期行动/狩猎/采集/本命蛊基础体验。
- `v0.8.0-b`：伏击全链路、突破/升仙失败、本命蛊受损、高级资源流通。
- `v0.8.0-c`：灾劫、因果债、天意压力、本命蛊和资源生态进入结局解析。

## v0.8.0-a1 凡战棋盘引擎竖切

更新时间：2026-05-10
分支：`codex/v080-a1-combat-engine`
状态：代码与回归已完成；待提交和推送。

本阶段只做本地凡战结算，不接 UI、不写 store、不提升 `SAVE_FORMAT_VERSION`。核心交付是 `src/engine/v080-battlefield-combat-engine.ts`，把首批 `GuExpressionSpec` 与 `KillerMoveExpressionSpec` 接入 5x3 棋盘，并把行动结果统一输出为 `BattleResolutionStep[]`。

已落地内容：

- `src/canon/battlefield-combat-rules.json`：5x3 棋盘、地形/遮蔽/阵位/危险格、射程 shape、命中/伤害/冷却/蓄势/撤退参数。
- `BattlefieldAction`、`BattlefieldActionValidation`、`BattlefieldActionResolution`、`BattlefieldPendingAction`、`BattlefieldCombatResult` 类型。
- `createBattlefieldCombatState`、`listBattlefieldActionTargets`、`validateBattlefieldAction`、`executeBattlefieldAction`、`advanceBattlefieldRound`、`interruptPendingBattlefieldAction` API。
- 凡蛊普通战斗门槛：`direct + mortal` 才默认可主动使用；`passive`、`scene_gated`、`mortal_forbidden` 不进入普通按钮候选。
- 杀招门槛：已学会、核心蛊、辅助蛊、真元、冷却、蓄势、打断与反噬都由本地引擎校验和结算。

a2 接口约束：

- 战斗 UI 只能消费 `BattleResolutionStep[]` 和 validation 结果，不得在组件内私算命中、伤害、胜负、撤退。
- GSAP/Motion 只播放 `move`、`gu_use`、`killer_move`、`hit`、`miss`、`status_apply`、`terrain_change`、`counter`、`resource_spend`、`failure`、`settlement` 等事实。
- 若后续战斗状态需要写入正式存档，必须另开迁移并提升存档格式；a1 仍保持纯运行时。

## v0.8.0-a2 战斗 UI 与动效竖切

更新时间：2026-05-10  
分支：`codex/v080-a2-combat-ui-motion`  
状态：实现完成，正在回归、提交与推送。

本阶段把 a1 的本地凡战引擎接入一个可操作的“演武战场”全屏 overlay。它只写入临时 UI/store 字段，不接正式剧情结算，不让 DeepSeek 写入战斗结果，不提升 `SAVE_FORMAT_VERSION`。

已落地内容：

- `BattlefieldCombatOverlay`：中央 5x3 棋盘、左侧我方单位、右侧敌方与战场情报、底部行动栏、战斗轨迹播放。
- 主游戏底部新增 `凡战` 入口；E2E harness 新增 `startBattlefieldDemo()`，用于非持久演武战场验收。
- combat store 新增 `battlefieldCombatState`、`battlefieldSelectedAction`、`battlefieldValidation`、`battlefieldPlaybackSteps`、`battlefieldTraceCursor` 等临时字段。
- 新增 store 动作：初始化演武战场、选择行动、选择目标、执行行动、推进回合、打断蓄势、关闭凡战 overlay。
- `NON_PERSISTED_KEYS` / save partialize 已排除所有 battlefield 临时字段，读档时也会重置。
- `v080-battlefield-ui-model.ts` 负责把玩家持有蛊虫或演武测试装配转换为行动卡，普通按钮候选继续排除 `passive`、`scene_gated`、`mortal_forbidden`。
- Motion 负责棋盘格、单位、行动面板、轨迹列表、状态条与 reduced-motion fallback；GSAP 只通过独立 effect layer 播放 `BattleResolutionStep` 的高强度演出，不与 Motion 控制同一 DOM 属性。

a3 入口：

- 剧情选择接入时只能读取 `sceneUtilities`、玩家持有蛊虫和 a1 validation 结果，正式 HP/状态/胜负仍必须由本地引擎结算。
- 若要把“战斗中断点”写入存档，先设计迁移和兼容测试，再提升 `SAVE_FORMAT_VERSION`。

## v0.8.0-a3 蛊虫剧情表现化

更新时间：2026-05-11  
分支：`codex/v080-a3-narrative-gu-expression`  
状态：实现完成，正在全量回归、提交与推送。

本阶段把首批蛊虫/杀招的 `sceneUtilities` 接入剧情选择、下一轮叙事上下文和候选校验。a3 不接正式战斗结算，不允许 DeepSeek 写奖励、掉落、胜负、HP/真元/状态结果，也不提升 `SAVE_FORMAT_VERSION`。

已落地内容：

- `src/canon/narrative-gu-scene-utility-map.json`：把现有 53 只蛊与 15 个杀招的剧情用途映射到固定分类，覆盖侦察、追踪、治疗、解毒、破障、遮掩、威慑、禁忌仪式、移动、防护、控制、信号、生存、炼化。
- `src/engine/v080-narrative-gu-affordances.ts`：从 `inventory`、`apertureInventory.gu`、临时授权 token 和已学杀招构建剧情可用能力；生成 prompt 注入段；校验 choice 与 `gu_use_suggestions`；生成“玩家本轮选择详情”上下文。
- `Choice.guAffordances` / `Choice.gu_affordance` 与 `state_update.gu_use_suggestions.add.utilityId/category/riskHint` 已进类型和 schema，保持 passthrough 兼容旧 AI 输出。
- `context-builder` 注入玩家当前可调用蛊虫用途、限制、风险和禁止越权规则。
- `response-pipeline` 把所选 choice 的文本、风险和蛊虫用途 metadata 注入下一轮，而不是只传 `choiceId`。
- `narrative-consistency` 会标注或降级不合规选择；文本中旧式“使用某蛊”也会被识别，未持有/未登记时降级为线索。
- `state-update-applier` 在正式执行 `gu_use_suggestions` 前再做本地校验；未持有、未知、用途不匹配、禁忌门槛不足、未过 `sceneValidated` 的候选不会执行正式效果。
- `ChoicePanel` 增加“蛊虫解法 / 缺少蛊虫 / 禁忌门槛 / 待校验”标签与 hover tooltip；Motion 只做标签、tooltip 和选择面板状态过渡，a3 不引入 GSAP。

a4/b1 入口：

- a3 的剧情蛊虫表现只产生“可用解法、缺蛊线索、禁忌门槛、候选建议”，不产生数值结算。
- 后续若把剧情选择带入凡战，应从 `guAffordances` 映射到 a1/a2 的本地战斗 action，由战斗引擎输出 `BattleResolutionStep[]`。
- 群像战斗和正式场景战写回留给 b1；仙蛊、仙蛊屋仍不进入常规运行时库。

## v0.8.0-b1 群像战斗演武竖切
更新时间：2026-05-11
分支：`codex/v080-b1-group-combat`
状态：实现中；引擎、store、UI 与基础回归已通过，E2E/全量回归待最终收口。

本阶段把 a1/a2 的 5x3 凡战棋盘扩展为群像战演武竖切，但不替换旧 `squadCombatState` / `SquadCombatOverlay`，不写入正式存档，不提升 `SAVE_FORMAT_VERSION`。DeepSeek 仍只能给剧情候选或战斗意图，HP、真元、状态、士气、目标、胜负、撤退和第三方介入全部由本地 battlefield 引擎结算。

已落地内容：

- `battlefield-combat-rules.json` 新增 `group` 规则段：士气、援护、协攻、伏击、观察揭示、阵位控制、第三方介入、保护/护送目标、撤退惩罚。
- `BattlefieldCombatState` 增加 `mode`、`activeUnitId`、`actedUnitIdsThisRound`、`morale`、`objectives`、`ambush`、`thirdParties`；`BattlefieldUnit` 增加 `role`、`morale`、`threat`、`guardTargetId`、`revealed`、`objectiveTags`。
- `BattlefieldActionType` 增加 `assist`、`guard`、`rally`、`formation`、`observe`；`BattleResolutionStepKind` 增加 `assist`、`guard`、`ambush`、`formation`、`objective`、`third_party`。
- `v080-battlefield-combat-engine.ts` 保留 a1 API，并新增 `createBattlefieldGroupCombatState`、`listBattlefieldTurnOrder`、`resolveBattlefieldEnemyTurn`、`resolveBattlefieldAmbushOpening`。
- `initBattlefieldGroupDemo` 和 `startBattlefieldGroupDemo()` 只创建非持久演武态；旧 `startBattlefieldDemo()` 继续可用。
- `BattlefieldCombatOverlay` 支持群像模式：点击我方单位切换 active actor，展示士气条、目标卡、中立/第三方单位、隐藏/伏击状态、阵位/援护/协攻/振奋/观察行动。
- Motion 负责单位切换、士气条、目标卡、行动卡与列表状态；GSAP 只在独立 effect layer 播放伏击、破阵、第三方入场、士气等高强度 step。

边界：

- b1 仍是演武竖切，不把战斗中断点写入 save。
- 5x3 棋盘继续沿用，重点做规则深度，不在本阶段扩大战场尺寸。
- 剧情选择若要进入群像战，只传入战斗意图或场景授权，正式结算仍由 `BattleResolutionStep[]` 事实驱动。

## v0.8.0-b1.1 可变战场规模与 7x5 群像演武
更新时间：2026-05-11
分支：`codex/v080-b11-variable-battlefield-scale`
状态：实现完成，已通过全量单测、构建、旧 5x3 群像 E2E、新 7x5 大阵 E2E 和长链路回归；等待提交与推送。

b1.1 的目标不是把所有战斗默认放大，而是把 battlefield 引擎从固定 5x3 抽象为可变战场尺寸，并新增一个 7x5 山谷伏击群像演武，用更大的空间验证视野、距离、阵位、护送出口和第三方入场。旧 5x3 `skirmish_5x3` 仍是默认入口，`ambush_7x5` 只作为群像大阵演武预设。

已落地内容：

- `battlefield-combat-rules.json` 新增 `gridPresets`：`skirmish_5x3` 与 `ambush_7x5`。7x5 预设包含山道、林地遮蔽、阵位节点、危险格、护送出口和第三方入场点。
- `CreateBattlefieldCombatInput` 支持 `gridPresetId` 与 `gridSize`；`createBattlefieldCombatState` 不再硬编码 5x3，射程枚举、遮蔽、边缘、护送目标、伏击观察、第三方入场均基于 `state.grid.width/height`。
- `BattlefieldCombatState.grid` 的宽高改为动态数值，并记录 `gridPresetId`；新增格子标签 `frontline`、`midline`、`backline`、`escort_exit`、`entry_point`。
- `createBattlefieldLargeGroupDemoState` 与 `initBattlefieldLargeGroupDemo` 创建非持久 7x5 山谷伏击群像演武；E2E harness 增加 `startBattlefieldLargeGroupDemo()`。
- `BattlefieldCombatOverlay` 棋盘列数改为动态 inline grid，桌面端展示紧凑 7x5，移动端用横向滚动承载大棋盘，行动栏和目标信息保持可读。

边界：

- b1.1 不替换旧 `squadCombatState` / `SquadCombatOverlay`。
- b1.1 不把战斗状态写入正式存档，不提升 `SAVE_FORMAT_VERSION`。
- 7x5 只扩大位置、视野、阵位、护送和目标空间，不新增仙蛊、仙蛊屋或正式大型战争系统。
- UI/store 仍只消费本地 battlefield 引擎输出；DeepSeek 不能写入 HP、士气、胜负、第三方入场或护送结算结果。

# v2.9.0 startup Skill 同步审计记录

日期：2026-05-24
状态：completed startup

## 触发原因

v2.9 开新版本专家团启动会，并新增长期路线兑现审计、文档代码一致性与 TODO 审计、D-290/F-290 前置授权包。

## 审计结果

| Skill | 状态 | 原因 |
|---|---|---|
| `reborn-expert-council` | updated | 当时从 v2.8 complete 更新为 v2.9 startup；D-290 后续已批准并由 rc audit 接续 |
| `game-dev-text` | updated | 同步 v2.9 仍为 docs/report-only/offline 安全收束，不改 runtime |
| `reverend-insanity-lore` | updated | 同步 MiroFish 当前 `not_needed` 与 blocking 升级条件 |
| `reborn-combat-motion` | no_update_needed | v2.9 startup 不触碰战斗表现、UI motion 或资产 |
| `mirofish-reborng-export` | no_update_needed | v2.9 startup 不触发 MiroFish export；只保留 blocking 升级条件 |

## Skill 文件健康门

需运行：

```powershell
npm run check:skills-health
```

若项目没有该 npm script，则按 `Skill同步审计制度.md` 手动检查：

- `SKILL.md` UTF-8 no BOM。
- frontmatter 可解析。
- ordinary skills 不堆积多个 current override。
- 历史状态留在 PROJECT-STATE/AGENTS/dashboard/historical-index，不塞回 skill 正文。

## 结论

startup skill sync 已完成。v2.9 后续 a0/a1/a2/b1/b2/b3/process/rc 若触发 scope 变化，继续按制度记录。

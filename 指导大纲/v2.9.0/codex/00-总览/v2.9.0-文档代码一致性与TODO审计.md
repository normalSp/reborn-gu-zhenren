# v2.9.0 文档代码一致性与 TODO 审计

日期：2026-05-24
状态：completed

## 审计范围

本次审计聚焦 v2.0-v2.8 已批准路线，尤其是 Agent Lab、eval farm、private canon boundary、multi-NPC Agent Lab 和 runtime admission。

完成收束时追加核对 v2.9 report-only pre-runtime go/no-go checker。

不把以下内容误判为缺口：

- 明确设计为 future gate 的内容。
- 明确设计为 docs/report-only/schema-first 的内容。
- UI input `placeholder` 属性。
- 测试 stub。
- 错误处理中的 `throw new Error`。
- 检查脚本自身用于拒绝 placeholder 的 matcher。

## 已执行核对

### 核心 runner 存在性

命令：

```powershell
Test-Path scripts/run-v210-agent-lab-offline-runner.mjs
Test-Path scripts/run-v220-agent-lab-expanded-offline-runner.mjs
Test-Path scripts/run-v230-agent-eval-farm-runner.mjs
Test-Path scripts/run-v260-private-canon-archive-boundary-check.mjs
Test-Path scripts/run-v270-multi-npc-agent-lab.mjs
Test-Path scripts/run-v280-runtime-admission-check.mjs
Test-Path scripts/run-v290-pre-runtime-go-no-go-check.mjs
```

结果：全部为 `True`。

### npm scripts 存在性

`package.json` 当前包含：

- `check:v210-agent-lab-offline`
- `check:v220-agent-lab-expanded-offline`
- `check:v230-agent-eval-farm`
- `check:v260-private-canon-archive-boundary`
- `check:v270-multi-npc-agent-lab`
- `check:v280-runtime-admission`
- `check:v290-pre-runtime-go-no-go`

### artifacts 存在性

启动审计核对了 v2.1/v2.2/v2.3/v2.6/v2.7/v2.8 既有报告路径，均存在；完成收束时追加核对 v2.9 报告路径，存在。

关键结果：

| 版本 | 核心结果 |
|---|---|
| v2.1 | 10/10 schema valid；10/10 acceptedForGate |
| v2.2 | 21/21 schema valid；21/21 acceptedForGate；rescoreStable=true |
| v2.3 | 17/17 schema valid；13/13 failure family；P0/P1 falseNegative=0 |
| v2.6 | 18/18 schema valid；18/18 acceptedForGate |
| v2.7 | 23/23 schema valid；23/23 acceptedForGate；falseNegative=0 |
| v2.8 | 16/16 schema valid；16/16 acceptedForGate；falseNegative=0 |
| v2.9 | 18/18 schema valid；acceptedForGate=true；runtimeImplementationApproved=false |

## TODO / placeholder 命中 triage

命令：

```powershell
rg -n "TODO|FIXME|TBD|placeholder|stub|not implemented|throw new Error\(|未实现|待办|占位" src scripts tests package.json
```

### 非问题命中

| 类别 | 例子 | 结论 |
|---|---|---|
| UI input placeholder | `TitleScreen.tsx`、`CharacterCreate.tsx`、`SettingsDialog.tsx` | 正常 UI 文案，不是功能占位 |
| 测试 stub | `deepseek.test.ts` 的 `vi.stubGlobal` | 正常测试隔离 |
| 错误处理 | 多个 runner / engine 的 `throw new Error` | 正常 fail-fast |
| gate matcher | `check-player-advocate-gate.mjs` 的 `placeholderPatterns` | 检查器功能本身 |
| synthetic fixture | v2.3 sample 中的 synthetic placeholder | 明确用于 hidden-content detector 路径，不是真实 hidden body |

### 需记录但不阻塞

| 文件 | 命中 | 审计判断 | 后续处理 |
|---|---|---|---|
| `src/components/game/AuctionPanel.tsx` | 注释称仙材/仙蛊方/杀招传承为占位 Tab | 旧高阶面板注释，当前代码已有 treasureItems 分支和购买逻辑；不属于 v2.x Agent Lab 路线 | 未来高阶经济/蛊仙/宝黄天阶段复核 |
| `src/store/index.ts` | 旧迁移注释称 P2 扩展字段占位 | 历史迁移兼容说明，不是 v2.x 新 TODO | 保持；新 save 字段仍必须单独批准 |
| `scripts/run-v130/v170/v180/v200-*` | `previous round summary placeholder` | live/replay probe 构造 carry 的测试占位文本，不是玩家 runtime 文本 | 保持；若 v3.0 live gate 复用，再在 v3.0 gate 标准化命名 |
| `tests/evals/v210-agent-lab/samples.json` | `l4_key_figure_placeholder` | 合成 L4 placeholder，不是真实原著命名 NPC | 保持；v3.0 若需要 L4 必须 future gate |

## 文档/代码一致性结论

未发现以下阻塞项：

- 文档声明已实现 runtime agent，但代码没有。
- 文档声明已启用 backend/BFF，但代码没有。
- 文档声明已引入外部 agent framework dependency/PoC，但代码没有。
- 文档声明已新增 save field 或 `SAVE_FORMAT_VERSION`，但代码缺迁移。
- 文档声明已调用 live DeepSeek，但没有报告路径。
- 文档声明已做 MiroFish blocking intake，但没有 request/export/intake review 链路。

当前更准确的说法是：v2.x Agent Lab 代码实现非常保守，绝大多数长期路线仍停在 report-only/offline/synthetic 边界内，和文档口径一致。

## v2.9 要继续防的漂移

v2.9 已完成并确认：

- 没有把 v2.8 `admissibleCandidate` 误写成 v3.0 runtime approval。
- 没有把 synthetic L4/L5 样本误升级成真实原著事实。
- 没有新增 agent runner/service 或后端。
- F-290 future gate 已进入 v2.9 go/no-go checklist 和 b1 checker 样本。

## 结论

本次完成审计通过。v2.9 可以收束为 completed；下一步只能建议进入 v3.0 专家团/设计门禁，不得直接实现 runtime agent、live probe、后端、PoC、MiroFish export 或 save/prompt 改动。

# v2.9.0 小版本执行路线图

日期：2026-05-24
状态：draft for D-290 approval

## startup

主题：专家团启动会、长期路线兑现审计、文档代码一致性与 TODO 审计。

状态：completed locally。

产物：

- `v2.9.0-专家团启动会纪要.md`
- `v2.9.0-长期路线按期实现审计.md`
- `v2.9.0-文档代码一致性与TODO审计.md`
- D-290 / F-290 前置授权包

## a0

主题：v2.8 复盘与 v2.9 安全收束范围冻结。

目标：

- 复核 v2.8 `admissibleCandidate=5 / labOnly=4 / rejected=7`。
- 确认哪些能力可作为 v3.0 讨论输入。
- 明确 v2.9 不实现 runtime agent。

退出条件：

- v2.9 scope freeze。
- MiroFish 当前 `not_needed` 或升级原因。
- Skill sync audit。

## a1

主题：v3.0 runtime agent contract / WorldCore post-check 设计门禁。

目标：

- 定义 `AgentProposal` runtime contract 草案。
- 定义 WorldCore post-check interface 草案。
- 定义拒绝项、人工复核项、user decision 项。

禁止：

- 不接 runtime。
- 不改 prompt。
- 不改 save。

## a2

主题：v3.0 live probe / Player Advocate / drift gate / rollback / old-save 门禁。

目标：

- 设计 v3.0 live probe 档位。
- 设计 Player Advocate agent metadata 模板。
- 设计 long narrative drift gate。
- 设计 old-save / rollback / failure recovery gate。

禁止：

- 不实跑 live。
- 不调用 DeepSeek。
- 不新增 report archive service。

## b1

主题：report-only v3.0 pre-runtime go/no-go checker 第一刀。

目标：

- 可选新增 self-owned zero-dependency report-only checker。
- 输入只读样本、已有 report 和 synthetic contracts。
- 输出 go/no-go report。

是否执行：需 D-290 明确批准。

## b2

主题：MiroFish / private canon / hidden leak / external framework / self-learning 统一复核。

目标：

- 复核 `not_needed -> blocking` 升级条件。
- 复核 private canon / hidden marker / source-pointer 边界。
- 复核 Hermes / framework landscape 只停留在参考或 future gate。
- 复核 self-learning 只能生成候选，不得自裁决。

## b3

主题：v3.0 启动准入清单与用户决策包。

目标：

- 输出 v3.0 D-300 / F-300 草案。
- 明确 v3.0 可做和不可做。
- 形成进入 v3.0 前最后一次用户拍板清单。

## process-1

主题：前置审批制度第五轮复核。

目标：

- 统计 v2.9 是否减少常规审批中断。
- 统计是否触发 F-290。
- 确认制度没有掩盖真正需要用户决策的事项。

## rc

主题：v2.9 质量收束与最终 go/no-go。

目标：

- 文档自检通过。
- 可选 checker 通过。
- Skill sync audit 通过。
- 输出最终 v3.0 go/no-go。

退出条件：

- 若 Go：只能进入 v3.0 专家团/设计门禁，不等于实现 runtime agent。
- 若 No-Go：列清阻塞项和下一版本修复路线。

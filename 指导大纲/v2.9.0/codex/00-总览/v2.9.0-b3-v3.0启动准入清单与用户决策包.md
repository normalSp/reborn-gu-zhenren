# v2.9.0-b3 v3.0 启动准入清单与用户决策包

日期：2026-05-24
状态：completed
冻结模型：`v290_b3_v30_startup_admission_package_v1`

## 一句话结论

v2.9 允许建议进入 v3.0 专家团/设计门禁，但不允许直接实现 runtime agent。v3.0 的第一句话应该是“有限 L2/L3 runtime agent 试点设计门禁”，不是“正式上线 agent 社会”。

## v3.0 Go 条件

当前满足：

- v2.0-v2.8 长期路线兑现审计通过。
- 文档/代码/TODO 审计未发现已批准 scope 阻塞缺口。
- v2.8 runtime admission checker 通过。
- v2.9 go/no-go checker 通过。
- MiroFish need 当前明确为 `not_needed`。
- D/F 授权制度已生效。

仍需 v3.0 单独审批：

- 是否进入 runtime agent 设计门禁。
- 是否允许 live probe 实跑。
- 是否允许 prompt/context 任何调整。
- 是否需要 save field 或保持非持久候选。

## v3.0 可建议的 D-300 草案

| ID | 建议 |
|---|---|
| D-300-001 | v3.0 主线为一个小区域内有限 L2/L3 runtime agent 试点设计门禁 |
| D-300-002 | 只允许 agent 输出候选和表达，WorldCore 保持最终裁决 |
| D-300-003 | 先做 a0/a1/a2 设计门禁，不直接进入 runtime 实现 |
| D-300-004 | live probe 实跑需单独批准样本、轮次、成本和接受标准 |
| D-300-005 | 若不新增 save field，必须证明 agent 候选不进入持久状态 |
| D-300-006 | 若新增 save field，必须同步 `SAVE_FORMAT_VERSION`、migration/default/tests |
| D-300-007 | MiroFish 当前可 `not_needed`；触发命名 NPC/真实 lore 立即 blocking |
| D-300-008 | external framework / self-learning / backend 继续 future gate |

## v3.0 必须默认 future gate 的 F-300 草案

- runtime agent implementation 本身，直到设计门禁通过。
- save field / save-format bump / `runFingerprint`。
- live DeepSeek 实跑。
- prompt/context/model/authority 扩展。
- MiroFish blocking。
- backend/BFF/service。
- external framework PoC/dependency/subagents。
- L4/L5 runtime。
- formal location/faction/identity/reward/NPC life-death。
- self-learning direct write。

## v3.0 No-Go 条件

以下任一出现，不建议进入实现：

- b1/b2/b3 发现 P0/P1 false negative。
- 需要 L4/L5 runtime。
- 需要 backend/service。
- 需要 DeepSeek visible lore/RAG。
- 需要命名 NPC/hidden-adjacent，但 MiroFish blocking 未完成。
- 需要 save-format bump，但用户未批准。
- 需要 external framework PoC/dependency/subagents，但用户未批准。

## 退出结论

b3 完成。v2.9 可以进入 process-1 和 rc 收束。

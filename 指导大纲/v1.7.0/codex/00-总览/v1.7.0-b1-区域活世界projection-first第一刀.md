# v1.7.0-b1 区域活世界 projection-first 第一刀

状态：已完成
日期：2026-05-21

## 结论

b1 已按 D-171 落地区域活世界 projection-first 第一刀：

- 新增 `src/engine/v170-regional-life-projection.ts`。
- 新增 `src/components/game/RegionalLifePanel.tsx`。
- `WorldHubPanel` 新增 `活世` 页签。
- 新增 focused unit：`src/engine/v170-regional-life-projection.test.ts`。
- 新增 focused e2e：`tests/e2e/v170-regional-life-projection.spec.ts`。
- 新增 live probe 工具：`scripts/run-v170-regional-life-live-probe.mjs` 与 `tests/evals/deepseek-v170-regional-life/samples.json`。

## 权威边界

本阶段保持：

- `SAVE_FORMAT_VERSION = 24`。
- 不新增 `regionalLifeState` / `areaLivingState`。
- 不新增 `regionalEventLedger` / `runFingerprint`。
- 不新增 store action。
- 不新增 migration/defaults。
- 不改 DeepSeek runtime prompt/context/API/model/authority。
- 不新增正式地点、正式商队/阵营、正式奖励、正式交易、NPC 生死或 hidden 可见事实。

## projection 输入

只读输入：

- v1.1 `routeLocationState` 与 `buildV110RouteLocationOverview()`。
- v1.2 `survivalEconomyState` 与 `buildV120LowRankSurvivalEconomyProjection()`。
- v1.3 `livingWorldState` 社会证据与 `buildV130SocialPressureProjection()`。
- v1.4 `buildV140RegionSampleProjection()`。
- v1.5 `buildV150ConflictAftermathProjection()`。
- v1.7-a2 `southern_border_low_rank_outer_edge_life_slice` reviewed source pointers。

## projection 输出

压力 deck 包含：

| id | 说明 | 不做什么 |
|---|---|---|
| `outer_edge_interrogation` | 外缘盘问 | 不解锁正式地点 |
| `caravan_contact_by_labor` | 商队短工窗口 | 不给正式商队身份 |
| `caravan_permission_chain` | 管事/报到前置 | 不给批准结论 |
| `low_status_labor` | 低阶劳作 | 不生成奖励 |
| `temporary_market_window` | 临时市场压价/拒绝 | 不生成价格表/库存 |
| `shelter_debt_window` | 临时遮蔽/人情压力 | 不写债务 ledger 或 NPC 命运 |
| `road_event_protocol` | 路途事件协议 | 不触发正式路线成功 |
| `far_city_as_pressure` | 远城目标压力 | 不开放完整商家城 |

## 同开局可重玩差异度

b1 差异来自本地 pressure deck 与叙事表达：盘问、短工、报到、压价、遮蔽、路途协议和远城压力。稳定事实不漂移：路线身份、地点、阵营、奖励、NPC 命运、hidden/canon 和 save format 都不变。

## 验证

已通过：

- `npm test -- src/engine/v170-regional-life-projection.test.ts`
- `npx tsc --noEmit --pretty false`
- `npm run test:e2e -- tests/e2e/v170-regional-life-projection.spec.ts`
- `npm run eval:deepseek:v170-regional-life-live:smoke`：clean smoke 8/8 accepted，P0/P1/P2=0/0/0。
- `npm run check:player-advocate-gate -- 指导大纲/v1.7.0/codex/00-总览/v1.7.0-b1-Player-Advocate-30轮走查记录.md 30`

## 结论

b1 通过。b2-b4 作为同一 projection 的组合态势、事件模板、反刷/旧档/回滚证据继续收束，不需要新增存档字段或 DeepSeek 权限。

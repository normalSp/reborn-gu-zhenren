# v1.7.0-b4 反刷、旧档、回滚与入口一致性

状态：已完成
日期：2026-05-21

## 反刷

- `RegionalLifePanel` 只读 store，不写 reward、inventory、currency、route/location/faction/NPC fate。
- 反复切换 `活世` 页签不会产生收益或状态。
- 同开局差异由 `variantIndex` / pressure deck 表达，不写 per-save 随机字段。

## 旧档兼容

- `SAVE_FORMAT_VERSION = 24` 不变。
- 空旧档 / 缺少路线、生存、社会、区域、冲突证据时返回 `needs_regional_context` 等待态。
- 不需要 migration/defaults。

## 回滚

如需回滚，可移除：

- `src/engine/v170-regional-life-projection.ts`
- `src/components/game/RegionalLifePanel.tsx`
- `WorldHubPanel` 的 `活世` 页签接入
- `src/engine/v170-regional-life-projection.test.ts`
- `tests/e2e/v170-regional-life-projection.spec.ts`
- v1.7 live probe 脚本与样本

不会影响 save migration 或已有 v1.1-v1.5 状态字段。

## 结论

b4 通过。

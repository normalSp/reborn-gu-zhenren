# v1.1.0 小版本执行路线图

日期：2026-05-20
状态：本地开发里程碑完成；D-025 小规模 live probe 已执行并记录

| 阶段 | 名称 | 交付 | 验收 |
|---|---|---|---|
| a0 | 长期路线重整与 v1.1 启动包 | v1.1-v2.0 路线草案、v1.1 启动审查、需求池、MiroFish 协议、测试矩阵、Git 计划、外部参考映射 | 初始决策已过；无 runtime |
| a1 | route/location/save-format 设计门禁 | 字段候选、权威边界、迁移方案、旧档默认、硬停规则；D-019 至 D-025 | 已通过：D-019 至 D-024 批准；D-025 后续单独批准并执行 |
| a2 | MiroFish intake 与字段表 | v1.1 三包/基础包主题切片、字段表、promote/defer/quarantine 表、测试样本吸收表 | 已完成；不整包吸收；无隐藏事实泄露 |
| b1 | 正式路线状态第一刀 | 写入最小 `routeLocationState`，bump `SAVE_FORMAT_VERSION = 23` | 已完成；旧档迁移、focused tests、e2e、Player Advocate、T0 soak 通过 |
| b2 | 区域状态与位置面板 | 玩家可读当前位置、来源路线、区域压力、下一步 | 已完成；不暗示完整南疆/商家城开放 |
| b3 | 前期账本压力回流 | 补给、追踪、身份、势力压力从旧账本回流到路线/区域状态 | 已完成；不写正式奖励、阵营、NPC 生死 |
| b4 | 南疆早期外缘行动承接 | 商队/散修/山路/城外缘低阶行动候选或最小正式行动 | 已完成；不开放完整区域、市场、商家城 |
| process-1 | 旧档迁移与预览回滚专项 | save fixture、production smoke、rollback checklist、GitHub Actions 证据 | 已完成；旧档不误迁移；生产预览不黑屏 |
| process-2 | 长线叙事漂移与知识库门禁 | 项目级漂移测试制度、全书知识库治理制度、知识库骨架、历史索引、入口压缩、skill 同步、v1.6 检查脚本规划 | 已完成；不扩 DeepSeek；后续阶段必须引用 |
| rc | v1.1 质量收束 | 全量测试、长测、Player Advocate、docs/Git/dashboard | 已完成；本地质量门通过，未自动部署 |

## 执行顺序

1. a0/a1/a2 已按顺序完成。
2. process-2 已在 runtime 前建立项目级制度和知识库治理边界。
3. b1 已按 D-019 至 D-023 的最小 scope 完成；新增 `routeLocationState`，未夹带奖励、阵营、NPC 生死或完整地点开放。
4. b2/b3/b4 通过路线面板、既有 v018/v100 账本和本地 route-location engine 收束到同一状态对象。
5. process-1 已通过旧档迁移、生产预览和回滚边界验证。
6. rc 已完成 180 轮 Player Advocate 和本地质量门。

## Player Advocate 轮次建议

- a0/a1/a2：纯文档/设计阶段已记录豁免。
- b1/b2/b3/b4/rc：已用 `v1.1.0-rc-Player-Advocate-180轮走查记录.md` 做集成走查，覆盖新档、旧档、极端意图、UI 位置心智、压力回流和长线漂移。
- gate：`npm run check:player-advocate-gate -- 指导大纲/v1.1.0/codex/00-总览/v1.1.0-rc-Player-Advocate-180轮走查记录.md 180` 通过。

## 长线叙事漂移计划

- a1/a2：已引用 `长线叙事漂移测试制度.md`，确认 route/location/save-format 和 MiroFish intake 的漂移风险。
- b1：T0 deterministic route/location soak 已纳入 `src/engine/v110-route-location-state.test.ts`，覆盖 8 类样本。
- b2/b3/b4：checkpoint 已纳入 180 轮 Player Advocate 集成记录。
- rc：T0 已跑；T1/T2 live DeepSeek probe 仍需用户单独批准成本、轮次和样本。
- v1.1 不把 live probe 作为默认 CI，也不把未跑 live 的内容写成已验证。

## 本地验证记录

- `npm test -- src/engine/v110-route-location-state.test.ts src/store/save-normalization.test.ts src/store/slices/livingWorldSlice.test.ts`：3 files，31 tests 通过。
- `npx tsc --noEmit --pretty false`：通过。
- `npm run check:player-visible-copy`：271 files scanned，通过。
- `npm run check:runtime-assets`：173 files，zero-byte=0。
- `npm run check:qingmao-assets`：23 entries checked。
- `npm test -- --reporter=dot`：141 files，778 tests 通过。
- `npm run build`：通过；仅 Rolldown plugin timings warning。
- `npm run test:e2e -- tests/e2e/v110-route-location-state.spec.ts`：1 test 通过。
- `npm run check:player-advocate-gate -- 指导大纲/v1.1.0/codex/00-总览/v1.1.0-rc-Player-Advocate-180轮走查记录.md 180`：180 轮通过，理解率 100.0%，confused=0。
- `npm run test:e2e`：91 tests 通过。
- `npm run test:e2e:long`：29 tests 通过。
- `npm run check:production-preview`：通过；首屏仍为 v1.0 public release label，符合未批准 v1.1 公开发布口径。

## Git 计划

- a0 启动包单独 commit。
- a1 设计门禁单独 commit。
- a2 MiroFish intake 单独 commit。
- process-2 制度门禁单独 commit。
- b1/b2/b3/b4/process-1/rc 均单独 commit。
- 不使用 `git add -A`，只 stage 当前阶段明确文件。
- 每个 runtime commit push 后等待 GitHub Actions 通过并补录 run id。

# v0.17.0-b2 低阶战斗轨迹与回流第一刀

日期：2026-05-18
状态：已完成
类型：runtime

## 交付

新增 v0.17 trace/backflow 解释层：

- `buildV017TraceReviewFromSteps`：读取既有 `BattleResolutionStep`，生成玩家可读轨迹复核。
- `buildV017OutcomeBackflowView`：读取既有 `BattleOutcomeSummary`，生成战后回流复核。
- `NarrativeCombatPanel` 在战后摘要下显示 v0.17 回流边界。

## 玩家体验

玩家能看到：

- 战斗是本地引擎结算。
- 战后回流已经写入行动账本。
- 文本层不能追加奖励、地点、阵营或 NPC 生死。

## 边界

- 不改变 `BattleOutcomeSummary` 类型。
- 不改变 action ledger schema。
- 不调用 live DeepSeek。
- 不把 trace 写成新存档字段。

## 验收

- focused unit 覆盖 trace/backflow 文案和边界。
- e2e 覆盖候选入场到 5x3 棋盘。

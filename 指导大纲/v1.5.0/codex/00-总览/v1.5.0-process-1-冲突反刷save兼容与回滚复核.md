# v1.5.0-process-1 冲突反刷、save 兼容与回滚复核

状态：已完成
日期：2026-05-21

## 反刷复核

| 项 | 结果 |
|---|---|
| 连续打开冲突页签 | 不生成奖励、不写状态 |
| 连续登记 v0.17 安全候选 | 只写既有 `flags.combatEventCandidates`，不发奖励 |
| 连续读取反制 / 小队提示 | 只读，不写 ledger |
| 诱导掉落、元石、稀有蛊、仙蛊 | forbiddenWrites 覆盖并在 UI 边界阻断 |

## save 兼容复核

本阶段没有新增持久字段：

- `SAVE_FORMAT_VERSION` 保持 24。
- 无 `conflictConsequenceState`。
- 无 `pursuitState`。
- 无 `combatAftermathState`。
- 无 migration/defaults。
- 空旧档 / v24 旧档可进入冲突页，显示等待冲突证据。

## 回滚复核

若需要回滚 v1.5 runtime 第一刀，回滚范围清晰：

- 移除 `src/engine/v150-conflict-aftermath-projection.ts`。
- 移除 `src/components/game/ConflictAftermathPanel.tsx`。
- 从 `WorldHubPanel` 移除 `conflict_aftermath` tab。
- 移除 v150 focused unit/e2e。

不需要：

- 存档迁移回滚。
- 数据库或云端回滚。
- DeepSeek prompt / model 回滚。
- 资产回滚。

## 结论

process-1 通过。v1.5 冲突解释层不可刷奖励，旧档兼容，回滚边界清楚。

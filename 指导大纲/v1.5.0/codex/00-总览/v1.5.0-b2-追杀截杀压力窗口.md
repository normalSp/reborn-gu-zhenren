# v1.5.0-b2 追杀 / 截杀压力窗口

状态：已完成
日期：2026-05-21

## 结论

b2 已通过 `V150ConflictAftermathProjection` 的 `pursuit_attention_window` 与 `route_ambush_risk` 覆盖追杀 / 截杀压力窗口。

## 覆盖内容

- 读取公开 `livingWorldState.factionPressure`、`npcMemories`、`actionConsequences`。
- 读取 route/location、region sample 与 combat candidate。
- 展示“注意窗口”“伏击风险”“降险建议”。
- UI 文案明确：这是风险解释，不是正式追杀、通缉、围剿、封锁或阵营敌对。

## 禁止项

- 不写 `formal_pursuit`。
- 不写 `formal_warrant`。
- 不写 `formal_blockade`。
- 不写 `formal_hostility`。
- 不写 NPC 捕获 / 死亡 / 背叛。
- 不让 DeepSeek 拥有追杀结论 authority。

## 验证

- focused unit 覆盖 `pursuit_attention_window` 可见和禁止写入。
- focused e2e 覆盖追杀注意窗口文案、隐藏词过滤和三个禁止字段不存在。
- Player Advocate rc 100 轮第 37-60 轮覆盖正式追杀、通缉、封锁、敌对、NPC fate 和奖励阻断。

## 结论

b2 通过。追杀 / 截杀仍是 projection-only 风险窗口，不是正式系统。

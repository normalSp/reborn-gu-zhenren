# v1.5.0-b1 冲突后果 projection-first 第一刀

状态：已完成
日期：2026-05-21

## 结论

b1 已按 D-151 落地 projection-first 冲突后果解释层：

- 新增 `src/engine/v150-conflict-aftermath-projection.ts`。
- 新增 `src/components/game/ConflictAftermathPanel.tsx`。
- `WorldHubPanel` 新增 `冲突` 页签。
- 新增 focused unit：`src/engine/v150-conflict-aftermath-projection.test.ts`。
- 新增 focused e2e：`tests/e2e/v150-conflict-aftermath-projection.spec.ts`。

## 权威边界

本阶段保持：

- `SAVE_FORMAT_VERSION = 24`。
- 不新增 `conflictConsequenceState` / `pursuitState` / `combatAftermathState`。
- 不新增 store action。
- 不新增 migration/defaults。
- 不改 DeepSeek prompt/context/API/model/authority。
- 不新增战斗动效或美术资产包。

## projection 输入

只读输入：

- v1.1 `routeLocationState` 和 `buildV110RouteLocationOverview()`。
- v1.2 `survivalEconomyState` 和 `buildV120LowRankSurvivalEconomyProjection()`。
- v1.3 `livingWorldState` 社会证据和 `buildV130SocialPressureProjection()`。
- v1.4 `buildV140RegionSampleProjection()`。
- v0.17 `listV017CombatPreparationViews()`、反制边界、小队/阵法提示、battle trace / outcome backflow view。
- `flags.combatEventCandidates`、`battlefieldPlaybackSteps`、`combatEncounterState.outcomeSummary`、local action ledger。

## projection 输出

四个 posture：

| posture | 说明 | 不做什么 |
|---|---|---|
| `route_ambush_risk` | 路线伏击风险 | 不触发正式战斗、地点、路线成功 |
| `pursuit_attention_window` | 追杀注意窗口 | 不写正式追杀、通缉、封锁、敌对 |
| `countermeasure_gap` | 杀招 / 蛊虫反制缺口 | 不私算伤害、胜负、奖励、传承 |
| `squad_formation_readiness` | 小队 / 阵法准备度 | 不写 NPC 生死、永久伤势、战局结论 |

## 验证

已通过：

- `npm test -- src/engine/v150-conflict-aftermath-projection.test.ts`
- `npx tsc --noEmit --pretty false`
- `npm run test:e2e -- tests/e2e/v150-conflict-aftermath-projection.spec.ts`
- `npm run check:player-advocate-gate -- 指导大纲/v1.5.0/codex/00-总览/v1.5.0-b1-Player-Advocate-30轮走查记录.md 30`

## 结论

b1 通过。可以把 b2/b3/b4 作为同一 projection 的可读性深化和流程证据继续收束，不需要新增存档字段或 DeepSeek 权限。

# v2.8.0 Git 提交与推送计划

日期：2026-05-24
状态：completed plan；implementation branch active

## 当前分支

`codex/v280-startup-runtime-admission-precheck`

## 基线

从 `codex/v270-startup-multi-npc-agent-lab` 的 v2.7 complete 基线切出。

## 提交边界

### startup commit

范围：

- 新增 `指导大纲/v2.8.0/codex/00-总览/` startup 文档。
- 更新 `指导大纲/项目仪表盘.md`。
- 更新 `AGENTS.md`。
- 更新 `.codex/skills/reborn-expert-council/references/PROJECT-STATE.md`。
- 必要时更新本地 skill current override。

不包含：

- runtime/source/UI/store/prompt/save。
- scripts/tests/npm script/artifacts，除非 D-280-007 后续获批。
- MiroFish export。
- external framework PoC/dependency/subagents。

### v2.8 completion commit

范围：

- a1/a2/b2/b3/process-1/rc 文档。
- b1 report-only checker、fixtures、npm script、artifact。
- dashboard / PROJECT-STATE / AGENTS / skill sync 更新。

## Stage 规则

- 禁止 `git add -A`。
- 只 stage 明确路径。
- 任何外部资产、MiroFish raw、`.env`、正式存档、未批准 artifact 不得 stage。

## 推送与 CI

v2.8 completion commit 推送当前分支并检查 GitHub Actions。

不自动部署 EdgeOne，不改 branch protection，不自动合并。

## 完成定义

v2.8 完成需要：

- 文档自检通过。
- `npm run check:v280-runtime-admission` 通过。
- 敏感边界扫描只命中禁止/future gate/测试语境。
- Skill sync audit 写入并通过 Skill 文件健康门。
- Dashboard/PROJECT-STATE/AGENTS 同步。
- 提交、推送、CI 可追踪。

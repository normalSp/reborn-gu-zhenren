# v2.8.0 启动审查与范围冻结

日期：2026-05-24
状态：completed；D-280 approved，F-280 future_gate_required

## 启动输入

| 输入 | 状态 | v2.8 采用 |
|---|---|---|
| `v2.7.0-rc-多NPC小势力AgentLab质量收束记录.md` | completed | v2.7 多 NPC / 小势力 Agent Lab 通过，作为准入证据 |
| `v2.7.0-b3-v2.8-runtime-admission-precheck.md` | completed | v2.8 只做 runtime admission precheck，不实现 runtime agent |
| `v2.0-v3.0-AgentLab到RuntimeAgent总体大纲.md` | current long-route baseline | v2.8 定位为 runtime agent 接入准入门禁 |
| `Agent-Framework-Landscape-2026吸收矩阵.md` | current landscape baseline | 继续参考，但 v2.8 不做 PoC、依赖、read-only scan |
| `前置批量审批与例外停机制度.md` | active process | v2.8 使用 D-280 前置授权包 |
| `Skill同步审计制度.md` | active process | startup 记录 skill sync 与文件健康门 |

## 范围冻结

v2.8 冻结为：

`Runtime Agent 接入准入门禁`

核心对象：

- L1 public pressure aggregation：是否可作为低风险背景压力进入未来设计。
- L2 candidate memory / reflection：是否具备 TTL、撤销、审计和 WorldCore rejection 条件。
- L2 squad / small-faction goal candidate：是否只保留为候选，不写事实。
- L3 current-scene intent / expression：是否可作为语言和意图候选，不写 outcome。
- L5 macro pressure：继续 report-only，不进入 runtime。
- self-learning candidate patch：继续 report-only，不自裁决。

## 默认不做

- 不进入 runtime agent。
- 不做后端、BFF、service、真实 job queue 或云存档。
- 不改 save schema，不 bump `SAVE_FORMAT_VERSION`，不新增 `runFingerprint`。
- 不调用 live DeepSeek，不改 prompt/context/model/authority。
- 不做 MiroFish export/intake，不吸收真实原著事实。
- 不接外部 agent framework PoC、依赖、read-only scan、subagents、patch artifact。
- 不写知识库正文、runtime canon、正式地点/阵营/身份/奖励/NPC 生死。

## MiroFish need level

初始：`not_needed`。

理由：

- 本轮只做准入门禁和 report-only evidence synthesis。
- 不触碰真实命名 NPC、真实势力结论、hidden-adjacent 或正式 lore。
- 不写知识库正文或 runtime canon。

升级为 `blocking` 的触发：

- 命名 NPC。
- 真实势力、真实地点或正式阵营结论。
- hidden-adjacent、方源公开证据或 L4/L5 原著锚点。
- 正式 lore 结论。
- 任何内容进入知识库正文、runtime canon、DeepSeek visible context 或正式测试事实。

## 启动出口

用户已审批：

- D-280-001 至 D-280-012 全部批准。
- F-280-001 至 F-280-012 全部保持 `future_gate_required`。

已按授权进入并完成 a1 runtime agent admission criteria 设计门禁。

# v3.5.0 Git 提交与推送计划

日期：2026-05-25
状态：completed locally；pending completion commit/push

## 分支

当前 v3.5 分支：

`codex/v350-startup-transient-agent-hardening`

## stage 规则

- 显式 stage 本次 v3.5 文档、入口同步和 skill audit。
- 不使用 `git add -A`。
- 不 stage 与 v3.5 startup 无关的脏文件。

## 提交边界

startup 提交：

`docs: 开启v3.5 transient agent硬化启动会`

completion 提交建议：

`feat: 完成v3.5 transient agent硬化`

本次完成提交包含：

- v3.5 lifecycle/hardening runtime 第一刀。
- v3.5 focused unit/e2e。
- v3.5 docs/PA/drift/rc closure。
- dashboard / AGENTS / PROJECT-STATE 同步。
- skill sync audit 记录。

不包含：

- store/prompt/save changes。
- live DeepSeek artifacts。
- MiroFish export/intake。
- backend/external framework PoC。

## 推送与 CI

推送后等待 GitHub Actions。

推送后等待 GitHub Actions deterministic quality gate 完成。

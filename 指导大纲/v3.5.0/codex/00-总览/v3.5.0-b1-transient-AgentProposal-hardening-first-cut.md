# v3.5.0-b1 transient AgentProposal hardening first cut

日期：2026-05-25
状态：completed
冻结版本：`v350_b1_transient_proposal_hardening_v1`

## 实现内容

新增：

- `src/engine/v350-transient-agent-proposal-hardening.ts`
- `src/engine/v350-transient-agent-proposal-hardening.test.ts`
- `tests/e2e/v350-runtime-agent-hardening.spec.ts`
- `package.json` script：`check:v350-transient-agent-hardening`

修改：

- `src/components/game/RuntimeAgentProposalPanel.tsx`
- `src/components/game/WorldHubPanel.tsx`
- `package.json` long e2e 覆盖增加 v3.5 spec。

## 行为

v3.5 继续复用 v3.4 的 runtime `意图` tab，但将表达链硬化为 lifecycle v2：

- `candidate`
- `rejected`
- `expired`
- `needs_user_decision`

这些都是 UI/display lifecycle，不是 store/save 状态。

## 聚焦验证

- `npm run check:v350-transient-agent-hardening -- --reporter=dot`：1 file / 4 tests passed。
- `npx tsc --noEmit --pretty false`：passed。
- `npm run check:v340-transient-agent-proposal -- --reporter=dot`：1 file / 5 tests passed。
- `npm run test:e2e -- tests/e2e/v340-runtime-agent-proposal.spec.ts tests/e2e/v350-runtime-agent-hardening.spec.ts`：2 passed。

## 未做

- 没有新增 store slice。
- 没有新增 save field。
- 没有 save migration。
- 没有 `SAVE_FORMAT_VERSION` bump。
- 没有 `runFingerprint`。
- 没有 live DeepSeek。
- 没有 MiroFish。
- 没有 backend/BFF/service。
- 没有 external framework / dependency / subagent。
- 没有正式地点、阵营、身份、奖励、NPC 生死。

## 结论

b1 通过。v3.5 hardening first cut 已落地。

# v3.5.0-b3 60轮 deterministic 漂移检查记录

日期：2026-05-25
状态：completed
版本：`v350_b3_deterministic_60_round_drift_gate_v1`

## 范围

本检查只验证本地 deterministic hardening，不验证 live narrative quality。

## 检查项

- candidate 不漂移成事实。
- rejected 不进入玩家事实层。
- expired 不保留旧窗口事实。
- needs_user_decision 不自动通过。
- 不写 save。
- 不新增 `runFingerprint`。
- 不调用 live DeepSeek。
- 不开放正式地点、阵营、身份、奖励、NPC 生死。

## 证据

`src/engine/v350-transient-agent-proposal-hardening.test.ts` 覆盖：

- `passes the 60-round deterministic same-start variation gate without runFingerprint`

当前聚焦结果：

- `npm run check:v350-transient-agent-hardening -- --reporter=dot`：4/4 passed。
- `runV350DeterministicProbe(60)`：60/60 accepted，0 failures，acceptedForGate=true。

## 结论

本地 60 轮 deterministic 漂移门通过。v3.5 仍未验证 live DeepSeek 叙事质量。

# v3.5.0-a2 candidate-vs-fact / rejection copy 门禁

日期：2026-05-25
状态：completed
冻结版本：`v350_a2_candidate_fact_rejection_copy_gate_v1`

## 目标

让玩家在 UI 中能直接分清：

- `candidate` 是候选表达，不是事实。
- `rejected` 是越权阻断，不是游戏已发生。
- `expired` 是旧窗口失效，不是隐藏结论。
- `needs_user_decision` 是 future gate，不是 runtime 承诺。

## 实现

修改：

- `src/components/game/RuntimeAgentProposalPanel.tsx`
- `src/components/game/WorldHubPanel.tsx`

新增 UI 区块：

- `v350-agent-proposal-lifecycle-audit`
- `v350-agent-proposal-lifecycle-summary`
- `v350-agent-proposal-lifecycle-details`
- `v350-agent-proposal-deterministic-result`

保留 v3.4 test id 兼容，避免破坏旧 E2E。

## 文案边界

UI 明确显示：

- 候选不等于事实。
- 不写存档。
- 不结算奖励。
- 不定 NPC 命运。
- live DeepSeek：否。
- MiroFish：否。
- backend：否。

## 结论

a2 通过。v3.5 可以进入 hardening first cut。

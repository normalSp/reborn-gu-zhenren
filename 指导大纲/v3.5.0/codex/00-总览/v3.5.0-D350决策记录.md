# v3.5.0 D-350 决策记录

日期：2026-05-25
状态：approved

## 用户批准

用户已批准：

- D-350-001 至 D-350-012。
- F-350-001 至 F-350-012 全部继续保持 `future_gate_required`。

## 本次授权打开什么

v3.5 只打开 `transient AgentProposal hardening-first`：

- 复盘 v3.4 最小 runtime first cut。
- 设计并实现 AgentProposal lifecycle v2。
- 强化 candidate / rejected / expired / needs_user_decision 的玩家可见表达。
- 做 deterministic same-start variation，不新增 `runFingerprint`。
- 做 20 轮 Player Advocate 与 60 轮 deterministic drift。

## 本次授权不打开什么

- 不新增 save field / migration / `SAVE_FORMAT_VERSION` / `runFingerprint`。
- 不新增 persistent runtime agent / agent memory save / agent state。
- 不调用 live DeepSeek，不改 prompt/context/model/authority。
- 不做 MiroFish export/intake，不使用真实原著事实、命名 NPC、hidden-adjacent 或正式 lore 结论。
- 不做 backend/BFF/service/job queue/eval archive/cloud save。
- 不引入 external framework PoC/dependency/read-only scan/subagent/patch artifact。
- 不开放 L4/L5、天道宿命 runtime 裁决、原著关键人物 runtime agent。
- 不开放正式地点、阵营、身份、奖励、NPC 生死、通缉、招揽、封锁。
- 不做 public wording、release、EdgeOne、法律/版权边界变化。

## 自动推进口径

Codex 可在 `/goal` 内连续完成 v3.5；若触发 `v3.5.0-例外停机清单.md` 任一项，必须立即停止并回到用户决策。

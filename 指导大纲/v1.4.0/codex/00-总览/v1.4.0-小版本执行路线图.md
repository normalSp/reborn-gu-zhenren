# v1.4.0 小版本执行路线图

状态：a1 已建立；等待用户审批 D-141 后进入 a2
日期：2026-05-21

## 总路线

`a0/a1/a2 -> b1/b2/b3/b4 -> process-1/process-2 -> rc`

## a0 启动治理包

状态：已建立。

产出：

- 专家团启动会纪要。
- 启动审查与范围冻结。
- 总体开发大纲。
- 需求决策池。
- 测试矩阵。
- MiroFish 资料需求协议。
- Git 计划。
- 真相源索引。

通过标准：

- 不写 runtime。
- 不新增 save 字段。
- 不扩大 DeepSeek 权限。
- 列出 D-140 系列。
- 更新仪表盘、PROJECT-STATE、AGENTS 和 handoff。

## a1 save-format / authority 设计门禁

状态：已建立。

目标：

- 评估是否需要 `SAVE_FORMAT_VERSION = 25`。
- 评估是否需要单一低阶区域聚合对象。
- 定义 projection-first 与最小持久化两条路线。
- 列出 a1 后必须由用户拍板的事项。

结论：

- 专家团建议 b1 保持 `SAVE_FORMAT_VERSION = 24`。
- 不新增 `regionSampleState` / `regionalSampleState`。
- b1 新增纯 engine projection helper，把既有 `routeLocationState`、`survivalEconomyState` 与 `livingWorldState` 证据组合成玩家可读区域样板。
- D-141-001 至 D-141-008 待用户审批。

## a2 MiroFish / 知识库 / 测试矩阵门禁

状态：待进入；等待 D-141 审批。

目标：

- 复核 v0.18 route/multi-region 相关 intake。
- 从全书基础包只取南疆早期低阶区域 topic slice 的 source pointer 和 coverage gap。
- 不整包导入知识库、runtime、canon 或 DeepSeek。

## b1-b4 候选

| 小版本 | 候选目标 | 默认边界 |
|---|---|---|
| b1 | 低阶区域 projection-first 第一刀 | 不写正式区域状态 |
| b2 | route/location + survival pressure 组合可读性 | 不成交、不发奖励、不写追捕成败 |
| b3 | 商队/散修/城外缘接触窗口 | 不加入势力、不解锁完整城市 |
| b4 | 区域事件模板与风险前置条件 | 不定案、不写 NPC 生死 |

## process / rc

- process-1：反刷、旧档兼容、回滚和测试矩阵复核。
- process-2：长线区域漂移与知识库复核。
- rc：Player Advocate 默认 100 轮；若新增持久字段或正式区域状态，提升到 120 轮。live probe 到 rc 前按实际 scope 给用户确认。

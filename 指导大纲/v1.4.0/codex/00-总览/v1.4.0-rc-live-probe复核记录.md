# v1.4.0-rc live probe 复核记录

状态：不执行 live probe；记录豁免原因
日期：2026-05-21

## 背景

D-140-010 已批准：v1.4 live probe 到 rc 前再确认成本、样本和轮次，不默认 live。

## 本轮复核

专家团结论：v1.4.0 当前 scope 不需要 live probe。

原因：

- 未改 DeepSeek runtime prompt。
- 未改 DeepSeek visible context。
- 未改 response pipeline、JSON schema、API retry 或 model。
- 未给 DeepSeek 区域、经济、社会、地点或奖励新 authority。
- 区域样板全部由本地 deterministic projection 产生。

## 如果未来要跑

只有在以下情况出现时才提交用户确认：

- DeepSeek 参与区域事件生成。
- UI 文案需要公开发布口径。
- context-builder 增加南疆/商家城/商队/散修安全摘要。
- 正式区域字段或正式地点/势力/交易被批准。

建议起步档位：

- model：`deepseek-v4-flash`
- samples：3
- rounds：3
- 通过线：P0=0，P1=0，accepted >= 90%，P2 <= 2

## 结论

本 rc 不执行 live probe；这是 explicit exemption，不是遗漏，也不表示 live narrative quality 已验证。

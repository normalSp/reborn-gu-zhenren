# v0.15.0 需求决策池

日期：2026-05-18
状态：文档启动版

## 已批准需求

### D-015-001 v0.15 主线：低阶蛊师经济、补给、炼养用深循环

状态：已批准。

内容：

- 低阶蛊师阶段先做完整。
- 玩家长期行动要受资源、补给、喂养、炼蛊失败、市场窗口和反刷压力影响。
- 不急着跳蛊仙，不做高阶资源。

### D-015-002 一次性准备 v0.15 所需 MiroFish requests

状态：已完成并通过 intake。

交付：

- 低阶经济/炼蛊/喂养包。
- 南疆低阶市场/商队/交易包。
- 黑市/委托/灰色交易边界包。

提交：

- `42c6841 docs: 验收v0.15 MiroFish交付包`

### D-015-003 先写文档和规则，不先写 runtime

状态：已批准。

内容：

- 先建立 v0.15 总览、大纲、路线图、需求池、真相源、MiroFish、Git、规则总纲、a1 门禁、字段表、测试矩阵。
- runtime 从 a1 之后按门禁推进。

### D-015-004 商队/市场窗口候选

状态：已批准，默认放在 b3。

内容：

- 商队到来、递话、询价、补给准备、公开理由可作为候选。
- 不开放完整坊市、完整商家城、正式商队加入、正式交易价格表。

### D-015-005 黑市/委托/灰色交易边界

状态：已批准但放在 b2 之后，默认 `deferred`。

内容：

- 可作为边界、反刷、诈骗/设局、势力关注测试样本。
- 不作为 v0.15 第一刀 runtime。
- 不开放正式黑市、委托代售、灰色交易收益。

## 待阶段复核需求

### D-015-006 是否新增经济持久字段

状态：待复核。

默认：不新增。

触发条件：

- 现有 `materialBag`、`flags`、`livingWorldState`、action ledger 无法表达资源/补给/喂养/炼蛊状态。

若触发：

- 必须用户决策。
- 必须 bump `SAVE_FORMAT_VERSION`。
- 必须添加 migration/defaults/tests。

### D-015-007 是否允许正式材料/元石结算

状态：待复核。

默认：第一刀保守，不开放新稳定刷取。

可接受方向：

- 小额、本地、上限明确、同场景反刷、来源可追踪。

不可接受：

- DeepSeek 发放。
- UI 私算。
- 无限刷材料/元石。
- 发放未注册蛊虫/蛊方/仙材。

### D-015-008 是否消耗库存进行炼蛊/喂养

状态：待复核。

默认：先做可读规则和缺口展示；若要真实消耗库存，必须有测试覆盖。

触发条件：

- b1/b2 需要正式闭环而不是只读提示。

### D-015-009 是否加入 live DeepSeek 玩家口感探针

状态：候选。

来源：

- v0.14 Player Advocate 多次提到更接近真实游玩。

默认：

- v0.15 a1/a2 不调用 live DeepSeek。
- 可在 b3 或 rc 加 5-10 轮 live narrative probe，但 DeepSeek 仍不能写奖励/材料/蛊方/市场结果。

### D-015-010 是否新建分支

状态：待复核。

默认：

- 当前继续 `codex/v013-npc-faction-reaction`，保持清晰 commit 边界。

可选：

- 进入 runtime 大改前新建 `codex/v015-low-rank-economy`，需要用户或 Git steward 决策。

## 当前发现与候选池

### F-015-001 补给准备行动样板

来源：v0.14 route continuation Player Advocate。

状态：进入 b1 候选。

### F-015-002 商队递话正式行动样板

来源：v0.14 阵营目标前置与路线承接样本。

状态：进入 b3 候选。

### F-015-003 残方/完整蛊方边界

来源：v0.10 资源/炼养用闭环与 v0.15 MiroFish economy pack。

状态：进入 b2 候选。

### F-015-004 黑市/委托边界不要抢跑

来源：v0.15 MiroFish optional/later 包。

状态：进入 b4 或 v0.16+，不阻塞 a1/a2/b1/b2。

### F-015-a1-001 a2 只能做候选规则池

来源：`v0.15.0-a1` 设计门禁。

状态：已完成。

说明：

- a2 可新增 canon/rule draft/helper/test。
- a2 不写 store、不改 UI、不发奖励、不新增 save 字段、不扩张 DeepSeek。

完成结果：

- 当前规则池：24 条 current rule drafts。
- 延期边界：10 条 deferred gray-trade boundaries。
- 验证通过：focused tests、TypeScript、full unit、build。

### F-015-a2-001 v0.15-a 完成后暂停进入 b1，先做美术资源盘点

来源：用户 2026-05-18 新指令。

状态：当前执行。

说明：

- `v0.15-a` 完成后不直接进入 `v0.15-b1`。
- 先统计项目内美术资源、重复/候选资源、v1.0 缺口和是否需要美术专项。
- 若发现重复图或同一功能多候选，列出让用户挑选，不自行删除。

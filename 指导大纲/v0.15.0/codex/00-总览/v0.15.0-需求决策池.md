# v0.15.0 需求决策池

日期：2026-05-18
状态：文档启动版

## 已批准需求

### D-015-001 v0.15 主线：低阶蛊师经济、补给、炼养用深循环

状态：已批准。

内容：

- 低阶蛊师阶段先做完整。
- 玩家长期行动要受资源、补给、喂养、炼蛊失败、市场窗口和反刷压力影响。
- 不急着跳蛊仙，不做高阶资源。

### D-015-002 一次性准备 v0.15 所需 MiroFish requests

状态：已完成并通过 intake。

交付：

- 低阶经济/炼蛊/喂养包。
- 南疆低阶市场/商队/交易包。
- 黑市/委托/灰色交易边界包。

提交：

- `42c6841 docs: 验收v0.15 MiroFish交付包`

### D-015-003 先写文档和规则，不先写 runtime

状态：已批准。

内容：

- 先建立 v0.15 总览、大纲、路线图、需求池、真相源、MiroFish、Git、规则总纲、a1 门禁、字段表、测试矩阵。
- runtime 从 a1 之后按门禁推进。

### D-015-004 商队/市场窗口候选

状态：已批准，默认放在 b3。

内容：

- 商队到来、递话、询价、补给准备、公开理由可作为候选。
- 不开放完整坊市、完整商家城、正式商队加入、正式交易价格表。

### D-015-005 黑市/委托/灰色交易边界

状态：已批准但放在 b2 之后，默认 `deferred`。

内容：

- 可作为边界、反刷、诈骗/设局、势力关注测试样本。
- 不作为 v0.15 第一刀 runtime。
- 不开放正式黑市、委托代售、灰色交易收益。

## 待阶段复核需求

### D-015-006 是否新增经济持久字段

状态：待复核。

默认：不新增。

触发条件：

- 现有 `materialBag`、`flags`、`livingWorldState`、action ledger 无法表达资源/补给/喂养/炼蛊状态。

若触发：

- 必须用户决策。
- 必须 bump `SAVE_FORMAT_VERSION`。
- 必须添加 migration/defaults/tests。

### D-015-007 是否允许正式材料/元石结算

状态：待复核。

默认：第一刀保守，不开放新稳定刷取。

可接受方向：

- 小额、本地、上限明确、同场景反刷、来源可追踪。

不可接受：

- DeepSeek 发放。
- UI 私算。
- 无限刷材料/元石。
- 发放未注册蛊虫/蛊方/仙材。

### D-015-008 是否消耗库存进行炼蛊/喂养

状态：待复核。

默认：先做可读规则和缺口展示；若要真实消耗库存，必须有测试覆盖。

触发条件：

- b1/b2 需要正式闭环而不是只读提示。

### D-015-009 是否加入 live DeepSeek 玩家口感探针

状态：候选。

来源：

- v0.14 Player Advocate 多次提到更接近真实游玩。

默认：

- v0.15 a1/a2 不调用 live DeepSeek。
- 可在 b3 或 rc 加 5-10 轮 live narrative probe，但 DeepSeek 仍不能写奖励/材料/蛊方/市场结果。

### D-015-010 是否新建分支

状态：待复核。

默认：

- 当前继续 `codex/v013-npc-faction-reaction`，保持清晰 commit 边界。

可选：

- 进入 runtime 大改前新建 `codex/v015-low-rank-economy`，需要用户或 Git steward 决策。

## 当前发现与候选池

### F-015-001 补给准备行动样板

来源：v0.14 route continuation Player Advocate。

状态：已完成。

完成结果：

- `qingmao_supply_feeding_preparation_probe` 已接入为本地前置行动。
- 写入既有 `livingWorldState` 和本地行动账本。
- 不发材料、不扣元石、不开放市场、不判定离山成功、不新增存档字段。
- Player Advocate 20 轮通过，下一步理解率 95%。

### F-015-002 商队递话正式行动样板

来源：v0.14 阵营目标前置与路线承接样本。

状态：本地质量门通过，待提交推送与远端 CI。

完成结果：

- `qingmao_market_window_probe` 已接入为本地候选窗口行动。
- 写入既有 `livingWorldState` 和本地行动账本。
- 不买卖、不写价格表、不开放库存、不加入商队、不开放黑市/委托、不新增存档字段、不扩张 DeepSeek 权限。
- focused unit、TypeScript、Playwright b3 e2e、Player Advocate 20 轮、full unit、build、asset scans、production-preview smoke 已通过。

### F-015-003 残方/完整蛊方边界

来源：v0.10 资源/炼养用闭环与 v0.15 MiroFish economy pack。

状态：已完成。

完成结果：

- `qingmao_refinement_boundary_probe` 已接入为本地边界试读行动。
- 写入既有 `livingWorldState` 和本地行动账本。
- 不消耗材料、不解锁完整蛊方、不判定炼成或失败结算、不新增存档字段、不扩张 DeepSeek 权限。
- focused unit、TypeScript、Playwright b2 e2e、Player Advocate 20 轮、full unit、build、asset scans、production-preview smoke 通过。
- `03a2f88 feat: 接入v0.15炼蛊残方边界样板` 已推送，GitHub Actions run `26022209682` 通过。

### F-015-004 黑市/委托边界不要抢跑

来源：v0.15 MiroFish optional/later 包。

状态：进入 b4 或 v0.16+，不阻塞 a1/a2/b1/b2。

### F-015-a1-001 a2 只能做候选规则池

来源：`v0.15.0-a1` 设计门禁。

状态：已完成。

说明：

- a2 可新增 canon/rule draft/helper/test。
- a2 不写 store、不改 UI、不发奖励、不新增 save 字段、不扩张 DeepSeek。

完成结果：

- 当前规则池：24 条 current rule drafts。
- 延期边界：10 条 deferred gray-trade boundaries。
- 验证通过：focused tests、TypeScript、full unit、build。

### F-015-a2-001 v0.15-a 完成后暂停进入 b1，先做美术资源盘点

来源：用户 2026-05-18 新指令。

状态：已完成。

说明：

- `v0.15-a` 完成后不直接进入 `v0.15-b1`。
- 先统计项目内美术资源、重复/候选资源、v1.0 缺口和是否需要美术专项。
- 若发现重复图或同一功能多候选，列出让用户挑选，不自行删除。

完成结果：

- 美术资源盘点第二版完成：全项目 445 个媒体文件，`doc/art/` 66 张图已纳入统计。
- 用户批准开 `v0.15-art-audit-1` 轻量专项。
- 方源/方正 style-sample 与风格母版 candidate 决定保留归档。
- roadmap 已接受的三张战斗候选和 `public/rebrng/scenes/s0-qingmao/` 三张 PNG 纳入 `review-only` 治理。
- v1.0 hero 三件套已选为 `selected_candidate`，复制到稳定 public 路径，但不绑定 UI/EdgeOne/OG meta。

### F-015-art-001 美术资源治理专项不扩大玩法权限

来源：`v0.15-art-audit-1`。

状态：已执行。

说明：

- `qingmao-visual-assets.json` 已补登记场景 SVG 与具体战斗 PNG。
- 只有 `active + runtime_active` 可被当前运行时自动消费。
- 具体人物战斗图为 `review-only + specific_scene_only`，进入 UI 前必须另有剧情节点、可见事实和玩家身份边界。
- hero 三件套仅是发布候选，不改变玩法、不自动部署、不承诺未实现内容。

### F-015-b1-001 补给整理冷却/递减收益

来源：`v0.15.0-b1` Player Advocate 轮次 13。

状态：future_sample_pool。

说明：

- 当前 b1 只做缺口整理和账本反馈，不给收益。
- 如果后续开放正式补给、库存或材料系统，需要考虑重复整理的冷却、递减收益或同场景上限。

### F-015-b1-002 经济叙事 live probe

来源：`v0.15.0-b1` Player Advocate 轮次 20。

状态：rc 候选。

说明：

- b1 确定性门禁已经证明本地行动边界，但真实叙事口感需要在 v0.15 rc 或叙事回流阶段追加少量 live narrative probe。
- DeepSeek 仍不能写奖励、材料、蛊方、市场、路线或经济结果。

### F-015-b2-001 正式炼蛊材料消耗/失败结算

来源：`v0.15.0-b2` Player Advocate 轮次 13。

状态：future_sample_pool，需用户决策。

说明：

- b2 只做残方和失败代价边界试读。
- 若后续要真实扣材料、写失败伤害、记录冷却、解锁蛊方或判定炼成，必须另开阶段并补测试。
- 可能涉及 inventory/materialBag、成功率、失败成本、反刷和存档策略，不能在 b2 偷跑。

### F-015-b2-002 炼蛊叙事 live probe

来源：`v0.15.0-b2` Player Advocate 轮次 20。

状态：rc 候选。

说明：

- b2 的边界可读性足够，但叙事张力偏工程化。
- v0.15 rc 可追加少量 live narrative probe，用来验证 DeepSeek 是否能写出炼蛊压力、缺项和失败风险，但仍不得写奖励、材料、蛊方、成功率或结算结果。

### F-015-b2-003 炼蛊试读冷却/反刷样本

来源：`v0.15.0-b2` Player Advocate 轮次 10。

状态：future_sample_pool。

说明：

- 当前试读没有收益，重复执行风险较低。
- 一旦未来开放真实材料、失败代价或配方推进，必须加入冷却、消耗、风险或同场景上限。

### F-015-b2-004 GitHub Actions Node 20 deprecation warning

来源：GitHub Actions run `26022209682` annotation。

状态：process_followup，不阻塞 b2。

说明：

- 远端 CI 已通过，但 GitHub 提示 `actions/checkout@v4` / `actions/setup-node@v4` 使用的 Node 20 runtime 将被平台淘汰。
- 这是 CI 平台维护风险，不影响游戏 runtime。
- 后续 process 小刀可升级 action 版本或设置 GitHub 推荐的 runtime 变量，避免未来远端门禁突然变红。

### F-015-b3-001 正式市场交易/价格表/库存系统

来源：`v0.15.0-b3` Player Advocate 轮次 10-12。

状态：future_sample_pool，需用户决策。

说明：

- b3 只做商队/市场候选窗口，不开放正式买卖。
- 若后续要写价格表、商店库存、买卖结算、材料/元石变化或商队加入，必须另开阶段并补经济模拟、反刷、旧档和 DeepSeek 越权测试。

### F-015-b3-002 市场窗口叙事 live probe

来源：`v0.15.0-b3` Player Advocate 轮次 20。

状态：rc 候选。

说明：

- b3 的候选窗口可读性成立，但商队氛围和交易压力还偏工程化。
- v0.15 rc 可追加少量 live narrative probe，用来验证 DeepSeek 是否能写出商队询价、身份担保、公开理由和风险氛围，但仍不得写价格、库存、奖励、交易结果或商队加入。

### F-015-b3-003 交易反刷与诈骗样本

来源：`v0.15.0-b3` Player Advocate 轮次 13-14。

状态：future_sample_pool，可进入 b4。

说明：

- 当前 b3 无收益，重复试探风险低。
- 一旦进入黑市、委托、代售或正式买卖，必须加入价格/库存上限、身份担保、骗局、设局、势力关注、冷却和收益递减测试。

# v0.15.0-a1 设计门禁

日期：2026-05-18
状态：已通过，见 `v0.15.0-a1-设计门禁执行记录.md`

## 目标

在写任何 v0.15 runtime 前，先冻结低阶经济/炼养用的权限、字段、测试矩阵和 stop gates。

a1 不写运行时代码，不新增存档字段，不开放奖励、材料、市场、黑市或炼蛊结果。

## 必须回答的问题

1. 哪些数据只属于候选？
2. 哪些数据可以成为 RebornG-owned rule draft？
3. 哪些 outcome 必须由本地 engine 结算？
4. 哪些内容只能 UI 展示，不能写 state？
5. 哪些行为会触发 save format 决策？
6. 哪些内容必须延后到 v0.16+？

## 门禁清单

| 门禁 | 要求 | 当前状态 |
|---|---|---|
| MiroFish intake | 三包通过 review | 已通过 |
| 版权/原文 | 不复制原著正文，不保留 quote-like 字段 | 已通过 |
| DeepSeek 权限 | narrative/candidate only | 待 a1 确认 |
| 材料/奖励 | local engine only | 待 a1 确认 |
| 市场/商队 | candidate window only | 待 a1 确认 |
| 黑市/委托 | deferred by default | 待 a1 确认 |
| save format | 默认不新增字段 | 待 a1 确认 |
| 测试矩阵 | 当前/未来/丢弃样本 triage | 待 a1 执行 |
| Player Advocate | runtime 小版本 20 轮，rc 60 轮 | 待 a1 确认 |
| a2 入口 | canon/rule draft only | 已通过 |

## a1 输出物

- 更新 `v0.15.0-a1-字段表.md`。
- 更新 `v0.15.0-a1-测试矩阵.md`。
- 必要时补充 `v0.15.0-需求决策池.md`。
- 记录是否允许进入 `v0.15.0-a2`。

## a1 非目标

- 不新增 canon JSON。
- 不新增 engine。
- 不接 UI。
- 不发奖励。
- 不写 store。
- 不调用 DeepSeek。
- 不跑 live narrative probe。

## 通过条件

- 用户不需要再为 a1 决策具体实现。
- 所有 stop gates 清晰。
- a2 的文件范围和测试方向清晰。

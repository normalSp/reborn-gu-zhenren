# v3.3.0-a0 v3.0 至 v3.2 证据复盘与 reopen 范围冻结

日期：2026-05-24
状态：completed
冻结模型：`v330_a0_f300_reopen_evidence_scope_v1`

## 目标

a0 复盘 v3.0-v3.2 的 report-only 证据，确认 v3.3 是否可以进入 F-300 reopen 决策包，并冻结 reopen 的讨论范围。

## 输入证据

| 版本 | 证据 | 关键结论 |
|---|---|---|
| v3.0 | runtime agent design gate checker | `runtimeImplementationApproved=false`；设计门禁可讨论，但未批准实装 |
| v3.1 | gate hardening checker | `v32RehearsalAdmissible=true`；WorldCore post-check / rollback / old-save 门禁继续加固 |
| v3.2 | runtime rehearsal checker | `v33DecisionPackageReady=true`；`runtimeImplementationApproved=false`；P0/P1 falseNegative=0/0 |

## 证据含义

当前证据证明：

- AgentProposal / WorldCore post-check / expression chain 的设计证据已经足够进入用户决策讨论。
- 负样本、future gate、rejected violation 已经形成可复核分类。
- 当前仍没有任何 player-facing runtime implementation 证据。
- 当前仍没有 live DeepSeek、old-save runtime、rollback runtime、MiroFish blocking、save-format、backend 或 external framework PoC 证据。

## reopen 范围冻结

v3.3 允许讨论：

- 是否值得在未来重开 F-300-001。
- 如果重开，第一刀应是 transient proposal-only 还是更重路线。
- 必须同时打开哪些 future gate。
- 需要什么测试、Player Advocate、live probe、rollback、old-save、drift gate。
- v3.4 应进入 implementation、继续 hardening，还是 defer。

v3.3 不允许默认执行：

- runtime agent implementation。
- agent 写事实、存档、runtime canon、地点、奖励、NPC 生死。
- 新 save field、`SAVE_FORMAT_VERSION` bump、`runFingerprint`。
- live DeepSeek、DeepSeek prompt/context/model/authority 改动。
- MiroFish export/intake 或真实 lore 吸收。
- backend/BFF/service 或 external framework PoC/dependency/subagent。

## a0 结论

v3.3 可以完成 F-300 reopen 决策包；不能把 v3.0-v3.2 的 report-only 证据解释为 runtime implementation approval。

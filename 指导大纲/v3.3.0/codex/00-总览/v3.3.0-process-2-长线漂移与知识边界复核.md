# v3.3.0 process-2 长线漂移与知识边界复核

日期：2026-05-24
状态：completed

## 复核范围

v3.3 是 F-300 reopen 决策包，不改 runtime、prompt、DeepSeek、MiroFish、知识库正文或 canon。process-2 只复核边界，不运行长线 live probe。

## live DeepSeek

状态：未调用。

原因：

- v3.3 不改 DeepSeek prompt/context/model/authority。
- v3.3 不产生 player-facing runtime 文本。
- v3.3 只做决策包和测试档位。

## Player Advocate

状态：未运行。

原因：

- v3.3 没有玩家可见 UI/runtime 改动。

未来要求：

- 若 v3.4 打开 transient proposal-only runtime，至少需要 10 轮 Player Advocate。
- 若叠加 live DeepSeek，建议 50 轮以上并记录模型、样本、轮次、成本、报告路径。

## MiroFish / 知识库

状态：MiroFish `not_needed`。

原因：

- v3.3 不吸收真实原著事实。
- 不使用命名 NPC、真实势力、hidden-adjacent、方源公开证据、L4/L5 fact anchor。
- 不写知识库正文、不 promotion runtime canon。

future blocking trigger：

- 未来路线若涉及真实 lore、命名 NPC、hidden-adjacent、正式 lore 结论，必须走 RebornG request -> MiroFish export -> RebornG intake review。

## 漂移风险

v3.3 本身无剧情漂移风险，因为没有 runtime 输出。但有“制度漂移”风险：后续可能把 `F-300 reopen 决策包完成` 误读为 `F-300 已打开`。

缓解：

- README、需求决策池、最终决策包、AGENTS、PROJECT-STATE 全部写明 runtime implementation 未批准。

## 结论

process-2 通过。v3.3 不需要 live probe 或 MiroFish intake；未来 implementation 必须按 b2 档位重新评估。

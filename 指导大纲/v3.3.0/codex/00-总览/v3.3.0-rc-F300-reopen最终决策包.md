# v3.3.0 rc F-300 reopen 最终决策包

日期：2026-05-24
状态：completed
冻结模型：`v330_rc_f300_reopen_final_decision_package_v1`

## 给用户的核心结论

v3.3 已完成 F-300 reopen 决策包。专家团不建议把 F-300 自动打开；建议下一步由用户单独决定 v3.4 走哪条路线。

## 当前可确认

- v3.0-v3.2 的 report-only 证据足以支持“进入 runtime agent 第一刀讨论”。
- 证据不等于 runtime implementation approval。
- 当前最稳妥的 implementation 候选是最小 transient proposal-only L2/L3 小区域第一刀。
- 不建议先做 persistent memory、runFingerprint、save-format 或所有 NPC live。

## v3.4 可选路线

| 路线 | 建议 | 需要用户批准 |
|---|---|---|
| 路线 1：继续 report-only hardening | 稳妥，可直接作为 v3.4 主线候选 | 新 D-340 授权 |
| 路线 2：最小 transient proposal-only runtime 第一刀 | 专家团推荐的受控实装路线 | 必须单独打开 F-330-001 / F-300-001 |
| 路线 3：继续 defer 到 v3.5+ | 可行但推进慢 | 新 D-340 授权 |

## 如果选择路线 2，默认仍不打开

- 不打开 save field / `SAVE_FORMAT_VERSION` / `runFingerprint`。
- 不调用 live DeepSeek。
- 不做 MiroFish export/intake。
- 不做 backend/BFF/service。
- 不引入 external framework PoC/dependency/subagent。
- 不做 L4/L5 runtime、天道/宿命 runtime、原著关键人物 runtime agent。
- 不开放正式地点、阵营、身份、奖励、NPC 生死、通缉、招揽、封锁。

这些都必须作为单独 future gate 另批。

## 路线 2 最小定义

若未来用户批准，第一刀应满足：

- L2/L3 小区域。
- transient AgentProposal。
- WorldCore post-check 最终裁决。
- expression-only。
- rejected proposal 不改变状态。
- 旧档打开不变。
- rollback 可验证。
- Player Advocate 至少 10 轮。
- deterministic replay 至少 30 轮。
- P0/P1 立即停机。

## 当前不建议

不建议 v3.4 直接做：

- persistent same-start variation。
- agent memory save。
- run fingerprint。
- live DeepSeek + runtime agent 同时第一刀。
- named NPC / real faction / hidden-adjacent。
- backend service。
- external framework PoC。
- all NPC live。
- high-rank combat runtime。

## v3.3 结论

`F-300 reopen decision package = complete`。

`runtimeImplementationApproved = false`。

下一步应开 v3.4 专家团启动会，让用户在路线 1 / 路线 2 / 路线 3 中拍板。v3.3 不替 v3.4 授权。

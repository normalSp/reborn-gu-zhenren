# v3.3.0 启动审查与范围冻结

日期：2026-05-24
状态：startup completed；waiting D-330 approval

## 启动条件

v3.2 已完成并给出以下准入证据：

- `v33DecisionPackageReady=true`。
- `runtimeImplementationApproved=false`。
- P0/P1 falseNegative=0/0。
- future gate 和 rejected violation 能稳定分类。

因此可以开启 v3.3 专家团启动会，但只能做 F-300 reopen 决策包。

## 范围冻结

v3.3 startup 只允许：

- 复盘 v3.0-v3.2 证据。
- 起草 F-300 reopen 决策包。
- 分析最小 runtime first cut 的收益、风险、成本、测试档位。
- 准备用户可一次性审批的 D-330/F-330。
- 更新 dashboard、AGENTS、PROJECT-STATE、skill override 和上下文交接。

v3.3 startup 不允许：

- 实现 runtime agent。
- 修改 runtime/source/UI/store/prompt/save。
- 新增 save field / `SAVE_FORMAT_VERSION` / `runFingerprint`。
- 调用 live DeepSeek 或改 prompt/context/model/authority。
- 做 MiroFish export/intake。
- 引入 backend/BFF/service 或 external framework PoC/dependency/subagent。
- 开放正式地点、阵营、身份、奖励、NPC 生死。
- 打开 L4/L5 runtime、天道/宿命 runtime、原著关键人物 runtime agent。

## v3.3 要回答的问题

- F-300-001 是否值得打开。
- 如果打开，是否必须同步打开 save-format / live DeepSeek / MiroFish / backend / external framework 等配套。
- 是否存在一个不新增 save field、不调用 live、不引入后端的最小 runtime first cut。
- 如果用户暂不打开 implementation，继续 hardening 的最小有价值路径是什么。

## 结论

v3.3 startup 条件满足。下一步等待用户审批 D-330/F-330；审批前不进入 `/goal` 连续开发。

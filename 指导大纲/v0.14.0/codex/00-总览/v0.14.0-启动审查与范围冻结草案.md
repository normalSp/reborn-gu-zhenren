# v0.14.0 启动审查与范围冻结草案

日期：2026-05-17
状态：用户已批准方向，待 a1 执行

## 用户决策

用户已批准：

1. v0.14 主线：`青茅后续路线承接`。
2. 第一段 v0.13 Player Advocate 发现进入 v0.14 候选。
3. v0.14 Player Advocate 对高风险小版本升级为 20 轮，rc 升级为 60 轮。
4. 先写好文档和规则，再进入 runtime。

## 本阶段必须解决

- 路线承接不是纯文本跳转。
- 路线选择必须读取青茅前期账本。
- 后续候选必须至少有一个能升级成正式前置行动样板。
- 玩家输入阵营目标时，系统要能显示条件和风险，而不是只说“不行”。
- 移动端社会影响和路线信息不能淹没玩家。

## 版本范围

### 进入范围

- 路线承接规则。
- 路线条件字段表。
- 路线条件测试矩阵。
- MiroFish v0.14 资料请求。
- Git 提交与推送计划。
- Player Advocate 升级轮次规则。
- 后续 runtime 的 `canon -> engine -> store -> UI -> tests/docs` 执行路线。

### 暂不进入范围

- 完整南疆地图。
- 商家城完整功能。
- 真实阵营转换。
- 正式通缉/招揽/任务/奖励网络。
- 新增蛊虫或资源经济深循环。
- 薄 BFF 或后端改造。
- EdgeOne 自动部署。

## 保存格式判断

当前默认不 bump `SAVE_FORMAT_VERSION`。

如后续要新增持久化 route state、region state、location history、route access ledger，则必须停止并让用户决策：

- 是否 bump save format。
- 默认值和旧档迁移。
- public/test-saves fixture。
- 长测和 Player Advocate 旧档轮次。

## DeepSeek 边界

DeepSeek 可写：

- 路线候选叙事。
- NPC/势力压力表达。
- 公开传闻。
- 玩家可见的下一步建议文本。

DeepSeek 不可写：

- 正式地点解锁。
- 阵营身份变化。
- 奖励。
- NPC 生死。
- 追杀/抓捕成功。
- 正史锚点或隐藏事实正文。

## MiroFish 需求判断

v0.14 涉及 route aftermath、南疆低阶路线、商家城入口候选。

当前建议：

- `qingmao_exit_route_aftermath_pack`：preferred，b2 前可能升级为 blocking。
- `southern_border_low_rank_route_pack`：preferred，a2/b2 前需要。
- `shang_clan_city_public_entry_pack`：optional，b2 后或 v0.15 前再决定。

## 进入 a1 条件

a1 可以立即启动，条件是：

- 本文档与大纲已落地。
- v0.14 真相源索引已建立。
- MiroFish 请求文件已准备好给用户转交。
- Player Advocate 升级规则已写入制度文档或 v0.14 规则。

# v0.14.0 Git 提交与推送计划

日期：2026-05-17
状态：第一版

## 当前分支

当前沿用：

`codex/v013-npc-faction-reaction`

进入 runtime 大改前可评估是否新建：

`codex/v014-route-continuation`

如果继续当前分支，必须保持小版本 commit 边界清晰。

## 提交原则

- 每个小版本至少一个清晰 commit。
- 文档/规则和 runtime 实现尽量分开。
- 不使用 `git add -A`。
- 只 stage 本阶段相关文件。
- 不混入 `RebornG_codebuddy.zip`、`artifacts/`、`bgm/`、`指导大纲/大方向/` 等历史未跟踪项。
- 不提交 secrets、`.env`、`.codex` runtime state、plugin cache、SQLite/log 文件。

## 推送原则

需要推送：

- v0.14 文档规则建立后。
- 每个 runtime 小版本验证通过后。
- rc 收束后。
- 跨工具/跨会话 continuation 前。

不自动推送：

- EdgeOne 自动部署。
- branch protection。
- auto merge。
- full Playwright required PR gate。

这些都需要用户另行决策。

## 推荐提交分组

| 阶段 | 提交主题 |
|---|---|
| 文档规则 | `docs: 建立v0.14路线承接规则` |
| process-0 | `docs: 建立v0.14 Git安全收束` |
| a1 | `docs: 收束v0.14路线承接设计门禁` |
| a2 | `feat: 建立v0.14路线条件读取` |
| b1 | `feat: 接入v0.14路线前置行动样板` |
| b2 | `feat: 建立v0.14青茅路线承接状态` |
| b3 | `feat: 展示v0.14阵营目标前置条件` |
| b4 | `feat: 优化v0.14路线承接可读性` |
| rc | `docs: 完成v0.14 rc质量收束` |

## 回滚锚点

v0.14 文档规则提交后，应作为第一个回滚锚点。

runtime 阶段如果出现路线/地点/阵营越权，应优先回滚对应小版本 commit，而不是重写历史。

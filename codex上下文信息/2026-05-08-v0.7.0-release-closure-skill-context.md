# 2026-05-08 v0.7.0 发布收束与 Skill 更新上下文交接

## 当前状态

- 当前分支：`codex/v070-release-closure`。
- 基线来源：`codex上下文信息/2026-05-08-v0.7.0-c-expansion-context.md`。
- 当前版本标识：`package.json = 0.7.0-pre.0`；`SAVE_FORMAT_VERSION = 13`。
- 用户长期要求：默认不使用子代理；阶段结束更新上下文交接文件并复读；每个阶段中文提交并 push；不确定项、冲突和更优方案必须直接沟通。

## 本次完成

- 新增发布收束核对表：`指导大纲/v0.7.0/codex/07-发布收束/v0.7.0-发布收束核对表.md`。
- 新增 skill 更新记录：`指导大纲/v0.7.0/codex/07-发布收束/game-dev-text-skill更新记录.md`。
- 实际更新用户级 skill：`C:\Users\11411\.codex\skills\game-dev-text\SKILL.md`。
- `game-dev-text` 已重写为 UTF-8 清晰版，版本为 `2.1.0`，并与 `reverend-insanity-lore` 明确分工。

## 关键结论

- v0.7.0-pre/a/b/c 的核心工程纵切已经达到“可验证地基”级别，但公布版还需要 P0 发布收束。
- P0 必须项：
  1. 真实音频资源落地并启用 manifest。
  2. 二创 provenance 全量审计。
  3. 宝黄天、成就、势力、仙窍、小队战、外派 100/300 回合套利复验。
  4. v070c 专项存档浏览器长测扩大。
  5. 旧大纲/旧审计 superseded 冲突清理。
- P1 建议项：
  - 小队外派 UI。
  - 地形/阵法战前解释。
  - 十绝体视觉与灾劫入口。
  - 方源/龙公角色曲真实文件和高光短播校准。
  - 更多 scene_gated 蛊虫使用入口。

## Skill 更新要点

- 新增 `RebornG 协作协议`：
  - 默认不使用子代理。
  - 先读最新 `codex上下文信息`、`指导大纲/v0.7.0/codex` 和大纲，再对照源码。
  - 按 canon 数据 -> 类型 -> engine -> store -> UI -> 测试/文档推进。
  - 阶段完成后中文提交并 push，更新并复读上下文交接。
  - 风险、冲突和更优方案必须直接和用户沟通。
- 删除旧版 skill 中不适配当前项目的规则：
  - 不再固定要求 `npm run lint/read_lints`。
  - 不再固化 `1 仙元石 = 100 元石`。
  - 不再把 `session_summary_*.md`、`.learnings` 放在最新上下文之前。
  - 不再重复维护大量蛊真人硬设定，而是调用 `reverend-insanity-lore`。

## 验证记录

- 已检查 `package.json`：当前 scripts 为 `dev/build/preview/test/test:watch/test:e2e/test:e2e:long`，没有 `lint`。
- 已检查 `public/audio/audio-source-manifest.json`：当前 BGM/SFX 均未启用真实 runtime 资源，仍需用户确认文件。
- 已检查 `src/canon/character-bgm-manifest.json`：方源《年轮》、龙公《春泥》已登记但 `runtimeEnabled=false`。
- 已检查 `测试存档/v0.7.0`：存在 16 个 v070c 专项存档。
- 已检查 `game-dev-text` skill 本体：不再包含旧 `read_lints`、固定旧汇率、`session_summary` 或 `.learnings` 优先入口。
- `npm test`：通过 41 个测试文件 / 298 个用例。
- `npm run build`：通过；Vite 500KB+ chunk 警告仍为非阻塞发布前优化项。
- `npm run test:e2e:long`：通过 3 条浏览器长测路径。

## 剩余风险

- B站 BV1khRLBUEpk 曲目与时间戳仍需人工通过浏览器或客户端复核；不能把未核对信息写死。
- 用户级 skill 不属于 RebornG Git 仓库，无法随项目 commit 一起版本化；本次通过项目内记录文件补足交接。
- 本次主要是发布收束文档和 skill 更新，没有补真实音频文件、外派 UI、地形 UI 或十绝体 UI 成品。

## 下一步建议

1. 用户提供或确认音频文件后，更新 `audio-source-manifest.json` 和 `character-bgm-manifest.json` 的 `runtimeEnabled`、来源、授权备注。
2. 增加 provenance 全量审计脚本或扩展 `provenance-gate.test.ts` 覆盖所有运行时池。
3. 增加 100/300 回合发布前总套利测试，覆盖宝黄天、成就、势力、仙窍、小队战和外派。
4. 补小队外派 UI、地形/阵法解释、十绝体视觉入口这三个体验缺口。

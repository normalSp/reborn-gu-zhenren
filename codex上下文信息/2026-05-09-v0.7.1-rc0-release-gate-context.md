# 2026-05-09 v0.7.1-rc.0 发布候选收束上下文交接

## 当前分支与版本

- 分支：`codex/v070-release-closure`
- 版本：`package.json = 0.7.1-rc.0`
- 存档格式：`SAVE_FORMAT_VERSION = 15`
- 本轮目标：进入 v0.8-a 前做 RC 扫尾，更新 skill 口径，清理发布文档冲突，确认私用角色曲阻塞项。

## 用户关键决策

- 先做 `v0.7.1-rc.0` 发布候选扫尾，再进入更深的 v0.8-a。
- `game-dev-text` 与 `reverend-insanity-lore` 都要更新。
- 用户确认的角色曲/社区曲阻塞 v0.8-a。
- 音频文件由用户本地提供；根目录 `bgm/` 只作为原始素材暂存区，runtime 只引用 `public/audio/bgm/...`。

## 已完成改动

- 版本标识：
  - `package.json`、`package-lock.json` 更新为 `0.7.1-rc.0`。
  - 标题页与游戏页页脚显示 `v0.7.1-rc.0`。
  - `src/canon/provenance-policy.json` 版本更新到 `0.7.1-rc.0`。
- Skill 更新：
  - `C:\Users\11411\.codex\skills\game-dev-text\SKILL.md` 更新到 `2.2.0`。
  - `C:\Users\11411\.codex\skills\reverend-insanity-lore\SKILL.md` 更新到 `0.2.0`。
  - 项目记录：`指导大纲/v0.7.0/codex/07-发布收束/game-dev-text-skill更新记录.md`。
- 发布文档：
  - 重写 `指导大纲/v0.7.0/codex/07-发布收束/v0.7.0-发布收束核对表.md`，统一 RC 事实源。
  - 重写 `指导大纲/v0.7.0/codex/07-发布收束/v0.7.0-发布说明.md`，转为 `v0.7.1-rc.0` 口径。
  - 新增 `指导大纲/v0.7.0/codex/07-发布收束/v0.7.1-rc0-角色曲缺失清单.md`。
  - 新增 `指导大纲/v0.7.0/codex/07-发布收束/v0.7.1-rc0-人工浏览器走查清单.md`。

## 音频阻塞状态

- 当前根目录 `bgm/` 只有歌词、清单、报告和 bug 文档，没有实际音频文件。
- 19 个 `user_supplied_reference` 曲目缺本地文件，保持 `runtimeEnabled=false`。
- 免费公布包仍可运行；但按用户最新决策，进入 v0.8-a 前必须补齐私用角色曲文件，或由用户重新决策解除阻塞。

## 验证结果

- `npm test`：通过，52 个测试文件 / 350 个用例。
- `npm run build`：通过；最大 chunk 为 `combat-squad` 约 423.54 kB，未出现 500KB+ 警告。
- `npm run test:e2e:long`：通过，12 条 Playwright 发布收束长测路径。

## 工作区注意事项

- 本轮不应提交无关改动：
  - `bug汇总/v0.6.0.md` 删除状态。
  - `dead_code_audit_report.md` 修改。
  - `.codex/` 未跟踪目录。
  - 根目录 `bgm/` 未跟踪原始素材/文档目录。
  - 根目录 `v0.7.0-每日例报-20260509.md` 未跟踪文件。
- 提交时只 stage 本轮 RC 相关文件和用户级 skill 更新记录对应的项目文档；用户级 skill 文件不在项目仓库内，需要在最终答复中说明已更新。

## 下一步入口

1. 若用户提供 19 个角色曲/名场面本地音频文件：整理到 `public/audio/bgm/...`，再启用 manifest，跑音频测试和长测。
2. 若用户决定解除角色曲阻塞：保持私用曲 disabled，直接进入 v0.8-a。
3. 进入 v0.8-a 前，建议用户按 `v0.7.1-rc0-人工浏览器走查清单.md` 做七出身开局和关键 UI 人工浏览器走查。

# 2026-05-07 v0.7.0-pre Codex 上下文交接

本文件只保存可交接的项目上下文、用户决策、源码状态和后续任务摘要。
不包含隐藏系统指令、开发者指令或不可公开的内部推理。

## 1. 用户目标

用户要求从专业游戏设计开发角度，使用 `game-dev-text`，在进入 v0.7.0 前完成可信审计：

- 项目含大量二创角色、仙蛊、杀招，不能全部当成原著内容。
- 需要判断 v0.7.0 前还要做什么。
- 因经常出现数值不统一、数值崩坏，需要判断是否重建数值闭环验证审计报告。
- 新文档统一放入 `指导大纲/v0.7.0/codex`，必要时分类。
- 完成后将用户和 Codex 的可交接上下文记录放入 `codex上下文信息`。

## 2. 已确认决策

- 二创治理采用“保留标注”：保留二创内容，但每条必须标注 `canon | derived | original | unknown` 并通过数值审计。
- `unknown` 不允许进入 v0.7.0 正式交付清单。
- 不复制新的原著全文，不从外部抓取资料；继续使用本地 `doc/original work/reverend-insanity.txt` 和项目 canon 做摘要索引。
- 本轮只落地可信审计、计划、上下文文档，不修改运行时代码。

## 3. 当前工程状态

- 当前分支：`codex/rebuild-v070-tracking`。
- 工作区已有上一轮 v0.7.0 相关源码改动和未跟踪文件，本轮不回退、不覆盖。
- `package.json` 版本仍是 `0.5.0`。
- `SAVE_FORMAT_VERSION = 11`，见 `src/store/initialState.ts:278`。
- 本轮验证：
  - `npm test` 通过，11 个测试文件 / 172 个用例。
  - `npm run build` 通过。
  - 构建仍有主 chunk `dist/assets/index-Bi0TNQVY.js` 约 1,388.58 kB 的 500KB+ 警告。

## 4. 本轮新增文档

- `指导大纲/v0.7.0/codex/00-总览/v0.7.0-进入前置闸门.md`
- `指导大纲/v0.7.0/codex/01-世界观与二创治理/二创内容治理审计.md`
- `指导大纲/v0.7.0/codex/02-数值闭环/数值闭环验证审计报告-v0.7.0-codex.md`
- `指导大纲/v0.7.0/codex/02-数值闭环/数值真相源矩阵.md`
- `指导大纲/v0.7.0/codex/03-执行计划/v0.7.0-前置修复路线图.md`

## 5. 关键审计结论

- 旧 `数值闭环验证审计报告-v0.7.0.md` 已不可信。它引用的宝黄天价格、5转势力收入、黑狱龙数值与当前源码不一致。
- 势力 tick 已接入 `advanceTurn`，当前问题是通胀和时间单位，而不是“无收入”。
- 宝黄天仙材、蛊方、杀招三类池已实现，当前问题是定价、去重、购买力和战力增幅闭环。
- 成就 26 条已补 `reward/progressMax`，当前问题转为奖励强度与重复杀招处理。
- canon 数据缺少正式 provenance 字段：一至五转蛊虫、仙蛊、杀招、NPC、训练场、遭遇均需要治理。
- 资源点扣费存在高风险逻辑：`immortalCurrency || currency` 与 `immortalCurrency !== undefined` 组合可能导致跨币种扣费污染。

## 6. 后续推荐任务

1. 先执行 provenance 数据闸门。
2. 再修货币策略和资源点扣费。
3. 然后补势力、宝黄天、仙窍、训练场、小队战的数值模拟测试。
4. 最后更新 v0.7.0 开发阶段跟踪，让它引用 codex 新报告作为真相源。

## 7. 继续开发时优先读取

1. `指导大纲/v0.7.0/codex/00-总览/v0.7.0-进入前置闸门.md`
2. `指导大纲/v0.7.0/codex/02-数值闭环/数值闭环验证审计报告-v0.7.0-codex.md`
3. `指导大纲/v0.7.0/codex/03-执行计划/v0.7.0-前置修复路线图.md`
4. `指导大纲/v0.7.0/v0.7.0-开发阶段跟踪.md`
5. `指导大纲/v0.7.0/v0.7.0-小队势力-成就重做-宝黄天完整版-设计大纲.md`

## 8. 2026-05-08 追加上下文：P2/P3 数值可信闸门

本轮用户要求继续进入 v0.7.0-pre 下一步，依据 `指导大纲/v0.7.0/codex` 文档推进“数值可信闸门”，而不是继续堆功能。

已使用技能：

- `game-dev-text`：按 canon 数据 → 类型 → engine → store → UI → 测试/文档顺序推进。
- `reverend-insanity-lore`：食料只作为蛊材/仙材用途标签；宝黄天是蛊仙级交易；AI 和运行时经济不得绕过真相源。

本轮落地：

- `src/engine/auction-engine.ts` 暴露当前宝黄天运行时定价快照，不直接改价。
- `src/engine/economy-simulation.ts` 新增宝黄天价格审计与 20/100/300 回合购买力表。
- `src/engine/material-source-audit.ts` 新增配方材料来源矩阵，覆盖商店、遭遇、训练场、势力、仙窍资源、宝黄天、剧情白名单、区域生成。
- `src/store/slices/guSlice.ts` 喂养改为优先消耗登记食料；缺食料或食料不匹配不恢复饥饿。
- `src/components/game/GuInventoryPanel.tsx` 去掉元石喂养入口，改用 `feedGuHunger` 结果提示。
- 新增测试：`auction-economy-loop.test.ts`、`recipe-source-closure.test.ts`、`gu-feeding-economy.test.ts`。
- 新增文档：`指导大纲/v0.7.0/codex/02-数值闭环/P2-P3-数值可信闸门推进记录.md`。

最新数值结论：

- 当前宝黄天仙蛊基础价严重偏低：六转 60 vs 候选 3600，七转 240 vs 候选 12000，八转 960 vs 候选 40000，九转 3800 vs 候选 120000。
- 当前低级仙材价 28/32/36 在候选 24-120 区间内，暂可保留。
- 当前六转完整蛊方/大目标运行时价约 162，明显偏低；杀招残方/完整传承公式与候选价一致。
- 候选模拟下六转稳定期每回合净得 36 仙元石：20 回合 720，不能稳定买六转仙蛊；100 回合 3600，可达成一个六转大目标；300 回合 10800，可形成多个六转资产但七转仍被 12000 门槛压住。

最新用户决策修正：

- 二创/unknown 内容可以进入 v0.7.0，但必须白名单、登记、标注、模拟覆盖。
- 原著流派 path 不允许自创；材料、蛊虫、仙蛊、杀招可二创，但不得绕过真相源和数值模拟。
- 合炼废弃，v0.7.0 只交付炼蛊、升炼、拆炼。

仍需用户决策：

1. 是否接受 `economy-balance.json` 的宝黄天候选价作为下一步运行时调价目标。
2. 六转稳定期收入是否暂定为每回合净得 36 仙元石；若偏快，建议改为 18-24 后重跑表。
3. 泛型蛊材是否继续作为中低转配方兜底；建议允许，但五转以上和仙蛊方逐步具体化。

## 9. 2026-05-08 追加上下文：喂养与仙窍经济闭环

本轮用户要求实施“v0.7.0-pre 喂养与仙窍经济闭环推进计划”，重点确认蛊虫/仙蛊食料能否在饿死前取得，并把六转稳定期收入从固定净值改成仙窍、势力、事件、喂养、炼蛊失败预留的综合账本。

已落地：

- 新增 `src/engine/feeding-rules.ts`，作为喂养规则真相源，包含 `FEED_MATERIAL_MAP`、`NON_MATERIAL_FEEDING_RULES`、`GU_HUNGER_CONFIG`、食料兜底策略与安全回合估算。
- `src/store/slices/guSlice.ts` 改为复用 `feeding-rules.ts`，并继续 re-export `GU_HUNGER_CONFIG` 以兼容旧测试。
- `src/engine/material-source-audit.ts` 新增 `GuFeedingClosureRow`、`generateGuFeedingClosureMatrix()`、`auditGuFeedingClosure()`。
- 当前喂养闭环审计覆盖 128 只蛊，`auditGuFeedingClosure()` 为 0 个 blocker。
- 非物质食料不进入 MaterialRegistry：例如 `仙元 / 时间流逝 / 灾劫 / 气运 / 天机碎片 / 情绪碎片 / 意志力 / 逆流之水 / 地脉之气`，改由事件、仙窍、训练场或特殊规则承接。
- `src/components/game/GuInventoryPanel.tsx` 增加食料、预计安全回合、来源、库存提示。
- `src/engine/economy-simulation.ts` 新增 `ApertureIncomeProfile`、`FactionIncomeProfile`、`FeedingExpenseProfile`、`AuctionPriceProfile` 与 `runIntegratedEconomySimulation()`。
- 一体化模拟结果：20 回合净 749 仙元石，100 回合净 3748，300 回合净 11246；20 回合买不起六转仙蛊，100 回合可完成一个六转大目标，300 回合可形成多个六转资产但七转仍被压住。

测试与构建：

- `npm test`：20 个测试文件 / 210 个用例通过。
- `npm run build`：通过，仍有既有 500KB+ 主包体积警告。

后续仍需推进：

1. 宝黄天运行时价格是否采用候选价，需要用户决策后再固化。
2. 需要把炼蛊 UI 的材料来源/缺口提示也接入 `material-source-audit`，避免显示“理论可炼但来源不可达”的配方。
3. 需要把势力 runtime 收益迁移到同类五账本模型，否则模拟可信、运行时仍可能通胀。

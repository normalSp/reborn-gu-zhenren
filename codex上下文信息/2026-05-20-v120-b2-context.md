# 2026-05-20 v1.2.0-b2 上下文交接

## 当前状态

- 分支：`codex/v120-b2-survival-ledger-bgm-runtime`
- 基线：从已推送的 `codex/v120-dirty-worktree-consolidation` 后续基线切出。
- 阶段：`v1.2.0-b2` 本地实现与验证完成，准备按明确路径提交并推送。
- 不涉及：EdgeOne 自动部署、公开发布口径、BFF/backend、DeepSeek 经济权限扩大、正式库存/价格/交易、真实消耗、炼蛊成功/失败结算、地点/阵营/奖励/NPC 生死。

## 用户批准

- D-122-001：`SAVE_FORMAT_VERSION = 24`。
- D-122-002：新增单一聚合对象 `survivalEconomyState`。
- D-122-003：只做最小 pressure ledger，记录压力、来源、证据和禁止项。
- D-122-004：正式库存/价格/交易/真实消耗/黑市/委托/稳定套利继续禁止。
- D-122-005：根目录 BGM 音频做一次正式 runtime 晋升专项。
- D-122-006：audio manifest 引用纳入 `check:runtime-assets`。
- D-122-007：每个新小版本或 standalone 专项先切语义分支，并写入 Git 推送制度。

## Runtime 变更

- 新增 `src/engine/v120-survival-economy-state.ts`：默认值、归一化、最小 ledger sync resolution。
- 新增 `src/store/slices/survivalEconomySlice.ts`：`syncSurvivalEconomyLedgerAction()` 只写 `survivalEconomyState` 和 `flags.lastSurvivalEconomyLedgerPatch`。
- `src/store/initialState.ts` 将 `SAVE_FORMAT_VERSION` 提升到 `24`，并加入默认 `survivalEconomyState`。
- `src/store/index.ts` 对旧档/异常档归一化 `survivalEconomyState`。
- `src/components/game/LowRankSurvivalEconomyPanel.tsx` 增加压力账本登记入口。
- `src/e2e/installE2eHarness.ts` 与 `tests/e2e/v120-low-rank-survival-economy.spec.ts` 覆盖 ledger 写入摘要。

## BGM 晋升专项

- `public/audio/ATTRIBUTION.txt`
- `public/audio/bgm/ATTRIBUTION.txt`
- `public/audio/bgm/BGM_SOURCING_GUIDE.md`
- `public/audio/bgm/MUSIC_MANIFEST.md`
- `scripts/check-runtime-assets.mjs`

结论：runtime 音频仍只从 `public/audio/...` 和 audio manifests 引用。根目录 `bgm/v0.7/*.MP3` 仍是本地 fan-pack staging，不作为 runtime 直接引用源。

## Git 制度

- 新增 `指导大纲/流程制度/Git分支切换与推送制度.md`。
- 更新 `指导大纲/流程制度/README.md` 与 `指导大纲/流程制度/Git脏区收束制度.md`。
- 规则：进入新小版本/专项前先切 `codex/<version>-<phase>-<topic>` 语义分支，记录基线，阶段完成后显式 stage、提交并推送；仍禁止 `git add -A` 和 EdgeOne 自动部署。

## 验证

- `npm test -- src/engine/v120-low-rank-survival-economy-projection.test.ts src/engine/v120-survival-economy-state.test.ts src/store/save-normalization.test.ts src/store/slices/survivalEconomySlice.test.ts --reporter=dot`：4 files，14 tests 通过。
- `npx tsc --noEmit --pretty false`：通过。
- `npm run test:e2e -- tests/e2e/v120-low-rank-survival-economy.spec.ts`：1 test 通过。
- `npm run check:player-advocate-gate -- 指导大纲/v1.2.0/codex/00-总览/v1.2.0-b2-Player-Advocate-20轮走查记录.md 20`：20 轮通过，理解率 100.0%，confused=0。
- `npm test`：143 files，784 tests 通过。
- `npm run build`：通过；仅 Rolldown plugin timings warning。
- `npm run check:runtime-assets`：163 runtime files，45 audio，83 imageMapRefs，71 audioManifestRefs，zero-byte=0。
- `npm run check:qingmao-assets`：23 entries 通过。
- `npm run check:player-visible-copy`：274 files scanned，通过。
- `npm run test:e2e`：63 tests 通过。
- `npm run test:e2e:long`：7 tests 通过。
- `npm run check:production-preview`：通过；生产预览仍显示已批准的 v1.0 public release label。

## 下一步

建议进入 `v1.2.0-b3` 前先开小型设计门禁，围绕炼养用准备、失败风险显示、维护周期可读性和禁止真实结算再做一次用户拍板。不得把 b2 ledger 误读成正式经济账本。

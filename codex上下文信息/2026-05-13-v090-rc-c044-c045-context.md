# 2026-05-13 v0.9.0-rc C-044/C-045 上下文

## 当前状态

- 工作分支：`codex/v090-b1-world-action-protocol`
- 当前阶段：`v0.9.0-rc` 发布候选收束
- 本轮专项：C-044 发布说明与手测走查最终化、C-045 chunk 体积专项
- 结论：R-001 TypeScript 类型债已关闭；R-002 chunk 体积风险已关闭；当前没有新的发布阻断。

## C-044 落地

- `v0.9.0-rc-发布说明草案.md` 更新为候选公告底稿。
- 明确推荐体验入口：古月开局、三王山小队战、宝黄天经济边界、福地/洞天边界、青茅凡战。
- 明确发布边界：不承诺全部蛊虫/杀招/区域剧情，不承诺正式模型已切便宜模型，不把青茅竖切当全局最终美术。
- `v0.9.0-rc-手测走查清单.md` 增加自动化门禁、青茅底图/入场演出、无 API key 场景和手测结论模板。

## C-045 落地

- 新增 `src/ui/dice-roll-bus.ts`。
- `DiceRollAnimation.tsx` 负责绑定和播放，`combatSlice.ts` 改为从非 React bus 触发掷骰。
- `vite.config.ts` 从旧 `manualChunks` 迁移到 Rolldown `output.codeSplitting.groups`。
- `combat-squad` 从历史 500KB+ 降至约 65KB。
- 最大业务 chunk 约 248KB；`npm run build` 不再出现 500KB+ chunk warning。

## 验证结果

- `npx tsc --noEmit --pretty false`：通过。
- `npm test`：通过，88 files / 541 tests。
- `npm run build`：通过，无 500KB+ chunk warning；仍可能出现非阻断 plugin timing 提示。
- `npm run check:runtime-assets`：通过，128 files；zero-byte=0。
- `npm run check:qingmao-assets`：通过，active=4、review-only=2、blocked=1。
- `npm run test:e2e:long`：通过，29 tests。
- `npx playwright test tests/e2e/v090-b3-qingmao-battlefield.spec.ts`：通过，4 tests。

## 剩余需要用户决策的事项

- 是否进入 b3/b3-5 对外素材审查，以及使用哪些截图/短录屏。
- DeepSeek eval 与 C-049 已在后续专项完成，当前默认模型为 `deepseek-v4-flash`；用户已决定不再评估其他模型。
- 是否把 `v0.9.0-rc` 升成正式 `v0.9.0` 发布口径。

## 下一步建议

继续 rc 收束，但下一步已经触及用户决策边界。建议先让用户选择：

1. 进入 b3/b3-5 对外素材审查。
2. DeepSeek 模型切换 eval 已由 C-049 收束；用户已决定不再评估其他模型。
3. 进入最终 rc 发布门禁。

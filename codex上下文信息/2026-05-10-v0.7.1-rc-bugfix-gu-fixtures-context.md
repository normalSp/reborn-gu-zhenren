# 2026-05-10 v0.7.1-rc realm bugfix 与测试存档重建上下文

## 当前状态

- 工作区：`D:\workspace\CodeBuddyWorkSpace\RebornG_codebuddy`
- 分支：`codex/v070-release-closure`
- 版本标识：`package.json = 0.7.1-rc.0`
- 存档版本：`SAVE_FORMAT_VERSION = 15`
- 本轮不升存档版本：修复范围是 DeepSeek `realm` 协议兼容、仙凡判定解析、测试夹具重建，不新增强制持久化字段。

## 本轮结论

- `[PIPE] ZOD_FAIL → state_update.player.realm` 已修复：schema 支持 `"二转中阶"` 字符串简写，并规整为 `{ action: "set", value }`。
- `AperturePanel.tsx` 仙窍 `.length` 崩溃已修复：UI 与 `immortalSlice` 对缺省数组/对象做兜底。
- 用户确认测试存档不需要旧字段兼容：不为了旧测试档继续扩展 `mortalAperture`、`resource_points`、`type:"immortal"` 读档迁移。
- `测试存档/v0.7.0` 已重建为当前格式：58 个 JSON，全部 `formatVersion=15`。
- 十绝体专项已覆盖 10 种自然十绝体 + 隐藏第十一绝体 `纯梦求真体`。

## 关键代码改动

- `src/schemas/narrative.schema.ts`
  - `RealmUpdate` 使用 preprocess，接受字符串简写和缺省 `action` 对象。
- `src/types/index.ts`
  - `StateUpdate.player.realm` 类型支持 `string | { action?: "set"; value: string }`。
- `src/store/slices/playerSlice.ts`
  - 中文一至九转解析，`applyStateUpdate` 同时支持字符串/对象 realm 更新。
- `src/engine/state-update-applier.ts`
  - 仙凡判定改用中文/数字境界解析，不再依赖 `parseInt`。
- `src/components/game/AperturePanel.tsx`
  - 仙窍视图对 `resource_nodes`、`dao_mark_density`、面积、流速、灾劫倒计时做渲染兜底。
- `src/store/slices/immortalSlice.ts`
  - 仙窍 tick、节点启停、新增/升级节点对缺省数组/对象做保护。
- `src/store/test-save-fixtures.test.ts`
  - 新增当前格式测试存档守门：formatVersion、可见叙事、attributes、凡人空窍、仙窍库存、11 种体质覆盖、亲和道途合法性。

## 测试存档策略

- 正式字段：
  - 凡人蛊师档：`state.aperture.type = "mortal"`。
  - 蛊仙档：`state.aperture.type = "福地"` 或 `"洞天"`，使用 `resource_nodes` 与 `dao_mark_density`。
  - 所有 JSON：`attributes`、`currentNarrative`、`playerRole="original_participant"`。
- 删除旧字段：
  - 不再写 `state.mortalAperture`。
  - 不再写 `aperture.resource_points`。
  - 不再写 `aperture.type = "immortal"`。
- 十绝体矩阵：
  - `28-十绝体-太日阳莽体.json`
  - `29-十绝体-古月阴荒体.json`
  - `30-十绝体-北冥冰魄体.json`
  - `31-十绝体-森海轮回体.json`
  - `32-十绝体-炎煌雷泽体.json`
  - `33-十绝体-万金妙华体.json`
  - `34-十绝体-大力真武体.json`
  - `35-十绝体-逍遥智心体.json`
  - `36-十绝体-厚土元央体.json`
  - `37-十绝体-宇宙大衍体.json`
  - `38-十绝体-纯梦求真体.json`
- 世界观边界：没有加入永生蛊，没有加入十转，没有给凡人档发放仙蛊；`纯梦求真体`标记为隐藏第十一绝体，不混作自然十绝体。

## 已验证命令

- `npm test -- src/store/test-save-fixtures.test.ts src/store/save-load-narrative.test.ts`
  - 通过：2 个测试文件 / 11 个用例。
- `npm test`
  - 通过：57 个测试文件 / 369 个用例。
- `npm run build`
  - 通过：最大 chunk 为 `combat-squad` 约 427.82 kB，无 500KB+ 警告。
- `npm run test:e2e:long`
  - 通过：18 条 Playwright 长测路径。
- 额外静态确认：
  - `测试存档/v0.7.0` 中无 `formatVersion: 13`。
  - 无 `mortalAperture`。
  - 无 `resource_points`。
  - 无 `type: "immortal"`。

## 文档记录

- 已更新：`测试存档/v0.7.0/存档索引.md`
- 已更新：`指导大纲/v0.7.0/codex/07-发布收束/v0.7.0-发布收束核对表.md`
- 本文件为下一轮上下文交接。

## 已知工作区状态

- 本轮改动涉及代码、测试、58 个 v0.7.0 测试存档、存档索引、发布收束核对表和本上下文文件。
- 进入本轮前已存在的无关变更未处理也未回退：
  - 删除：`bug汇总/v0.6.0.md`
  - 修改：`dead_code_audit_report.md`
  - 未跟踪：`.codex/`
  - 未跟踪：`bgm/`
  - 未跟踪：根目录每日例报文件

## 下一轮建议

- 若继续 v0.7.1 RC，可人工浏览器走查 28-38 十绝体矩阵档，重点看空窍面板、亲和道途、禁制提示和压力条。
- 若进入 v0.8-a，可承接 `指导大纲/v0.8.0/v0.8.0-正史锚点-IF宿命双路线-剧情完整体-设计大纲.md`，不要把复杂灾劫、升仙失败、高级采集等深系统回填进 v0.7.1。

# 2026-05-09 v0.8.0-pre 剧情地基上下文交接

## 当前分支与版本

- 当前分支：`codex/v070-release-closure`
- 当前版本标识：`package.json = 0.7.0-pre.0`
- 当前存档格式：`SAVE_FORMAT_VERSION = 14`
- 本轮范围：先完成 v0.8.0 详细大纲，再落地 v0.8.0-pre 的剧情锚点、IF 宿命矩阵、天意账本、结局边界与 DeepSeek 候选闸门。

## 用户关键决策

- 默认不使用子代理。
- v0.8.0 目标是“剧情做到原著最新章节骨架”，但不原创补完原著真正终局。
- 采用正史模式 + IF 模式。
- IF 模式采用护宿命 / 毁宿命双路线对立。
- “天意反噬失败”“隐世长生线”这类说法不严谨，必须改为更贴近原著的宿命状态、天意压力、疯魔求证未证线。
- 无极魔尊在疯魔窟吞噬混沌求十转失败；原著未确认永生，v0.8.0 不开放“炼成永生蛊”或真正永生结局。

## 已完成提交

1. `741ede6 v0.8.0：收束构建分包并消除主包警告`
   - `App`、`GameScreen` 重面板懒加载。
   - Vite manual chunks 拆分 `react-vendor`、`ui-vendor`、`engine`、`canon-*`、`audio`、`combat-squad`。
   - `npm run build` 无 500KB+ 警告。
2. `4541602 v0.8.0：新增正史锚点与IF宿命双路线大纲`
   - 新增 `指导大纲/v0.8.0/v0.8.0-正史锚点-IF宿命双路线-剧情完整版-设计大纲.md`。
3. 本交接对应的待提交阶段：`v0.8.0：建立剧情锚点宿命与结局地基`

## 本轮源码改动

- 新增 `src/canon/canon-anchor-registry.json`
  - 十个剧情锚点：青茅山、商家城、三王山、王庭、义天山、逆流河、梦境/影宗、宿命战、尊者博弈、天庭末章。
- 新增 `src/canon/fate-branch-matrix.json`
  - `fateState = intact | fractured | destroyed`。
  - 护宿命与毁宿命双路线，以及 `protect_fate / break_fate / faction_shift / npc_survival / resource_control / venerable_balance / heaven_will_debt` 偏移轴。
- 新增 `src/canon/heaven-will-rules.json`
  - 天意注意、修正、排斥、模糊度账本。
  - 天意不是万能失败按钮。
- 新增 `src/canon/ending-outcomes.json`
  - 结局族群与永生/十转禁区。
- 修改 `src/types/index.ts`
  - 新增 v0.8 剧情类型与 `StateUpdate` 三类候选字段。
- 修改 `src/schemas/narrative.schema.ts`
  - 显式接受 `story_event_candidates`、`if_branch_candidates`、`canon_anchor_pressure`。
- 新增 `src/engine/v080-narrative-engine.ts`
  - 锚点校验、IF 候选校验、天意触发、结局解析、结局禁词扫描。
- 修改 `src/engine/state-update-applier.ts`
  - DeepSeek 提交的 v0.8 剧情内容只作为候选/压力记录。
  - 正史模式拦截 IF 候选，IF 模式才会写入 `flags.ifBranchVectors`。
- 修改 `src/engine/context-builder.ts`
  - 注入 v0.8 剧情锚点与宿命闸门。
  - 禁止 DeepSeek 直接改宿命状态、关键 NPC 生死、锚点结果、正式结局、永生/十转定论。
- 新增测试：
  - `src/engine/v080-narrative-engine.test.ts`
  - `src/engine/v080-narrative-schema.test.ts`

## 新增文档

- `指导大纲/v0.8.0/codex/00-总览/v0.8.0-开发阶段跟踪.md`
- `指导大纲/v0.8.0/codex/02-执行记录/v0.8.0-pre-剧情地基落地记录.md`

## 验证结果

- `npm test -- src/engine/v080-narrative-engine.test.ts src/engine/v080-narrative-schema.test.ts`：通过，2 个测试文件 / 6 个用例。
- `npm test`：通过，43 个测试文件 / 310 个用例。
- `npm run build`：通过，无 500KB+ 警告；最大 chunk 为 `combat-squad` 约 376KB。

## 下一步入口

1. 提交并 push 本轮 v0.8.0-pre 剧情地基。
2. v0.8.0-a 开始时先做“章节 → CanonAnchor 映射表”，避免 `currentChapterId` 和 `CanonAnchor.id` 混用。
3. v0.8.0-a 优先落地前中期四锚点：
   - 青茅山
   - 商家城
   - 三王山
   - 王庭
4. 每个锚点要有：
   - 正史模式可玩侧线
   - IF 模式可偏移点
   - 道心/声望/关系/经济回流
   - 天意/因果压力记录
   - 浏览器长测存档

## 仍需注意

- 工作区仍有不属于本轮的无关项：`dead_code_audit_report.md`、`.codex/`、根目录 `bgm/`。不要提交这些内容。
- v0.7.0 免费音频包已完成，用户确认曲单仍是 `user_supplied_reference`，未拿到本地原曲文件前不要启用 `nonprofit_fan_pack`。
- v0.8.0 不应把“永生”做成可达成数值目标；如果未来用户想做原创补完，应作为独立非正史扩展包。

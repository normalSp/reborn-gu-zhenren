# 2026-05-08 v0.7.0-pre 可信前置收束 v2 上下文交接

## 用户目标

- v0.7.0-pre 必须补齐最初需求漏项：原创身份锚点、人物图鉴 contact、事件日志详情/归档、死亡总结与定场诗、战斗与小队设计包。
- 完成后要能可信进入 v0.7.0-a/b/c；大战斗完整实现、小队外派完整玩法、杀招特效播放、LLM 第三方介入完整运行时延后到 a/b/c。
- 每次阶段结束更新上下文交接文件；不写入隐藏系统/开发者指令或密钥。

## 本轮完成项

### M11 叙事身份与人物图鉴

- `src/engine/context-builder.ts`
  - 玩家名缺失时使用“无名蛊师”。
  - `playerRole=original_participant` 写入 playerStateJSON。
  - 明确玩家是原创蛊师，方源只能作为原著 NPC、远景事件或剧情交会对象。
  - DeepSeek state_update 字段速查新增 `npc_contacts`。
- `src/types/index.ts`
  - 新增 `PlayerIdentityContext`、`NpcContact`、`NpcContactStatus`。
  - `StateUpdate` 新增 `npc_contacts.add`。
- `src/schemas/narrative.schema.ts`
  - Zod 新增 `npc_contacts.add`。
- `src/store/slices/factionSlice.ts`
  - 新增 `npcContacts` 与 `addNpcContact`，支持 `heard/seen/interacted` 状态合并，同名 NPC 不重复计数。
- `src/components/game/CharacterPanel.tsx`
  - 人物图鉴合并正式关系、动态 NPC、contact 三类来源。
  - 势力声望继续显示最近原因、权益、风险。

### M12 事件日志与死亡记录

- `src/store/slices/gameLogSlice.ts`
  - `GameLogEntry` 扩展 `summary/detail/location/actors/gains/losses/danger/importance/archived`。
  - 新增 `gameLogArchive` 与 `archiveGameLog`。
  - `GameLogCategory` 补 `map/faction/death`，对齐已有调用。
- `src/components/game/EventLogPanel.tsx`
  - 事件日志改为滚动、分页、详情查看、归档计数。
- `src/types/index.ts`
  - `DeathRecord` 扩展 `lifeSummary/closingPoem/poemTitle/majorChoices/deathCauseTags/generatedAt`。
- `src/engine/death-record.ts`
  - 新增本地兜底死亡记录生成，包含原创定场诗。
- `src/engine/response-pipeline.ts`、`src/store/slices/playerSlice.ts`
  - 死亡时填充扩展 DeathRecord。
- `src/components/game/GameOverScreen.tsx`
  - 展示一生摘要、关键选择、定场诗，并可导出死亡记录 JSON。

### M13 战斗与小队设计包

- `src/canon/battle-design-pack.json`
- `src/canon/battle-asset-manifest.json`
- `src/canon/squad-growth-design-spec.json`
- `指导大纲/v0.7.0/codex/05-战斗小队/战斗与小队深度设计包.md`

设计边界：pre 只交付协议和数据入口；完整战斗、小队外派、多线剧情和特效进入 v0.7.0-a/b/c。

### M14 事件政策真实接入

- `src/engine/state-update-applier.ts`
  - 接入 `event_policy`，按事件类型结算道心、势力声望、日志。
  - 接入 `attribute_mutations.add`，通过 `AttributeMutationPolicy` 校验后才允许改变四维；NPC/队友目标暂存为后续小队系统线索。
- `src/engine/context-builder.ts`
  - DeepSeek 字段速查加入 `event_policy`、`attribute_mutations`。

### M16 文档与测试

- 新增 `src/engine/v070-pre-narrative-gates.test.ts`。
- 新增 `指导大纲/v0.7.0/codex/04-交互叙事/M11-M16-可信前置收束落地记录.md`。

## 验证结果

- `npm test`：27 个测试文件 / 235 个用例通过。
- `npm run build`：通过；仍有主包 500KB+ 的既有 Vite 警告，非本轮阻塞项。
- 浏览器冒烟：完成“商队·求胜之路 → 商家 → 金银蛊 → 进入游戏 → 人物图鉴 → 事件日志”路径。
  - Playwright 报告 0 console error。
  - 事件日志详情/分页入口可见。
  - 人物图鉴打开正常，势力声望权益/风险显示正常。
- 为避免泄露本地密钥，浏览器自动化临时日志已删除；上下文文件不记录任何密钥。

## 仍未完成的 v0.7.0-pre 工作

1. M15 三路径浏览器手测还剩两条：
   - 三王山前夜。
   - 蛊仙期。
2. 100/300 回合套利检查已有自动测试覆盖核心模型，但仍建议补一份浏览器/手测记录：
   - 宝黄天购买。
   - 成就奖励。
   - 势力收益。
   - 仙窍产出。
   - 元石调息。
   - 食料与炼蛊来源。
3. 鸿运齐天蛊目标选择 UI 尚未细化。
4. scene-gated 蛊虫由 DeepSeek 给出选项后的引擎校验仍需继续细化。
5. 全库 provenance 字段策略已固化，但历史 JSON 仍未逐条补齐。

## v0.7.0-a/b/c 入口

- v0.7.0-a：基于 `battle-design-pack.json` 做确定性战斗引擎骨架、流派轻量克制、道痕/转数压制、仙蛊唯一和高成本规则。
- v0.7.0-b：基于 `squad-growth-design-spec.json` 做少而深的小队战、小队信任/忠诚/野心/恐惧、核心成员事件。
- v0.7.0-c：接入 `battle-asset-manifest.json`，做杀招/重要仙蛊闪图、外派多线剧情回流、受控 LLM 第三方介入。

## 注意事项

- 工作区已有大量前序 v0.7.0-pre 改动，本轮没有回滚任何前序或用户改动。
- 当前分支仍为 `codex/rebuild-v070-tracking`。
- `package.json` 仍为 `0.5.0`，`SAVE_FORMAT_VERSION` 仍为 `11`；版本统一仍是发布前工程可信闸门的一部分。

# 2026-05-25 v3.8 startup context

当前分支：`codex/v380-startup-proposal-graph-stability`。

用户要求：开 `v3.8` 专家团启动会。

已做：

- 确认 `v3.7.0` 完成：completion commit `8ef17c5c`，GitHub Actions `26386740563` passed。
- 建立 v3.8 startup docs：`指导大纲/v3.8.0/codex/00-总览/`。
- v3.8 建议主线：`transient proposal graph 长期稳定性与多小势力压力复核`。
- 当前 D-380 仍为 `pending_user_decision`，F-380 全部建议保持 `future_gate_required`。

硬边界：

- 不新增 save field / save-format bump / `runFingerprint`。
- 不接入 live DeepSeek。
- 不做 MiroFish export/intake。
- 不建 backend/BFF。
- 不引入外部 framework PoC/dependency/subagent。
- 不开放 L4/L5、正式地点/阵营/身份/奖励/NPC 生死/通缉/招揽/封锁。

下一步：

- 等用户审批 `D-380-001` 至 `D-380-012`。
- 若用户批准，可进入 v3.8 开发。
- 若用户修改路线，先更新前置授权包和例外停机清单。

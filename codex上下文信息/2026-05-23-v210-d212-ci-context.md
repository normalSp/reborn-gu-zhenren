# 2026-05-23 v2.1 D-212 决策与 CI 初诊交接

当前分支：`codex/v210-d212-sdk-intake-ci-triage`

## 本轮完成

- 记录 D-212 用户决策：
  - D-212-001：批准 license / SBOM / 架构适配评估。
  - D-212-002：隔离 PoC 暂不批准，需先解释后逐项申请。
  - D-212-003：如后续需要，先做架构评审，看收益与实际必要性。
  - D-212-004：需要时先做风险收益报告。
  - D-212-005：patch artifact 需先解释，不因本轮批准。
  - D-212-006：禁止外部 SDK/agent 真实写项目、跑命令、操作 git。
- 新增 `v2.1.0-D212决策记录与CI初诊.md`，解释隔离 PoC、patch artifact，并记录 GitHub CI 初诊。
- 同步 v2.1 README、需求池、路线图、测试矩阵、Git 计划、Skill 审计、项目仪表盘、PROJECT-STATE、AGENTS。
- 同步 skills：
  - `reborn-expert-council` -> `0.1.133`
  - `game-dev-text` -> `2.4.01`
  - `reverend-insanity-lore` -> `0.3.89`

## CI 初诊

GitHub 最新 D-211 分支 run `26299081294` 通过。

旧失败来自早前 a1/a2 分支：

- `26293409163`：`npm ci` 拉取 `zustand-5.0.12.tgz` 时 `registry.npmmirror.com` 504。
- `26294936890`：`npm ci` 拉取 `zod-4.3.6.tgz` 时 `registry.npmmirror.com` 504。
- `26297456419`：`npm ci` 拉取 `why-is-node-running-2.3.0.tgz` 时 `registry.npmmirror.com` 504。

结论：这是旧分支 npm 镜像源拉包超时，不是 TypeScript、unit test、build、asset scan 或 runtime 逻辑失败。当前不改 `.github/workflows/ci.yml` 或 `package-lock.json`。如果未来当前分支重复出现，可单独开 `CI registry resilience` 小专项。

## 子代理结论

本轮没有启用子代理。原因：

- D-211-008 仍未批准。
- 本次 CI 初诊可由主线程 `gh` 日志完成。
- 根因不是复杂代码失败，不值得破坏当前单线程审计边界。

## 下一步

可继续进入 `v2.1.0-b1`，但仍只能执行 D-211 受限边界：

- TypeScript-first。
- zero-dependency。
- dry-run only。
- report-only/offline runner。
- report 写入 `artifacts/v2.1.0/`。
- 不做外部 framework PoC。
- 不调用 live DeepSeek。
- 不启用子代理。
- 不改 runtime/save/DeepSeek/MiroFish/BFF/backend。

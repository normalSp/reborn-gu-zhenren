# 2026-05-09 v0.7.0 真实音频资源落地上下文交接

## 当前分支与版本

- 当前分支：`codex/v070-release-closure`
- 当前版本标识：`package.json = 0.7.0-pre.0`
- 当前存档格式：`SAVE_FORMAT_VERSION = 14`
- 本轮范围：完成 v0.7.0 发布收束中最后的音频 P0，采用“双包模式 + 免费替代素材”。

## 用户最新决策

- 不使用子代理。
- 真实音频资源需要落地，但免费公布包优先使用可验证免费素材。
- 用户人工核对的 B 站/社区角色曲清单作为角色曲设计依据：方源-年轮、龙公-春泥、白凝冰-山山而川/征途漫漫、商心慈-天若有情、黑楼兰-长安姑娘、盗天魔尊-无名的人、红莲魔尊-若梦/平生不晚、幽魂魔尊-紫荆花盛开、薄青-牵丝戏、古月药乐-青衣、凤九歌-知我、定场诗通用 boss area、吴帅-虞美人、太白云生-Cry For Me、三王山炼制定仙游-精卫琵琶版、墨瑶-唯一、马鸿运-zood 丁真版、柳贯一-Lightning Moment dj、无极魔尊-Lightning Moment dj。
- 上述用户确认曲目只登记为 `user_supplied_reference`，不自动从网络抓取，不进入免费 runtime；等待用户本地提供文件后可启用 `nonprofit_fan_pack`。

## 本轮已完成

1. 免费 runtime BGM/SFX 落地
   - 五域 BGM 已有真实免费文件：南疆、北原、东海、西漠、中州。
   - 战斗 BGM 已有真实免费文件：普通/小队战、Boss 战。
   - 场景 BGM 已有真实免费 fallback：死亡定场诗、逆流河。
   - SFX 已有真实免费文件：打击 3、成就解锁、UI 点击、UI 确认。
   - 所有新增 runtime 文件均为非 0 字节文件。

2. 音频真相源与双包策略
   - 新增 `src/canon/audio-source-manifest.json` 作为运行时真相源。
   - `public/audio/audio-source-manifest.json` 保持为发布镜像。
   - manifest 区分 `free_public_pack`、`nonprofit_fan_pack`、`user_supplied`。
   - 禁止 AI/合成 BGM 与 AI/合成战斗/成就音效进入 runtime。

3. 角色曲触发兼容
   - `src/canon/character-bgm-manifest.json` 已登记用户确认角色曲和免费 fallback。
   - `src/engine/character-bgm-trigger.ts` 已修正 actor 匹配：同一角色存在禁用私用曲与启用 fallback 时，不会被禁用条目挡住。

4. Runtime 音频路径接入
   - `src/store/slices/soundSlice.ts` 更新五域 BGM、死亡定场诗路径。
   - `CombatOverlay.tsx`、`SquadCombatOverlay.tsx` 不再硬编码缺失的 `/audio/bgm/combat.mp3`，改用 `SPECIAL_BGM.combat`。
   - `src/utils/audio.ts` 将 UI 确认/保存音效映射到真实免费素材。
   - `src/engine/audio-resource-policy.ts` 不再从 `public/audio` import JSON，避免 Vite public import warning。

5. 文档与发布核对
   - 新增 `指导大纲/v0.7.0/codex/07-发布收束/v0.7.0-真实音频资源落地记录.md`。
   - 更新 `指导大纲/v0.7.0/codex/07-发布收束/v0.7.0-发布收束核对表.md`，记录 2026-05-09 音频收束状态。
   - 重写 `public/audio/ATTRIBUTION.txt`、`public/audio/bgm/ATTRIBUTION.txt`、`public/audio/bgm/BGM_SOURCING_GUIDE.md`、`public/audio/bgm/MUSIC_MANIFEST.md`。

## 验证结果

- `npm test -- src/engine/audio-resource-policy.test.ts`：通过 1 个测试文件 / 5 个用例。
- `npm test`：通过 41 个测试文件 / 304 个用例。
- `npm run build`：通过；仅保留 Vite 500KB+ chunk 非阻塞警告。
- `npm run test:e2e:long`：通过 12 条 Playwright 长测路径。

## 当前发布判断

- 从免费公布包角度看，v0.7.0 的 pre/a/b/c 与发布收束 P0 已经完成到可进入发布候选验证的状态。
- 从“用户确认原曲/社区角色曲全量实装”角度看，尚未完成，因为这些文件需要用户本地提供后才能启用 `nonprofit_fan_pack`。
- 除音频原曲文件外，当前发布核对表没有新的 P0 阻塞项。

## 剩余非阻塞优化

1. 角色曲私用包导入
   - 后续可增加本地导入脚本或管理 UI：检查用户提供文件、校验路径、切换 `nonprofit_fan_pack`。

2. 免费 BGM 质量继续筛选
   - 当前免费素材满足可运行和归因要求，但不是最终最贴脸体验；可继续按五域、名场面、人物气质筛选更好的免费素材。

3. 构建体积优化
   - `npm run build` 仍有 Vite 500KB+ chunk 警告，建议发布候选前单独做代码分包。

4. 人工整档体验复验
   - 自动长测已通过，但发布候选前建议用一转商队、三王山、蛊仙期各打一轮人工整档体验，重点听音量、循环点、事件音效是否突兀。

## 工作区注意事项

- 本轮不要提交无关脏项：`.codex/`、根目录 `bgm/`、`dead_code_audit_report.md`。
- 本轮应提交音频文件、音频 manifest、音频 runtime 接入、音频文档、发布核对表和本交接文件。
